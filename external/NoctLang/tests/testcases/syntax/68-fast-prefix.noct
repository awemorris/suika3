__fast func add_fast(a: int, b: int): int {
    return a + b;
}

static __inline __fast func twice_fast(value: int): int {
    return value * 2;
}

func main() {
    print(add_fast(20, 22));
    print(twice_fast(21));
}
