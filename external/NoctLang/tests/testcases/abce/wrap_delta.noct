func main() {
    var p = Packed.uint8(10);
    for (i in 0..10) {
        p[i] = i;
    }
    var delta = 2147483643;
    var sum = 0;
    for (i in 0..10) {
        sum = sum + p[i + delta];
    }
    print(sum);
}
