func main() {
    var n = 5000000;
    var p = Packed.int64(n);
    for (i in 0..n) {
        p[i] = i * 3;
    }
    var sum = 0;
    for (i in 0..n) {
        sum = sum + p[i];
    }
    print(sum);
}
