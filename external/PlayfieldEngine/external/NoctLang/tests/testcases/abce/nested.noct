func main() {
    var p = Packed.uint8(16);
    for (i in 0..16) {
        p[i] = i;
    }
    var sum = 0;
    for (i in 0..4) {
        for (j in 0..4) {
            sum = sum + p[i * 4 + j];
        }
    }
    print(sum);
}
