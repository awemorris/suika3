__accel func reduce(
    source: rpackedint32,
    destination: rpackedint32,
    n: int): void {
    var sum: int = 1;
    for (i in 0..n) {
        sum += source[i];
    }
    destination[0] = sum;
}

func main() {
}
