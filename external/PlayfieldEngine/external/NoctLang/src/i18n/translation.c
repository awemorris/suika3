#include <noct/noct.h>
#include <string.h>

const char *noct_get_system_language(void);

NOCT_DLL const char *noct_gettext(const char *msg)
{
    const char *lang_code = noct_get_system_language();

    if (strcmp(lang_code, "zh-cn") == 0) lang_code = "zh";
    if (strcmp(lang_code, "zh-tw") == 0) lang_code = "tw";

    if (strcmp(msg, "  --gc-graduate-size=N ... second GC space size in bytes\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --gc-graduate-size=N ... second GC space size in bytes\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --gc-graduate-size=N ... tamaño del segundo espacio GC en bytes\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --gc-graduate-size=N ... taille du second espace GC en octets\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --gc-graduate-size=N ... Größe des zweiten GC-Speicherbereichs in Byte\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --gc-graduate-size=N ... dimensione del secondo spazio GC in byte\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --gc-graduate-size=N ... μέγεθος του δεύτερου χώρου GC σε byte\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --gc-graduate-size=N ... размер второй области GC в байтах\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --gc-graduate-size=N ... 第二 GC 空间大小（字节）\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --gc-graduate-size=N ... 第二 GC 空間大小（位元組）\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --gc-graduate-size=N ... 第 2 GC 領域のサイズ（バイト単位）\n";
        return "  --gc-graduate-size=N ... second GC space size in bytes\n";
    }
    if (strcmp(msg, "  --gc-lop-threshold=N ... move objects larger than N-bytes to final GC space\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --gc-lop-threshold=N ... move objects larger than N-bytes to final GC space\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --gc-lop-threshold=N ... mover objetos mayores de N bytes al espacio GC final\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --gc-lop-threshold=N ... deplacer les objets de plus de N octets vers lespace GC final\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --gc-lop-threshold=N ... Objekte groser als N Byte in den finalen GC-Speicherbereich verschieben\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --gc-lop-threshold=N ... sposta gli oggetti più grandi di N byte nello spazio GC finale\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --gc-lop-threshold=N ... μεταφορά αντικειμένων μεγαλύτερων από N byte στον τελικό χώρο GC\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --gc-lop-threshold=N ... перемещать объекты больше N байт в финальную область GC\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --gc-lop-threshold=N ... 将大于 N 字节的对象移动到最终 GC 空间\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --gc-lop-threshold=N ... 將大於 N 位元組的物件移動到最終 GC 空間\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --gc-lop-threshold=N ... N バイトを超えるオブジェクトを最終 GC 領域へ移動\n";
        return "  --gc-lop-threshold=N ... move objects larger than N-bytes to final GC space\n";
    }
    if (strcmp(msg, "  --gc-nursery-size=N  ... first GC space size in bytes\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --gc-nursery-size=N  ... first GC space size in bytes\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --gc-nursery-size=N  ... tamaño del primer espacio GC en bytes\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --gc-nursery-size=N  ... taille du premier espace GC en octets\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --gc-nursery-size=N  ... Größe des ersten GC-Speicherbereichs in Byte\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --gc-nursery-size=N  ... dimensione del primo spazio GC in byte\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --gc-nursery-size=N  ... μέγεθος του πρώτου χώρου GC σε byte\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --gc-nursery-size=N  ... размер первой области GC в байтах\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --gc-nursery-size=N  ... 第一 GC 空间大小（字节）\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --gc-nursery-size=N  ... 第一 GC 空間大小（位元組）\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --gc-nursery-size=N  ... 第 1 GC 領域のサイズ（バイト単位）\n";
        return "  --gc-nursery-size=N  ... first GC space size in bytes\n";
    }
    if (strcmp(msg, "  --gc-tenure-size=N   ... final GC space size in bytes\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --gc-tenure-size=N   ... final GC space size in bytes\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --gc-tenure-size=N   ... tamaño del espacio GC final en bytes\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --gc-tenure-size=N   ... taille de l’espace GC final en octets\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --gc-tenure-size=N   ... Größe des finalen GC-Speicherbereichs in Byte\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --gc-tenure-size=N   ... dimensione dello spazio GC finale in byte\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --gc-tenure-size=N   ... μέγεθος του τελικού χώρου GC σε byte\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --gc-tenure-size=N   ... размер финальной области GC в байтах\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --gc-tenure-size=N   ... 最终 GC 空间大小（字节）\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --gc-tenure-size=N   ... 最終 GC 空間大小（位元組）\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --gc-tenure-size=N   ... 最終 GC 領域のサイズ（バイト単位）\n";
        return "  --gc-tenure-size=N   ... final GC space size in bytes\n";
    }
    if (strcmp(msg, "  --gpu-list           ... list suitable GPU device selectors\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --gpu-list           ... list suitable GPU device selectors\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --gpu-list           ... listar selectores de dispositivos GPU adecuados\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --gpu-list           ... répertorier les sélecteurs de périphériques GPU appropriés\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --gpu-list           ... geeignete GPU-Geräteselektoren auflisten\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --gpu-list           ... elenca i selettori adatti per dispositivi GPU\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --gpu-list           ... εμφάνιση κατάλληλων επιλογέων συσκευής GPU\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --gpu-list           ... вывести подходящие селекторы устройств GPU\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --gpu-list           ... 列出合适的 GPU 设备选择器\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --gpu-list           ... 列出合適的 GPU 裝置選擇器\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --gpu-list           ... 適切なGPUデバイスセレクターを一覧表示\n";
        return "  --gpu-list           ... list suitable GPU device selectors\n";
    }
    if (strcmp(msg, "  --gpu[=SELECTOR]     ... use GPU acceleration for source execution\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --gpu[=SELECTOR]     ... use GPU acceleration for source execution\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --gpu[=SELECTOR]     ... usar aceleración GPU para ejecutar el código fuente\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --gpu[=SELECTOR]     ... utiliser l’accélération GPU pour exécuter le code source\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --gpu[=SELECTOR]     ... GPU-Beschleunigung zur Ausführung des Quellcodes verwenden\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --gpu[=SELECTOR]     ... usa l’accelerazione GPU per eseguire il codice sorgente\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --gpu[=SELECTOR]     ... χρήση επιτάχυνσης GPU για την εκτέλεση του πηγαίου κώδικα\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --gpu[=SELECTOR]     ... использовать ускорение GPU для выполнения исходного кода\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --gpu[=SELECTOR]     ... 使用 GPU 加速执行源代码\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --gpu[=SELECTOR]     ... 使用 GPU 加速執行原始碼\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --gpu[=SELECTOR]     ... ソースコードの実行にGPUアクセラレーションを使用\n";
        return "  --gpu[=SELECTOR]     ... use GPU acceleration for source execution\n";
    }
    if (strcmp(msg, "  --path=DIR1:DIR2     ... append source-module search paths\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --path=DIR1:DIR2     ... append source-module search paths\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --path=DIR1:DIR2     ... añadir rutas de búsqueda de módulos fuente\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --path=DIR1:DIR2     ... ajouter des chemins de recherche de modules sources\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --path=DIR1:DIR2     ... Suchpfade für Quellmodule anhängen\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --path=DIR1:DIR2     ... aggiunge percorsi di ricerca dei moduli sorgente\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --path=DIR1:DIR2     ... προσθήκη διαδρομών αναζήτησης μονάδων πηγαίου κώδικα\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --path=DIR1:DIR2     ... добавить пути поиска исходных модулей\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --path=DIR1:DIR2     ... 追加源代码模块搜索路径\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --path=DIR1:DIR2     ... 附加原始碼模組搜尋路徑\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --path=DIR1:DIR2     ... ソースモジュールの検索パスを追加\n";
        return "  --path=DIR1:DIR2     ... append source-module search paths\n";
    }
    if (strcmp(msg, "  --simd-info          ... report successfully vectorized loops\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  --simd-info          ... report successfully vectorized loops\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  --simd-info          ... informar de los bucles vectorizados correctamente\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  --simd-info          ... signaler les boucles vectorisées avec succès\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  --simd-info          ... erfolgreich vektorisierte Schleifen melden\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  --simd-info          ... segnala i cicli vettorizzati correttamente\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  --simd-info          ... αναφορά των βρόχων που διανυσματοποιήθηκαν επιτυχώς\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  --simd-info          ... сообщить об успешно векторизованных циклах\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  --simd-info          ... 报告已成功向量化的循环\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  --simd-info          ... 報告已成功向量化的迴圈\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  --simd-info          ... 正常にベクトル化されたループを報告\n";
        return "  --simd-info          ... report successfully vectorized loops\n";
    }
    if (strcmp(msg, "  -O, -O0..-O3, -O9    ... optimization preset\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  -O, -O0..-O3, -O9    ... optimization preset\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  -O, -O0..-O3, -O9    ... preajuste de optimización\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  -O, -O0..-O3, -O9    ... préréglage d’optimisation\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  -O, -O0..-O3, -O9    ... Optimierungsvoreinstellung\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  -O, -O0..-O3, -O9    ... preimpostazione di ottimizzazione\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  -O, -O0..-O3, -O9    ... προεπιλογή βελτιστοποίησης\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  -O, -O0..-O3, -O9    ... предустановка оптимизации\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  -O, -O0..-O3, -O9    ... 优化预设\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  -O, -O0..-O3, -O9    ... 最佳化預設\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  -O, -O0..-O3, -O9    ... 最適化プリセット\n";
        return "  -O, -O0..-O3, -O9    ... optimization preset\n";
    }
    if (strcmp(msg, "  -j, -j0              ... eager JIT (default), or interpreter only\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  -j, -j0              ... eager JIT (default), or interpreter only\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  -j, -j0              ... compilación JIT anticipada (predeterminada), o solo intérprete\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  -j, -j0              ... compilation JIT anticipée (par défaut), ou interpréteur uniquement\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  -j, -j0              ... sofortige JIT-Kompilierung (Standard) oder nur Interpreter\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  -j, -j0              ... compilazione JIT anticipata (predefinita), oppure solo interprete\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  -j, -j0              ... άμεση μεταγλώττιση JIT (προεπιλογή) ή μόνο διερμηνευτής\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  -j, -j0              ... немедленная JIT-компиляция (по умолчанию) или только интерпретатор\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  -j, -j0              ... 预先 JIT 编译（默认），或仅使用解释器\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  -j, -j0              ... 預先 JIT 編譯（預設），或僅使用直譯器\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  -j, -j0              ... 先行JITコンパイル（デフォルト）、またはインタプリタのみ\n";
        return "  -j, -j0              ... eager JIT (default), or interpreter only\n";
    }
    if (strcmp(msg, "  noct --ansic <out-file> <in-files>  ... convert to a C source file\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... convert to a C source file\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... convertir en un archivo fuente en C\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... convertir en un fichier source C\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... in eine C-Quelldatei umwandeln\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... convertire in un file sorgente C\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... μετατροπή σε αρχείο πηγαίου κώδικα C\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... преобразовать в исходный файл C\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... 转换为 C 源代码文件\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  noct --ansic <out-file> <in-files>  ... 轉換為 C 原始碼檔案\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  noct --ansic <出力> <入力...>       ... C ソースに変換します\n";
        return "  noct --ansic <out-file> <in-files>  ... convert to a C source file\n";
    }
    if (strcmp(msg, "  noct --compile <in-files>           ... convert to bytecode files\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  noct --compile <in-files>           ... convert to bytecode files\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  noct --compile <in-files>           ... convertir en archivos de bytecode\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  noct --compile <in-files>           ... convertir en fichiers bytecode\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  noct --compile <in-files>           ... in Bytecode-Dateien umwandeln\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  noct --compile <in-files>           ... convertire in file di bytecode\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  noct --compile <in-files>           ... μετατροπή σε αρχεία bytecode\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  noct --compile <in-files>           ... преобразовать в байткод-файлы\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  noct --compile <in-files>           ... 转换为字节码文件\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  noct --compile <in-files>           ... 轉換為位元組碼檔案\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  noct --compile <入力...>            ... バイトコードに変換します\n";
        return "  noct --compile <in-files>           ... convert to bytecode files\n";
    }
    if (strcmp(msg, "  noct --elisp <out-file> <in-files>  ... convert to an Emacs Lisp source file\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... convert to an Emacs Lisp source file\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... convertir en un archivo fuente Emacs Lisp\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... convertir en un fichier source Emacs Lisp\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... in eine Emacs-Lisp-Quelldatei umwandeln\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... convertire in un file sorgente Emacs Lisp\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... μετατροπή σε αρχείο πηγαίου κώδικα Emacs Lisp\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... преобразовать в исходный файл Emacs Lisp\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... 转换为 Emacs Lisp 源代码文件\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  noct --elisp <out-file> <in-files>  ... 轉換為 Emacs Lisp 原始碼檔案\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  noct --elisp <出力> <入力...>       ... Emacs Lisp ソースに変換します\n";
        return "  noct --elisp <out-file> <in-files>  ... convert to an Emacs Lisp source file\n";
    }
    if (strcmp(msg, "  noct --scheme <out-file> <in-files> ... convert to an Scheme source file\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  noct --scheme <out-file> <in-files> ... convert to an Scheme source file\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  noct --scheme <out-file> <in-files> ... convertir en un archivo fuente Scheme\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  noct --scheme <out-file> <in-files> ... convertir en un fichier source Scheme\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  noct --scheme <out-file> <in-files> ... in eine Scheme-Quelldatei umwandeln\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  noct --scheme <out-file> <in-files> ... convertire in un file sorgente Scheme\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  noct --scheme <out-file> <in-files> ... μετατροπή σε αρχείο πηγαίου κώδικα Scheme\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  noct --scheme <out-file> <in-files> ... преобразовать в исходный файл Scheme\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  noct --scheme <out-file> <in-files> ... 转换为 Scheme 源代码文件\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  noct --scheme <out-file> <in-files> ... 轉換為 Scheme 原始碼檔案\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  noct --scheme <出力> <入力...>      ... Scheme ソースに変換します\n";
        return "  noct --scheme <out-file> <in-files> ... convert to an Scheme source file\n";
    }
    if (strcmp(msg, "  noct <vm-options> <files>           ... run a program\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "  noct <vm-options> <files>           ... run a program\n";
        if (strncmp(lang_code, "es", 2) == 0) return "  noct <vm-options> <files>           ... ejecutar un programa\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "  noct <vm-options> <files>           ... exécuter un programme\n";
        if (strncmp(lang_code, "de", 2) == 0) return "  noct <vm-options> <files>           ... ein Programm ausführen\n";
        if (strncmp(lang_code, "it", 2) == 0) return "  noct <vm-options> <files>           ... eseguire un programma\n";
        if (strncmp(lang_code, "el", 2) == 0) return "  noct <vm-options> <files>           ... εκτέλεση προγράμματος\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "  noct <vm-options> <files>           ... запуск программы\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "  noct <vm-options> <files>           ... 运行程序\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "  noct <vm-options> <files>           ... 執行程式\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "  noct <vm-options> <入力...>         ... プログラムを実行します\n";
        return "  noct <vm-options> <files>           ... run a program\n";
    }
    if (strcmp(msg, "%s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s";
        if (strncmp(lang_code, "es", 2) == 0) return "%s";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s";
        if (strncmp(lang_code, "de", 2) == 0) return "%s";
        if (strncmp(lang_code, "it", 2) == 0) return "%s";
        if (strncmp(lang_code, "el", 2) == 0) return "%s";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s";
        return "%s";
    }
    if (strcmp(msg, "%s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s\n";
        return "%s\n";
    }
    if (strcmp(msg, "%s accelerator %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "Acelerador %s %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Accélérateur %s %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "%s-Beschleuniger %s.";
        if (strncmp(lang_code, "it", 2) == 0) return "Acceleratore %s %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Επιταχυντής %s %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ускоритель %s %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器%s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器%s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sアクセラレータ%s。";
        return "%s accelerator %s.";
    }
    if (strcmp(msg, "%s accelerator %s: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator %s: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Acelerador %s %s: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Accélérateur %s %s : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "%s-Beschleuniger %s: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Acceleratore %s %s: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Επιταχυντής %s %s: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ускоритель %s %s: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器%s：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器%s：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sアクセラレータ%s: %s";
        return "%s accelerator %s: %s";
    }
    if (strcmp(msg, "%s accelerator context is detached.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator context is detached.";
        if (strncmp(lang_code, "es", 2) == 0) return "El contexto del acelerador %s está desconectado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le contexte de l’accélérateur %s est détaché.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Kontext des %s-Beschleunigers ist getrennt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il contesto dell’acceleratore %s è scollegato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το περιβάλλον του επιταχυντή %s έχει αποσυνδεθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Контекст ускорителя %s отсоединён.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器上下文已分离。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器內容已中斷連結。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sアクセラレータのコンテキストが切り離されています。";
        return "%s accelerator context is detached.";
    }
    if (strcmp(msg, "%s accelerator program changed during begin.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator program changed during begin.";
        if (strncmp(lang_code, "es", 2) == 0) return "El programa del acelerador %s cambió durante el inicio.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le programme de l’accélérateur %s a changé pendant le démarrage.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Programm des %s-Beschleunigers wurde während des Starts geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il programma dell’acceleratore %s è cambiato durante l’avvio.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πρόγραμμα του επιταχυντή %s άλλαξε κατά την έναρξη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Программа ускорителя %s изменилась во время запуска.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器程序在开始期间发生了变化。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器程式在開始期間發生了變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "開始処理中に%sアクセラレータのプログラムが変更されました。";
        return "%s accelerator program changed during begin.";
    }
    if (strcmp(msg, "%s accelerator program is not published.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator program is not published.";
        if (strncmp(lang_code, "es", 2) == 0) return "El programa del acelerador %s no está publicado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le programme de l’accélérateur %s n’est pas publié.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Programm des %s-Beschleunigers ist nicht veröffentlicht.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il programma dell’acceleratore %s non è pubblicato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πρόγραμμα του επιταχυντή %s δεν έχει δημοσιευτεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Программа ускорителя %s не опубликована.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器程序尚未发布。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器程式尚未發佈。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sアクセラレータのプログラムが公開されていません。";
        return "%s accelerator program is not published.";
    }
    if (strcmp(msg, "%s accelerator session changed during dispatch.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator session changed during dispatch.";
        if (strncmp(lang_code, "es", 2) == 0) return "La sesión del acelerador %s cambió durante el despacho.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La session de l’accélérateur %s a changé pendant le lancement.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Sitzung des %s-Beschleunigers wurde während des Dispatch geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La sessione dell’acceleratore %s è cambiata durante il dispatch.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συνεδρία του επιταχυντή %s άλλαξε κατά την αποστολή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сеанс ускорителя %s изменился во время диспетчеризации.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器会话在调度期间发生了变化。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器工作階段在調度期間發生了變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ディスパッチ中に%sアクセラレータのセッションが変更されました。";
        return "%s accelerator session changed during dispatch.";
    }
    if (strcmp(msg, "%s accelerator session changed during finish.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator session changed during finish.";
        if (strncmp(lang_code, "es", 2) == 0) return "La sesión del acelerador %s cambió durante la finalización.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La session de l’accélérateur %s a changé pendant la finalisation.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Sitzung des %s-Beschleunigers wurde während des Abschlusses geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La sessione dell’acceleratore %s è cambiata durante il completamento.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συνεδρία του επιταχυντή %s άλλαξε κατά την ολοκλήρωση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сеанс ускорителя %s изменился во время завершения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器会话在完成期间发生了变化。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器工作階段在完成期間發生了變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "完了処理中に%sアクセラレータのセッションが変更されました。";
        return "%s accelerator session changed during finish.";
    }
    if (strcmp(msg, "%s accelerator session changed while publishing results.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s accelerator session changed while publishing results.";
        if (strncmp(lang_code, "es", 2) == 0) return "La sesión del acelerador %s cambió mientras se publicaban los resultados.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La session de l’accélérateur %s a changé pendant la publication des résultats.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Sitzung des %s-Beschleunigers wurde während der Veröffentlichung der Ergebnisse geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La sessione dell’acceleratore %s è cambiata durante la pubblicazione dei risultati.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συνεδρία του επιταχυντή %s άλλαξε κατά τη δημοσίευση των αποτελεσμάτων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сеанс ускорителя %s изменился во время публикации результатов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器会话在发布结果时发生了变化。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器工作階段在發佈結果時發生了變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "結果の公開中に%sアクセラレータのセッションが変更されました。";
        return "%s accelerator session changed while publishing results.";
    }
    if (strcmp(msg, "%s failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "%s falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "%s ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "%s non è riuscito.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το %s απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sに失敗しました。";
        return "%s failed.";
    }
    if (strcmp(msg, "%s is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "%s no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s n’est pas disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "%s ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "%s non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το %s δεν είναι διαθέσιμο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s недоступен.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sは利用できません。";
        return "%s is unavailable.";
    }
    if (strcmp(msg, "%s() is not defined\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s() is not defined\n";
        if (strncmp(lang_code, "es", 2) == 0) return "%s() no está definida\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s() n’est pas définie\n";
        if (strncmp(lang_code, "de", 2) == 0) return "%s() ist nicht definiert\n";
        if (strncmp(lang_code, "it", 2) == 0) return "%s() non è definita\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η %s() δεν έχει οριστεί\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s() не определена\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s() 未定义\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s() 未定義\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s()が定義されていません\n";
        return "%s() is not defined\n";
    }
    if (strcmp(msg, "%s() must have zero or one parameter\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s() must have zero or one parameter\n";
        if (strncmp(lang_code, "es", 2) == 0) return "%s() debe tener cero o un parámetro\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s() doit avoir zéro ou un paramètre\n";
        if (strncmp(lang_code, "de", 2) == 0) return "%s() muss null oder einen Parameter haben\n";
        if (strncmp(lang_code, "it", 2) == 0) return "%s() deve avere zero o un parametro\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η %s() πρέπει να έχει μηδέν ή μία παράμετρο\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s() должна иметь ноль или один параметр\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s() 必须有零个或一个形参\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s() 必須有零個或一個參數\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s()には0個または1個のパラメータが必要です\n";
        return "%s() must have zero or one parameter\n";
    }
    if (strcmp(msg, "%s(): Function arguments not match.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s(): Function arguments not match.";
        if (strncmp(lang_code, "es", 2) == 0) return "%s(): Los argumentos de la función no coinciden.";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s(): Les arguments de la fonction ne correspondent pas.";
        if (strncmp(lang_code, "de", 2) == 0) return "%s(): Die Funktionsargumente stimmen nicht überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "%s(): Gli argomenti della funzione non corrispondono.";
        if (strncmp(lang_code, "el", 2) == 0) return "%s(): Τα ορίσματα της συνάρτησης δεν ταιριάζουν.";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s(): Аргументы функции не совпадают.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s(): 函数参数不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s(): 函式參數不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s(): 関数の引数が一致しません。";
        return "%s(): Function arguments not match.";
    }
    if (strcmp(msg, "%s(): argument type mismatch (expected %s).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s(): argument type mismatch (expected %s).";
        if (strncmp(lang_code, "es", 2) == 0) return "%s(): el tipo del argumento no coincide (se esperaba %s).";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s() : le type de l’argument ne correspond pas (type attendu : %s).";
        if (strncmp(lang_code, "de", 2) == 0) return "%s(): Argumenttyp stimmt nicht überein (%s erwartet).";
        if (strncmp(lang_code, "it", 2) == 0) return "%s(): il tipo dell'argomento non corrisponde (previsto %s).";
        if (strncmp(lang_code, "el", 2) == 0) return "%s(): ασυμφωνία τύπου ορίσματος (αναμενόταν %s).";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s(): несоответствие типа аргумента (ожидался %s).";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s()：实参类型不匹配（应为 %s）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s()：引數型別不相符（預期為 %s）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s(): 引数の型が一致しません（%s が必要です）。";
        return "%s(): argument type mismatch (expected %s).";
    }
    if (strcmp(msg, "%s(): local type mismatch (expected %s).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s(): local type mismatch (expected %s).";
        if (strncmp(lang_code, "es", 2) == 0) return "%s(): el tipo de la variable local no coincide (se esperaba %s).";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s() : le type de la variable locale ne correspond pas (type attendu : %s).";
        if (strncmp(lang_code, "de", 2) == 0) return "%s(): Typ der lokalen Variable stimmt nicht überein (%s erwartet).";
        if (strncmp(lang_code, "it", 2) == 0) return "%s(): il tipo della variabile locale non corrisponde (previsto %s).";
        if (strncmp(lang_code, "el", 2) == 0) return "%s(): ασυμφωνία τύπου τοπικής μεταβλητής (αναμενόταν %s).";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s(): несоответствие типа локальной переменной (ожидался %s).";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s()：局部变量类型不匹配（应为 %s）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s()：區域變數型別不相符（預期為 %s）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s(): ローカル変数の型が一致しません（%s が必要です）。";
        return "%s(): local type mismatch (expected %s).";
    }
    if (strcmp(msg, "%s(): return type mismatch (expected %s).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s(): return type mismatch (expected %s).";
        if (strncmp(lang_code, "es", 2) == 0) return "%s(): el tipo de retorno no coincide (se esperaba %s).";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s() : le type de retour ne correspond pas (type attendu : %s).";
        if (strncmp(lang_code, "de", 2) == 0) return "%s(): Rückgabetyp stimmt nicht überein (%s erwartet).";
        if (strncmp(lang_code, "it", 2) == 0) return "%s(): il tipo restituito non corrisponde (previsto %s).";
        if (strncmp(lang_code, "el", 2) == 0) return "%s(): ασυμφωνία τύπου επιστροφής (αναμενόταν %s).";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s(): несоответствие возвращаемого типа (ожидался %s).";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s()：返回类型不匹配（应为 %s）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s()：傳回型別不相符（預期為 %s）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s(): 戻り値の型が一致しません（%s が必要です）。";
        return "%s(): return type mismatch (expected %s).";
    }
    if (strcmp(msg, "%s: Out of memory while parsing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s: Out of memory while parsing.";
        if (strncmp(lang_code, "es", 2) == 0) return "%s: No hay suficiente memoria.";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s: Mémoire insuffisante lors de l'analyse.";
        if (strncmp(lang_code, "de", 2) == 0) return "%s: Nicht genügend Speicher beim Parsen.";
        if (strncmp(lang_code, "it", 2) == 0) return "%s: Non c'è abbastanza memoria durante il parsing.";
        if (strncmp(lang_code, "el", 2) == 0) return "%s: Δεν υπάρχει αρκετή μνήμη κατά την ανάλυση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s: Недостаточно памяти при разборе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s：解析时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s: 解析時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s: メモリが足りません。";
        return "%s: Out of memory while parsing.";
    }
    if (strcmp(msg, "%s:%d: Error: %s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "%s:%d: Error: %s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "%s:%d: Error: %s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "%s:%d: Erreur: %s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "%s:%d: Fehler: %s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "%s:%d: Errore: %s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "%s:%d: Σφάλμα: %s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "%s:%d: Ошибка: %s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s:%d: 错误: %s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s:%d: 錯誤: %s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s:%d: エラー: %s\n";
        return "%s:%d: Error: %s\n";
    }
    if (strcmp(msg, "--gpu-list may be specified only once.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "--gpu-list may be specified only once.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "--gpu-list solo puede especificarse una vez.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "--gpu-list ne peut être spécifiée qu’une seule fois.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "--gpu-list darf nur einmal angegeben werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "--gpu-list può essere specificata una sola volta.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η --gpu-list μπορεί να καθοριστεί μόνο μία φορά.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Параметр --gpu-list можно указать только один раз.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "--gpu-list 只能指定一次。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "--gpu-list 只能指定一次。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "--gpu-list は一度だけ指定できます。\n";
        return "--gpu-list may be specified only once.\n";
    }
    if (strcmp(msg, "--gpu-list must be used by itself.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "--gpu-list must be used by itself.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "--gpu-list debe usarse por sí sola.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "--gpu-list doit être utilisée seule.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "--gpu-list muss allein verwendet werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "--gpu-list deve essere usata da sola.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η --gpu-list πρέπει να χρησιμοποιείται μόνη της.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Параметр --gpu-list должен использоваться отдельно.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "--gpu-list 必须单独使用。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "--gpu-list 必須單獨使用。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "--gpu-list は単独で使用する必要があります。\n";
        return "--gpu-list must be used by itself.\n";
    }
    if (strcmp(msg, "A __fast array index must be int or long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast array index must be int or long.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un índice de array __fast debe ser int o long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un index de tableau __fast doit être int ou long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein __fast-Array-Index muss int oder long sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un indice di array __fast deve essere int o long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένας δείκτης πίνακα __fast πρέπει να είναι int ή long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс массива __fast должен иметь тип int или long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 数组索引必须为 int 或 long。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 陣列索引必須為 int 或 long。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 配列のインデックスは int または long である必要があります。";
        return "A __fast array index must be int or long.";
    }
    if (strcmp(msg, "A __fast assignment requires an exact type match.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast assignment requires an exact type match.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una asignación __fast requiere una coincidencia exacta de tipos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une affectation __fast exige une correspondance exacte des types.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine __fast-Zuweisung erfordert eine exakte Typübereinstimmung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un'assegnazione __fast richiede una corrispondenza esatta dei tipi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια ανάθεση __fast απαιτεί ακριβή αντιστοίχιση τύπων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Присваивание __fast требует точного совпадения типов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 赋值要求类型完全匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 指派要求型別完全相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 代入では型が完全に一致する必要があります。";
        return "A __fast assignment requires an exact type match.";
    }
    if (strcmp(msg, "A __fast assignment target must be a typed local.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast assignment target must be a typed local.";
        if (strncmp(lang_code, "es", 2) == 0) return "El destino de una asignación __fast debe ser una variable local con tipo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La cible d’une affectation __fast doit être une variable locale typée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Ziel einer __fast-Zuweisung muss eine typisierte lokale Variable sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "La destinazione di un'assegnazione __fast deve essere una variabile locale tipizzata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο στόχος ανάθεσης __fast πρέπει να είναι τοπική μεταβλητή με τύπο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Цель присваивания __fast должна быть типизированной локальной переменной.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 赋值目标必须是有类型的局部变量。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 指派目標必須是有型別的區域變數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 代入の対象は型付きローカル変数である必要があります。";
        return "A __fast assignment target must be a typed local.";
    }
    if (strcmp(msg, "A __fast binary intrinsic requires operands of the same type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast binary intrinsic requires operands of the same type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función intrínseca binaria __fast requiere operandos del mismo tipo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction intrinsèque binaire __fast exige des opérandes de même type.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine binäre intrinsische __fast-Funktion erfordert Operanden desselben Typs.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione intrinseca binaria __fast richiede operandi dello stesso tipo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια δυαδική ενσωματωμένη συνάρτηση __fast απαιτεί τελεστέους του ίδιου τύπου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Бинарная встроенная функция __fast требует операнды одного типа.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 二元内置函数要求操作数类型相同。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 二元內建函式要求運算元型別相同。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の二項組み込み関数ではオペランドの型が同じである必要があります。";
        return "A __fast binary intrinsic requires operands of the same type.";
    }
    if (strcmp(msg, "A __fast binary operation requires equal primitive operand types.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast binary operation requires equal primitive operand types.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una operación binaria __fast requiere tipos primitivos de operandos iguales.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une opération binaire __fast exige des types primitifs d’opérandes identiques.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine binäre __fast-Operation erfordert identische primitive Operandentypen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un'operazione binaria __fast richiede tipi primitivi degli operandi uguali.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια δυαδική πράξη __fast απαιτεί ίδιους πρωτογενείς τύπους τελεστέων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Бинарная операция __fast требует одинаковые примитивные типы операндов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 二元运算要求操作数的基本类型相同。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 二元運算要求運算元的基本型別相同。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の二項演算ではオペランドのプリミティブ型が同一である必要があります。";
        return "A __fast binary operation requires equal primitive operand types.";
    }
    if (strcmp(msg, "A __fast condition must have type int.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast condition must have type int.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una condición __fast debe tener el tipo int.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une condition __fast doit être de type int.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine __fast-Bedingung muss den Typ int haben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una condizione __fast deve avere tipo int.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια συνθήκη __fast πρέπει να έχει τύπο int.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Условие __fast должно иметь тип int.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 条件的类型必须为 int。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 條件的型別必須為 int。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 条件の型は int である必要があります。";
        return "A __fast condition must have type int.";
    }
    if (strcmp(msg, "A __fast expression is too deeply nested.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast expression is too deeply nested.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una expresión __fast está anidada a demasiada profundidad.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une expression __fast est imbriquée trop profondément.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein __fast-Ausdruck ist zu tief verschachtelt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un'espressione __fast è annidata troppo profondamente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια έκφραση __fast είναι υπερβολικά βαθιά εμφωλευμένη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выражение __fast имеет слишком большую глубину вложенности.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 表达式嵌套过深。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 運算式巢狀層級過深。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 式のネストが深すぎます。";
        return "A __fast expression is too deeply nested.";
    }
    if (strcmp(msg, "A __fast func may call only a direct __fast function or intrinsic.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast func may call only a direct __fast function or intrinsic.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una func __fast solo puede llamar directamente a una función __fast o una función intrínseca.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une func __fast ne peut appeler directement qu’une fonction __fast ou une fonction intrinsèque.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine __fast-func darf nur eine direkte __fast-Funktion oder eine intrinsische Funktion aufrufen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una func __fast può chiamare direttamente solo una funzione __fast o una funzione intrinseca.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια func __fast μπορεί να καλεί απευθείας μόνο μια συνάρτηση __fast ή μια ενσωματωμένη συνάρτηση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "func __fast может напрямую вызывать только функцию __fast или встроенную функцию.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 只能直接调用 __fast 函数或内置函数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 只能直接呼叫 __fast 函式或內建函式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func は直接の __fast 関数または組み込み関数のみ呼び出せます。";
        return "A __fast func may call only a direct __fast function or intrinsic.";
    }
    if (strcmp(msg, "A __fast func must declare its return type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast func must declare its return type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una func __fast debe declarar su tipo de retorno.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une func __fast doit déclarer son type de retour.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine __fast-func muss ihren Rückgabetyp deklarieren.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una func __fast deve dichiarare il proprio tipo restituito.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια func __fast πρέπει να δηλώνει τον τύπο επιστροφής της.";
        if (strncmp(lang_code, "ru", 2) == 0) return "func __fast должна объявлять возвращаемый тип.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 必须声明其返回类型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 必須宣告其傳回型別。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func は戻り値の型を宣言する必要があります。";
        return "A __fast func must declare its return type.";
    }
    if (strcmp(msg, "A __fast func parameter must be primitive or shaped rpacked.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast func parameter must be primitive or shaped rpacked.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un parámetro de func __fast debe ser primitivo o un rpacked con forma.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un paramètre de func __fast doit être primitif ou un rpacked doté d’une forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Parameter einer __fast-func muss primitiv oder ein rpacked mit Form sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un parametro di func __fast deve essere primitivo o un rpacked con forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια παράμετρος func __fast πρέπει να είναι πρωτογενής ή rpacked με σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Параметр func __fast должен быть примитивным или rpacked с формой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 形参必须为基本类型或带形状的 rpacked。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 參數必須為基本型別或具有形狀的 rpacked。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func のパラメータはプリミティブ型または形状付き rpacked である必要があります。";
        return "A __fast func parameter must be primitive or shaped rpacked.";
    }
    if (strcmp(msg, "A __fast func return type must be void, int, long, float, or double.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast func return type must be void, int, long, float, or double.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tipo de retorno de una func __fast debe ser void, int, long, float o double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le type de retour d’une func __fast doit être void, int, long, float ou double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Rückgabetyp einer __fast-func muss void, int, long, float oder double sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il tipo restituito da una func __fast deve essere void, int, long, float o double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τύπος επιστροφής μιας func __fast πρέπει να είναι void, int, long, float ή double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Возвращаемый тип func __fast должен быть void, int, long, float или double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 的返回类型必须为 void、int、long、float 或 double。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 的傳回型別必須為 void、int、long、float 或 double。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func の戻り値の型は void、int、long、float、double のいずれかである必要があります。";
        return "A __fast func return type must be void, int, long, float, or double.";
    }
    if (strcmp(msg, "A __fast func supports only ranged for loops.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast func supports only ranged for loops.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una func __fast solo admite bucles for con rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une func __fast ne prend en charge que les boucles for avec plage.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine __fast-func unterstützt nur bereichsgebundene for-Schleifen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una func __fast supporta solo cicli for con intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια func __fast υποστηρίζει μόνο βρόχους for με εύρος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "func __fast поддерживает только циклы for с диапазоном.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 仅支持范围 for 循环。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 僅支援範圍 for 迴圈。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func は範囲指定の for ループのみサポートします。";
        return "A __fast func supports only ranged for loops.";
    }
    if (strcmp(msg, "A __fast function name conflicts with a compiler intrinsic.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast function name conflicts with a compiler intrinsic.";
        if (strncmp(lang_code, "es", 2) == 0) return "El nombre de una función __fast entra en conflicto con una función intrínseca del compilador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nom d’une fonction __fast entre en conflit avec une fonction intrinsèque du compilateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Name einer __fast-Funktion steht im Konflikt mit einer intrinsischen Compiler-Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il nome di una funzione __fast è in conflitto con una funzione intrinseca del compilatore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όνομα μιας συνάρτησης __fast συγκρούεται με ενσωματωμένη συνάρτηση του μεταγλωττιστή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Имя функции __fast конфликтует со встроенной функцией компилятора.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 函数名称与编译器内置函数冲突。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 函式名稱與編譯器內建函式衝突。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 関数名がコンパイラの組み込み関数と競合しています。";
        return "A __fast function name conflicts with a compiler intrinsic.";
    }
    if (strcmp(msg, "A __fast integer operation requires int or long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast integer operation requires int or long.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una operación entera __fast requiere int o long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une opération entière __fast exige int ou long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine ganzzahlige __fast-Operation erfordert int oder long.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un'operazione intera __fast richiede int o long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια ακέραια πράξη __fast απαιτεί int ή long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Целочисленная операция __fast требует тип int или long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 整数运算要求类型为 int 或 long。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 整數運算要求型別為 int 或 long。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の整数演算には int または long が必要です。";
        return "A __fast integer operation requires int or long.";
    }
    if (strcmp(msg, "A __fast intrinsic requires a statically typed numeric argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast intrinsic requires a statically typed numeric argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función intrínseca __fast requiere un argumento numérico con tipo estático.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction intrinsèque __fast exige un argument numérique typé statiquement.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine intrinsische __fast-Funktion erfordert ein statisch typisiertes numerisches Argument.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione intrinseca __fast richiede un argomento numerico tipizzato staticamente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια ενσωματωμένη συνάρτηση __fast απαιτεί στατικά τυποποιημένο αριθμητικό όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Встроенная функция __fast требует числовой аргумент со статическим типом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 内置函数要求实参为静态类型的数值。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 內建函式要求引數為靜態型別的數值。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 組み込み関数には静的に型付けされた数値引数が必要です。";
        return "A __fast intrinsic requires a statically typed numeric argument.";
    }
    if (strcmp(msg, "A __fast logical operand must have type int.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast logical operand must have type int.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un operando lógico __fast debe tener el tipo int.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un opérande logique __fast doit être de type int.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein logischer __fast-Operand muss den Typ int haben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un operando logico __fast deve avere tipo int.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένας λογικός τελεστέος __fast πρέπει να έχει τύπο int.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Логический операнд __fast должен иметь тип int.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 逻辑操作数的类型必须为 int。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 邏輯運算元的型別必須為 int。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の論理オペランドの型は int である必要があります。";
        return "A __fast logical operand must have type int.";
    }
    if (strcmp(msg, "A __fast numeric intrinsic requires numeric arguments.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast numeric intrinsic requires numeric arguments.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función intrínseca numérica __fast requiere argumentos numéricos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction intrinsèque numérique __fast exige des arguments numériques.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine numerische intrinsische __fast-Funktion erfordert numerische Argumente.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione intrinseca numerica __fast richiede argomenti numerici.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια αριθμητική ενσωματωμένη συνάρτηση __fast απαιτεί αριθμητικά ορίσματα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Числовая встроенная функция __fast требует числовые аргументы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 数值内置函数要求数值实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 數值內建函式要求數值引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の数値組み込み関数には数値引数が必要です。";
        return "A __fast numeric intrinsic requires numeric arguments.";
    }
    if (strcmp(msg, "A __fast packed access is provably out of bounds.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast packed access is provably out of bounds.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se puede demostrar que un acceso packed __fast está fuera de los límites.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Il est démontrable qu’un accès packed __fast est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein packed-Zugriff in __fast liegt nachweislich außerhalb der Grenzen.";
        if (strncmp(lang_code, "it", 2) == 0) return "È dimostrabile che un accesso packed __fast è fuori dai limiti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια πρόσβαση packed __fast είναι αποδεδειγμένα εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Доказано, что доступ __fast к packed выходит за границы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "可证明 __fast packed 访问越界。";
        if (strncmp(lang_code, "tw", 2) == 0) return "可證明 __fast packed 存取越界。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の packed アクセスは明らかに境界外です。";
        return "A __fast packed access is provably out of bounds.";
    }
    if (strcmp(msg, "A __fast packed store requires an exact element type match.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast packed store requires an exact element type match.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un almacenamiento packed __fast requiere una coincidencia exacta del tipo de elemento.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un stockage packed __fast exige une correspondance exacte du type d’élément.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein packed-Speichervorgang in __fast erfordert eine exakte Übereinstimmung des Elementtyps.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una memorizzazione packed __fast richiede una corrispondenza esatta del tipo di elemento.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια αποθήκευση packed __fast απαιτεί ακριβή αντιστοίχιση τύπου στοιχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Запись __fast в packed требует точного совпадения типа элемента.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast packed 存储要求元素类型完全匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast packed 儲存要求元素型別完全相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の packed への格納では要素型が完全に一致する必要があります。";
        return "A __fast packed store requires an exact element type match.";
    }
    if (strcmp(msg, "A __fast parameter shape has more than 8 dimensions.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast parameter shape has more than 8 dimensions.";
        if (strncmp(lang_code, "es", 2) == 0) return "La forma de un parámetro __fast tiene más de 8 dimensiones.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La forme d’un paramètre __fast comporte plus de 8 dimensions.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Form eines __fast-Parameters hat mehr als 8 Dimensionen.";
        if (strncmp(lang_code, "it", 2) == 0) return "La forma di un parametro __fast ha più di 8 dimensioni.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το σχήμα μιας παραμέτρου __fast έχει περισσότερες από 8 διαστάσεις.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Форма параметра __fast имеет более 8 измерений.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形参的形状超过 8 维。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 參數的形狀超過 8 維。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast パラメータの形状が 8 次元を超えています。";
        return "A __fast parameter shape has more than 8 dimensions.";
    }
    if (strcmp(msg, "A __fast ranged loop requires matching int or long bounds.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast ranged loop requires matching int or long bounds.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un bucle con rango __fast requiere límites int o long coincidentes.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une boucle avec plage __fast exige des limites correspondantes de type int ou long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine bereichsgebundene __fast-Schleife erfordert übereinstimmende Grenzen vom Typ int oder long.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un ciclo con intervallo __fast richiede limiti int o long corrispondenti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένας βρόχος εύρους __fast απαιτεί αντίστοιχα όρια int ή long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Цикл __fast с диапазоном требует совпадающие границы типа int или long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 范围循环要求边界均为相同的 int 或 long 类型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 範圍迴圈要求邊界均為相同的 int 或 long 型別。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の範囲ループでは境界の型がともに int または long で一致する必要があります。";
        return "A __fast ranged loop requires matching int or long bounds.";
    }
    if (strcmp(msg, "A __fast return value does not match its declared type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast return value does not match its declared type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un valor de retorno __fast no coincide con su tipo declarado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une valeur de retour __fast ne correspond pas à son type déclaré.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein __fast-Rückgabewert entspricht nicht seinem deklarierten Typ.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un valore restituito __fast non corrisponde al tipo dichiarato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια τιμή επιστροφής __fast δεν αντιστοιχεί στον δηλωμένο τύπο της.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Возвращаемое значение __fast не соответствует объявленному типу.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 返回值与其声明的类型不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 傳回值與其宣告的型別不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の戻り値が宣言された型と一致しません。";
        return "A __fast return value does not match its declared type.";
    }
    if (strcmp(msg, "A __fast shape extent must be int or long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast shape extent must be int or long.";
        if (strncmp(lang_code, "es", 2) == 0) return "La extensión de una forma __fast debe ser int o long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’étendue d’une forme __fast doit être int ou long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausdehnung einer __fast-Form muss int oder long sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'estensione di una forma __fast deve essere int o long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έκταση ενός σχήματος __fast πρέπει να είναι int ή long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер измерения формы __fast должен иметь тип int или long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形状的维度大小必须为 int 或 long。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 形狀的維度大小必須為 int 或 long。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 形状の次元サイズは int または long である必要があります。";
        return "A __fast shape extent must be int or long.";
    }
    if (strcmp(msg, "A __fast shape extent must be positive.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast shape extent must be positive.";
        if (strncmp(lang_code, "es", 2) == 0) return "La extensión de una forma __fast debe ser positiva.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’étendue d’une forme __fast doit être positive.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausdehnung einer __fast-Form muss positiv sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'estensione di una forma __fast deve essere positiva.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έκταση ενός σχήματος __fast πρέπει να είναι θετική.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер измерения формы __fast должен быть положительным.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形状的维度大小必须为正数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 形狀的維度大小必須為正數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 形状の次元サイズは正である必要があります。";
        return "A __fast shape extent must be positive.";
    }
    if (strcmp(msg, "A __fast subscript base must be a shaped rpacked parameter.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast subscript base must be a shaped rpacked parameter.";
        if (strncmp(lang_code, "es", 2) == 0) return "La base de un subíndice __fast debe ser un parámetro rpacked con forma.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La base d’un indice __fast doit être un paramètre rpacked doté d’une forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Basis eines __fast-Indexausdrucks muss ein rpacked-Parameter mit Form sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "La base di un pedice __fast deve essere un parametro rpacked con forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η βάση ενός δείκτη __fast πρέπει να είναι παράμετρος rpacked με σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Основание индекса __fast должно быть параметром rpacked с формой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 下标的基值必须是带形状的 rpacked 形参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 下標的基底必須是具有形狀的 rpacked 參數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast 添字の基底は形状付き rpacked パラメータである必要があります。";
        return "A __fast subscript base must be a shaped rpacked parameter.";
    }
    if (strcmp(msg, "A __fast transcendental intrinsic requires float or double.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast transcendental intrinsic requires float or double.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función intrínseca trascendental __fast requiere float o double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction intrinsèque transcendante __fast exige float ou double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine transzendente intrinsische __fast-Funktion erfordert float oder double.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione intrinseca trascendente __fast richiede float o double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια υπερβατική ενσωματωμένη συνάρτηση __fast απαιτεί float ή double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Трансцендентная встроенная функция __fast требует тип float или double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 超越内置函数要求类型为 float 或 double。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 超越內建函式要求型別為 float 或 double。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の超越組み込み関数には float または double が必要です。";
        return "A __fast transcendental intrinsic requires float or double.";
    }
    if (strcmp(msg, "A __fast unary operand must be primitive.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A __fast unary operand must be primitive.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un operando unario __fast debe ser primitivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un opérande unaire __fast doit être primitif.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein unärer __fast-Operand muss primitiv sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un operando unario __fast deve essere primitivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένας μοναδιαίος τελεστέος __fast πρέπει να είναι πρωτογενής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Унарный операнд __fast должен иметь примитивный тип.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 一元操作数必须为基本类型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 一元運算元必須為基本型別。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の単項オペランドはプリミティブ型である必要があります。";
        return "A __fast unary operand must be primitive.";
    }
    if (strcmp(msg, "A binary __fast intrinsic requires operands of the same type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A binary __fast intrinsic requires operands of the same type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función intrínseca binaria __fast requiere operandos del mismo tipo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction intrinsèque binaire __fast exige des opérandes de même type.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine binäre intrinsische __fast-Funktion erfordert Operanden desselben Typs.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione intrinseca binaria __fast richiede operandi dello stesso tipo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια δυαδική ενσωματωμένη συνάρτηση __fast απαιτεί τελεστέους του ίδιου τύπου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Бинарная встроенная функция __fast требует операнды одного типа.";
        if (strncmp(lang_code, "zh", 2) == 0) return "二元 __fast 内置函数要求操作数类型相同。";
        if (strncmp(lang_code, "tw", 2) == 0) return "二元 __fast 內建函式要求運算元型別相同。";
        if (strncmp(lang_code, "ja", 2) == 0) return "二項 __fast 組み込み関数ではオペランドの型が同じである必要があります。";
        return "A binary __fast intrinsic requires operands of the same type.";
    }
    if (strcmp(msg, "A direct __fast call argument does not match the callee type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A direct __fast call argument does not match the callee type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un argumento de una llamada directa __fast no coincide con el tipo de la función llamada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un argument d’un appel direct __fast ne correspond pas au type de la fonction appelée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Argument eines direkten __fast-Aufrufs entspricht nicht dem Typ der aufgerufenen Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un argomento di una chiamata diretta __fast non corrisponde al tipo della funzione chiamata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένα όρισμα άμεσης κλήσης __fast δεν αντιστοιχεί στον τύπο της καλούμενης συνάρτησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент прямого вызова __fast не соответствует типу вызываемой функции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "直接 __fast 调用的实参与被调用方类型不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "直接 __fast 呼叫的引數與被呼叫端型別不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接 __fast 呼び出しの引数が呼び出し先の型と一致しません。";
        return "A direct __fast call argument does not match the callee type.";
    }
    if (strcmp(msg, "A direct __fast call has the wrong argument count.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A direct __fast call has the wrong argument count.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una llamada directa __fast tiene una cantidad incorrecta de argumentos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un appel direct __fast comporte un nombre incorrect d’arguments.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein direkter __fast-Aufruf hat die falsche Anzahl von Argumenten.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una chiamata diretta __fast ha un numero errato di argomenti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια άμεση κλήση __fast έχει εσφαλμένο πλήθος ορισμάτων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Прямой вызов __fast содержит неверное количество аргументов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "直接 __fast 调用的实参数量错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "直接 __fast 呼叫的引數數量錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接 __fast 呼び出しの引数の数が正しくありません。";
        return "A direct __fast call has the wrong argument count.";
    }
    if (strcmp(msg, "A direct __fast call packed argument has the wrong element type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A direct __fast call packed argument has the wrong element type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un argumento packed de una llamada directa __fast tiene un tipo de elemento incorrecto.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un argument packed d’un appel direct __fast possède un type d’élément incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein packed-Argument eines direkten __fast-Aufrufs hat den falschen Elementtyp.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un argomento packed di una chiamata diretta __fast ha il tipo di elemento errato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένα όρισμα packed άμεσης κλήσης __fast έχει εσφαλμένο τύπο στοιχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент packed прямого вызова __fast имеет неверный тип элемента.";
        if (strncmp(lang_code, "zh", 2) == 0) return "直接 __fast 调用的 packed 实参元素类型错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "直接 __fast 呼叫的 packed 引數元素型別錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接 __fast 呼び出しの packed 引数の要素型が正しくありません。";
        return "A direct __fast call packed argument has the wrong element type.";
    }
    if (strcmp(msg, "A direct __fast call packed argument must be a parameter.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A direct __fast call packed argument must be a parameter.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un argumento packed de una llamada directa __fast debe ser un parámetro.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un argument packed d’un appel direct __fast doit être un paramètre.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein packed-Argument eines direkten __fast-Aufrufs muss ein Parameter sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un argomento packed di una chiamata diretta __fast deve essere un parametro.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένα όρισμα packed άμεσης κλήσης __fast πρέπει να είναι παράμετρος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент packed прямого вызова __fast должен быть параметром.";
        if (strncmp(lang_code, "zh", 2) == 0) return "直接 __fast 调用的 packed 实参必须是形参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "直接 __fast 呼叫的 packed 引數必須是參數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接 __fast 呼び出しの packed 引数はパラメータである必要があります。";
        return "A direct __fast call packed argument must be a parameter.";
    }
    if (strcmp(msg, "A direct __fast call packed shape does not match the callee view.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A direct __fast call packed shape does not match the callee view.";
        if (strncmp(lang_code, "es", 2) == 0) return "La forma packed de una llamada directa __fast no coincide con la vista de la función llamada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La forme packed d’un appel direct __fast ne correspond pas à la vue de la fonction appelée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die packed-Form eines direkten __fast-Aufrufs entspricht nicht der Ansicht der aufgerufenen Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "La forma packed di una chiamata diretta __fast non corrisponde alla vista della funzione chiamata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το σχήμα packed μιας άμεσης κλήσης __fast δεν αντιστοιχεί στην προβολή της καλούμενης συνάρτησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Форма packed прямого вызова __fast не соответствует представлению вызываемой функции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "直接 __fast 调用的 packed 形状与被调用方视图不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "直接 __fast 呼叫的 packed 形狀與被呼叫端檢視不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接 __fast 呼び出しの packed 形状が呼び出し先のビューと一致しません。";
        return "A direct __fast call packed shape does not match the callee view.";
    }
    if (strcmp(msg, "A direct __fast call packed shape rank does not match.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A direct __fast call packed shape rank does not match.";
        if (strncmp(lang_code, "es", 2) == 0) return "El número de dimensiones de la forma packed de una llamada directa __fast no coincide.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nombre de dimensions de la forme packed d’un appel direct __fast ne correspond pas.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dimensionsanzahl der packed-Form eines direkten __fast-Aufrufs stimmt nicht überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il numero di dimensioni della forma packed di una chiamata diretta __fast non corrisponde.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο αριθμός διαστάσεων του σχήματος packed μιας άμεσης κλήσης __fast δεν αντιστοιχεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Число измерений формы packed прямого вызова __fast не совпадает.";
        if (strncmp(lang_code, "zh", 2) == 0) return "直接 __fast 调用的 packed 形状维数不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "直接 __fast 呼叫的 packed 形狀維度數不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接 __fast 呼び出しの packed 形状の次元数が一致しません。";
        return "A direct __fast call packed shape rank does not match.";
    }
    if (strcmp(msg, "A dynamic __fast shape extent must name an int or long parameter.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A dynamic __fast shape extent must name an int or long parameter.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una extensión dinámica de forma __fast debe nombrar un parámetro int o long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une étendue dynamique de forme __fast doit désigner un paramètre int ou long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine dynamische Ausdehnung einer __fast-Form muss einen int- oder long-Parameter benennen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un'estensione dinamica di forma __fast deve indicare un parametro int o long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια δυναμική έκταση σχήματος __fast πρέπει να ονομάζει παράμετρο int ή long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Динамический размер измерения формы __fast должен ссылаться на параметр типа int или long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 动态形状的维度大小必须指定一个 int 或 long 形参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 動態形狀的維度大小必須指定一個 int 或 long 參數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast の動的な形状の次元サイズは int または long パラメータを指定する必要があります。";
        return "A dynamic __fast shape extent must name an int or long parameter.";
    }
    if (strcmp(msg, "A function return type cannot have a shape.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A function return type cannot have a shape.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tipo de retorno de una función no puede tener forma.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le type de retour d’une fonction ne peut pas avoir de forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Rückgabetyp einer Funktion darf keine Form haben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il tipo restituito da una funzione non può avere una forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τύπος επιστροφής μιας συνάρτησης δεν μπορεί να έχει σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Возвращаемый тип функции не может иметь форму.";
        if (strncmp(lang_code, "zh", 2) == 0) return "函数返回类型不能带有形状。";
        if (strncmp(lang_code, "tw", 2) == 0) return "函式傳回型別不能具有形狀。";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数の戻り値の型に形状を指定することはできません。";
        return "A function return type cannot have a shape.";
    }
    if (strcmp(msg, "A local used inside __fast func must have an exact primitive type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A local used inside __fast func must have an exact primitive type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una variable local usada dentro de una func __fast debe tener un tipo primitivo exacto.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une variable locale utilisée dans une func __fast doit avoir un type primitif exact.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine in einer __fast-func verwendete lokale Variable muss einen exakten primitiven Typ haben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una variabile locale usata in una func __fast deve avere un tipo primitivo esatto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια τοπική μεταβλητή που χρησιμοποιείται μέσα σε func __fast πρέπει να έχει ακριβή πρωτογενή τύπο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Локальная переменная внутри func __fast должна иметь точный примитивный тип.";
        if (strncmp(lang_code, "zh", 2) == 0) return "在 __fast func 内使用的局部变量必须具有确切的基本类型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "在 __fast func 內使用的區域變數必須具有確切的基本型別。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func 内で使用するローカル変数は厳密なプリミティブ型を持つ必要があります。";
        return "A local used inside __fast func must have an exact primitive type.";
    }
    if (strcmp(msg, "A multi-dimensional subscript requires a directly named shaped parameter.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A multi-dimensional subscript requires a directly named shaped parameter.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un subíndice multidimensional requiere un parámetro con forma nombrado directamente.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un indice multidimensionnel exige un paramètre doté d’une forme et nommé directement.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein mehrdimensionaler Indexausdruck erfordert einen direkt benannten Parameter mit Form.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un pedice multidimensionale richiede un parametro con forma indicato direttamente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένας πολυδιάστατος δείκτης απαιτεί άμεσα κατονομασμένη παράμετρο με σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Многомерный индекс требует непосредственно именованный параметр с формой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "多维下标要求直接命名的带形状形参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "多維下標要求直接命名且具有形狀的參數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "多次元の添字には直接名前を指定した形状付きパラメータが必要です。";
        return "A multi-dimensional subscript requires a directly named shaped parameter.";
    }
    if (strcmp(msg, "A non-void __fast func must return a value.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A non-void __fast func must return a value.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una func __fast que no sea void debe devolver un valor.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une func __fast non-void doit retourner une valeur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine __fast-func mit einem anderen Rückgabetyp als void muss einen Wert zurückgeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una func __fast non void deve restituire un valore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια func __fast που δεν είναι void πρέπει να επιστρέφει τιμή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "func __fast с типом, отличным от void, должна возвращать значение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "非 void 的 __fast func 必须返回值。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非 void 的 __fast func 必須傳回值。";
        if (strncmp(lang_code, "ja", 2) == 0) return "void 以外の __fast func は値を返す必要があります。";
        return "A non-void __fast func must return a value.";
    }
    if (strcmp(msg, "A primitive __fast parameter cannot have a shape.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A primitive __fast parameter cannot have a shape.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un parámetro primitivo __fast no puede tener forma.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un paramètre primitif __fast ne peut pas avoir de forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein primitiver __fast-Parameter darf keine Form haben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un parametro primitivo __fast non può avere una forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια πρωτογενής παράμετρος __fast δεν μπορεί να έχει σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Примитивный параметр __fast не может иметь форму.";
        if (strncmp(lang_code, "zh", 2) == 0) return "基本类型的 __fast 形参不能带有形状。";
        if (strncmp(lang_code, "tw", 2) == 0) return "基本型別的 __fast 參數不能具有形狀。";
        if (strncmp(lang_code, "ja", 2) == 0) return "プリミティブ型の __fast パラメータに形状を指定することはできません。";
        return "A primitive __fast parameter cannot have a shape.";
    }
    if (strcmp(msg, "A primitive __fast parameter must use int, long, float, or double exactly.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A primitive __fast parameter must use int, long, float, or double exactly.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un parámetro primitivo __fast debe usar exactamente int, long, float o double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un paramètre primitif __fast doit utiliser exactement int, long, float ou double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein primitiver __fast-Parameter muss exakt int, long, float oder double verwenden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un parametro primitivo __fast deve usare esattamente int, long, float o double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια πρωτογενής παράμετρος __fast πρέπει να χρησιμοποιεί ακριβώς int, long, float ή double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Примитивный параметр __fast должен иметь строго тип int, long, float или double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "基本类型的 __fast 形参必须严格使用 int、long、float 或 double。";
        if (strncmp(lang_code, "tw", 2) == 0) return "基本型別的 __fast 參數必須嚴格使用 int、long、float 或 double。";
        if (strncmp(lang_code, "ja", 2) == 0) return "プリミティブ型の __fast パラメータには厳密に int、long、float、double のいずれかを使用する必要があります。";
        return "A primitive __fast parameter must use int, long, float, or double exactly.";
    }
    if (strcmp(msg, "A restricted packed __fast parameter requires an exact shape.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A restricted packed __fast parameter requires an exact shape.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un parámetro packed restringido __fast requiere una forma exacta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un paramètre packed restreint __fast exige une forme exacte.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein eingeschränkter packed-Parameter in __fast erfordert eine exakte Form.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un parametro packed limitato __fast richiede una forma esatta.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια περιορισμένη παράμετρος packed __fast απαιτεί ακριβές σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ограниченный параметр packed в __fast требует точную форму.";
        if (strncmp(lang_code, "zh", 2) == 0) return "受限的 packed __fast 形参要求确切的形状。";
        if (strncmp(lang_code, "tw", 2) == 0) return "受限的 packed __fast 參數要求確切的形狀。";
        if (strncmp(lang_code, "ja", 2) == 0) return "制限付き packed __fast パラメータには厳密な形状が必要です。";
        return "A restricted packed __fast parameter requires an exact shape.";
    }
    if (strcmp(msg, "A restricted packed type is only valid for a parameter.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A restricted packed type is only valid for a parameter.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un tipo packed restringido solo es válido para un parámetro.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un type packed restreint n’est valide que pour un paramètre.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein eingeschränkter packed-Typ ist nur für einen Parameter gültig.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un tipo packed limitato è valido solo per un parametro.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένας περιορισμένος τύπος packed είναι έγκυρος μόνο για παράμετρο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ограниченный тип packed допустим только для параметра.";
        if (strncmp(lang_code, "zh", 2) == 0) return "受限的 packed 类型仅对形参有效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "受限的 packed 型別僅適用於參數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "制限付き packed 型はパラメータにのみ使用できます。";
        return "A restricted packed type is only valid for a parameter.";
    }
    if (strcmp(msg, "A shaped __fast parameter must be a Packed type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A shaped __fast parameter must be a Packed type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un parámetro __fast con forma debe ser de tipo Packed.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un paramètre __fast doté d’une forme doit être de type Packed.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein __fast-Parameter mit Form muss ein Packed-Typ sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un parametro __fast con forma deve essere di tipo Packed.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια παράμετρος __fast με σχήμα πρέπει να είναι τύπου Packed.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Параметр __fast с формой должен иметь тип Packed.";
        if (strncmp(lang_code, "zh", 2) == 0) return "带形状的 __fast 形参必须为 Packed 类型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "具有形狀的 __fast 參數必須為 Packed 型別。";
        if (strncmp(lang_code, "ja", 2) == 0) return "形状付き __fast パラメータは Packed 型である必要があります。";
        return "A shaped __fast parameter must be a Packed type.";
    }
    if (strcmp(msg, "A transcendental __fast intrinsic requires float or double.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A transcendental __fast intrinsic requires float or double.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función intrínseca trascendental __fast requiere float o double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction intrinsèque transcendante __fast exige float ou double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine transzendente intrinsische __fast-Funktion erfordert float oder double.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione intrinseca trascendente __fast richiede float o double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια υπερβατική ενσωματωμένη συνάρτηση __fast απαιτεί float ή double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Трансцендентная встроенная функция __fast требует тип float или double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "超越 __fast 内置函数要求类型为 float 或 double。";
        if (strncmp(lang_code, "tw", 2) == 0) return "超越 __fast 內建函式要求型別為 float 或 double。";
        if (strncmp(lang_code, "ja", 2) == 0) return "超越 __fast 組み込み関数には float または double が必要です。";
        return "A transcendental __fast intrinsic requires float or double.";
    }
    if (strcmp(msg, "A typed function must return a value.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A typed function must return a value.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función con tipo debe devolver un valor.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction typée doit retourner une valeur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine typisierte Funktion muss einen Wert zurückgeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione tipizzata deve restituire un valore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια συνάρτηση με τύπο πρέπει να επιστρέφει τιμή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Типизированная функция должна возвращать значение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "有类型的函数必须返回值。";
        if (strncmp(lang_code, "tw", 2) == 0) return "有型別的函式必須傳回值。";
        if (strncmp(lang_code, "ja", 2) == 0) return "型付き関数は値を返す必要があります。";
        return "A typed function must return a value.";
    }
    if (strcmp(msg, "A void function cannot return a value.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "A void function cannot return a value.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una función void no puede devolver un valor.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une fonction void ne peut pas retourner de valeur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine void-Funktion darf keinen Wert zurückgeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Una funzione void non può restituire un valore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια συνάρτηση void δεν μπορεί να επιστρέφει τιμή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Функция void не может возвращать значение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "void 函数不能返回值。";
        if (strncmp(lang_code, "tw", 2) == 0) return "void 函式不能傳回值。";
        if (strncmp(lang_code, "ja", 2) == 0) return "void 関数は値を返せません。";
        return "A void function cannot return a value.";
    }
    if (strcmp(msg, "Accelerator Packed argument is missing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator Packed argument is missing.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el argumento Packed del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’argument Packed de l’accélérateur est manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Packed-Argument des Beschleunigers fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Manca l'argomento Packed dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το όρισμα Packed του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует аргумент Packed ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器的 Packed 实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器的 Packed 引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの Packed 引数がありません。";
        return "Accelerator Packed argument is missing.";
    }
    if (strcmp(msg, "Accelerator Packed element type does not match.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator Packed element type does not match.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tipo de elemento Packed del acelerador no coincide.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le type d’élément Packed de l’accélérateur ne correspond pas.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Packed-Elementtyp des Beschleunigers stimmt nicht überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il tipo di elemento Packed dell'acceleratore non corrisponde.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τύπος στοιχείου Packed του επιταχυντή δεν αντιστοιχεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Тип элемента Packed ускорителя не совпадает.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器的 Packed 元素类型不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器的 Packed 元素型別不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの Packed 要素型が一致しません。";
        return "Accelerator Packed element type does not match.";
    }
    if (strcmp(msg, "Accelerator VM and environment do not match.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator VM and environment do not match.";
        if (strncmp(lang_code, "es", 2) == 0) return "La VM del acelerador y el entorno no coinciden.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La VM de l’accélérateur et l’environnement ne correspondent pas.";
        if (strncmp(lang_code, "de", 2) == 0) return "Beschleuniger-VM und Umgebung stimmen nicht überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "La VM dell'acceleratore e l'ambiente non corrispondono.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η VM του επιταχυντή και το περιβάλλον δεν αντιστοιχούν.";
        if (strncmp(lang_code, "ru", 2) == 0) return "VM ускорителя и среда не совпадают.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器 VM 与环境不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器 VM 與環境不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの VM と環境が一致しません。";
        return "Accelerator VM and environment do not match.";
    }
    if (strcmp(msg, "Accelerator access has an invalid effect.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator access has an invalid effect.";
        if (strncmp(lang_code, "es", 2) == 0) return "El acceso del acelerador tiene un efecto no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’accès à l’accélérateur a un effet non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerzugriff hat einen ungültigen Effekt.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'accesso dell'acceleratore ha un effetto non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η πρόσβαση του επιταχυντή έχει μη έγκυρη επίδραση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Доступ ускорителя имеет недопустимый эффект.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器访问具有无效效果。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器存取具有無效效果。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータアクセスの効果が無効です。";
        return "Accelerator access has an invalid effect.";
    }
    if (strcmp(msg, "Accelerator access has no buffer binding.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator access has no buffer binding.";
        if (strncmp(lang_code, "es", 2) == 0) return "El acceso del acelerador no tiene vinculación de búfer.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’accès à l’accélérateur n’a pas de liaison de tampon.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerzugriff hat keine Pufferbindung.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'accesso dell'acceleratore non ha un'associazione di buffer.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η πρόσβαση του επιταχυντή δεν έχει δέσμευση ενδιάμεσης μνήμης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Доступ ускорителя не имеет привязки буфера.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器访问没有缓冲区绑定。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器存取沒有緩衝區繫結。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータアクセスにバッファのバインディングがありません。";
        return "Accelerator access has no buffer binding.";
    }
    if (strcmp(msg, "Accelerator access references an unknown buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator access references an unknown buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "El acceso del acelerador hace referencia a un búfer desconocido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’accès à l’accélérateur fait référence à un tampon inconnu.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerzugriff verweist auf einen unbekannten Puffer.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'accesso dell'acceleratore fa riferimento a un buffer sconosciuto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η πρόσβαση του επιταχυντή αναφέρεται σε άγνωστη ενδιάμεση μνήμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Доступ ускорителя ссылается на неизвестный буфер.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器访问引用了未知缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器存取參考了未知緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータアクセスが不明なバッファを参照しています。";
        return "Accelerator access references an unknown buffer.";
    }
    if (strcmp(msg, "Accelerator argument count changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator argument count changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La cantidad de argumentos del acelerador ha cambiado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nombre d’arguments de l’accélérateur a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Anzahl der Beschleunigerargumente hat sich geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il numero di argomenti dell'acceleratore è cambiato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πλήθος ορισμάτων του επιταχυντή άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Количество аргументов ускорителя изменилось.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器实参数量已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器引數數量已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの引数の数が変更されました。";
        return "Accelerator argument count changed.";
    }
    if (strcmp(msg, "Accelerator argument count is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator argument count is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "La cantidad de argumentos del acelerador es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nombre d’arguments de l’accélérateur est trop élevé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Anzahl der Beschleunigerargumente ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il numero di argomenti dell'acceleratore è troppo elevato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πλήθος ορισμάτων του επιταχυντή είναι υπερβολικά μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Количество аргументов ускорителя слишком велико.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器实参数量过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器引數數量過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの引数の数が多すぎます。";
        return "Accelerator argument count is too large.";
    }
    if (strcmp(msg, "Accelerator backend failed to prepare a program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator backend failed to prepare a program.";
        if (strncmp(lang_code, "es", 2) == 0) return "El backend del acelerador no pudo preparar un programa.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le backend de l’accélérateur n’a pas pu préparer un programme.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Beschleuniger-Backend konnte kein Programm vorbereiten.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il backend dell'acceleratore non è riuscito a preparare un programma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το backend του επιταχυντή απέτυχε να προετοιμάσει πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Бэкенду ускорителя не удалось подготовить программу.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器后端无法准备程序。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器後端無法準備程式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのバックエンドがプログラムを準備できませんでした。";
        return "Accelerator backend failed to prepare a program.";
    }
    if (strcmp(msg, "Accelerator backend returned no program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator backend returned no program.";
        if (strncmp(lang_code, "es", 2) == 0) return "El backend del acelerador no devolvió ningún programa.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le backend de l’accélérateur n’a retourné aucun programme.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Beschleuniger-Backend hat kein Programm zurückgegeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il backend dell'acceleratore non ha restituito alcun programma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το backend του επιταχυντή δεν επέστρεψε πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Бэкенд ускорителя не вернул программу.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器后端未返回程序。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器後端未傳回程式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのバックエンドからプログラムが返されませんでした。";
        return "Accelerator backend returned no program.";
    }
    if (strcmp(msg, "Accelerator buffer access is out of range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator buffer access is out of range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El acceso al búfer del acelerador está fuera de rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’accès au tampon de l’accélérateur est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Zugriff auf den Beschleunigerpuffer liegt außerhalb des Bereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'accesso al buffer dell'acceleratore è fuori intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η πρόσβαση στην ενδιάμεση μνήμη του επιταχυντή είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Доступ к буферу ускорителя вне диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区访问超出范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區存取超出範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのバッファアクセスが範囲外です。";
        return "Accelerator buffer access is out of range.";
    }
    if (strcmp(msg, "Accelerator buffer has no required range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator buffer has no required range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer del acelerador no tiene el rango requerido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon de l’accélérateur n’a pas la plage requise.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerpuffer hat nicht den erforderlichen Bereich.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer dell'acceleratore non ha l'intervallo richiesto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη του επιταχυντή δεν έχει το απαιτούμενο εύρος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер ускорителя не имеет требуемого диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区没有所需范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區沒有必要的範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのバッファに必要な範囲がありません。";
        return "Accelerator buffer has no required range.";
    }
    if (strcmp(msg, "Accelerator buffer has no runtime argument slot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator buffer has no runtime argument slot.";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer del acelerador no tiene una ranura de argumento del entorno de ejecución.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon de l’accélérateur n’a pas d’emplacement d’argument de l’environnement d’exécution.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerpuffer hat keinen Laufzeitargument-Slot.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer dell'acceleratore non ha uno slot per l'argomento dell'ambiente di esecuzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη του επιταχυντή δεν έχει θέση ορίσματος περιβάλλοντος εκτέλεσης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер ускорителя не имеет слота аргумента среды выполнения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区没有运行时实参槽位。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區沒有執行階段引數槽位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのバッファに実行環境引数のスロットがありません。";
        return "Accelerator buffer has no runtime argument slot.";
    }
    if (strcmp(msg, "Accelerator buffer range overflowed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator buffer range overflowed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se produjo un desbordamiento del rango del búfer del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage du tampon de l’accélérateur a subi un dépassement de capacité.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Bereich des Beschleunigerpuffers ist übergelaufen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Si è verificato un overflow dell'intervallo del buffer dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπήρξε υπερχείλιση του εύρους της ενδιάμεσης μνήμης του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение диапазона буфера ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区范围溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區範圍溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのバッファ範囲がオーバーフローしました。";
        return "Accelerator buffer range overflowed.";
    }
    if (strcmp(msg, "Accelerator buffer size overflowed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator buffer size overflowed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se produjo un desbordamiento del tamaño del búfer del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du tampon de l’accélérateur a subi un dépassement de capacité.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe des Beschleunigerpuffers ist übergelaufen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Si è verificato un overflow della dimensione del buffer dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπήρξε υπερχείλιση του μεγέθους της ενδιάμεσης μνήμης του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение размера буфера ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区大小溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區大小溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのバッファサイズがオーバーフローしました。";
        return "Accelerator buffer size overflowed.";
    }
    if (strcmp(msg, "Accelerator context is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator context is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "El contexto del acelerador no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le contexte de l’accélérateur est indisponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerkontext ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il contesto dell'acceleratore non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το περιβάλλον του επιταχυντή δεν είναι διαθέσιμο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Контекст ускорителя недоступен.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器上下文不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器內容無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのコンテキストを使用できません。";
        return "Accelerator context is unavailable.";
    }
    if (strcmp(msg, "Accelerator context reference limit reached.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator context reference limit reached.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se alcanzó el límite de referencias del contexto del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La limite de références du contexte de l’accélérateur est atteinte.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Referenzgrenze des Beschleunigerkontexts wurde erreicht.";
        if (strncmp(lang_code, "it", 2) == 0) return "È stato raggiunto il limite di riferimenti del contesto dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Συμπληρώθηκε το όριο αναφορών του περιβάλλοντος του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Достигнут предел ссылок на контекст ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "已达到加速器上下文引用限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "已達到加速器內容參考限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのコンテキスト参照の上限に達しました。";
        return "Accelerator context reference limit reached.";
    }
    if (strcmp(msg, "Accelerator device buffer is read before definition.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator device buffer is read before definition.";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer del dispositivo del acelerador se lee antes de su definición.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon du périphérique de l’accélérateur est lu avant sa définition.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Gerätepuffer des Beschleunigers wird vor seiner Definition gelesen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer del dispositivo dell'acceleratore viene letto prima della definizione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη συσκευής του επιταχυντή διαβάζεται πριν από τον ορισμό της.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер устройства ускорителя читается до определения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备缓冲区在定义前被读取。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置緩衝區在定義前被讀取。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスバッファが定義前に読み取られています。";
        return "Accelerator device buffer is read before definition.";
    }
    if (strcmp(msg, "Accelerator device buffer size is not positive.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator device buffer size is not positive.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del búfer del dispositivo del acelerador no es positivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du tampon du périphérique de l’accélérateur n’est pas positive.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe des Gerätepuffers des Beschleunigers ist nicht positiv.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del buffer del dispositivo dell'acceleratore non è positiva.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος της ενδιάμεσης μνήμης συσκευής του επιταχυντή δεν είναι θετικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер буфера устройства ускорителя не является положительным.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备缓冲区大小不是正数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置緩衝區大小不是正數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスバッファサイズが正ではありません。";
        return "Accelerator device buffer size is not positive.";
    }
    if (strcmp(msg, "Accelerator device buffer size is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator device buffer size is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del búfer del dispositivo del acelerador es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du tampon du périphérique de l’accélérateur est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe des Gerätepuffers des Beschleunigers ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del buffer del dispositivo dell'acceleratore è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος της ενδιάμεσης μνήμης συσκευής του επιταχυντή είναι υπερβολικά μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер буфера устройства ускорителя слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备缓冲区大小过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置緩衝區大小過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスバッファサイズが大きすぎます。";
        return "Accelerator device buffer size is too large.";
    }
    if (strcmp(msg, "Accelerator device buffer size overflowed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator device buffer size overflowed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se produjo un desbordamiento del tamaño del búfer del dispositivo del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du tampon du périphérique de l’accélérateur a subi un dépassement de capacité.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe des Gerätepuffers des Beschleunigers ist übergelaufen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Si è verificato un overflow della dimensione del buffer del dispositivo dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπήρξε υπερχείλιση του μεγέθους της ενδιάμεσης μνήμης συσκευής του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение размера буфера устройства ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备缓冲区大小溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置緩衝區大小溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスバッファサイズがオーバーフローしました。";
        return "Accelerator device buffer size overflowed.";
    }
    if (strcmp(msg, "Accelerator device-list visitor failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator device-list visitor failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El visitante de la lista de dispositivos del acelerador falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le visiteur de la liste des périphériques de l’accélérateur a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Besucher der Beschleuniger-Geräteliste ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il visitatore dell'elenco dei dispositivi dell'acceleratore non è riuscito.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο επισκέπτης λίστας συσκευών του επιταχυντή απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой посетителя списка устройств ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备列表访问器失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置清單訪客失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスリストビジターが失敗しました。";
        return "Accelerator device-list visitor failed.";
    }
    if (strcmp(msg, "Accelerator dispatch range is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator dispatch range is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango de despacho del acelerador es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage de lancement de l’accélérateur est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Dispatch-Bereich des Beschleunigers ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'intervallo di dispatch dell'acceleratore è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αποστολής του επιταχυντή είναι υπερβολικά μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон диспетчеризации ускорителя слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器调度范围过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器調度範圍過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのディスパッチ範囲が大きすぎます。";
        return "Accelerator dispatch range is too large.";
    }
    if (strcmp(msg, "Accelerator function is not registered in the HIR table.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator function is not registered in the HIR table.";
        if (strncmp(lang_code, "es", 2) == 0) return "La función del acelerador no está registrada en la tabla HIR.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La fonction de l’accélérateur n’est pas enregistrée dans la table HIR.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Beschleunigerfunktion ist nicht in der HIR-Tabelle registriert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La funzione dell'acceleratore non è registrata nella tabella HIR.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συνάρτηση του επιταχυντή δεν είναι καταχωρισμένη στον πίνακα HIR.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Функция ускорителя не зарегистрирована в таблице HIR.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器函数未在 HIR 表中注册。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器函式未在 HIR 表中註冊。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ関数が HIR テーブルに登録されていません。";
        return "Accelerator function is not registered in the HIR table.";
    }
    if (strcmp(msg, "Accelerator load has no buffer binding.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator load has no buffer binding.";
        if (strncmp(lang_code, "es", 2) == 0) return "La carga del acelerador no tiene vinculación de búfer.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le chargement de l’accélérateur n’a pas de liaison de tampon.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Ladevorgang des Beschleunigers hat keine Pufferbindung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il caricamento dell'acceleratore non ha un'associazione di buffer.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η φόρτωση του επιταχυντή δεν έχει δέσμευση ενδιάμεσης μνήμης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Загрузка ускорителя не имеет привязки буфера.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器加载没有缓冲区绑定。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器載入沒有緩衝區繫結。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのロードにバッファのバインディングがありません。";
        return "Accelerator load has no buffer binding.";
    }
    if (strcmp(msg, "Accelerator local name collision.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator local name collision.";
        if (strncmp(lang_code, "es", 2) == 0) return "Colisión de nombres de variables locales del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Collision de noms de variables locales de l’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Namenskollision bei lokalen Variablen des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Collisione di nomi delle variabili locali dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Σύγκρουση ονομάτων τοπικών μεταβλητών του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Конфликт имён локальных переменных ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器局部变量名称冲突。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域變數名稱衝突。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのローカル変数名が競合しています。";
        return "Accelerator local name collision.";
    }
    if (strcmp(msg, "Accelerator local name is too long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator local name is too long.";
        if (strncmp(lang_code, "es", 2) == 0) return "El nombre de la variable local del acelerador es demasiado largo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nom de la variable locale de l’accélérateur est trop long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Name der lokalen Beschleunigervariable ist zu lang.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il nome della variabile locale dell'acceleratore è troppo lungo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όνομα τοπικής μεταβλητής του επιταχυντή είναι υπερβολικά μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Имя локальной переменной ускорителя слишком длинное.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器局部变量名称过长。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域變數名稱過長。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのローカル変数名が長すぎます。";
        return "Accelerator local name is too long.";
    }
    if (strcmp(msg, "Accelerator loop start overflowed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator loop start overflowed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se produjo un desbordamiento del inicio del bucle del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le début de la boucle de l’accélérateur a subi un dépassement de capacité.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Schleifenanfang des Beschleunigers ist übergelaufen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Si è verificato un overflow dell'inizio del ciclo dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπήρξε υπερχείλιση της αρχής βρόχου του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение начала цикла ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器循环起始值溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器迴圈起始值溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのループ開始値がオーバーフローしました。";
        return "Accelerator loop start overflowed.";
    }
    if (strcmp(msg, "Accelerator output argument is missing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator output argument is missing.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el argumento de salida del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’argument de sortie de l’accélérateur est manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Ausgabeargument des Beschleunigers fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Manca l'argomento di output dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το όρισμα εξόδου του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует аргумент вывода ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器输出实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器輸出引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの出力引数がありません。";
        return "Accelerator output argument is missing.";
    }
    if (strcmp(msg, "Accelerator output size changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator output size changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño de la salida del acelerador ha cambiado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille de la sortie de l’accélérateur a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe der Beschleunigerausgabe hat sich geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione dell'output dell'acceleratore è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος εξόδου του επιταχυντή άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер вывода ускорителя изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器输出大小已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器輸出大小已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの出力サイズが変更されました。";
        return "Accelerator output size changed.";
    }
    if (strcmp(msg, "Accelerator output snapshot is missing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator output snapshot is missing.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la instantánea de salida del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’instantané de sortie de l’accélérateur est manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausgabemomentaufnahme des Beschleunigers fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Manca l'istantanea dell'output dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το στιγμιότυπο εξόδου του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует снимок вывода ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器输出快照。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器輸出快照。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの出力スナップショットがありません。";
        return "Accelerator output snapshot is missing.";
    }
    if (strcmp(msg, "Accelerator output type changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator output type changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tipo de salida del acelerador ha cambiado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le type de sortie de l’accélérateur a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Typ der Beschleunigerausgabe hat sich geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il tipo di output dell'acceleratore è cambiato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τύπος εξόδου του επιταχυντή άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Тип вывода ускорителя изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器输出类型已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器輸出型別已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの出力型が変更されました。";
        return "Accelerator output type changed.";
    }
    if (strcmp(msg, "Accelerator plan does not match live HIR.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator plan does not match live HIR.";
        if (strncmp(lang_code, "es", 2) == 0) return "El plan del acelerador no coincide con el HIR activo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le plan de l’accélérateur ne correspond pas au HIR actif.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerplan stimmt nicht mit dem aktiven HIR überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il piano dell'acceleratore non corrisponde all'HIR attivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το σχέδιο του επιταχυντή δεν αντιστοιχεί στο ενεργό HIR.";
        if (strncmp(lang_code, "ru", 2) == 0) return "План ускорителя не соответствует активному HIR.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器计划与当前 HIR 不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器計畫與目前 HIR 不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのプランが現在の HIR と一致しません。";
        return "Accelerator plan does not match live HIR.";
    }
    if (strcmp(msg, "Accelerator program validation failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator program validation failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La validación del programa del acelerador falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La validation du programme de l’accélérateur a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Validierung des Beschleunigerprogramms ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "La convalida del programma dell'acceleratore non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επικύρωση του προγράμματος του επιταχυντή απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой проверки программы ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器程序验证失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器程式驗證失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのプログラム検証に失敗しました。";
        return "Accelerator program validation failed.";
    }
    if (strcmp(msg, "Accelerator reduction has no result entry.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator reduction has no result entry.";
        if (strncmp(lang_code, "es", 2) == 0) return "La reducción del acelerador no tiene una entrada de resultado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La réduction de l’accélérateur n’a pas d’entrée de résultat.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Reduktion des Beschleunigers hat keinen Ergebniseintrag.";
        if (strncmp(lang_code, "it", 2) == 0) return "La riduzione dell'acceleratore non ha una voce di risultato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αναγωγή του επιταχυντή δεν έχει καταχώριση αποτελέσματος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Редукция ускорителя не имеет записи результата.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器归约没有结果条目。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器歸約沒有結果項目。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのリダクションに結果エントリがありません。";
        return "Accelerator reduction has no result entry.";
    }
    if (strcmp(msg, "Accelerator scalar argument is missing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar argument is missing.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el argumento escalar del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’argument scalaire de l’accélérateur est manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Skalarargument des Beschleunigers fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Manca l'argomento scalare dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το βαθμωτό όρισμα του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует скалярный аргумент ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器标量实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器純量引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー引数がありません。";
        return "Accelerator scalar argument is missing.";
    }
    if (strcmp(msg, "Accelerator scalar metadata is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar metadata is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los metadatos escalares del acelerador son demasiado grandes.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées scalaires de l’accélérateur sont trop volumineuses.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Skalarmetadaten des Beschleunigers sind zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "I metadati scalari dell'acceleratore sono troppo grandi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα βαθμωτά μεταδεδομένα του επιταχυντή είναι υπερβολικά μεγάλα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Скалярные метаданные ускорителя слишком велики.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量元数据过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量中繼資料過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラーメタデータが大きすぎます。";
        return "Accelerator scalar metadata is too large.";
    }
    if (strcmp(msg, "Accelerator scalar result argument is missing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar result argument is missing.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el argumento del resultado escalar del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’argument du résultat scalaire de l’accélérateur est manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Argument für das skalare Ergebnis des Beschleunigers fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Manca l'argomento del risultato scalare dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το όρισμα βαθμωτού αποτελέσματος του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует аргумент скалярного результата ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器标量结果实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器純量結果引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果引数がありません。";
        return "Accelerator scalar result argument is missing.";
    }
    if (strcmp(msg, "Accelerator scalar result has no local.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar result has no local.";
        if (strncmp(lang_code, "es", 2) == 0) return "El resultado escalar del acelerador no tiene una variable local.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le résultat scalaire de l’accélérateur n’a pas de variable locale.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das skalare Ergebnis des Beschleunigers hat keine lokale Variable.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il risultato scalare dell'acceleratore non ha una variabile locale.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το βαθμωτό αποτέλεσμα του επιταχυντή δεν έχει τοπική μεταβλητή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Скалярный результат ускорителя не имеет локальной переменной.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果没有局部变量。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果沒有區域變數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果にローカル変数がありません。";
        return "Accelerator scalar result has no local.";
    }
    if (strcmp(msg, "Accelerator scalar result identity changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar result identity changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La identidad del resultado escalar del acelerador ha cambiado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’identité du résultat scalaire de l’accélérateur a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Identität des skalaren Ergebnisses des Beschleunigers hat sich geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'identità del risultato scalare dell'acceleratore è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ταυτότητα του βαθμωτού αποτελέσματος του επιταχυντή άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Идентификатор скалярного результата ускорителя изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果标识已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果識別已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果の識別情報が変更されました。";
        return "Accelerator scalar result identity changed.";
    }
    if (strcmp(msg, "Accelerator scalar result is not available.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar result is not available.";
        if (strncmp(lang_code, "es", 2) == 0) return "El resultado escalar del acelerador no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le résultat scalaire de l’accélérateur n’est pas disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das skalare Ergebnis des Beschleunigers ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il risultato scalare dell'acceleratore non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το βαθμωτό αποτέλεσμα του επιταχυντή δεν είναι διαθέσιμο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Скалярный результат ускорителя недоступен.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果を使用できません。";
        return "Accelerator scalar result is not available.";
    }
    if (strcmp(msg, "Accelerator scalar result metadata is missing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar result metadata is missing.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan los metadatos del resultado escalar del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées du résultat scalaire de l’accélérateur sont manquantes.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Metadaten des skalaren Ergebnisses des Beschleunigers fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Mancano i metadati del risultato scalare dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν τα μεταδεδομένα βαθμωτού αποτελέσματος του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют метаданные скалярного результата ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器标量结果元数据。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器純量結果中繼資料。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果メタデータがありません。";
        return "Accelerator scalar result metadata is missing.";
    }
    if (strcmp(msg, "Accelerator scalar result metadata is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator scalar result metadata is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los metadatos del resultado escalar del acelerador son demasiado grandes.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées du résultat scalaire de l’accélérateur sont trop volumineuses.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Metadaten des skalaren Ergebnisses des Beschleunigers sind zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "I metadati del risultato scalare dell'acceleratore sono troppo grandi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα μεταδεδομένα βαθμωτού αποτελέσματος του επιταχυντή είναι υπερβολικά μεγάλα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Метаданные скалярного результата ускорителя слишком велики.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果元数据过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果中繼資料過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果メタデータが大きすぎます。";
        return "Accelerator scalar result metadata is too large.";
    }
    if (strcmp(msg, "Accelerator store has no buffer binding.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator store has no buffer binding.";
        if (strncmp(lang_code, "es", 2) == 0) return "El almacenamiento del acelerador no tiene vinculación de búfer.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le stockage de l’accélérateur n’a pas de liaison de tampon.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Speichervorgang des Beschleunigers hat keine Pufferbindung.";
        if (strncmp(lang_code, "it", 2) == 0) return "La memorizzazione dell'acceleratore non ha un'associazione di buffer.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αποθήκευση του επιταχυντή δεν έχει δέσμευση ενδιάμεσης μνήμης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Запись ускорителя не имеет привязки буфера.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器存储没有缓冲区绑定。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器儲存沒有緩衝區繫結。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのストアにバッファのバインディングがありません。";
        return "Accelerator store has no buffer binding.";
    }
    if (strcmp(msg, "Accelerator trip count overflowed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Accelerator trip count overflowed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se produjo un desbordamiento de la cantidad de iteraciones del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nombre d’itérations de l’accélérateur a subi un dépassement de capacité.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Iterationsanzahl des Beschleunigers ist übergelaufen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Si è verificato un overflow del numero di iterazioni dell'acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπήρξε υπερχείλιση του πλήθους επαναλήψεων του επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение количества итераций ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器迭代次数溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器迭代次數溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの反復回数がオーバーフローしました。";
        return "Accelerator trip count overflowed.";
    }
    if (strcmp(msg, "Adding file %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Adding file %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Añadiendo el archivo %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Ajout du fichier %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Datei %s wird hinzugefügt.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Aggiunta del file %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Προσθήκη του αρχείου %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Добавление файла %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "正在添加文件 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "正在新增檔案 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル %s を追加しています。\n";
        return "Adding file %s.\n";
    }
    if (strcmp(msg, "An accelerator is already attached to this VM.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "An accelerator is already attached to this VM.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ya hay un acelerador conectado a esta VM.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un accélérateur est déjà attaché à cette VM.";
        if (strncmp(lang_code, "de", 2) == 0) return "An diese VM ist bereits ein Beschleuniger angefügt.";
        if (strncmp(lang_code, "it", 2) == 0) return "A questa VM è già collegato un acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένας επιταχυντής είναι ήδη συνδεδεμένος σε αυτήν τη VM.";
        if (strncmp(lang_code, "ru", 2) == 0) return "К этой VM уже подключён ускоритель.";
        if (strncmp(lang_code, "zh", 2) == 0) return "此 VM 已附加加速器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "此 VM 已連接加速器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "この VM には既にアクセラレータが接続されています。";
        return "An accelerator is already attached to this VM.";
    }
    if (strcmp(msg, "An active Vulkan buffer has no storage.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "An active Vulkan buffer has no storage.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un búfer Vulkan activo no tiene almacenamiento.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un tampon Vulkan actif n’a pas de stockage.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein aktiver Vulkan-Puffer hat keinen Speicher.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un buffer Vulkan attivo non ha archiviazione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια ενεργή ενδιάμεση μνήμη Vulkan δεν έχει χώρο αποθήκευσης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Активный буфер Vulkan не имеет хранилища.";
        if (strncmp(lang_code, "zh", 2) == 0) return "活动的 Vulkan 缓冲区没有存储。";
        if (strncmp(lang_code, "tw", 2) == 0) return "作用中的 Vulkan 緩衝區沒有儲存空間。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクティブな Vulkan バッファにストレージがありません。";
        return "An active Vulkan buffer has no storage.";
    }
    if (strcmp(msg, "An inactive Vulkan buffer requested a transfer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "An inactive Vulkan buffer requested a transfer.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un búfer Vulkan inactivo solicitó una transferencia.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un tampon Vulkan inactif a demandé un transfert.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein inaktiver Vulkan-Puffer hat eine Übertragung angefordert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un buffer Vulkan inattivo ha richiesto un trasferimento.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μια ανενεργή ενδιάμεση μνήμη Vulkan ζήτησε μεταφορά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неактивный буфер Vulkan запросил передачу.";
        if (strncmp(lang_code, "zh", 2) == 0) return "非活动的 Vulkan 缓冲区请求了传输。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非作用中的 Vulkan 緩衝區要求傳輸。";
        if (strncmp(lang_code, "ja", 2) == 0) return "非アクティブな Vulkan バッファが転送を要求しました。";
        return "An inactive Vulkan buffer requested a transfer.";
    }
    if (strcmp(msg, "Application container cannot be used as a module: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Application container cannot be used as a module: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "El contenedor de la aplicación no puede usarse como módulo: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le conteneur de l’application ne peut pas être utilisé comme module : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Anwendungscontainer kann nicht als Modul verwendet werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Il contenitore dell'applicazione non può essere usato come modulo: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο περιέκτης της εφαρμογής δεν μπορεί να χρησιμοποιηθεί ως μονάδα: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Контейнер приложения нельзя использовать как модуль: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "应用程序容器不能用作模块：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "應用程式容器不能用作模組：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーションコンテナをモジュールとして使用できません: %s";
        return "Application container cannot be used as a module: %s";
    }
    if (strcmp(msg, "Application contains an unreachable module.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Application contains an unreachable module.";
        if (strncmp(lang_code, "es", 2) == 0) return "La aplicación contiene un módulo inaccesible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’application contient un module inaccessible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Anwendung enthält ein nicht erreichbares Modul.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'applicazione contiene un modulo irraggiungibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η εφαρμογή περιέχει μη προσβάσιμη μονάδα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Приложение содержит недостижимый модуль.";
        if (strncmp(lang_code, "zh", 2) == 0) return "应用程序包含不可达模块。";
        if (strncmp(lang_code, "tw", 2) == 0) return "應用程式包含無法到達的模組。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーションに到達不能なモジュールが含まれています。";
        return "Application contains an unreachable module.";
    }
    if (strcmp(msg, "Application output %s collides with input %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Application output %s collides with input %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La salida %s de la aplicación entra en conflicto con la entrada %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "La sortie %s de l’application entre en conflit avec l’entrée %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Anwendungsausgabe %s steht im Konflikt mit der Eingabe %s.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "L'output %s dell'applicazione è in conflitto con l'input %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έξοδος %s της εφαρμογής συγκρούεται με την είσοδο %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вывод приложения %s конфликтует с вводом %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "应用程序输出 %s 与输入 %s 冲突。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "應用程式輸出 %s 與輸入 %s 衝突。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーションの出力 %s が入力 %s と競合しています。\n";
        return "Application output %s collides with input %s.\n";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a dictionary.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a dictionary.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es un diccionario.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas un dictionnaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist kein Dictionary.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un dizionario.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι λεξικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является словарем.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是字典。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是字典。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が辞書ではありません。";
        return "Argument (%d: %s) is not a dictionary.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a double.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a double.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es un número double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas un double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist kein Double.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是 double。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是 double。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が double ではありません。";
        return "Argument (%d: %s) is not a double.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a float.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a float.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es un número decimal.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas un nombre à virgule flottante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist keine Gleitkommazahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un numero decimale.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι δεκαδικός αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является числом с плавающей точкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是浮点数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是浮點數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が浮動小数点数ではありません。";
        return "Argument (%d: %s) is not a float.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a function.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es una función.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas une fonction.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist keine Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è una funzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι συνάρτηση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является функцией.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是函数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是函式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が関数ではありません。";
        return "Argument (%d: %s) is not a function.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a long.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es un long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas un long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist kein Long.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是 long。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是 long。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が long ではありません。";
        return "Argument (%d: %s) is not a long.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a packed of type %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a packed of type %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "El argumento (%d: %s) no es un packed de tipo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’argument (%d : %s) n’est pas un packed de type %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Argument (%d: %s) ist kein packed vom Typ %s.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un packed di tipo %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι packed τύπου %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является packed типа %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "实参（%d：%s）不是 %s 类型的 packed。";
        if (strncmp(lang_code, "tw", 2) == 0) return "引數（%d：%s）不是 %s 型別的 packed。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数（%d: %s）は型 %s の packed ではありません。";
        return "Argument (%d: %s) is not a packed of type %s.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a packed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a packed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El argumento (%d: %s) no es un packed.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’argument (%d : %s) n’est pas un packed.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Argument (%d: %s) ist kein packed.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un packed.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι packed.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является packed.";
        if (strncmp(lang_code, "zh", 2) == 0) return "实参（%d：%s）不是 packed。";
        if (strncmp(lang_code, "tw", 2) == 0) return "引數（%d：%s）不是 packed。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数（%d: %s）は packed ではありません。";
        return "Argument (%d: %s) is not a packed.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not a string.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not a string.";
        if (strncmp(lang_code, "es", 2) == 0) return "El argumento (%d: %s) no es una cadena.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’argument (%d : %s) n’est pas une chaîne de caractères.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Argument (%d: %s) ist keine Zeichenkette.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è una stringa.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι συμβολοσειρά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является строкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "实参（%d：%s）不是字符串。";
        if (strncmp(lang_code, "tw", 2) == 0) return "引數（%d：%s）不是字串。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数（%d: %s）は文字列ではありません。";
        return "Argument (%d: %s) is not a string.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not an array.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not an array.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es una matriz.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas un tableau.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist kein Array.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un array.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι πίνακας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является массивом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是数组。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是陣列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が配列ではありません。";
        return "Argument (%d: %s) is not an array.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not an int.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not an int.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es un número int.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas un int.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist keine ganze int.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un numero int.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι ακέραιος αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является целым числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是 int。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是 int。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が int ではありません。";
        return "Argument (%d: %s) is not an int.";
    }
    if (strcmp(msg, "Argument (%d: %s) is not an integer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Argument (%d: %s) is not an integer.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento (%d: %s) no es un número entero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'argument (%d : %s) n'est pas un entier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument (%d: %s) ist keine ganze Zahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'argomento (%d: %s) non è un numero intero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όρισμα (%d: %s) δεν είναι ακέραιος αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Аргумент (%d: %s) не является целым числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数 (%d: %s) 不是整数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數 (%d: %s) 不是整數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数 (%d: %s) が整数ではありません。";
        return "Argument (%d: %s) is not an integer.";
    }
    if (strcmp(msg, "Array index %ld is out-of-range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Array index %ld is out-of-range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El índice del array %ld está fuera de rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'index du tableau %ld est hors de portée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Array-Index %ld ist außerhalb des Bereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'indice dell'array %ld è fuori intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο δείκτης πίνακα %ld είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс массива %ld вне диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "数组索引 %ld 超出范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "陣列索引 %ld 超出範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "配列のインデックス %ld が範囲外です。";
        return "Array index %ld is out-of-range.";
    }
    if (strcmp(msg, "BeUI is not open.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI is not open.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI no está abierta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI n’est pas ouverte.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI ist nicht geöffnet.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI non è aperta.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το BeUI δεν είναι ανοικτό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI не открыт.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI 未打开。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI 未開啟。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI が開いていません。";
        return "BeUI is not open.";
    }
    if (strcmp(msg, "BeUI.destroyImage received an invalid handle.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.destroyImage received an invalid handle.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.destroyImage recibió un identificador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.destroyImage a reçu un handle non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.destroyImage hat ein ungültiges Handle erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.destroyImage ha ricevuto un handle non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.destroyImage έλαβε μη έγκυρο handle.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.destroyImage получил недопустимый дескриптор.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.destroyImage 收到了无效句柄。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.destroyImage 收到無效控制代碼。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.destroyImage が無効なハンドルを受け取りました。";
        return "BeUI.destroyImage received an invalid handle.";
    }
    if (strcmp(msg, "BeUI.drawImage failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.drawImage failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.drawImage falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.drawImage a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.drawImage ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.drawImage non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.drawImage απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.drawImage.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.drawImage 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.drawImage 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.drawImage に失敗しました。";
        return "BeUI.drawImage failed.";
    }
    if (strcmp(msg, "BeUI.drawImagePattern failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.drawImagePattern failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.drawImagePattern falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.drawImagePattern a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.drawImagePattern ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.drawImagePattern non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.drawImagePattern απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.drawImagePattern.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.drawImagePattern 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.drawImagePattern 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.drawImagePattern に失敗しました。";
        return "BeUI.drawImagePattern failed.";
    }
    if (strcmp(msg, "BeUI.drawImagePattern received an invalid argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.drawImagePattern received an invalid argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.drawImagePattern recibió un argumento no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.drawImagePattern a reçu un argument non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.drawImagePattern hat ein ungültiges Argument erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.drawImagePattern ha ricevuto un argomento non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.drawImagePattern έλαβε μη έγκυρο όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.drawImagePattern получил недопустимый аргумент.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.drawImagePattern 收到了无效实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.drawImagePattern 收到無效引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.drawImagePattern が無効な引数を受け取りました。";
        return "BeUI.drawImagePattern received an invalid argument.";
    }
    if (strcmp(msg, "BeUI.drawImageRegion failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.drawImageRegion failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.drawImageRegion falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.drawImageRegion a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.drawImageRegion ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.drawImageRegion non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.drawImageRegion απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.drawImageRegion.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.drawImageRegion 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.drawImageRegion 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.drawImageRegion に失敗しました。";
        return "BeUI.drawImageRegion failed.";
    }
    if (strcmp(msg, "BeUI.drawText failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.drawText failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.drawText falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.drawText a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.drawText ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.drawText non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.drawText απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.drawText.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.drawText 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.drawText 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.drawText に失敗しました。";
        return "BeUI.drawText failed.";
    }
    if (strcmp(msg, "BeUI.drawText received an invalid argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.drawText received an invalid argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.drawText recibió un argumento no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.drawText a reçu un argument non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.drawText hat ein ungültiges Argument erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.drawText ha ricevuto un argomento non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.drawText έλαβε μη έγκυρο όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.drawText получил недопустимый аргумент.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.drawText 收到了无效实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.drawText 收到無效引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.drawText が無効な引数を受け取りました。";
        return "BeUI.drawText received an invalid argument.";
    }
    if (strcmp(msg, "BeUI.fill failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.fill failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.fill falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.fill a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.fill ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.fill non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.fill απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.fill.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.fill 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.fill 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.fill に失敗しました。";
        return "BeUI.fill failed.";
    }
    if (strcmp(msg, "BeUI.fill received an invalid argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.fill received an invalid argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.fill recibió un argumento no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.fill a reçu un argument non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.fill hat ein ungültiges Argument erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.fill ha ricevuto un argomento non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.fill έλαβε μη έγκυρο όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.fill получил недопустимый аргумент.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.fill 收到了无效实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.fill 收到無效引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.fill が無効な引数を受け取りました。";
        return "BeUI.fill received an invalid argument.";
    }
    if (strcmp(msg, "BeUI.flush failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.flush failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.flush falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.flush a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.flush ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.flush non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.flush απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.flush.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.flush 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.flush 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.flush に失敗しました。";
        return "BeUI.flush failed.";
    }
    if (strcmp(msg, "BeUI.getImageHeight received an invalid handle.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.getImageHeight received an invalid handle.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.getImageHeight recibió un identificador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.getImageHeight a reçu un handle non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.getImageHeight hat ein ungültiges Handle erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.getImageHeight ha ricevuto un handle non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.getImageHeight έλαβε μη έγκυρο handle.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.getImageHeight получил недопустимый дескриптор.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.getImageHeight 收到了无效句柄。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.getImageHeight 收到無效控制代碼。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.getImageHeight が無効なハンドルを受け取りました。";
        return "BeUI.getImageHeight received an invalid handle.";
    }
    if (strcmp(msg, "BeUI.getImageWidth received an invalid handle.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.getImageWidth received an invalid handle.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.getImageWidth recibió un identificador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.getImageWidth a reçu un handle non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.getImageWidth hat ein ungültiges Handle erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.getImageWidth ha ricevuto un handle non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.getImageWidth έλαβε μη έγκυρο handle.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.getImageWidth получил недопустимый дескриптор.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.getImageWidth 收到了无效句柄。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.getImageWidth 收到無效控制代碼。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.getImageWidth が無効なハンドルを受け取りました。";
        return "BeUI.getImageWidth received an invalid handle.";
    }
    if (strcmp(msg, "BeUI.getMilliseconds is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.getMilliseconds is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.getMilliseconds no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.getMilliseconds est indisponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.getMilliseconds ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.getMilliseconds non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.getMilliseconds δεν είναι διαθέσιμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.getMilliseconds недоступен.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.getMilliseconds 不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.getMilliseconds 無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.getMilliseconds を使用できません。";
        return "BeUI.getMilliseconds is unavailable.";
    }
    if (strcmp(msg, "BeUI.initWithHint expects 8 or 24 bits per pixel.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.initWithHint expects 8 or 24 bits per pixel.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.initWithHint espera 8 o 24 bits por píxel.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.initWithHint attend 8 ou 24 bits par pixel.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.initWithHint erwartet 8 oder 24 Bit pro Pixel.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.initWithHint prevede 8 o 24 bit per pixel.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.initWithHint αναμένει 8 ή 24 bit ανά pixel.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.initWithHint ожидает 8 или 24 бита на пиксель.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.initWithHint 要求每像素 8 或 24 位。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.initWithHint 要求每像素 8 或 24 位元。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.initWithHint にはピクセルあたり 8 または 24 ビットが必要です。";
        return "BeUI.initWithHint expects 8 or 24 bits per pixel.";
    }
    if (strcmp(msg, "BeUI.isKeyDown received an invalid argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.isKeyDown received an invalid argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.isKeyDown recibió un argumento no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.isKeyDown a reçu un argument non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.isKeyDown hat ein ungültiges Argument erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.isKeyDown ha ricevuto un argomento non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.isKeyDown έλαβε μη έγκυρο όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.isKeyDown получил недопустимый аргумент.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.isKeyDown 收到了无效实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.isKeyDown 收到無效引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.isKeyDown が無効な引数を受け取りました。";
        return "BeUI.isKeyDown received an invalid argument.";
    }
    if (strcmp(msg, "BeUI.line failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.line failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.line falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.line a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.line ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.line non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.line απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.line.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.line 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.line 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.line に失敗しました。";
        return "BeUI.line failed.";
    }
    if (strcmp(msg, "BeUI.line received an invalid argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.line received an invalid argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.line recibió un argumento no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.line a reçu un argument non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.line hat ein ungültiges Argument erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.line ha ricevuto un argomento non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.line έλαβε μη έγκυρο όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.line получил недопустимый аргумент.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.line 收到了无效实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.line 收到無效引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.line が無効な引数を受け取りました。";
        return "BeUI.line received an invalid argument.";
    }
    if (strcmp(msg, "BeUI.loadImage expects a byte array.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.loadImage expects a byte array.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.loadImage espera un array de bytes.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.loadImage attend un tableau d’octets.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.loadImage erwartet ein Byte-Array.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.loadImage prevede un array di byte.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.loadImage αναμένει πίνακα byte.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.loadImage ожидает массив байтов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.loadImage 要求字节数组。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.loadImage 要求位元組陣列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.loadImage にはバイト配列が必要です。";
        return "BeUI.loadImage expects a byte array.";
    }
    if (strcmp(msg, "BeUI.loadImage received an unsupported image.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.loadImage received an unsupported image.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.loadImage recibió una imagen no admitida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.loadImage a reçu une image non prise en charge.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.loadImage hat ein nicht unterstütztes Bild erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.loadImage ha ricevuto un'immagine non supportata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.loadImage έλαβε μη υποστηριζόμενη εικόνα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.loadImage получил неподдерживаемое изображение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.loadImage 收到了不支持的图像。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.loadImage 收到不支援的影像。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.loadImage がサポートされていない画像を受け取りました。";
        return "BeUI.loadImage received an unsupported image.";
    }
    if (strcmp(msg, "BeUI.patternFill failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.patternFill failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.patternFill falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.patternFill a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.patternFill ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.patternFill non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.patternFill απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой BeUI.patternFill.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.patternFill 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.patternFill 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.patternFill に失敗しました。";
        return "BeUI.patternFill failed.";
    }
    if (strcmp(msg, "BeUI.patternFill received an invalid argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.patternFill received an invalid argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.patternFill recibió un argumento no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.patternFill a reçu un argument non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.patternFill hat ein ungültiges Argument erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.patternFill ha ricevuto un argomento non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.patternFill έλαβε μη έγκυρο όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.patternFill получил недопустимый аргумент.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.patternFill 收到了无效实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.patternFill 收到無效引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.patternFill が無効な引数を受け取りました。";
        return "BeUI.patternFill received an invalid argument.";
    }
    if (strcmp(msg, "BeUI.sleep is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.sleep is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.sleep no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.sleep est indisponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.sleep ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.sleep non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.sleep δεν είναι διαθέσιμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.sleep недоступен.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.sleep 不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.sleep 無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.sleep を使用できません。";
        return "BeUI.sleep is unavailable.";
    }
    if (strcmp(msg, "BeUI.sleep received an invalid argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "BeUI.sleep received an invalid argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "BeUI.sleep recibió un argumento no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "BeUI.sleep a reçu un argument non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "BeUI.sleep hat ein ungültiges Argument erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "BeUI.sleep ha ricevuto un argomento non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η BeUI.sleep έλαβε μη έγκυρο όρισμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "BeUI.sleep получил недопустимый аргумент.";
        if (strncmp(lang_code, "zh", 2) == 0) return "BeUI.sleep 收到了无效实参。";
        if (strncmp(lang_code, "tw", 2) == 0) return "BeUI.sleep 收到無效引數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "BeUI.sleep が無効な引数を受け取りました。";
        return "BeUI.sleep received an invalid argument.";
    }
    if (strcmp(msg, "Branch target not found.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Branch target not found.";
        if (strncmp(lang_code, "es", 2) == 0) return "El destino de la rama no se ha encontrado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Cible de branchement introuvable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Sprungziel nicht gefunden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Destinazione del ramo non trovata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν βρέθηκε ο στόχος διακλάδωσης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Цель перехода не найдена.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未找到分支目标。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未找到分支目標。";
        if (strncmp(lang_code, "ja", 2) == 0) return "分岐先がみつかりません。";
        return "Branch target not found.";
    }
    if (strcmp(msg, "Branch target too far.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Branch target too far.";
        if (strncmp(lang_code, "es", 2) == 0) return "El destino del salto está demasiado lejos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La cible de branchement est trop éloignée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Sprungziel ist zu weit entfernt.";
        if (strncmp(lang_code, "it", 2) == 0) return "La destinazione del salto è troppo lontana.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο στόχος διακλάδωσης είναι υπερβολικά μακριά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Цель перехода слишком далеко.";
        if (strncmp(lang_code, "zh", 2) == 0) return "分支目标过远。";
        if (strncmp(lang_code, "tw", 2) == 0) return "分支目標過遠。";
        if (strncmp(lang_code, "ja", 2) == 0) return "分岐先が遠すぎます。";
        return "Branch target too far.";
    }
    if (strcmp(msg, "Broken bytecode.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Broken bytecode.";
        if (strncmp(lang_code, "es", 2) == 0) return "Bytecode corrupto.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Bytecode corrompu.";
        if (strncmp(lang_code, "de", 2) == 0) return "Beschädigter Bytecode.";
        if (strncmp(lang_code, "it", 2) == 0) return "Bytecode corrotto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κατεστραμμένο bytecode.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Повреждённый байткод.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码损坏。";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼損毀。";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコードが壊れています。";
        return "Broken bytecode.";
    }
    if (strcmp(msg, "Bytecode input is not recompiled: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Bytecode input is not recompiled: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "La entrada de bytecode no se vuelve a compilar: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’entrée en bytecode n’est pas recompilée : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Bytecode-Eingabe wird nicht neu kompiliert: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "L'input bytecode non viene ricompilato: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Η είσοδος bytecode δεν επαναμεταγλωττίζεται: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Входной байткод не перекомпилируется: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码输入不会重新编译：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼輸入不會重新編譯：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコード入力は再コンパイルされません: %s";
        return "Bytecode input is not recompiled: %s";
    }
    if (strcmp(msg, "Bytecode input is not recompiled: %s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Bytecode input is not recompiled: %s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La entrada de bytecode no se vuelve a compilar: %s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’entrée en bytecode n’est pas recompilée : %s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Bytecode-Eingabe wird nicht neu kompiliert: %s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "L'input bytecode non viene ricompilato: %s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η είσοδος bytecode δεν επαναμεταγλωττίζεται: %s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Входной байткод не перекомпилируется: %s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码输入不会重新编译：%s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼輸入不會重新編譯：%s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコード入力は再コンパイルされません: %s\n";
        return "Bytecode input is not recompiled: %s\n";
    }
    if (strcmp(msg, "Bytecode is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Bytecode is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bytecode es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bytecode est trop volumineux.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Bytecode ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il bytecode è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το bytecode είναι υπερβολικά μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Байткод слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコードが大きすぎます。";
        return "Bytecode is too large.";
    }
    if (strcmp(msg, "Bytecode module is too large: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Bytecode module is too large: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "El módulo de bytecode es demasiado grande: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le module de bytecode est trop volumineux : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Bytecode-Modul ist zu groß: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Il modulo bytecode è troppo grande: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μονάδα bytecode είναι υπερβολικά μεγάλη: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Модуль байткода слишком велик: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码模块过大：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼模組過大：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコードモジュールが大きすぎます: %s";
        return "Bytecode module is too large: %s";
    }
    if (strcmp(msg, "Bytecode program is too large.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Bytecode program is too large.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "El programa de bytecode es demasiado grande.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le programme de bytecode est trop volumineux.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Bytecode-Programm ist zu groß.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Il programma bytecode è troppo grande.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πρόγραμμα bytecode είναι υπερβολικά μεγάλο.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Программа байткода слишком велика.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码程序过大。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼程式過大。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコードプログラムが大きすぎます。\n";
        return "Bytecode program is too large.\n";
    }
    if (strcmp(msg, "Call to non-fast function '%s' is not allowed inside __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Call to non-fast function '%s' is not allowed inside __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se permite llamar a la función que no es __fast '%s' dentro de una func __fast.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’appel de la fonction non-__fast « %s » n’est pas autorisé dans une func __fast.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Aufruf der Nicht-__fast-Funktion '%s' ist innerhalb einer __fast-func nicht zulässig.";
        if (strncmp(lang_code, "it", 2) == 0) return "La chiamata alla funzione non __fast '%s' non è consentita in una func __fast.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η κλήση της συνάρτησης '%s' που δεν είναι __fast δεν επιτρέπεται μέσα σε func __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов функции '%s', не являющейся __fast, запрещён внутри func __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不允许在 __fast func 内调用非 __fast 函数 '%s'。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不允許在 __fast func 內呼叫非 __fast 函式 '%s'。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast ではない関数 '%s' を __fast func 内で呼び出すことはできません。";
        return "Call to non-fast function '%s' is not allowed inside __fast func.";
    }
    if (strcmp(msg, "Cannot accept a connection.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot accept a connection.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede aceptar una conexión.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’accepter une connexion.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine Verbindung kann nicht angenommen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile accettare una connessione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αποδοχή σύνδεσης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся принять соединение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法接受连接。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法接受連線。";
        if (strncmp(lang_code, "ja", 2) == 0) return "接続を受け入れられません。";
        return "Cannot accept a connection.";
    }
    if (strcmp(msg, "Cannot allocate application output path.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot allocate application output path.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede asignar la ruta de salida de la aplicación.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’allouer le chemin de sortie de l’application.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Ausgabepfad der Anwendung kann nicht reserviert werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile allocare il percorso di output dell'applicazione.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εκχώρηση της διαδρομής εξόδου της εφαρμογής.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся выделить путь вывода приложения.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法分配应用程序输出路径。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法配置應用程式輸出路徑。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーションの出力パスを割り当てられません。\n";
        return "Cannot allocate application output path.\n";
    }
    if (strcmp(msg, "Cannot allocate bytecode output paths.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot allocate bytecode output paths.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pueden asignar las rutas de salida del bytecode.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’allouer les chemins de sortie du bytecode.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Bytecode-Ausgabepfade können nicht reserviert werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile allocare i percorsi di output del bytecode.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εκχώρηση των διαδρομών εξόδου bytecode.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся выделить пути вывода байткода.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法分配字节码输出路径。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法配置位元組碼輸出路徑。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコードの出力パスを割り当てられません。\n";
        return "Cannot allocate bytecode output paths.\n";
    }
    if (strcmp(msg, "Cannot allocate output path for %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot allocate output path for %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede asignar la ruta de salida para %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’allouer le chemin de sortie pour %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Ausgabepfad für %s kann nicht reserviert werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile allocare il percorso di output per %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εκχώρηση της διαδρομής εξόδου για %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся выделить путь вывода для %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法为 %s 分配输出路径。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法為 %s 配置輸出路徑。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s の出力パスを割り当てられません。\n";
        return "Cannot allocate output path for %s.\n";
    }
    if (strcmp(msg, "Cannot assign to 'let' variable '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot assign to 'let' variable '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede asignar a la variable 'let' '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’affecter une valeur à la variable 'let' '%s'.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der 'let'-Variable '%s' kann kein Wert zugewiesen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile assegnare alla variabile 'let' '%s'.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η ανάθεση στη μεταβλητή 'let' '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нельзя присвоить значение переменной 'let' '%s'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法向 'let' 变量 '%s' 赋值。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法指派值給 'let' 變數 '%s'。";
        if (strncmp(lang_code, "ja", 2) == 0) return "'let' 変数 '%s' には代入できません。";
        return "Cannot assign to 'let' variable '%s'.";
    }
    if (strcmp(msg, "Cannot assign to constant \"%s\".") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot assign to constant \"%s\".";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede asignar a la constante \"%s\".";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’affecter une valeur à la constante \"%s\".";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Konstante \"%s\" kann kein Wert zugewiesen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile assegnare alla costante \"%s\".";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η ανάθεση στη σταθερά \"%s\".";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нельзя присвоить значение константе \"%s\".";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法向常量 \"%s\" 赋值。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法指派值給常數 \"%s\"。";
        if (strncmp(lang_code, "ja", 2) == 0) return "定数 \"%s\" には代入できません。";
        return "Cannot assign to constant \"%s\".";
    }
    if (strcmp(msg, "Cannot bind to %s:%d. (%s)") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot bind to %s:%d. (%s)";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede vincular a %s:%d. (%s)";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’établir une liaison avec %s:%d. (%s)";
        if (strncmp(lang_code, "de", 2) == 0) return "Bindung an %s:%d nicht möglich. (%s)";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile effettuare l'associazione a %s:%d. (%s)";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η δέσμευση στο %s:%d. (%s)";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся выполнить привязку к %s:%d. (%s)";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法绑定到 %s:%d。（%s）";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法繫結至 %s:%d。（%s）";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s:%d にバインドできません。（%s）";
        return "Cannot bind to %s:%d. (%s)";
    }
    if (strcmp(msg, "Cannot change the socket mode.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot change the socket mode.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede cambiar el modo del socket.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de modifier le mode du socket.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Socketmodus kann nicht geändert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile cambiare la modalità del socket.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αλλαγή της λειτουργίας του socket.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся изменить режим сокета.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法更改套接字模式。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法變更通訊端模式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソケットモードを変更できません。";
        return "Cannot change the socket mode.";
    }
    if (strcmp(msg, "Cannot close file %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot close file %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede cerrar el archivo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de fermer le fichier %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Datei %s kann nicht geschlossen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile chiudere il file %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατό το κλείσιμο του αρχείου %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся закрыть файл %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法关闭文件 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法關閉檔案 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル %s を閉じられません。";
        return "Cannot close file %s.";
    }
    if (strcmp(msg, "Cannot collect bytecode link names from %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot collect bytecode link names from %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pueden recopilar los nombres de enlace de bytecode de %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de collecter les noms de liaison de bytecode à partir de %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Bytecode-Linknamen aus %s können nicht erfasst werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile raccogliere i nomi di collegamento del bytecode da %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η συλλογή των ονομάτων σύνδεσης bytecode από το %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся собрать имена связей байткода из %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法从 %s 收集字节码链接名称。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法從 %s 收集位元組碼連結名稱。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s からバイトコードのリンク名を収集できません。";
        return "Cannot collect bytecode link names from %s.";
    }
    if (strcmp(msg, "Cannot collect bytecode prototypes from %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot collect bytecode prototypes from %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pueden recopilar los prototipos de bytecode de %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de collecter les prototypes de bytecode à partir de %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Bytecode-Prototypen aus %s können nicht erfasst werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile raccogliere i prototipi del bytecode da %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η συλλογή των πρωτοτύπων bytecode από το %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся собрать прототипы байткода из %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法从 %s 收集字节码原型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法從 %s 收集位元組碼原型。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s からバイトコードのプロトタイプを収集できません。";
        return "Cannot collect bytecode prototypes from %s.";
    }
    if (strcmp(msg, "Cannot connect to %s:%d.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot connect to %s:%d.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede conectar a %s:%d.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de se connecter à %s:%d.";
        if (strncmp(lang_code, "de", 2) == 0) return "Verbindung mit %s:%d nicht möglich.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile connettersi a %s:%d.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η σύνδεση στο %s:%d.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся подключиться к %s:%d.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法连接到 %s:%d。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法連線至 %s:%d。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s:%d に接続できません。";
        return "Cannot connect to %s:%d.";
    }
    if (strcmp(msg, "Cannot copy this value type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot copy this value type.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede copiar este tipo de valor.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de copier ce type de valeur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Dieser Werttyp kann nicht kopiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile copiare questo tipo di valore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αντιγραφή αυτού του τύπου τιμής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нельзя скопировать значение этого типа.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法复制此值类型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法複製此值型別。";
        if (strncmp(lang_code, "ja", 2) == 0) return "この値の型はコピーできません。";
        return "Cannot copy this value type.";
    }
    if (strcmp(msg, "Cannot create a mutex.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot create a mutex.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede crear un mutex.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de créer un mutex.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Mutex kann nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un mutex.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η δημιουργία mutex.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся создать мьютекс.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建互斥锁。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立互斥鎖。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ミューテックスを作成できません。";
        return "Cannot create a mutex.";
    }
    if (strcmp(msg, "Cannot create a thread.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot create a thread.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede crear un hilo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de créer un thread.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Thread kann nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un thread.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η δημιουργία νήματος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся создать поток.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建线程。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立執行緒。";
        if (strncmp(lang_code, "ja", 2) == 0) return "スレッドを作成できません。";
        return "Cannot create a thread.";
    }
    if (strcmp(msg, "Cannot create application %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot create application %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede crear la aplicación %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de créer l’application %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Anwendung %s kann nicht erstellt werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare l'applicazione %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η δημιουργία της εφαρμογής %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся создать приложение %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建应用程序 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立應用程式 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーション %s を作成できません。\n";
        return "Cannot create application %s.\n";
    }
    if (strcmp(msg, "Cannot create file mapping (Windows error %lu).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot create file mapping (Windows error %lu).";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede crear el mapeo de archivos (error de Windows %lu).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de créer le mappage de fichier (erreur Windows %lu).";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateizuordnung kann nicht erstellt werden (Windows-Fehler %lu).";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare la mappatura di file (errore Windows %lu).";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η δημιουργία αντιστοίχισης αρχείου (σφάλμα Windows %lu).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся создать отображение файла (ошибка Windows %lu).";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建文件映射（Windows 错误 %lu）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立檔案對應（Windows 錯誤 %lu）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングを作成できません（Windows エラー %lu）。";
        return "Cannot create file mapping (Windows error %lu).";
    }
    if (strcmp(msg, "Cannot create output directory %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot create output directory %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede crear el directorio de salida %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de créer le répertoire de sortie %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Ausgabeverzeichnis %s kann nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare la directory di output %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η δημιουργία του καταλόγου εξόδου %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся создать каталог вывода %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建输出目录 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立輸出目錄 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "出力ディレクトリ %s を作成できません。";
        return "Cannot create output directory %s.";
    }
    if (strcmp(msg, "Cannot derive a portable source name for %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot derive a portable source name for %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede derivar un nombre de fuente portable para %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de dériver un nom de source portable pour %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Für %s kann kein portabler Quellname abgeleitet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile derivare un nome sorgente portabile per %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εξαγωγή φορητού ονόματος πηγής για το %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся вывести переносимое имя исходного кода для %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法为 %s 派生可移植的源名称。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法為 %s 推導可攜式原始碼名稱。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s の移植可能なソース名を導出できません。";
        return "Cannot derive a portable source name for %s.";
    }
    if (strcmp(msg, "Cannot determine file size.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot determine file size.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede determinar el tamaño del archivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de déterminer la taille du fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateigröße kann nicht bestimmt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile determinare la dimensione del file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατός ο προσδιορισμός του μεγέθους του αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся определить размер файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法确定文件大小。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法判斷檔案大小。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルサイズを特定できません。";
        return "Cannot determine file size.";
    }
    if (strcmp(msg, "Cannot determine mapped file size (Windows error %lu).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot determine mapped file size (Windows error %lu).";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede determinar el tamaño del archivo mapeado (error de Windows %lu).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de déterminer la taille du fichier mappé (erreur Windows %lu).";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe der zugeordneten Datei kann nicht bestimmt werden (Windows-Fehler %lu).";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile determinare la dimensione del file mappato (errore Windows %lu).";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατός ο προσδιορισμός του μεγέθους του αντιστοιχισμένου αρχείου (σφάλμα Windows %lu).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся определить размер отображённого файла (ошибка Windows %lu).";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法确定映射文件大小（Windows 错误 %lu）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法判斷對應檔案大小（Windows 錯誤 %lu）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "マッピングされたファイルのサイズを特定できません（Windows エラー %lu）。";
        return "Cannot determine mapped file size (Windows error %lu).";
    }
    if (strcmp(msg, "Cannot determine mapped file size: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot determine mapped file size: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede determinar el tamaño del archivo mapeado: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de déterminer la taille du fichier mappé : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe der zugeordneten Datei kann nicht bestimmt werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile determinare la dimensione del file mappato: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατός ο προσδιορισμός του μεγέθους του αντιστοιχισμένου αρχείου: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся определить размер отображённого файла: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法确定映射文件大小：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法判斷對應檔案大小：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "マッピングされたファイルのサイズを特定できません: %s";
        return "Cannot determine mapped file size: %s";
    }
    if (strcmp(msg, "Cannot determine the file mapping allocation granularity.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot determine the file mapping allocation granularity.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede determinar la granularidad de asignación del mapeo de archivos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de déterminer la granularité d’allocation du mappage de fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Allokationsgranularität der Dateizuordnung kann nicht bestimmt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile determinare la granularità di allocazione della mappatura di file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατός ο προσδιορισμός της λεπτομέρειας εκχώρησης της αντιστοίχισης αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся определить гранулярность выделения отображения файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法确定文件映射分配粒度。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法判斷檔案對應配置粒度。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングの割り当て粒度を特定できません。";
        return "Cannot determine the file mapping allocation granularity.";
    }
    if (strcmp(msg, "Cannot determine the file mapping page size.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot determine the file mapping page size.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede determinar el tamaño de página del mapeo de archivos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de déterminer la taille de page du mappage de fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Seitengröße der Dateizuordnung kann nicht bestimmt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile determinare la dimensione della pagina della mappatura di file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατός ο προσδιορισμός του μεγέθους σελίδας της αντιστοίχισης αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся определить размер страницы отображения файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法确定文件映射页大小。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法判斷檔案對應頁面大小。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングのページサイズを特定できません。";
        return "Cannot determine the file mapping page size.";
    }
    if (strcmp(msg, "Cannot find %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot find %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede encontrar %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de trouver %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "%s konnte nicht gefunden werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile trovare %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν ήταν δυνατή η εύρεση του %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось найти %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法找到 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法找到 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sがみつかりません。\n";
        return "Cannot find %s.\n";
    }
    if (strcmp(msg, "Cannot find function %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot find function %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se ha encontrado la función %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de trouver la fonction %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Funktion %s nicht gefunden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Funzione %s non trovata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν βρέθηκε η συνάρτηση %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Функция %s не найдена.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法找到函数 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法找到函式 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数%sがみつかりません。";
        return "Cannot find function %s.";
    }
    if (strcmp(msg, "Cannot flush file mapping (Windows error %lu).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot flush file mapping (Windows error %lu).";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede vaciar el mapeo de archivos (error de Windows %lu).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de vider le mappage de fichier (erreur Windows %lu).";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateizuordnung kann nicht geschrieben werden (Windows-Fehler %lu).";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile scaricare la mappatura di file (errore Windows %lu).";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εκκαθάριση της αντιστοίχισης αρχείου (σφάλμα Windows %lu).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся сбросить отображение файла (ошибка Windows %lu).";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法刷新文件映射（Windows 错误 %lu）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法排清檔案對應（Windows 錯誤 %lu）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングをフラッシュできません（Windows エラー %lu）。";
        return "Cannot flush file mapping (Windows error %lu).";
    }
    if (strcmp(msg, "Cannot flush file mapping: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot flush file mapping: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede vaciar el mapeo de archivos: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de vider le mappage de fichier : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateizuordnung kann nicht geschrieben werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile scaricare la mappatura di file: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εκκαθάριση της αντιστοίχισης αρχείου: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся сбросить отображение файла: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法刷新文件映射：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法排清檔案對應：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングをフラッシュできません: %s";
        return "Cannot flush file mapping: %s";
    }
    if (strcmp(msg, "Cannot flush mapped file (Windows error %lu).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot flush mapped file (Windows error %lu).";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede vaciar el archivo mapeado (error de Windows %lu).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de vider le fichier mappé (erreur Windows %lu).";
        if (strncmp(lang_code, "de", 2) == 0) return "Die zugeordnete Datei kann nicht geschrieben werden (Windows-Fehler %lu).";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile scaricare il file mappato (errore Windows %lu).";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εκκαθάριση του αντιστοιχισμένου αρχείου (σφάλμα Windows %lu).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся сбросить отображённый файл (ошибка Windows %lu).";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法刷新映射文件（Windows 错误 %lu）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法排清對應檔案（Windows 錯誤 %lu）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "マッピングされたファイルをフラッシュできません（Windows エラー %lu）。";
        return "Cannot flush mapped file (Windows error %lu).";
    }
    if (strcmp(msg, "Cannot initialize the socket library.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot initialize the socket library.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede inicializar la biblioteca de sockets.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’initialiser la bibliothèque de sockets.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Socketbibliothek kann nicht initialisiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile inizializzare la libreria dei socket.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αρχικοποίηση της βιβλιοθήκης socket.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся инициализировать библиотеку сокетов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法初始化套接字库。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法初始化通訊端程式庫。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソケットライブラリを初期化できません。";
        return "Cannot initialize the socket library.";
    }
    if (strcmp(msg, "Cannot inspect source module %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot inspect source module %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede inspeccionar el módulo de código fuente %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’inspecter le module de code source %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Quellcode-Modul %s kann nicht untersucht werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile esaminare il modulo di codice sorgente %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η επιθεώρηση της μονάδας πηγαίου κώδικα %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся проверить модуль исходного кода %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法检查源代码模块 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法檢查原始碼模組 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソースコードモジュール %s を検査できません。";
        return "Cannot inspect source module %s.";
    }
    if (strcmp(msg, "Cannot install the REPL interrupt handler.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot install the REPL interrupt handler.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede instalar el manejador de interrupciones del REPL.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’installer le gestionnaire d’interruptions du REPL.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Der REPL-Unterbrechungshandler kann nicht installiert werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile installare il gestore delle interruzioni del REPL.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εγκατάσταση του χειριστή διακοπών REPL.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся установить обработчик прерываний REPL.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法安装 REPL 中断处理程序。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法安裝 REPL 中斷處理常式。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "REPL の割り込みハンドラをインストールできません。\n";
        return "Cannot install the REPL interrupt handler.\n";
    }
    if (strcmp(msg, "Cannot listen on %s:%d.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot listen on %s:%d.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede escuchar en %s:%d.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’écouter sur %s:%d.";
        if (strncmp(lang_code, "de", 2) == 0) return "Auf %s:%d kann nicht gelauscht werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile restare in ascolto su %s:%d.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η ακρόαση στο %s:%d.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся прослушивать %s:%d.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法在 %s:%d 上侦听。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法在 %s:%d 上接聽。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s:%d で待ち受けできません。";
        return "Cannot listen on %s:%d.";
    }
    if (strcmp(msg, "Cannot map file %s: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot map file %s: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede mapear el archivo %s: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de mapper le fichier %s : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Datei %s kann nicht zugeordnet werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile mappare il file %s: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αντιστοίχιση του αρχείου %s: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся отобразить файл %s: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法映射文件 %s：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對應檔案 %s：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル %s をマッピングできません: %s";
        return "Cannot map file %s: %s";
    }
    if (strcmp(msg, "Cannot map file view (Windows error %lu).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot map file view (Windows error %lu).";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede mapear la vista del archivo (error de Windows %lu).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de mapper la vue du fichier (erreur Windows %lu).";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateiansicht kann nicht zugeordnet werden (Windows-Fehler %lu).";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile mappare la vista del file (errore Windows %lu).";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αντιστοίχιση της προβολής αρχείου (σφάλμα Windows %lu).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся отобразить представление файла (ошибка Windows %lu).";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法映射文件视图（Windows 错误 %lu）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對應檔案檢視（Windows 錯誤 %lu）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルビューをマッピングできません（Windows エラー %lu）。";
        return "Cannot map file view (Windows error %lu).";
    }
    if (strcmp(msg, "Cannot open file %s for mapping: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot open file %s for mapping: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede abrir el archivo %s para mapearlo: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’ouvrir le fichier %s pour le mapper : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Datei %s kann nicht zur Zuordnung geöffnet werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile aprire il file %s per la mappatura: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατό το άνοιγμα του αρχείου %s για αντιστοίχιση: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся открыть файл %s для отображения: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法打开文件 %s 进行映射：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法開啟檔案 %s 進行對應：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル %s をマッピング用に開けません: %s";
        return "Cannot open file %s for mapping: %s";
    }
    if (strcmp(msg, "Cannot open file %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot open file %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede abrir el archivo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d'ouvrir le fichier %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Datei %s kann nicht geöffnet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile aprire il file %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατό το άνοιγμα του αρχείου %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся открыть файл %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法打开文件 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法開啟檔案 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル%sを開けません。";
        return "Cannot open file %s.";
    }
    if (strcmp(msg, "Cannot open file %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot open file %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede abrir el archivo %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d'ouvrir le fichier %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Datei %s kann nicht geöffnet werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile aprire il file %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατό το άνοιγμα του αρχείου %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся открыть файл %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法打开文件 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法開啟檔案 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル%sを開けません。\n";
        return "Cannot open file %s.\n";
    }
    if (strcmp(msg, "Cannot open file for mapping (Windows error %lu).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot open file for mapping (Windows error %lu).";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede abrir el archivo para mapearlo (error de Windows %lu).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’ouvrir le fichier pour le mapper (erreur Windows %lu).";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Datei kann nicht zur Zuordnung geöffnet werden (Windows-Fehler %lu).";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile aprire il file per la mappatura (errore Windows %lu).";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατό το άνοιγμα του αρχείου για αντιστοίχιση (σφάλμα Windows %lu).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся открыть файл для отображения (ошибка Windows %lu).";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法打开文件进行映射（Windows 错误 %lu）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法開啟檔案進行對應（Windows 錯誤 %lu）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルをマッピング用に開けません（Windows エラー %lu）。";
        return "Cannot open file for mapping (Windows error %lu).";
    }
    if (strcmp(msg, "Cannot open module %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot open module %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede abrir el módulo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’ouvrir le module %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Modul %s kann nicht geöffnet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile aprire il modulo %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατό το άνοιγμα της μονάδας %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся открыть модуль %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法打开模块 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法開啟模組 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュール %s を開けません。";
        return "Cannot open module %s.";
    }
    if (strcmp(msg, "Cannot read file %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot read file %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede leer el archivo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de lire le fichier %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Datei %s kann nicht gelesen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile leggere il file %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η ανάγνωση του αρχείου %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся прочитать файл %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法读取文件 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法讀取檔案 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル %s を読み取れません。";
        return "Cannot read file %s.";
    }
    if (strcmp(msg, "Cannot read file %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot read file %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede leer el archivo %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de lire le fichier %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Datei %s kann nicht gelesen werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile leggere il file %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η ανάγνωση του αρχείου %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось прочитать файл %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法读取文件 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法讀取檔案 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル%sを読み込めません。\n";
        return "Cannot read file %s.\n";
    }
    if (strcmp(msg, "Cannot read module %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot read module %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede leer el módulo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de lire le module %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Modul %s kann nicht gelesen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile leggere il modulo %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η ανάγνωση της μονάδας %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся прочитать модуль %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法读取模块 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法讀取模組 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュール %s を読み取れません。";
        return "Cannot read module %s.";
    }
    if (strcmp(msg, "Cannot receive from the connection.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot receive from the connection.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede recibir desde la conexión.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de recevoir des données depuis la connexion.";
        if (strncmp(lang_code, "de", 2) == 0) return "Von der Verbindung können keine Daten empfangen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile ricevere dalla connessione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η λήψη από τη σύνδεση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся получить данные из соединения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法从连接接收数据。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法從連線接收資料。";
        if (strncmp(lang_code, "ja", 2) == 0) return "接続から受信できません。";
        return "Cannot receive from the connection.";
    }
    if (strcmp(msg, "Cannot resolve required module '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot resolve required module '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede resolver el módulo requerido '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de résoudre le module requis '%s'.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das erforderliche Modul '%s' kann nicht aufgelöst werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile risolvere il modulo richiesto '%s'.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η επίλυση της απαιτούμενης μονάδας '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся разрешить требуемый модуль '%s'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法解析所需模块 '%s'。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法解析必要的模組 '%s'。";
        if (strncmp(lang_code, "ja", 2) == 0) return "必要なモジュール '%s' を解決できません。";
        return "Cannot resolve required module '%s'.";
    }
    if (strcmp(msg, "Cannot resolve the address %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot resolve the address %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede resolver la dirección %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de résoudre l’adresse %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Adresse %s kann nicht aufgelöst werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile risolvere l'indirizzo %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η επίλυση της διεύθυνσης %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся разрешить адрес %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法解析地址 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法解析位址 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アドレス %s を解決できません。";
        return "Cannot resolve the address %s.";
    }
    if (strcmp(msg, "Cannot send to the connection.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot send to the connection.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede enviar a la conexión.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’envoyer des données à la connexion.";
        if (strncmp(lang_code, "de", 2) == 0) return "An die Verbindung können keine Daten gesendet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile inviare alla connessione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αποστολή στη σύνδεση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся отправить данные в соединение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法向连接发送数据。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法將資料傳送至連線。";
        if (strncmp(lang_code, "ja", 2) == 0) return "接続に送信できません。";
        return "Cannot send to the connection.";
    }
    if (strcmp(msg, "Cannot unmap file mapping: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot unmap file mapping: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede desmapear el mapeo de archivos: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de démapper le mappage de fichier : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateizuordnung kann nicht aufgehoben werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile annullare la mappatura di file: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η κατάργηση της αντιστοίχισης αρχείου: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся отменить отображение файла: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法取消文件映射：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法取消檔案對應：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングを解除できません: %s";
        return "Cannot unmap file mapping: %s";
    }
    if (strcmp(msg, "Cannot unmap file view (Windows error %lu).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot unmap file view (Windows error %lu).";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede desmapear la vista del archivo (error de Windows %lu).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible de démapper la vue du fichier (erreur Windows %lu).";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Zuordnung der Dateiansicht kann nicht aufgehoben werden (Windows-Fehler %lu).";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile annullare la mappatura della vista del file (errore Windows %lu).";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η κατάργηση της αντιστοίχισης προβολής αρχείου (σφάλμα Windows %lu).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся отменить отображение представления файла (ошибка Windows %lu).";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法取消文件视图映射（Windows 错误 %lu）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法取消檔案檢視對應（Windows 錯誤 %lu）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルビューのマッピングを解除できません（Windows エラー %lu）。";
        return "Cannot unmap file view (Windows error %lu).";
    }
    if (strcmp(msg, "Cannot wait for socket events.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot wait for socket events.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede esperar a los eventos del socket.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’attendre les événements du socket.";
        if (strncmp(lang_code, "de", 2) == 0) return "Auf Socketereignisse kann nicht gewartet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile attendere gli eventi del socket.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η αναμονή για συμβάντα socket.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся ожидать события сокета.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法等待套接字事件。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法等候通訊端事件。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソケットイベントを待機できません。";
        return "Cannot wait for socket events.";
    }
    if (strcmp(msg, "Cannot write file %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Cannot write file %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede escribir el archivo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Impossible d’écrire le fichier %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Datei %s kann nicht geschrieben werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile scrivere il file %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι δυνατή η εγγραφή του αρχείου %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удаётся записать файл %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法写入文件 %s。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法寫入檔案 %s。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル %s に書き込めません。";
        return "Cannot write file %s.";
    }
    if (strcmp(msg, "Character class too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Character class too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "La clase de caracteres es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La classe de caractères est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Zeichenklasse ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "La classe di caratteri è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η κλάση χαρακτήρων είναι υπερβολικά μεγάλη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Класс символов слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字符类过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "字元類別過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "文字クラスが大きすぎます。";
        return "Character class too large.";
    }
    if (strcmp(msg, "Circular require involving %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Circular require involving %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "Dependencia require circular que implica a %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Dépendance require circulaire impliquant %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zirkuläres require unter Beteiligung von %s.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dipendenza require circolare che coinvolge %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κυκλικό require που περιλαμβάνει το %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Циклический require с участием %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "涉及 %s 的循环 require。";
        if (strncmp(lang_code, "tw", 2) == 0) return "涉及 %s 的循環 require。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s が関与する循環 require です。";
        return "Circular require involving %s.";
    }
    if (strcmp(msg, "Code too big.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Code too big.";
        if (strncmp(lang_code, "es", 2) == 0) return "El código es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le code est trop volumineux.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Code ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il codice è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο κώδικας είναι πολύ μεγάλος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Код слишком большой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "代码过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "程式碼過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "コードが大きすぎます。";
        return "Code too big.";
    }
    if (strcmp(msg, "Condition is not a number.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Condition is not a number.";
        if (strncmp(lang_code, "es", 2) == 0) return "La condición no es un número.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La condition n’est pas un nombre.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Bedingung ist keine Zahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "La condizione non è un numero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συνθήκη δεν είναι αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Условие не является числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "条件不是数字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "條件不是數字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "条件が数値ではありません。";
        return "Condition is not a number.";
    }
    if (strcmp(msg, "CreateProcess() failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "CreateProcess() failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "CreateProcess() falló.";
        if (strncmp(lang_code, "fr", 2) == 0) return "CreateProcess() a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "CreateProcess() ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "CreateProcess() non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η CreateProcess() απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой CreateProcess().";
        if (strncmp(lang_code, "zh", 2) == 0) return "CreateProcess() 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "CreateProcess() 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "CreateProcess() に失敗しました。";
        return "CreateProcess() failed.";
    }
    if (strcmp(msg, "Decimal integer literal is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Decimal integer literal is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El literal entero decimal es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le littéral entier décimal est trop grand.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das dezimale Ganzzahlliteral ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il letterale intero decimale è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η δεκαδική ακέραια σταθερά είναι υπερβολικά μεγάλη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Десятичный целочисленный литерал слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "十进制整数字面量过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "十進位整數常值過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "10 進整数リテラルが大きすぎます。";
        return "Decimal integer literal is too large.";
    }
    if (strcmp(msg, "Detached accelerator context.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Detached accelerator context.";
        if (strncmp(lang_code, "es", 2) == 0) return "Contexto del acelerador desconectado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Contexte de l’accélérateur détaché.";
        if (strncmp(lang_code, "de", 2) == 0) return "Beschleunigerkontext wurde getrennt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Contesto dell'acceleratore scollegato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποσυνδεδεμένο περιβάλλον επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Контекст ускорителя отсоединён.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器上下文已分离。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器內容已中斷連接。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのコンテキストが切断されています。";
        return "Detached accelerator context.";
    }
    if (strcmp(msg, "Dictionary index %ld is out-of-range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Dictionary index %ld is out-of-range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El índice del diccionario %ld está fuera de rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'index du dictionnaire %ld est hors de portée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wörterbuch-Index %ld ist außerhalb des Bereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'indice del dizionario %ld è fuori intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο δείκτης λεξικού %ld είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс словаря %ld вне диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字典索引 %ld 超出范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "字典索引 %ld 超出範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "辞書のインデックス %ld は範囲外です。";
        return "Dictionary index %ld is out-of-range.";
    }
    if (strcmp(msg, "Dictionary is frozen.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Dictionary is frozen.";
        if (strncmp(lang_code, "es", 2) == 0) return "El diccionario está congelado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le dictionnaire est figé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Dictionary ist eingefroren.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dizionario è bloccato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το λεξικό είναι παγωμένο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Словарь заморожен.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字典已冻结。";
        if (strncmp(lang_code, "tw", 2) == 0) return "字典已凍結。";
        if (strncmp(lang_code, "ja", 2) == 0) return "辞書は凍結されています。";
        return "Dictionary is frozen.";
    }
    if (strcmp(msg, "Dictionary key \"%s\" not found.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Dictionary key \"%s\" not found.";
        if (strncmp(lang_code, "es", 2) == 0) return "La llave \"%s\" no se encontró en el diccionario.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La clé \"%s\" du dictionnaire est introuvable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Schlüssel \"%s\" im Dictionary nicht gefunden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiave \"%s\" non trovata nel dizionario.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το κλειδί \"%s\" δεν βρέθηκε στο λεξικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ключ \"%s\" не найден в словаре.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未找到字典键 \"%s\"。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未找到字典鍵 \"%s\"。";
        if (strncmp(lang_code, "ja", 2) == 0) return "辞書のキー \"%s\" がみつかりません。";
        return "Dictionary key \"%s\" not found.";
    }
    if (strcmp(msg, "Direct or mutually recursive __fast calls are not supported.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct or mutually recursive __fast calls are not supported.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se admiten llamadas __fast recursivas directas ni mutuas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les appels __fast récursifs directs ou mutuels ne sont pas pris en charge.";
        if (strncmp(lang_code, "de", 2) == 0) return "Direkte oder gegenseitig rekursive __fast-Aufrufe werden nicht unterstützt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Le chiamate __fast ricorsive dirette o reciproche non sono supportate.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υποστηρίζονται άμεσες ή αμοιβαία αναδρομικές κλήσεις __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Прямые и взаимно рекурсивные вызовы __fast не поддерживаются.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持直接或相互递归的 __fast 调用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援直接或相互遞迴的 __fast 呼叫。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接または相互再帰する __fast 呼び出しはサポートされていません。";
        return "Direct or mutually recursive __fast calls are not supported.";
    }
    if (strcmp(msg, "Direct3D 12 buffer extent mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 buffer extent mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "La extensión del búfer de Direct3D 12 no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’étendue du tampon Direct3D 12 ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausdehnung des Direct3D 12-Puffers stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "L'estensione del buffer Direct3D 12 non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έκταση της ενδιάμεσης μνήμης Direct3D 12 δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несоответствие размера измерения буфера Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 缓冲区维度大小不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 緩衝區維度大小不相符";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 バッファの次元サイズが一致しません";
        return "Direct3D 12 buffer extent mismatch";
    }
    if (strcmp(msg, "Direct3D 12 buffer is not word aligned") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 buffer is not word aligned";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer de Direct3D 12 no está alineado a palabra";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon Direct3D 12 n’est pas aligné sur un mot";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Direct3D 12-Puffer ist nicht wortausgerichtet";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer Direct3D 12 non è allineato alla parola";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη Direct3D 12 δεν είναι ευθυγραμμισμένη σε λέξη";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер Direct3D 12 не выровнен по слову";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 缓冲区未按字对齐";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 緩衝區未按字組對齊";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 バッファがワード境界にアラインされていません";
        return "Direct3D 12 buffer is not word aligned";
    }
    if (strcmp(msg, "Direct3D 12 buffer is too large for one UAV") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 buffer is too large for one UAV";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer de Direct3D 12 es demasiado grande para una sola UAV";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon Direct3D 12 est trop grand pour une seule UAV";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Direct3D 12-Puffer ist zu groß für eine einzelne UAV";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer Direct3D 12 è troppo grande per una sola UAV";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη Direct3D 12 είναι υπερβολικά μεγάλη για ένα UAV";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер Direct3D 12 слишком велик для одного UAV";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 缓冲区对于单个 UAV 而言过大";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 緩衝區對單一 UAV 而言過大";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 バッファが 1 個の UAV には大きすぎます";
        return "Direct3D 12 buffer is too large for one UAV";
    }
    if (strcmp(msg, "Direct3D 12 buffer metadata changed") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 buffer metadata changed";
        if (strncmp(lang_code, "es", 2) == 0) return "Los metadatos del búfer de Direct3D 12 han cambiado";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées du tampon Direct3D 12 ont changé";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Metadaten des Direct3D 12-Puffers haben sich geändert";
        if (strncmp(lang_code, "it", 2) == 0) return "I metadati del buffer Direct3D 12 sono cambiati";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα μεταδεδομένα της ενδιάμεσης μνήμης Direct3D 12 άλλαξαν";
        if (strncmp(lang_code, "ru", 2) == 0) return "Метаданные буфера Direct3D 12 изменились";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 缓冲区元数据已更改";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 緩衝區中繼資料已變更";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 バッファのメタデータが変更されました";
        return "Direct3D 12 buffer metadata changed";
    }
    if (strcmp(msg, "Direct3D 12 buffer metadata does not match the program") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 buffer metadata does not match the program";
        if (strncmp(lang_code, "es", 2) == 0) return "Los metadatos del búfer de Direct3D 12 no coinciden con el programa";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées du tampon Direct3D 12 ne correspondent pas au programme";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Metadaten des Direct3D 12-Puffers stimmen nicht mit dem Programm überein";
        if (strncmp(lang_code, "it", 2) == 0) return "I metadati del buffer Direct3D 12 non corrispondono al programma";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα μεταδεδομένα της ενδιάμεσης μνήμης Direct3D 12 δεν αντιστοιχούν στο πρόγραμμα";
        if (strncmp(lang_code, "ru", 2) == 0) return "Метаданные буфера Direct3D 12 не соответствуют программе";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 缓冲区元数据与程序不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 緩衝區中繼資料與程式不相符";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 バッファのメタデータがプログラムと一致しません";
        return "Direct3D 12 buffer metadata does not match the program";
    }
    if (strcmp(msg, "Direct3D 12 buffer size overflow") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 buffer size overflow";
        if (strncmp(lang_code, "es", 2) == 0) return "Desbordamiento del tamaño del búfer de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "Dépassement de capacité de la taille du tampon Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Überlauf der Größe des Direct3D 12-Puffers";
        if (strncmp(lang_code, "it", 2) == 0) return "Overflow della dimensione del buffer Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερχείλιση μεγέθους ενδιάμεσης μνήμης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переполнение размера буфера Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 缓冲区大小溢出";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 緩衝區大小溢位";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 バッファサイズのオーバーフロー";
        return "Direct3D 12 buffer size overflow";
    }
    if (strcmp(msg, "Direct3D 12 buffer table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 buffer table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de búferes de Direct3D 12 no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des tampons Direct3D 12 ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Puffertabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei buffer Direct3D 12 non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας ενδιάμεσων μνημών Direct3D 12 δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несоответствие таблицы буферов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 缓冲区表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 緩衝區表不相符";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 バッファテーブルが一致しません";
        return "Direct3D 12 buffer table mismatch";
    }
    if (strcmp(msg, "Direct3D 12 dispatch exceeds 65535 workgroups") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 dispatch exceeds 65535 workgroups";
        if (strncmp(lang_code, "es", 2) == 0) return "El despacho de Direct3D 12 supera los 65535 grupos de trabajo";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le lancement Direct3D 12 dépasse 65535 groupes de travail";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Direct3D 12-Dispatch überschreitet 65535 Arbeitsgruppen";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispatch Direct3D 12 supera 65535 gruppi di lavoro";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αποστολή Direct3D 12 υπερβαίνει τις 65535 ομάδες εργασίας";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диспетчеризация Direct3D 12 превышает 65535 рабочих групп";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 调度超过 65535 个工作组";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 調度超過 65535 個工作群組";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 ディスパッチが 65535 ワークグループを超えています";
        return "Direct3D 12 dispatch exceeds 65535 workgroups";
    }
    if (strcmp(msg, "Direct3D 12 execution is not recording") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 execution is not recording";
        if (strncmp(lang_code, "es", 2) == 0) return "La ejecución de Direct3D 12 no está grabando";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’exécution Direct3D 12 n’est pas en cours d’enregistrement";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Ausführung zeichnet nicht auf";
        if (strncmp(lang_code, "it", 2) == 0) return "L'esecuzione Direct3D 12 non sta registrando";
        if (strncmp(lang_code, "el", 2) == 0) return "Η εκτέλεση Direct3D 12 δεν καταγράφει";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выполнение Direct3D 12 не записывается";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 执行未在记录";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 執行未在記錄";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 の実行は記録中ではありません";
        return "Direct3D 12 execution is not recording";
    }
    if (strcmp(msg, "Direct3D 12 fence counter overflowed") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 fence counter overflowed";
        if (strncmp(lang_code, "es", 2) == 0) return "Se produjo un desbordamiento del contador de la valla de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le compteur de la barrière Direct3D 12 a subi un dépassement de capacité";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Fence-Zähler von Direct3D 12 ist übergelaufen";
        if (strncmp(lang_code, "it", 2) == 0) return "Si è verificato un overflow del contatore del fence Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπήρξε υπερχείλιση του μετρητή φράκτη Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение счётчика fence Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 栅栏计数器溢出";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 柵欄計數器溢位";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 のフェンスカウンタがオーバーフローしました";
        return "Direct3D 12 fence counter overflowed";
    }
    if (strcmp(msg, "Direct3D 12 output table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 output table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de salida de Direct3D 12 no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table de sortie Direct3D 12 ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Ausgabetabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella di output Direct3D 12 non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας εξόδου Direct3D 12 δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несоответствие таблицы вывода Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 输出表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 輸出表不相符";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 出力テーブルが一致しません";
        return "Direct3D 12 output table mismatch";
    }
    if (strcmp(msg, "Direct3D 12 result byte count overflow") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 result byte count overflow";
        if (strncmp(lang_code, "es", 2) == 0) return "Desbordamiento de la cantidad de bytes del resultado de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "Dépassement de capacité du nombre d’octets du résultat Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Überlauf der Byteanzahl des Direct3D 12-Ergebnisses";
        if (strncmp(lang_code, "it", 2) == 0) return "Overflow del numero di byte del risultato Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερχείλιση πλήθους byte του αποτελέσματος Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переполнение количества байтов результата Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 结果字节数溢出";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 結果位元組數量溢位";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 結果のバイト数がオーバーフローしました";
        return "Direct3D 12 result byte count overflow";
    }
    if (strcmp(msg, "Direct3D 12 result table changed") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 result table changed";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de resultados de Direct3D 12 ha cambiado";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des résultats Direct3D 12 a changé";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Ergebnistabelle hat sich geändert";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei risultati Direct3D 12 è cambiata";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας αποτελεσμάτων Direct3D 12 άλλαξε";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица результатов Direct3D 12 изменилась";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 结果表已更改";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 結果表已變更";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 結果テーブルが変更されました";
        return "Direct3D 12 result table changed";
    }
    if (strcmp(msg, "Direct3D 12 result table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 result table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de resultados de Direct3D 12 no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des résultats Direct3D 12 ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Ergebnistabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei risultati Direct3D 12 non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας αποτελεσμάτων Direct3D 12 δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несоответствие таблицы результатов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 结果表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 結果表不相符";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 結果テーブルが一致しません";
        return "Direct3D 12 result table mismatch";
    }
    if (strcmp(msg, "Direct3D 12 scalar byte count overflow") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 scalar byte count overflow";
        if (strncmp(lang_code, "es", 2) == 0) return "Desbordamiento de la cantidad de bytes escalares de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "Dépassement de capacité du nombre d’octets scalaires Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Überlauf der skalaren Byteanzahl von Direct3D 12";
        if (strncmp(lang_code, "it", 2) == 0) return "Overflow del numero di byte scalari Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερχείλιση πλήθους βαθμωτών byte του Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переполнение количества скалярных байтов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 标量字节数溢出";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 純量位元組數量溢位";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 スカラーのバイト数がオーバーフローしました";
        return "Direct3D 12 scalar byte count overflow";
    }
    if (strcmp(msg, "Direct3D 12 scalar table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 scalar table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de escalares de Direct3D 12 no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des scalaires Direct3D 12 ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Skalartabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella degli scalari Direct3D 12 non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας βαθμωτών Direct3D 12 δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несоответствие таблицы скаляров Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 标量表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 純量表不相符";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 スカラーテーブルが一致しません";
        return "Direct3D 12 scalar table mismatch";
    }
    if (strcmp(msg, "Direct3D 12 scalar table overflow") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 scalar table overflow";
        if (strncmp(lang_code, "es", 2) == 0) return "Desbordamiento de la tabla de escalares de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "Dépassement de capacité de la table des scalaires Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Überlauf der Direct3D 12-Skalartabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "Overflow della tabella degli scalari Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερχείλιση πίνακα βαθμωτών Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переполнение таблицы скаляров Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 标量表溢出";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 純量表溢位";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 スカラーテーブルがオーバーフローしました";
        return "Direct3D 12 scalar table overflow";
    }
    if (strcmp(msg, "Direct3D 12 supports only 32-bit buffers") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Direct3D 12 supports only 32-bit buffers";
        if (strncmp(lang_code, "es", 2) == 0) return "Direct3D 12 solo admite búferes de 32 bits";
        if (strncmp(lang_code, "fr", 2) == 0) return "Direct3D 12 ne prend en charge que les tampons 32 bits";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12 unterstützt nur 32-Bit-Puffer";
        if (strncmp(lang_code, "it", 2) == 0) return "Direct3D 12 supporta solo buffer a 32 bit";
        if (strncmp(lang_code, "el", 2) == 0) return "Το Direct3D 12 υποστηρίζει μόνο ενδιάμεσες μνήμες 32 bit";
        if (strncmp(lang_code, "ru", 2) == 0) return "Direct3D 12 поддерживает только 32-битные буферы";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 仅支持 32 位缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 僅支援 32 位元緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 は 32 ビットバッファのみサポートします";
        return "Direct3D 12 supports only 32-bit buffers";
    }
    if (strcmp(msg, "Directory path must not be empty.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Directory path must not be empty.";
        if (strncmp(lang_code, "es", 2) == 0) return "La ruta del directorio no debe estar vacía.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le chemin du répertoire ne doit pas être vide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Verzeichnispfad darf nicht leer sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il percorso della directory non deve essere vuoto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η διαδρομή καταλόγου δεν πρέπει να είναι κενή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Путь к каталогу не должен быть пустым.";
        if (strncmp(lang_code, "zh", 2) == 0) return "目录路径不能为空。";
        if (strncmp(lang_code, "tw", 2) == 0) return "目錄路徑不得為空。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ディレクトリパスを空にすることはできません。";
        return "Directory path must not be empty.";
    }
    if (strcmp(msg, "Division by zero.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Division by zero.";
        if (strncmp(lang_code, "es", 2) == 0) return "División por cero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Division par zéro.";
        if (strncmp(lang_code, "de", 2) == 0) return "Division durch Null.";
        if (strncmp(lang_code, "it", 2) == 0) return "Divisione per zero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Διαίρεση με το μηδέν.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Деление на ноль.";
        if (strncmp(lang_code, "zh", 2) == 0) return "被零除。";
        if (strncmp(lang_code, "tw", 2) == 0) return "被零除。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ゼロによる除算です。";
        return "Division by zero.";
    }
    if (strcmp(msg, "Duplicate Noct App input: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate Noct App input: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Entrada duplicada de Noct App: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Entrée Noct App en double : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppelte Noct App-Eingabe: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Input Noct App duplicato: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπη είσοδος Noct App: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующийся ввод Noct App: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "重复的 Noct App 输入：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "重複的 Noct App 輸入：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "Noct App 入力が重複しています: %s";
        return "Duplicate Noct App input: %s";
    }
    if (strcmp(msg, "Duplicate accelerator region index.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate accelerator region index.";
        if (strncmp(lang_code, "es", 2) == 0) return "Índice de región del acelerador duplicado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Index de région de l’accélérateur en double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppelter Regionsindex des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Indice di regione dell'acceleratore duplicato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπος δείκτης περιοχής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующийся индекс области ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器区域索引重复。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域索引重複。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのリージョンインデックスが重複しています。";
        return "Duplicate accelerator region index.";
    }
    if (strcmp(msg, "Duplicate accelerator scalar result argument.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate accelerator scalar result argument.";
        if (strncmp(lang_code, "es", 2) == 0) return "Argumento de resultado escalar del acelerador duplicado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Argument de résultat scalaire de l’accélérateur en double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppeltes Argument für das skalare Ergebnis des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Argomento del risultato scalare dell'acceleratore duplicato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπο όρισμα βαθμωτού αποτελέσματος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующийся аргумент скалярного результата ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果实参重复。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果引數重複。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果引数が重複しています。";
        return "Duplicate accelerator scalar result argument.";
    }
    if (strcmp(msg, "Duplicate input module path: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate input module path: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Ruta de módulo de entrada duplicada: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Chemin de module d’entrée en double : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppelter Pfad des Eingabemoduls: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Percorso del modulo di input duplicato: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπη διαδρομή μονάδας εισόδου: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующийся путь входного модуля: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "输入模块路径重复：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "輸入模組路徑重複：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "入力モジュールのパスが重複しています: %s";
        return "Duplicate input module path: %s";
    }
    if (strcmp(msg, "Duplicate logical source name for %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate logical source name for %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "Nombre lógico de código fuente duplicado para %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nom logique de code source en double pour %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppelter logischer Quellname für %s.";
        if (strncmp(lang_code, "it", 2) == 0) return "Nome logico del codice sorgente duplicato per %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπο λογικό όνομα πηγαίου κώδικα για το %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующееся логическое имя исходного кода для %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 的逻辑源名称重复。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 的邏輯原始碼名稱重複。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%s の論理ソース名が重複しています。";
        return "Duplicate logical source name for %s.";
    }
    if (strcmp(msg, "Duplicate public symbol \"%s\" (Duplicate function in module closure).") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate public symbol \"%s\" (Duplicate function in module closure).";
        if (strncmp(lang_code, "es", 2) == 0) return "Símbolo público duplicado \"%s\" (función duplicada en el cierre de módulos).";
        if (strncmp(lang_code, "fr", 2) == 0) return "Symbole public \"%s\" en double (fonction en double dans la fermeture des modules).";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppeltes öffentliches Symbol \"%s\" (doppelte Funktion im Modulabschluss).";
        if (strncmp(lang_code, "it", 2) == 0) return "Simbolo pubblico \"%s\" duplicato (funzione duplicata nella chiusura dei moduli).";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπο δημόσιο σύμβολο \"%s\" (διπλότυπη συνάρτηση στο κλείσιμο μονάδων).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующийся открытый символ \"%s\" (дублирующаяся функция в замыкании модулей).";
        if (strncmp(lang_code, "zh", 2) == 0) return "公共符号 \"%s\" 重复（模块闭包中存在重复函数）。";
        if (strncmp(lang_code, "tw", 2) == 0) return "公用符號 \"%s\" 重複（模組閉包中存在重複函式）。";
        if (strncmp(lang_code, "ja", 2) == 0) return "公開シンボル \"%s\" が重複しています（モジュールクロージャ内の関数が重複しています）。";
        return "Duplicate public symbol \"%s\" (Duplicate function in module closure).";
    }
    if (strcmp(msg, "Duplicate static declaration '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate static declaration '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "Declaración static duplicada '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Déclaration static '%s' en double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppelte static-Deklaration '%s'.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dichiarazione static '%s' duplicata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπη δήλωση static '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующееся объявление static '%s'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "static 声明 '%s' 重复。";
        if (strncmp(lang_code, "tw", 2) == 0) return "static 宣告 '%s' 重複。";
        if (strncmp(lang_code, "ja", 2) == 0) return "static 宣言 '%s' が重複しています。";
        return "Duplicate static declaration '%s'.";
    }
    if (strcmp(msg, "Duplicate top-level declaration '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Duplicate top-level declaration '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "Declaración de nivel superior duplicada '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Déclaration de premier niveau '%s' en double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Doppelte Deklaration auf oberster Ebene '%s'.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dichiarazione di primo livello '%s' duplicata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Διπλότυπη δήλωση ανώτατου επιπέδου '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дублирующееся объявление верхнего уровня '%s'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "顶层声明 '%s' 重复。";
        if (strncmp(lang_code, "tw", 2) == 0) return "頂層宣告 '%s' 重複。";
        if (strncmp(lang_code, "ja", 2) == 0) return "トップレベル宣言 '%s' が重複しています。";
        return "Duplicate top-level declaration '%s'.";
    }
    if (strcmp(msg, "Element %d is not a dictionary.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not a dictionary.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es un diccionario.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas un dictionnaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist kein Dictionary.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è un dizionario.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το στοιχείο %d δεν είναι λεξικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является словарем.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是字典。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是字典。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が辞書ではありません。";
        return "Element %d is not a dictionary.";
    }
    if (strcmp(msg, "Element %d is not a double.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not a double.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es un double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas un double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist kein Double.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è un double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το στοιχείο %d δεν είναι double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是 double。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是 double。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が double ではありません。";
        return "Element %d is not a double.";
    }
    if (strcmp(msg, "Element %d is not a float.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not a float.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es un número decimal.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas un nombre à virgule flottante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist keine Gleitkommazahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è un numero decimale.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το στοιχείο %d δεν είναι δεκαδικός αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является числом с плавающей точкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是浮点数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是浮點數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が浮動小数点数ではありません。";
        return "Element %d is not a float.";
    }
    if (strcmp(msg, "Element %d is not a function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not a function.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es una función.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas une fonction.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist keine Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è una funzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "L'elemento %d non è una funzione.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является функцией.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是函数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是函式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が関数ではありません。";
        return "Element %d is not a function.";
    }
    if (strcmp(msg, "Element %d is not a long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not a long.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es un long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas un long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist kein Long.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è un long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το στοιχείο %d δεν είναι long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是 long。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是 long。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が long ではありません。";
        return "Element %d is not a long.";
    }
    if (strcmp(msg, "Element %d is not a string.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not a string.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es una cadena.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas une chaîne de caractères.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist keine Zeichenkette.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è una stringa.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το στοιχείο %d δεν είναι συμβολοσειρά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является строкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是字符串。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是字串。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が文字列ではありません。";
        return "Element %d is not a string.";
    }
    if (strcmp(msg, "Element %d is not an array.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not an array.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es una matriz.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas un tableau.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist kein Array.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è un array.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το στοιχείο %d δεν είναι πίνακας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является массивом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是数组。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是陣列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が配列ではありません。";
        return "Element %d is not an array.";
    }
    if (strcmp(msg, "Element %d is not an int.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Element %d is not an int.";
        if (strncmp(lang_code, "es", 2) == 0) return "El elemento %d no es un int.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'élément %d n'est pas un int.";
        if (strncmp(lang_code, "de", 2) == 0) return "Element %d ist kein Int.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'elemento %d non è un int.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το στοιχείο %d δεν είναι int.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Элемент %d не является int.";
        if (strncmp(lang_code, "zh", 2) == 0) return "第 %d 个元素不是 int。";
        if (strncmp(lang_code, "tw", 2) == 0) return "第 %d 個元素不是 int。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%d 番目の要素が int ではありません。";
        return "Element %d is not an int.";
    }
    if (strcmp(msg, "Empty accelerator function plan.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Empty accelerator function plan.";
        if (strncmp(lang_code, "es", 2) == 0) return "Plan de función del acelerador vacío.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Plan de fonction d’accélérateur vide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Leerer Beschleuniger-Funktionsplan.";
        if (strncmp(lang_code, "it", 2) == 0) return "Piano della funzione dell’acceleratore vuoto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κενό σχέδιο συνάρτησης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Пустой план функции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器函数计划为空。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器函式計畫為空。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ関数プランが空です。";
        return "Empty accelerator function plan.";
    }
    if (strcmp(msg, "Empty accelerator region program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Empty accelerator region program.";
        if (strncmp(lang_code, "es", 2) == 0) return "Programa de región del acelerador vacío.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Programme de région d’accélérateur vide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Leeres Beschleuniger-Regionsprogramm.";
        if (strncmp(lang_code, "it", 2) == 0) return "Programma della regione dell’acceleratore vuoto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κενό πρόγραμμα περιοχής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Пустая программа области ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器区域程序为空。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域程式為空。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリージョンプログラムが空です。";
        return "Empty accelerator region program.";
    }
    if (strcmp(msg, "Empty array.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Empty array.";
        if (strncmp(lang_code, "es", 2) == 0) return "Arreglo vacío.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Tableau vide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Leeres Array.";
        if (strncmp(lang_code, "it", 2) == 0) return "Array vuoto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κενός πίνακας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Пустой массив.";
        if (strncmp(lang_code, "zh", 2) == 0) return "空数组。";
        if (strncmp(lang_code, "tw", 2) == 0) return "空陣列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "配列が空です。";
        return "Empty array.";
    }
    if (strcmp(msg, "Entering REPL mode.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Entering REPL mode.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Entrando en modo REPL.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Entrée en mode REPL.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "REPL-Modus wird gestartet.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Accesso alla modalità REPL.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Είσοδος σε λειτουργία REPL.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переход в режим REPL.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "进入 REPL 模式。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "進入 REPL 模式。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "REPLモードに入ります。\n";
        return "Entering REPL mode.\n";
    }
    if (strcmp(msg, "Error: %s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: %s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: %s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : %s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: %s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: %s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: %s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: %s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：%s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：%s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: %s\n";
        return "Error: %s\n";
    }
    if (strcmp(msg, "Error: %s:%d: %s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: %s:%d: %s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: %s:%d: %s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur: %s:%d: %s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: %s:%d: %s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: %s:%d: %s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: %s:%d: %s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: %s:%d: %s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误: %s:%d: %s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤: %s:%d: %s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: %s:%d: %s\n";
        return "Error: %s:%d: %s\n";
    }
    if (strcmp(msg, "Error: Cannot assemble application records.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: Cannot assemble application records.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: No se pueden ensamblar los registros de la aplicación.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : impossible d’assembler les enregistrements de l’application.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: Anwendungsdatensätze können nicht assembliert werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: impossibile assemblare i record dell’applicazione.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Δεν είναι δυνατή η συναρμολόγηση των εγγραφών της εφαρμογής.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: не удалось собрать записи приложения.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：无法汇编应用程序记录。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：無法組譯應用程式記錄。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: アプリケーションレコードをアセンブルできません。\n";
        return "Error: Cannot assemble application records.\n";
    }
    if (strcmp(msg, "Error: Cannot prepare application module %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: Cannot prepare application module %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: No se puede preparar el módulo de aplicación %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : impossible de préparer le module d’application %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: Anwendungsmodul %s kann nicht vorbereitet werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: impossibile preparare il modulo dell’applicazione %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Δεν είναι δυνατή η προετοιμασία της μονάδας εφαρμογής %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: не удалось подготовить модуль приложения %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：无法准备应用程序模块 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：無法準備應用程式模組 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: アプリケーションモジュール %s を準備できません。\n";
        return "Error: Cannot prepare application module %s.\n";
    }
    if (strcmp(msg, "Error: Invalid application at byte %lu: %s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: Invalid application at byte %lu: %s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: Aplicación no válida en el byte %lu: %s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : application non valide à l’octet %lu : %s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: Ungültige Anwendung bei Byte %lu: %s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: applicazione non valida al byte %lu: %s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Μη έγκυρη εφαρμογή στο byte %lu: %s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: недопустимое приложение в байте %lu: %s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：应用程序在字节 %lu 处无效：%s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：應用程式在位元組 %lu 處無效：%s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: バイト %lu に無効なアプリケーションがあります: %s\n";
        return "Error: Invalid application at byte %lu: %s\n";
    }
    if (strcmp(msg, "Error: Invalid application source name.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: Invalid application source name.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: Nombre de origen de aplicación no válido.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : nom de source d’application non valide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: Ungültiger Anwendungsquellname.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: nome sorgente dell’applicazione non valido.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Μη έγκυρο όνομα πηγής εφαρμογής.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: недопустимое имя источника приложения.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：应用程序源名称无效。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：應用程式來源名稱無效。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: アプリケーションのソース名が無効です。\n";
        return "Error: Invalid application source name.\n";
    }
    if (strcmp(msg, "Error: Invalid bytecode source name.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: Invalid bytecode source name.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: Nombre de origen de bytecode no válido.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : nom de source de bytecode non valide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: Ungültiger Bytecode-Quellname.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: nome sorgente del bytecode non valido.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Μη έγκυρο όνομα πηγής bytecode.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: недопустимое имя источника байткода.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：字节码源名称无效。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：位元組碼來源名稱無效。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: バイトコードのソース名が無効です。\n";
        return "Error: Invalid bytecode source name.\n";
    }
    if (strcmp(msg, "Error: __fast func is not supported by the Emacs Lisp transpiler.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: __fast func is not supported by the Emacs Lisp transpiler.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: El transpilador de Emacs Lisp no admite __fast func.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : le transpileur Emacs Lisp ne prend pas en charge __fast func.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: __fast func wird vom Emacs Lisp-Transpiler nicht unterstützt.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: __fast func non è supportata dal transpiler Emacs Lisp.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Το __fast func δεν υποστηρίζεται από τον transpiler Emacs Lisp.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: __fast func не поддерживается транспилятором Emacs Lisp.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：Emacs Lisp 转译器不支持 __fast func。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：Emacs Lisp 轉譯器不支援 __fast func。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: Emacs Lispトランスパイラは__fast funcをサポートしていません。\n";
        return "Error: __fast func is not supported by the Emacs Lisp transpiler.\n";
    }
    if (strcmp(msg, "Error: __fast func is not supported by the Scheme transpiler.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: __fast func is not supported by the Scheme transpiler.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: El transpilador de Scheme no admite __fast func.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : le transpileur Scheme ne prend pas en charge __fast func.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: __fast func wird vom Scheme-Transpiler nicht unterstützt.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: __fast func non è supportata dal transpiler Scheme.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Το __fast func δεν υποστηρίζεται από τον transpiler Scheme.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: __fast func не поддерживается транспилятором Scheme.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：Scheme 转译器不支持 __fast func。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：Scheme 轉譯器不支援 __fast func。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: Schemeトランスパイラは__fast funcをサポートしていません。\n";
        return "Error: __fast func is not supported by the Scheme transpiler.\n";
    }
    if (strcmp(msg, "Error: require is not supported by the C transpiler.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: require is not supported by the C transpiler.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: El transpilador de C no admite require.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : le transpileur C ne prend pas en charge require.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: require wird vom C-Transpiler nicht unterstützt.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: require non è supportato dal transpiler C.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Το require δεν υποστηρίζεται από τον transpiler C.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: require не поддерживается транспилятором C.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：C 转译器不支持 require。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：C 轉譯器不支援 require。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: Cトランスパイラはrequireをサポートしていません。\n";
        return "Error: require is not supported by the C transpiler.\n";
    }
    if (strcmp(msg, "Error: require is not supported by the Emacs Lisp transpiler.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: require is not supported by the Emacs Lisp transpiler.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: El transpilador de Emacs Lisp no admite require.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : le transpileur Emacs Lisp ne prend pas en charge require.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: require wird vom Emacs Lisp-Transpiler nicht unterstützt.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: require non è supportato dal transpiler Emacs Lisp.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Το require δεν υποστηρίζεται από τον transpiler Emacs Lisp.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: require не поддерживается транспилятором Emacs Lisp.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：Emacs Lisp 转译器不支持 require。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：Emacs Lisp 轉譯器不支援 require。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: Emacs Lispトランスパイラはrequireをサポートしていません。\n";
        return "Error: require is not supported by the Emacs Lisp transpiler.\n";
    }
    if (strcmp(msg, "Error: require is not supported by the Scheme transpiler.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Error: require is not supported by the Scheme transpiler.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error: El transpilador de Scheme no admite require.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur : le transpileur Scheme ne prend pas en charge require.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler: require wird vom Scheme-Transpiler nicht unterstützt.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore: require non è supportato dal transpiler Scheme.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα: Το require δεν υποστηρίζεται από τον transpiler Scheme.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка: require не поддерживается транспилятором Scheme.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "错误：Scheme 转译器不支持 require。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "錯誤：Scheme 轉譯器不支援 require。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "エラー: Schemeトランスパイラはrequireをサポートしていません。\n";
        return "Error: require is not supported by the Scheme transpiler.\n";
    }
    if (strcmp(msg, "Every __fast func parameter requires a type annotation.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Every __fast func parameter requires a type annotation.";
        if (strncmp(lang_code, "es", 2) == 0) return "Cada parámetro de una __fast func requiere una anotación de tipo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Chaque paramètre d’une __fast func nécessite une annotation de type.";
        if (strncmp(lang_code, "de", 2) == 0) return "Jeder Parameter einer __fast func erfordert eine Typanmerkung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ogni parametro di una __fast func richiede un’annotazione di tipo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κάθε παράμετρος ενός __fast func απαιτεί επισημείωση τύπου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Для каждого параметра __fast func требуется аннотация типа.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 的每个形参都需要类型注解。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 的每個參數都需要型別註釋。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast funcの各パラメータには型注釈が必要です。";
        return "Every __fast func parameter requires a type annotation.";
    }
    if (strcmp(msg, "Every local in a __fast func requires an exact primitive type annotation.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Every local in a __fast func requires an exact primitive type annotation.";
        if (strncmp(lang_code, "es", 2) == 0) return "Cada variable local de una __fast func requiere una anotación de tipo primitivo exacta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Chaque variable locale d’une __fast func nécessite une annotation de type primitif exacte.";
        if (strncmp(lang_code, "de", 2) == 0) return "Jede lokale Variable einer __fast func erfordert eine exakte Anmerkung eines primitiven Typs.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ogni variabile locale di una __fast func richiede un’annotazione esatta del tipo primitivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κάθε τοπική μεταβλητή ενός __fast func απαιτεί ακριβή επισημείωση πρωτογενούς τύπου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Для каждой локальной переменной __fast func требуется точная аннотация примитивного типа.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 的每个局部变量都需要精确的基本类型注解。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 的每個區域變數都需要精確的基本型別註釋。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast funcの各ローカル変数には正確なプリミティブ型注釈が必要です。";
        return "Every local in a __fast func requires an exact primitive type annotation.";
    }
    if (strcmp(msg, "Every reachable path of a non-void __fast func must return a value.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Every reachable path of a non-void __fast func must return a value.";
        if (strncmp(lang_code, "es", 2) == 0) return "Cada ruta alcanzable de una __fast func no void debe devolver un valor.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Chaque chemin accessible d’une __fast func non void doit renvoyer une valeur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Jeder erreichbare Pfad einer __fast func mit einem anderen Rückgabetyp als void muss einen Wert zurückgeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ogni percorso raggiungibile di una __fast func non void deve restituire un valore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κάθε προσπελάσιμη διαδρομή ενός __fast func που δεν είναι void πρέπει να επιστρέφει τιμή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Каждый достижимый путь __fast func с типом возврата, отличным от void, должен возвращать значение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "非 void 的 __fast func 的每条可达路径都必须返回值。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非 void 的 __fast func 的每條可達路徑都必須傳回值。";
        if (strncmp(lang_code, "ja", 2) == 0) return "void以外を返す__fast funcの到達可能な各パスは値を返す必要があります。";
        return "Every reachable path of a non-void __fast func must return a value.";
    }
    if (strcmp(msg, "Exact read byte count must be positive.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Exact read byte count must be positive.";
        if (strncmp(lang_code, "es", 2) == 0) return "La cantidad exacta de bytes de lectura debe ser positiva.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nombre exact d’octets à lire doit être positif.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die exakte Anzahl der zu lesenden Bytes muss positiv sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il numero esatto di byte da leggere deve essere positivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το ακριβές πλήθος byte ανάγνωσης πρέπει να είναι θετικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Точное количество считываемых байтов должно быть положительным.";
        if (strncmp(lang_code, "zh", 2) == 0) return "精确读取字节数必须为正数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "精確讀取位元組數必須為正數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "正確な読み取りバイト数は正である必要があります。";
        return "Exact read byte count must be positive.";
    }
    if (strcmp(msg, "Exceeded the maximum argument count.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Exceeded the maximum argument count.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se ha superado el número máximo de argumentos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nombre maximal d'arguments dépassé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die maximale Anzahl von Argumenten wurde überschritten.";
        if (strncmp(lang_code, "it", 2) == 0) return "È stato superato il numero massimo di argomenti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπέρβαση του μέγιστου αριθμού ορισμάτων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Превышено максимальное количество аргументов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "超过了最大参数数量。";
        if (strncmp(lang_code, "tw", 2) == 0) return "超過了最大參數數量。";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数が多すぎます。";
        return "Exceeded the maximum argument count.";
    }
    if (strcmp(msg, "Exceeded the maximum parameter count.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Exceeded the maximum parameter count.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se ha superado la cantidad máxima de parámetros.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nombre maximal de paramètres a été dépassé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die maximale Parameteranzahl wurde überschritten.";
        if (strncmp(lang_code, "it", 2) == 0) return "È stato superato il numero massimo di parametri.";
        if (strncmp(lang_code, "el", 2) == 0) return "Έγινε υπέρβαση του μέγιστου πλήθους παραμέτρων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Превышено максимальное количество параметров.";
        if (strncmp(lang_code, "zh", 2) == 0) return "超过最大形参数量。";
        if (strncmp(lang_code, "tw", 2) == 0) return "超過最大參數數量。";
        if (strncmp(lang_code, "ja", 2) == 0) return "パラメータの最大数を超えました。";
        return "Exceeded the maximum parameter count.";
    }
    if (strcmp(msg, "Exception target too far.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Exception target too far.";
        if (strncmp(lang_code, "es", 2) == 0) return "El destino de la excepción está demasiado lejos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La cible de l’exception est trop éloignée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Ausnahmeziel ist zu weit entfernt.";
        if (strncmp(lang_code, "it", 2) == 0) return "La destinazione dell’eccezione è troppo lontana.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο στόχος εξαίρεσης είναι πολύ μακριά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Цель исключения находится слишком далеко.";
        if (strncmp(lang_code, "zh", 2) == 0) return "异常目标太远。";
        if (strncmp(lang_code, "tw", 2) == 0) return "例外狀況目標太遠。";
        if (strncmp(lang_code, "ja", 2) == 0) return "例外ターゲットが遠すぎます。";
        return "Exception target too far.";
    }
    if (strcmp(msg, "Executable bytecode wrapper cannot be required: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Executable bytecode wrapper cannot be required: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "No se puede requerir el contenedor de bytecode ejecutable: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’enveloppe de bytecode exécutable ne peut pas être chargée avec require : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Der ausführbare Bytecode-Wrapper kann nicht mit require geladen werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Il wrapper del bytecode eseguibile non può essere caricato con require: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εκτελέσιμο περίβλημα bytecode δεν μπορεί να φορτωθεί με require: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Исполняемую оболочку байткода нельзя загрузить через require: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法通过 require 加载可执行字节码包装器：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法透過 require 載入可執行位元組碼包裝器：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "実行可能バイトコードラッパーをrequireできません: %s";
        return "Executable bytecode wrapper cannot be required: %s";
    }
    if (strcmp(msg, "Failed to acquire the OpenGL ES compiler context.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to acquire the OpenGL ES compiler context.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo adquirir el contexto del compilador OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’acquisition du contexte du compilateur OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Compilerkontext konnte nicht abgerufen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile acquisire il contesto del compilatore OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία απόκτησης του περιβάλλοντος μεταγλωττιστή OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось получить контекст компилятора OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法获取 OpenGL ES 编译器上下文。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法取得 OpenGL ES 編譯器內容。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンパイラコンテキストを取得できませんでした。";
        return "Failed to acquire the OpenGL ES compiler context.";
    }
    if (strcmp(msg, "Failed to allocate Vulkan descriptor sets.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to allocate Vulkan descriptor sets.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudieron asignar los conjuntos de descriptores Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’allocation des ensembles de descripteurs Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Deskriptorsätze konnten nicht zugewiesen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile allocare i set di descrittori Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία εκχώρησης συνόλων περιγραφέων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось выделить наборы дескрипторов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法分配 Vulkan 描述符集。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法配置 Vulkan 描述元集。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスクリプタセットを割り当てられませんでした。";
        return "Failed to allocate Vulkan descriptor sets.";
    }
    if (strcmp(msg, "Failed to allocate a Vulkan command buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to allocate a Vulkan command buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo asignar un búfer de comandos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’allocation d’un tampon de commandes Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Vulkan-Befehlspuffer konnte nicht zugewiesen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile allocare un buffer dei comandi Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία εκχώρησης ενδιάμεσης μνήμης εντολών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось выделить буфер команд Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法分配 Vulkan 命令缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法配置 Vulkan 命令緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanコマンドバッファを割り当てられませんでした。";
        return "Failed to allocate a Vulkan command buffer.";
    }
    if (strcmp(msg, "Failed to allocate an OpenGL ES buffer name.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to allocate an OpenGL ES buffer name.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo asignar un nombre de búfer OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’allocation d’un nom de tampon OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein OpenGL ES-Puffername konnte nicht zugewiesen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile allocare un nome di buffer OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία εκχώρησης ονόματος ενδιάμεσης μνήμης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось выделить имя буфера OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法分配 OpenGL ES 缓冲区名称。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法配置 OpenGL ES 緩衝區名稱。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファ名を割り当てられませんでした。";
        return "Failed to allocate an OpenGL ES buffer name.";
    }
    if (strcmp(msg, "Failed to analyze accelerator loop.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to analyze accelerator loop.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo analizar el bucle del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’analyse de la boucle de l’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Beschleunigerschleife konnte nicht analysiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile analizzare il ciclo dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ανάλυσης του βρόχου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось проанализировать цикл ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法分析加速器循环。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法分析加速器迴圈。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータループを解析できませんでした。";
        return "Failed to analyze accelerator loop.";
    }
    if (strcmp(msg, "Failed to begin a Vulkan command buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to begin a Vulkan command buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo iniciar un búfer de comandos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec du démarrage d’un tampon de commandes Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Vulkan-Befehlspuffer konnte nicht begonnen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile avviare un buffer dei comandi Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία έναρξης ενδιάμεσης μνήμης εντολών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось начать запись в буфер команд Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法开始 Vulkan 命令缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法開始 Vulkan 命令緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanコマンドバッファを開始できませんでした。";
        return "Failed to begin a Vulkan command buffer.";
    }
    if (strcmp(msg, "Failed to classify accelerator loop.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to classify accelerator loop.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo clasificar el bucle del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la classification de la boucle de l’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Beschleunigerschleife konnte nicht klassifiziert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile classificare il ciclo dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ταξινόμησης του βρόχου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось классифицировать цикл ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法对加速器循环进行分类。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對加速器迴圈進行分類。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータループを分類できませんでした。";
        return "Failed to classify accelerator loop.";
    }
    if (strcmp(msg, "Failed to classify accelerator reduction.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to classify accelerator reduction.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo clasificar la reducción del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la classification de la réduction de l’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Beschleunigerreduktion konnte nicht klassifiziert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile classificare la riduzione dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ταξινόμησης της αναγωγής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось классифицировать редукцию ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法对加速器归约进行分类。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對加速器歸約進行分類。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリダクションを分類できませんでした。";
        return "Failed to classify accelerator reduction.";
    }
    if (strcmp(msg, "Failed to compile a Direct3D 12 compute shader.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to compile a Direct3D 12 compute shader.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo compilar un shader de cómputo Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la compilation d’un shader de calcul Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Direct3D 12-Compute-Shader konnte nicht kompiliert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile compilare un compute shader Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία μεταγλώττισης compute shader Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось скомпилировать вычислительный шейдер Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法编译 Direct3D 12 计算着色器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法編譯 Direct3D 12 計算著色器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12コンピュートシェーダーをコンパイルできませんでした。";
        return "Failed to compile a Direct3D 12 compute shader.";
    }
    if (strcmp(msg, "Failed to compile an OpenGL ES compute shader.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to compile an OpenGL ES compute shader.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo compilar un shader de cómputo OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la compilation d’un shader de calcul OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein OpenGL ES-Compute-Shader konnte nicht kompiliert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile compilare un compute shader OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία μεταγλώττισης compute shader OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось скомпилировать вычислительный шейдер OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法编译 OpenGL ES 计算着色器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法編譯 OpenGL ES 計算著色器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンピュートシェーダーをコンパイルできませんでした。";
        return "Failed to compile an OpenGL ES compute shader.";
    }
    if (strcmp(msg, "Failed to create a Direct3D 12 compute pipeline.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Direct3D 12 compute pipeline.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear una pipeline de cómputo Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’une pipeline de calcul Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine Direct3D 12-Compute-Pipeline konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare una pipeline di calcolo Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας pipeline υπολογισμού Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать вычислительный конвейер Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 计算管线。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 計算管線。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12コンピュートパイプラインを作成できませんでした。";
        return "Failed to create a Direct3D 12 compute pipeline.";
    }
    if (strcmp(msg, "Failed to create a Direct3D 12 root signature.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Direct3D 12 root signature.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear una firma raíz Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’une signature racine Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine Direct3D 12-Root-Signatur konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare una firma radice Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας ριζικής υπογραφής Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать корневую сигнатуру Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 根签名。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 根簽章。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12ルートシグネチャを作成できませんでした。";
        return "Failed to create a Direct3D 12 root signature.";
    }
    if (strcmp(msg, "Failed to create a Vulkan 1.2 instance.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan 1.2 instance.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear una instancia de Vulkan 1.2.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’une instance Vulkan 1.2.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine Vulkan 1.2-Instanz konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un’istanza Vulkan 1.2.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας παρουσίας Vulkan 1.2.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать экземпляр Vulkan 1.2.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 1.2 实例。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 1.2 執行個體。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan 1.2インスタンスを作成できませんでした。";
        return "Failed to create a Vulkan 1.2 instance.";
    }
    if (strcmp(msg, "Failed to create a Vulkan command pool.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan command pool.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear un pool de comandos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’un pool de commandes Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Vulkan-Befehlspool konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un pool di comandi Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας δεξαμενής εντολών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать пул команд Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 命令池。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 命令集區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanコマンドプールを作成できませんでした。";
        return "Failed to create a Vulkan command pool.";
    }
    if (strcmp(msg, "Failed to create a Vulkan completion fence.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan completion fence.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear una valla de finalización Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’une barrière d’achèvement Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine Vulkan-Abschluss-Fence konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare una fence di completamento Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας φράκτη ολοκλήρωσης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать fence завершения Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 完成栅栏。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 完成柵欄。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan完了フェンスを作成できませんでした。";
        return "Failed to create a Vulkan completion fence.";
    }
    if (strcmp(msg, "Failed to create a Vulkan compute pipeline.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan compute pipeline.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear una pipeline de cómputo Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’une pipeline de calcul Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine Vulkan-Compute-Pipeline konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare una pipeline di calcolo Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας pipeline υπολογισμού Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать вычислительный конвейер Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 计算管线。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 計算管線。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanコンピュートパイプラインを作成できませんでした。";
        return "Failed to create a Vulkan compute pipeline.";
    }
    if (strcmp(msg, "Failed to create a Vulkan data buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan data buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear un búfer de datos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’un tampon de données Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Vulkan-Datenpuffer konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un buffer di dati Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας ενδιάμεσης μνήμης δεδομένων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать буфер данных Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 数据缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 資料緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanデータバッファを作成できませんでした。";
        return "Failed to create a Vulkan data buffer.";
    }
    if (strcmp(msg, "Failed to create a Vulkan descriptor layout.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan descriptor layout.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear un diseño de descriptores Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’une disposition de descripteurs Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Vulkan-Deskriptorlayout konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un layout di descrittori Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας διάταξης περιγραφέων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать схему дескрипторов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 描述符布局。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 描述元配置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスクリプタレイアウトを作成できませんでした。";
        return "Failed to create a Vulkan descriptor layout.";
    }
    if (strcmp(msg, "Failed to create a Vulkan pipeline layout.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan pipeline layout.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear un diseño de pipeline Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’une disposition de pipeline Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Vulkan-Pipeline-Layout konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un layout di pipeline Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας διάταξης pipeline Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать схему конвейера Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 管线布局。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 管線配置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanパイプラインレイアウトを作成できませんでした。";
        return "Failed to create a Vulkan pipeline layout.";
    }
    if (strcmp(msg, "Failed to create a Vulkan shader module.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create a Vulkan shader module.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear un módulo de shader Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’un module de shader Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein Vulkan-Shader-Modul konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un modulo shader Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας μονάδας shader Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать модуль шейдера Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 着色器模块。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 著色器模組。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanシェーダーモジュールを作成できませんでした。";
        return "Failed to create a Vulkan shader module.";
    }
    if (strcmp(msg, "Failed to create an OpenGL ES compute program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create an OpenGL ES compute program.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear un programa de cómputo OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’un programme de calcul OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein OpenGL ES-Compute-Programm konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un programma di calcolo OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας προγράμματος υπολογισμού OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать вычислительную программу OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 OpenGL ES 计算程序。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 OpenGL ES 計算程式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンピュートプログラムを作成できませんでした。";
        return "Failed to create an OpenGL ES compute program.";
    }
    if (strcmp(msg, "Failed to create an OpenGL ES compute shader.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create an OpenGL ES compute shader.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear un shader de cómputo OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création d’un shader de calcul OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein OpenGL ES-Compute-Shader konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare un compute shader OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας compute shader OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать вычислительный шейдер OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 OpenGL ES 计算着色器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 OpenGL ES 計算著色器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンピュートシェーダーを作成できませんでした。";
        return "Failed to create an OpenGL ES compute shader.";
    }
    if (strcmp(msg, "Failed to create the Direct3D 12 DXGI factory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the Direct3D 12 DXGI factory.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear la fábrica DXGI de Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création de la fabrique DXGI Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-DXGI-Factory konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare la factory DXGI Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας του εργοστασίου DXGI Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать фабрику DXGI Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 DXGI 工厂。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 DXGI Factory。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12 DXGIファクトリを作成できませんでした。";
        return "Failed to create the Direct3D 12 DXGI factory.";
    }
    if (strcmp(msg, "Failed to create the Direct3D 12 compute queue.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the Direct3D 12 compute queue.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear la cola de cómputo Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création de la file d’attente de calcul Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Compute-Warteschlange konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare la coda di calcolo Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας της ουράς υπολογισμού Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать вычислительную очередь Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 计算队列。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 計算佇列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12コンピュートキューを作成できませんでした。";
        return "Failed to create the Direct3D 12 compute queue.";
    }
    if (strcmp(msg, "Failed to create the Direct3D 12 queue fence.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the Direct3D 12 queue fence.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear la valla de cola Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création de la barrière de file d’attente Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Direct3D 12-Warteschlangen-Fence konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare la fence della coda Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας του φράκτη ουράς Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать fence очереди Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 队列栅栏。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 佇列柵欄。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12キューフェンスを作成できませんでした。";
        return "Failed to create the Direct3D 12 queue fence.";
    }
    if (strcmp(msg, "Failed to create the Vulkan descriptor pool.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the Vulkan descriptor pool.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear el pool de descriptores Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création du pool de descripteurs Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Deskriptorpool konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare il pool di descrittori Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας της δεξαμενής περιγραφέων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать пул дескрипторов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 描述符池。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 描述元集區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスクリプタプールを作成できませんでした。";
        return "Failed to create the Vulkan descriptor pool.";
    }
    if (strcmp(msg, "Failed to create the Vulkan scalar buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the Vulkan scalar buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear el búfer escalar Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création du tampon scalaire Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Skalarpuffer konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare il buffer scalare Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας της βαθμωτής ενδιάμεσης μνήμης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать скалярный буфер Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 标量缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 純量緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラーバッファを作成できませんでした。";
        return "Failed to create the Vulkan scalar buffer.";
    }
    if (strcmp(msg, "Failed to create the Vulkan scalar-result buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the Vulkan scalar-result buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear el búfer de resultado escalar Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création du tampon de résultat scalaire Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Puffer für skalare Ergebnisse konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare il buffer del risultato scalare Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας της ενδιάμεσης μνήμης βαθμωτού αποτελέσματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать буфер скалярного результата Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Vulkan 标量结果缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Vulkan 純量結果緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラー結果バッファを作成できませんでした。";
        return "Failed to create the Vulkan scalar-result buffer.";
    }
    if (strcmp(msg, "Failed to create the selected Direct3D 12 device.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the selected Direct3D 12 device.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear el dispositivo Direct3D 12 seleccionado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création du périphérique Direct3D 12 sélectionné.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das ausgewählte Direct3D 12-Gerät konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare il dispositivo Direct3D 12 selezionato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας της επιλεγμένης συσκευής Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать выбранное устройство Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建所选的 Direct3D 12 设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立所選的 Direct3D 12 裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したDirect3D 12デバイスを作成できませんでした。";
        return "Failed to create the selected Direct3D 12 device.";
    }
    if (strcmp(msg, "Failed to create the selected Vulkan compute device.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to create the selected Vulkan compute device.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo crear el dispositivo de cómputo Vulkan seleccionado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la création du périphérique de calcul Vulkan sélectionné.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das ausgewählte Vulkan-Compute-Gerät konnte nicht erstellt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile creare il dispositivo di calcolo Vulkan selezionato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας της επιλεγμένης συσκευής υπολογισμού Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось создать выбранное вычислительное устройство Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建所选的 Vulkan 计算设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立所選的 Vulkan 計算裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したVulkanコンピュートデバイスを作成できませんでした。";
        return "Failed to create the selected Vulkan compute device.";
    }
    if (strcmp(msg, "Failed to enumerate Vulkan physical devices.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to enumerate Vulkan physical devices.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudieron enumerar los dispositivos físicos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’énumération des périphériques physiques Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die physischen Vulkan-Geräte konnten nicht aufgelistet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile enumerare i dispositivi fisici Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία απαρίθμησης των φυσικών συσκευών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось перечислить физические устройства Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法枚举 Vulkan 物理设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法列舉 Vulkan 實體裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan物理デバイスを列挙できませんでした。";
        return "Failed to enumerate Vulkan physical devices.";
    }
    if (strcmp(msg, "Failed to expand the CS segment limit.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to expand the CS segment limit.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo ampliar el límite del segmento CS.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’extension de la limite du segment CS.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die CS-Segmentgrenze konnte nicht erweitert werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile estendere il limite del segmento CS.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία επέκτασης του ορίου τμήματος CS.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось расширить предел сегмента CS.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法扩大 CS 段限制。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法擴大 CS 區段限制。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "CSセグメントの上限を拡張できませんでした。\n";
        return "Failed to expand the CS segment limit.\n";
    }
    if (strcmp(msg, "Failed to finish the Vulkan command buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to finish the Vulkan command buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo finalizar el búfer de comandos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la finalisation du tampon de commandes Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Befehlspuffer konnte nicht abgeschlossen werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile completare il buffer dei comandi Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ολοκλήρωσης της ενδιάμεσης μνήμης εντολών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось завершить буфер команд Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法完成 Vulkan 命令缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法完成 Vulkan 命令緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanコマンドバッファを完了できませんでした。";
        return "Failed to finish the Vulkan command buffer.";
    }
    if (strcmp(msg, "Failed to flush Vulkan data-buffer memory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to flush Vulkan data-buffer memory.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo vaciar la memoria del búfer de datos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec du vidage de la mémoire du tampon de données Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Speicher des Vulkan-Datenpuffers konnte nicht geschrieben werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile scaricare la memoria del buffer di dati Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία εκκαθάρισης της μνήμης ενδιάμεσης αποθήκευσης δεδομένων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось сбросить память буфера данных Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法刷新 Vulkan 数据缓冲区内存。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法排清 Vulkan 資料緩衝區記憶體。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanデータバッファメモリをフラッシュできませんでした。";
        return "Failed to flush Vulkan data-buffer memory.";
    }
    if (strcmp(msg, "Failed to flush Vulkan scalar memory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to flush Vulkan scalar memory.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo vaciar la memoria escalar Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec du vidage de la mémoire scalaire Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Skalarspeicher konnte nicht geschrieben werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile scaricare la memoria scalare Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία εκκαθάρισης της βαθμωτής μνήμης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось сбросить скалярную память Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法刷新 Vulkan 标量内存。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法排清 Vulkan 純量記憶體。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラーメモリをフラッシュできませんでした。";
        return "Failed to flush Vulkan scalar memory.";
    }
    if (strcmp(msg, "Failed to flush Vulkan scalar-result memory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to flush Vulkan scalar-result memory.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo vaciar la memoria de resultado escalar Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec du vidage de la mémoire de résultat scalaire Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Speicher für skalare Ergebnisse konnte nicht geschrieben werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile scaricare la memoria del risultato scalare Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία εκκαθάρισης της μνήμης βαθμωτού αποτελέσματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось сбросить память скалярного результата Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法刷新 Vulkan 标量结果内存。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法排清 Vulkan 純量結果記憶體。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラー結果メモリをフラッシュできませんでした。";
        return "Failed to flush Vulkan scalar-result memory.";
    }
    if (strcmp(msg, "Failed to generate an OpenGL ES compute shader.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to generate an OpenGL ES compute shader.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo generar un shader de cómputo OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la génération d’un shader de calcul OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein OpenGL ES-Compute-Shader konnte nicht generiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile generare un compute shader OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δημιουργίας compute shader OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось сгенерировать вычислительный шейдер OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法生成 OpenGL ES 计算着色器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法產生 OpenGL ES 計算著色器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンピュートシェーダーを生成できませんでした。";
        return "Failed to generate an OpenGL ES compute shader.";
    }
    if (strcmp(msg, "Failed to initialize a headless OpenGL ES 3.1 context.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to initialize a headless OpenGL ES 3.1 context.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo inicializar un contexto OpenGL ES 3.1 sin interfaz gráfica.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’initialisation d’un contexte OpenGL ES 3.1 sans affichage.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein monitorloser OpenGL ES 3.1-Kontext konnte nicht initialisiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile inizializzare un contesto OpenGL ES 3.1 headless.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αρχικοποίησης περιβάλλοντος OpenGL ES 3.1 χωρίς οθόνη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось инициализировать контекст OpenGL ES 3.1 без дисплея.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法初始化无头 OpenGL ES 3.1 上下文。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法初始化無頭 OpenGL ES 3.1 內容。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ヘッドレスOpenGL ES 3.1コンテキストを初期化できませんでした。";
        return "Failed to initialize a headless OpenGL ES 3.1 context.";
    }
    if (strcmp(msg, "Failed to initialize the Direct3D 12 queue mutex.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to initialize the Direct3D 12 queue mutex.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo inicializar el mutex de la cola Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’initialisation du mutex de la file d’attente Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Mutex der Direct3D 12-Warteschlange konnte nicht initialisiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile inizializzare il mutex della coda Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αρχικοποίησης του mutex ουράς Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось инициализировать мьютекс очереди Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法初始化 Direct3D 12 队列互斥锁。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法初始化 Direct3D 12 佇列互斥鎖。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12キューミューテックスを初期化できませんでした。";
        return "Failed to initialize the Direct3D 12 queue mutex.";
    }
    if (strcmp(msg, "Failed to initialize the OpenGL ES context mutex.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to initialize the OpenGL ES context mutex.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo inicializar el mutex del contexto OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’initialisation du mutex du contexte OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Mutex des OpenGL ES-Kontexts konnte nicht initialisiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile inizializzare il mutex del contesto OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αρχικοποίησης του mutex περιβάλλοντος OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось инициализировать мьютекс контекста OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法初始化 OpenGL ES 上下文互斥锁。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法初始化 OpenGL ES 內容互斥鎖。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンテキストミューテックスを初期化できませんでした。";
        return "Failed to initialize the OpenGL ES context mutex.";
    }
    if (strcmp(msg, "Failed to initialize the Vulkan queue mutex.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to initialize the Vulkan queue mutex.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo inicializar el mutex de la cola Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’initialisation du mutex de la file d’attente Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Mutex der Vulkan-Warteschlange konnte nicht initialisiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile inizializzare il mutex della coda Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αρχικοποίησης του mutex ουράς Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось инициализировать мьютекс очереди Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法初始化 Vulkan 队列互斥锁。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法初始化 Vulkan 佇列互斥鎖。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanキューミューテックスを初期化できませんでした。";
        return "Failed to initialize the Vulkan queue mutex.";
    }
    if (strcmp(msg, "Failed to invalidate Vulkan output memory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to invalidate Vulkan output memory.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo invalidar la memoria de salida Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’invalidation de la mémoire de sortie Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Ausgabespeicher konnte nicht invalidiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile invalidare la memoria di output Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ακύρωσης της μνήμης εξόδου Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось инвалидировать память вывода Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法使 Vulkan 输出内存失效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法使 Vulkan 輸出記憶體失效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan出力メモリを無効化できませんでした。";
        return "Failed to invalidate Vulkan output memory.";
    }
    if (strcmp(msg, "Failed to invalidate Vulkan scalar-result memory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to invalidate Vulkan scalar-result memory.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo invalidar la memoria de resultado escalar Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’invalidation de la mémoire de résultat scalaire Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Speicher für skalare Ergebnisse konnte nicht invalidiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile invalidare la memoria del risultato scalare Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ακύρωσης της μνήμης βαθμωτού αποτελέσματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось инвалидировать память скалярного результата Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法使 Vulkan 标量结果内存失效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法使 Vulkan 純量結果記憶體失效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラー結果メモリを無効化できませんでした。";
        return "Failed to invalidate Vulkan scalar-result memory.";
    }
    if (strcmp(msg, "Failed to link an OpenGL ES compute program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to link an OpenGL ES compute program.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo enlazar un programa de cómputo OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’édition des liens d’un programme de calcul OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein OpenGL ES-Compute-Programm konnte nicht gelinkt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile collegare un programma di calcolo OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία σύνδεσης προγράμματος υπολογισμού OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось скомпоновать вычислительную программу OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法链接 OpenGL ES 计算程序。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法連結 OpenGL ES 計算程式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンピュートプログラムをリンクできませんでした。";
        return "Failed to link an OpenGL ES compute program.";
    }
    if (strcmp(msg, "Failed to lock accelerator rewrite commit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to lock accelerator rewrite commit.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo bloquear la confirmación de reescritura del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec du verrouillage de la validation de réécriture de l’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Commit der Beschleunigerumschreibung konnte nicht gesperrt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile bloccare il commit della riscrittura dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία κλειδώματος της οριστικοποίησης επανεγγραφής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось заблокировать фиксацию перезаписи ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法锁定加速器重写提交。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法鎖定加速器重寫提交。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ書き換えコミットをロックできませんでした。";
        return "Failed to lock accelerator rewrite commit.";
    }
    if (strcmp(msg, "Failed to make the OpenGL ES context current.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to make the OpenGL ES context current.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo hacer actual el contexto OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’activation du contexte OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Kontext konnte nicht zum aktuellen Kontext gemacht werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile rendere corrente il contesto OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ορισμού του περιβάλλοντος OpenGL ES ως τρέχοντος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось сделать контекст OpenGL ES текущим.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法将 OpenGL ES 上下文设为当前上下文。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法將 OpenGL ES 內容設為目前內容。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンテキストをカレントにできませんでした。";
        return "Failed to make the OpenGL ES context current.";
    }
    if (strcmp(msg, "Failed to map an OpenGL ES output buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to map an OpenGL ES output buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo mapear un búfer de salida OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec du mappage d’un tampon de sortie OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein OpenGL ES-Ausgabepuffer konnte nicht abgebildet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile mappare un buffer di output OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αντιστοίχισης ενδιάμεσης μνήμης εξόδου OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось отобразить буфер вывода OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法映射 OpenGL ES 输出缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對應 OpenGL ES 輸出緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファをマッピングできませんでした。";
        return "Failed to map an OpenGL ES output buffer.";
    }
    if (strcmp(msg, "Failed to map the OpenGL ES result buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to map the OpenGL ES result buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo mapear el búfer de resultados OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec du mappage du tampon de résultats OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Ergebnispuffer konnte nicht abgebildet werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile mappare il buffer dei risultati OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αντιστοίχισης της ενδιάμεσης μνήμης αποτελεσμάτων OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось отобразить буфер результатов OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法映射 OpenGL ES 结果缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對應 OpenGL ES 結果緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果バッファをマッピングできませんでした。";
        return "Failed to map the OpenGL ES result buffer.";
    }
    if (strcmp(msg, "Failed to normalize accelerator index.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to normalize accelerator index.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo normalizar el índice del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la normalisation de l’index de l’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Beschleunigerindex konnte nicht normalisiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile normalizzare l’indice dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία κανονικοποίησης του δείκτη επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось нормализовать индекс ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法规范化加速器索引。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法正規化加速器索引。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータインデックスを正規化できませんでした。";
        return "Failed to normalize accelerator index.";
    }
    if (strcmp(msg, "Failed to open file \"%s\".\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to open file \"%s\".\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo abrir el archivo \"%s\".\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’ouverture du fichier « %s ».\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Datei „%s“ konnte nicht geöffnet werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile aprire il file \"%s\".\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ανοίγματος του αρχείου \"%s\".\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось открыть файл \"%s\".\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法打开文件“%s”。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法開啟檔案「%s」。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル「%s」を開けませんでした。\n";
        return "Failed to open file \"%s\".\n";
    }
    if (strcmp(msg, "Failed to query the Vulkan loader version.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to query the Vulkan loader version.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo consultar la versión del cargador Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’interrogation de la version du chargeur Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Version des Vulkan-Laders konnte nicht abgefragt werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile interrogare la versione del loader Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία ερωτήματος της έκδοσης του φορτωτή Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось запросить версию загрузчика Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法查询 Vulkan 加载器版本。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法查詢 Vulkan 載入器版本。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanローダーのバージョンを照会できませんでした。";
        return "Failed to query the Vulkan loader version.";
    }
    if (strcmp(msg, "Failed to release the OpenGL ES compiler context.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to release the OpenGL ES compiler context.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo liberar el contexto del compilador OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la libération du contexte du compilateur OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Compilerkontext konnte nicht freigegeben werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile rilasciare il contesto del compilatore OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αποδέσμευσης του περιβάλλοντος μεταγλωττιστή OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось освободить контекст компилятора OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法释放 OpenGL ES 编译器上下文。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法釋放 OpenGL ES 編譯器內容。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンパイラコンテキストを解放できませんでした。";
        return "Failed to release the OpenGL ES compiler context.";
    }
    if (strcmp(msg, "Failed to release the OpenGL ES context.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to release the OpenGL ES context.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo liberar el contexto OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la libération du contexte OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Kontext konnte nicht freigegeben werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile rilasciare il contesto OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αποδέσμευσης του περιβάλλοντος OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось освободить контекст OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法释放 OpenGL ES 上下文。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法釋放 OpenGL ES 內容。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンテキストを解放できませんでした。";
        return "Failed to release the OpenGL ES context.";
    }
    if (strcmp(msg, "Failed to reserve accelerator programs.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to reserve accelerator programs.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo reservar espacio para los programas del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la réservation des programmes de l’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Für Beschleunigerprogramme konnte kein Speicher reserviert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile riservare i programmi dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία δέσμευσης χώρου για τα προγράμματα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось зарезервировать место для программ ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法为加速器程序预留空间。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法為加速器程式保留空間。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータプログラム用の領域を予約できませんでした。";
        return "Failed to reserve accelerator programs.";
    }
    if (strcmp(msg, "Failed to serialize a Direct3D 12 root signature.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Failed to serialize a Direct3D 12 root signature.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se pudo serializar una firma raíz Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la sérialisation d’une signature racine Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine Direct3D 12-Root-Signatur konnte nicht serialisiert werden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Impossibile serializzare una firma radice Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία σειριοποίησης ριζικής υπογραφής Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось сериализовать корневую сигнатуру Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法序列化 Direct3D 12 根签名。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法序列化 Direct3D 12 根簽章。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12ルートシグネチャをシリアライズできませんでした。";
        return "Failed to serialize a Direct3D 12 root signature.";
    }
    if (strcmp(msg, "File close error.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File close error.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error al cerrar el archivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur de fermeture du fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler beim Schließen der Datei.";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore di chiusura del file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα κλεισίματος αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка закрытия файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件关闭错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案關閉錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルのクローズエラーです。";
        return "File close error.";
    }
    if (strcmp(msg, "File handle belongs to a different VM.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File handle belongs to a different VM.";
        if (strncmp(lang_code, "es", 2) == 0) return "El identificador de archivo pertenece a una VM diferente.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le handle de fichier appartient à une autre VM.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Datei-Handle gehört zu einer anderen VM.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’handle del file appartiene a una VM diversa.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο χειριστής αρχείου ανήκει σε διαφορετική VM.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Дескриптор файла принадлежит другой VM.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件句柄属于其他 VM。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案控制代碼屬於其他 VM。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルハンドルは別のVMに属しています。";
        return "File handle belongs to a different VM.";
    }
    if (strcmp(msg, "File handle is invalid.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File handle is invalid.";
        if (strncmp(lang_code, "es", 2) == 0) return "El identificador de archivo no es válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le handle de fichier n’est pas valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Datei-Handle ist ungültig.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’handle del file non è valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο χειριστής αρχείου δεν είναι έγκυρος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый дескриптор файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件句柄无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案控制代碼無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルハンドルが無効です。";
        return "File handle is invalid.";
    }
    if (strcmp(msg, "File handle kind mismatch.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File handle kind mismatch.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tipo del identificador de archivo no coincide.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le type du handle de fichier ne correspond pas.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Art des Datei-Handles stimmt nicht überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il tipo dell’handle del file non corrisponde.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το είδος του χειριστή αρχείου δεν αντιστοιχεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Тип дескриптора файла не совпадает.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件句柄种类不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案控制代碼種類不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルハンドルの種類が一致しません。";
        return "File handle kind mismatch.";
    }
    if (strcmp(msg, "File is closed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File is closed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El archivo está cerrado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le fichier est fermé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Datei ist geschlossen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il file è chiuso.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το αρχείο είναι κλειστό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Файл закрыт.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件已关闭。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案已關閉。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルは閉じられています。";
        return "File is closed.";
    }
    if (strcmp(msg, "File mapping is not supported on this platform.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping is not supported on this platform.";
        if (strncmp(lang_code, "es", 2) == 0) return "El mapeo de archivos no es compatible con esta plataforma.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le mappage de fichier n’est pas pris en charge sur cette plateforme.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateizuordnung wird auf dieser Plattform nicht unterstützt.";
        if (strncmp(lang_code, "it", 2) == 0) return "La mappatura di file non è supportata su questa piattaforma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αντιστοίχιση αρχείου δεν υποστηρίζεται σε αυτήν την πλατφόρμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отображение файла не поддерживается на этой платформе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "此平台不支持文件映射。";
        if (strncmp(lang_code, "tw", 2) == 0) return "此平台不支援檔案對應。";
        if (strncmp(lang_code, "ja", 2) == 0) return "このプラットフォームではファイルマッピングがサポートされていません。";
        return "File mapping is not supported on this platform.";
    }
    if (strcmp(msg, "File mapping must allow reading or writing.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping must allow reading or writing.";
        if (strncmp(lang_code, "es", 2) == 0) return "El mapeo de archivos debe permitir la lectura o la escritura.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le mappage de fichier doit autoriser la lecture ou l’écriture.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Dateizuordnung muss Lesen oder Schreiben zulassen.";
        if (strncmp(lang_code, "it", 2) == 0) return "La mappatura di file deve consentire la lettura o la scrittura.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αντιστοίχιση αρχείου πρέπει να επιτρέπει ανάγνωση ή εγγραφή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отображение файла должно разрешать чтение или запись.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射必须允许读取或写入。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應必須允許讀取或寫入。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングでは読み取りまたは書き込みを許可する必要があります。";
        return "File mapping must allow reading or writing.";
    }
    if (strcmp(msg, "File mapping offset and size must be integers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping offset and size must be integers.";
        if (strncmp(lang_code, "es", 2) == 0) return "El desplazamiento y el tamaño del mapeo de archivos deben ser enteros.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le décalage et la taille du mappage de fichier doivent être des entiers.";
        if (strncmp(lang_code, "de", 2) == 0) return "Versatz und Größe der Dateizuordnung müssen ganze Zahlen sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’offset e la dimensione della mappatura di file devono essere interi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μετατόπιση και το μέγεθος της αντιστοίχισης αρχείου πρέπει να είναι ακέραιοι αριθμοί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Смещение и размер отображения файла должны быть целыми числами.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射偏移量和大小必须为整数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應位移和大小必須為整數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングのオフセットとサイズは整数である必要があります。";
        return "File mapping offset and size must be integers.";
    }
    if (strcmp(msg, "File mapping offset and size must not be negative.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping offset and size must not be negative.";
        if (strncmp(lang_code, "es", 2) == 0) return "El desplazamiento y el tamaño del mapeo de archivos no deben ser negativos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le décalage et la taille du mappage de fichier ne doivent pas être négatifs.";
        if (strncmp(lang_code, "de", 2) == 0) return "Versatz und Größe der Dateizuordnung dürfen nicht negativ sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’offset e la dimensione della mappatura di file non devono essere negativi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μετατόπιση και το μέγεθος της αντιστοίχισης αρχείου δεν πρέπει να είναι αρνητικά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Смещение и размер отображения файла не должны быть отрицательными.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射偏移量和大小不得为负数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應位移和大小不得為負數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングのオフセットとサイズを負にすることはできません。";
        return "File mapping offset and size must not be negative.";
    }
    if (strcmp(msg, "File mapping offset is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping offset is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El desplazamiento del mapeo de archivos es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le décalage du mappage de fichier est trop grand.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Versatz der Dateizuordnung ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’offset della mappatura di file è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μετατόπιση της αντιστοίχισης αρχείου είναι πολύ μεγάλη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Смещение отображения файла слишком велико.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射偏移量过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應位移過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングのオフセットが大きすぎます。";
        return "File mapping offset is too large.";
    }
    if (strcmp(msg, "File mapping permissions must be 0 or 1.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping permissions must be 0 or 1.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los permisos del mapeo de archivos deben ser 0 o 1.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les autorisations du mappage de fichier doivent valoir 0 ou 1.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Berechtigungen der Dateizuordnung müssen 0 oder 1 sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "I permessi della mappatura di file devono essere 0 o 1.";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα δικαιώματα αντιστοίχισης αρχείου πρέπει να είναι 0 ή 1.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Разрешения отображения файла должны иметь значение 0 или 1.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射权限必须为 0 或 1。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應權限必須為 0 或 1。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングの権限は0または1である必要があります。";
        return "File mapping permissions must be 0 or 1.";
    }
    if (strcmp(msg, "File mapping range exceeds the file size.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping range exceeds the file size.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango del mapeo de archivos supera el tamaño del archivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage du mappage de fichier dépasse la taille du fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Bereich der Dateizuordnung überschreitet die Dateigröße.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo della mappatura di file supera la dimensione del file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αντιστοίχισης αρχείου υπερβαίνει το μέγεθος του αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон отображения файла превышает размер файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射范围超过文件大小。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應範圍超過檔案大小。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングの範囲がファイルサイズを超えています。";
        return "File mapping range exceeds the file size.";
    }
    if (strcmp(msg, "File mapping range is not aligned to the element size.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping range is not aligned to the element size.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango del mapeo de archivos no está alineado con el tamaño del elemento.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage du mappage de fichier n’est pas alignée sur la taille de l’élément.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Bereich der Dateizuordnung ist nicht an der Elementgröße ausgerichtet.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo della mappatura di file non è allineato alla dimensione dell’elemento.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αντιστοίχισης αρχείου δεν είναι ευθυγραμμισμένο με το μέγεθος στοιχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон отображения файла не выровнен по размеру элемента.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射范围未按元素大小对齐。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應範圍未依元素大小對齊。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングの範囲が要素サイズにアラインされていません。";
        return "File mapping range is not aligned to the element size.";
    }
    if (strcmp(msg, "File mapping range is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping range is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango del mapeo de archivos es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage du mappage de fichier est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Bereich der Dateizuordnung ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo della mappatura di file è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αντιστοίχισης αρχείου είναι πολύ μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон отображения файла слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射范围过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應範圍過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングの範囲が大きすぎます。";
        return "File mapping range is too large.";
    }
    if (strcmp(msg, "File mapping size is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping size is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del mapeo de archivos es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du mappage de fichier est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe der Dateizuordnung ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione della mappatura di file è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος της αντιστοίχισης αρχείου είναι πολύ μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер отображения файла слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射大小过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應大小過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングのサイズが大きすぎます。";
        return "File mapping size is too large.";
    }
    if (strcmp(msg, "File mapping size must be greater than zero.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File mapping size must be greater than zero.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del mapeo de archivos debe ser mayor que cero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du mappage de fichier doit être supérieure à zéro.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe der Dateizuordnung muss größer als null sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione della mappatura di file deve essere maggiore di zero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος της αντιστοίχισης αρχείου πρέπει να είναι μεγαλύτερο από μηδέν.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер отображения файла должен быть больше нуля.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件映射大小必须大于零。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案對應大小必須大於零。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルマッピングのサイズは0より大きい必要があります。";
        return "File mapping size must be greater than zero.";
    }
    if (strcmp(msg, "File read error.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File read error.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de lectura del archivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur de lecture du fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler beim Lesen der Datei.";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore di lettura del file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα ανάγνωσης αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка чтения файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件读取错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案讀取錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルの読み取りエラーです。";
        return "File read error.";
    }
    if (strcmp(msg, "File seek error.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File seek error.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de posicionamiento en el archivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur de positionnement dans le fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler beim Positionieren in der Datei.";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore di posizionamento nel file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα αναζήτησης θέσης στο αρχείο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка позиционирования в файле.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件定位错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案定位錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルのシークエラーです。";
        return "File seek error.";
    }
    if (strcmp(msg, "File tell error.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File tell error.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error al obtener la posición del archivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur de lecture de la position dans le fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler beim Ermitteln der Dateiposition.";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore nel recupero della posizione del file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα λήψης της θέσης αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка получения позиции в файле.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件位置获取错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案位置取得錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイル位置の取得エラーです。";
        return "File tell error.";
    }
    if (strcmp(msg, "File write error.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File write error.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de escritura en el archivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur d’écriture dans le fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler beim Schreiben der Datei.";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore di scrittura del file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα εγγραφής αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка записи файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件写入错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案寫入錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ファイルの書き込みエラーです。";
        return "File write error.";
    }
    if (strcmp(msg, "File.writeAll range is out-of-bounds.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "File.writeAll range is out-of-bounds.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango de File.writeAll está fuera de los límites.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage de File.writeAll est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Bereich von File.writeAll liegt außerhalb der Grenzen.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo di File.writeAll è fuori dai limiti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος του File.writeAll είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон File.writeAll выходит за допустимые границы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "File.writeAll 范围越界。";
        if (strncmp(lang_code, "tw", 2) == 0) return "File.writeAll 範圍越界。";
        if (strncmp(lang_code, "ja", 2) == 0) return "File.writeAllの範囲が境界外です。";
        return "File.writeAll range is out-of-bounds.";
    }
    if (strcmp(msg, "Fill value is not a number.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Fill value is not a number.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de relleno no es un número.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur de remplissage n’est pas un nombre.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Füllwert ist keine Zahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore di riempimento non è un numero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή συμπλήρωσης δεν είναι αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение заполнения не является числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "填充值不是数字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "填充值不是數字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "フィル値が数値ではありません。";
        return "Fill value is not a number.";
    }
    if (strcmp(msg, "GPU acceleration is available only when running Noct source.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "GPU acceleration is available only when running Noct source.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La aceleración por GPU solo está disponible al ejecutar código fuente Noct.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’accélération GPU n’est disponible que lors de l’exécution de code source Noct.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "GPU-Beschleunigung ist nur beim Ausführen von Noct-Quellcode verfügbar.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "L’accelerazione GPU è disponibile solo durante l’esecuzione di codice sorgente Noct.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επιτάχυνση GPU είναι διαθέσιμη μόνο κατά την εκτέλεση πηγαίου κώδικα Noct.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ускорение GPU доступно только при выполнении исходного кода Noct.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "GPU 加速仅在运行 Noct 源代码时可用。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "GPU 加速僅在執行 Noct 原始碼時可用。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "GPUアクセラレーションはNoctソースコードの実行時にのみ使用できます。\n";
        return "GPU acceleration is available only when running Noct source.\n";
    }
    if (strcmp(msg, "GPU acceleration is not available in this build.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "GPU acceleration is not available in this build.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La aceleración por GPU no está disponible en esta compilación.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’accélération GPU n’est pas disponible dans cette version.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "GPU-Beschleunigung ist in diesem Build nicht verfügbar.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "L’accelerazione GPU non è disponibile in questa build.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επιτάχυνση GPU δεν είναι διαθέσιμη σε αυτήν την έκδοση.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ускорение GPU недоступно в этой сборке.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "此构建中不提供 GPU 加速。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "此組建中不提供 GPU 加速。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "このビルドではGPUアクセラレーションを使用できません。\n";
        return "GPU acceleration is not available in this build.\n";
    }
    if (strcmp(msg, "GPU acceleration may be selected only once.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "GPU acceleration may be selected only once.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La aceleración por GPU solo se puede seleccionar una vez.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’accélération GPU ne peut être sélectionnée qu’une seule fois.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "GPU-Beschleunigung kann nur einmal ausgewählt werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "L’accelerazione GPU può essere selezionata una sola volta.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επιτάχυνση GPU μπορεί να επιλεγεί μόνο μία φορά.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ускорение GPU можно выбрать только один раз.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "GPU 加速只能选择一次。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "GPU 加速只能選取一次。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "GPUアクセラレーションは一度だけ選択できます。\n";
        return "GPU acceleration may be selected only once.\n";
    }
    if (strcmp(msg, "GPU device name must not be empty.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "GPU device name must not be empty.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "El nombre del dispositivo GPU no debe estar vacío.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nom du périphérique GPU ne doit pas être vide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Name des GPU-Geräts darf nicht leer sein.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Il nome del dispositivo GPU non deve essere vuoto.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όνομα της συσκευής GPU δεν πρέπει να είναι κενό.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Имя устройства GPU не должно быть пустым.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "GPU 设备名称不得为空。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "GPU 裝置名稱不得為空。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "GPUデバイス名を空にすることはできません。\n";
        return "GPU device name must not be empty.\n";
    }
    if (strcmp(msg, "GPU enumeration failed.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "GPU enumeration failed.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error al enumerar las GPU.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’énumération des GPU.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "GPU-Auflistung fehlgeschlagen.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Enumerazione delle GPU non riuscita.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η απαρίθμηση GPU απέτυχε.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось перечислить GPU.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "GPU 枚举失败。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "GPU 列舉失敗。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "GPUの列挙に失敗しました。\n";
        return "GPU enumeration failed.\n";
    }
    if (strcmp(msg, "GPU enumeration failed: %s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "GPU enumeration failed: %s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error al enumerar las GPU: %s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’énumération des GPU : %s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "GPU-Auflistung fehlgeschlagen: %s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Enumerazione delle GPU non riuscita: %s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η απαρίθμηση GPU απέτυχε: %s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось перечислить GPU: %s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "GPU 枚举失败：%s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "GPU 列舉失敗：%s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "GPUの列挙に失敗しました: %s\n";
        return "GPU enumeration failed: %s\n";
    }
    if (strcmp(msg, "Generated Vulkan SPIR-V is invalid:\x20") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Generated Vulkan SPIR-V is invalid:\x20";
        if (strncmp(lang_code, "es", 2) == 0) return "El SPIR-V de Vulkan generado no es válido:\x20";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le SPIR-V Vulkan généré n’est pas valide :\x20";
        if (strncmp(lang_code, "de", 2) == 0) return "Das generierte Vulkan-SPIR-V ist ungültig:\x20";
        if (strncmp(lang_code, "it", 2) == 0) return "Lo SPIR-V Vulkan generato non è valido:\x20";
        if (strncmp(lang_code, "el", 2) == 0) return "Το παραγόμενο SPIR-V Vulkan δεν είναι έγκυρο:\x20";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сгенерированный SPIR-V Vulkan недопустим:\x20";
        if (strncmp(lang_code, "zh", 2) == 0) return "生成的 Vulkan SPIR-V 无效：\x20";
        if (strncmp(lang_code, "tw", 2) == 0) return "產生的 Vulkan SPIR-V 無效：\x20";
        if (strncmp(lang_code, "ja", 2) == 0) return "生成されたVulkan SPIR-Vが無効です:\x20";
        return "Generated Vulkan SPIR-V is invalid:\x20";
    }
    if (strcmp(msg, "Generated accelerator IR failed validation.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Generated accelerator IR failed validation.";
        if (strncmp(lang_code, "es", 2) == 0) return "La validación del IR del acelerador generado ha fallado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La validation de l’IR d’accélérateur généré a échoué.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Validierung der generierten Beschleuniger-IR ist fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "La convalida dell’IR dell’acceleratore generato non è riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επικύρωση του παραγόμενου IR επιταχυντή απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Проверка сгенерированного IR ускорителя завершилась с ошибкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "生成的加速器 IR 验证失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "產生的加速器 IR 驗證失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "生成されたアクセラレータIRの検証に失敗しました。";
        return "Generated accelerator IR failed validation.";
    }
    if (strcmp(msg, "Global symbol '%s' is not available inside __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Global symbol '%s' is not available inside __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "El símbolo global '%s' no está disponible dentro de una __fast func.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le symbole global '%s' n’est pas disponible dans une __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das globale Symbol '%s' ist innerhalb einer __fast func nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il simbolo globale '%s' non è disponibile all’interno di una __fast func.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το καθολικό σύμβολο '%s' δεν είναι διαθέσιμο μέσα σε ένα __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Глобальный символ '%s' недоступен внутри __fast func.";
        if (strncmp(lang_code, "zh", 2) == 0) return "全局符号 '%s' 在 __fast func 内不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "全域符號 '%s' 在 __fast func 內不可用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "グローバルシンボル'%s'は__fast func内では使用できません。";
        return "Global symbol '%s' is not available inside __fast func.";
    }
    if (strcmp(msg, "Inactive OpenGL ES buffers cannot request host transfers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Inactive OpenGL ES buffers cannot request host transfers.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los búferes OpenGL ES inactivos no pueden solicitar transferencias del host.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les tampons OpenGL ES inactifs ne peuvent pas demander de transferts depuis l’hôte.";
        if (strncmp(lang_code, "de", 2) == 0) return "Inaktive OpenGL ES-Puffer können keine Host-Übertragungen anfordern.";
        if (strncmp(lang_code, "it", 2) == 0) return "I buffer OpenGL ES inattivi non possono richiedere trasferimenti dall’host.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι ανενεργές ενδιάμεσες μνήμες OpenGL ES δεν μπορούν να ζητήσουν μεταφορές κεντρικού συστήματος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неактивные буферы OpenGL ES не могут запрашивать передачи с хоста.";
        if (strncmp(lang_code, "zh", 2) == 0) return "非活动 OpenGL ES 缓冲区无法请求主机传输。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非作用中 OpenGL ES 緩衝區無法要求主機傳輸。";
        if (strncmp(lang_code, "ja", 2) == 0) return "非アクティブなOpenGL ESバッファはホスト転送を要求できません。";
        return "Inactive OpenGL ES buffers cannot request host transfers.";
    }
    if (strcmp(msg, "Incompatible duplicate __fast prototype '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Incompatible duplicate __fast prototype '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "Prototipo __fast duplicado e incompatible '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Prototype __fast dupliqué incompatible '%s'.";
        if (strncmp(lang_code, "de", 2) == 0) return "Inkompatibler doppelter __fast-Prototyp '%s'.";
        if (strncmp(lang_code, "it", 2) == 0) return "Prototipo __fast duplicato incompatibile '%s'.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη συμβατό διπλό πρωτότυπο __fast '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несовместимый дублирующий прототип __fast '%s'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "重复的 __fast 原型 '%s' 不兼容。";
        if (strncmp(lang_code, "tw", 2) == 0) return "重複的 __fast 原型 '%s' 不相容。";
        if (strncmp(lang_code, "ja", 2) == 0) return "重複する__fastプロトタイプ'%s'に互換性がありません。";
        return "Incompatible duplicate __fast prototype '%s'.";
    }
    if (strcmp(msg, "Incomplete Vulkan enumeration function table.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Incomplete Vulkan enumeration function table.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tabla de funciones de enumeración Vulkan incompleta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Table de fonctions d’énumération Vulkan incomplète.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unvollständige Vulkan-Auflistungsfunktionstabelle.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tabella delle funzioni di enumerazione Vulkan incompleta.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ελλιπής πίνακας συναρτήσεων απαρίθμησης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неполная таблица функций перечисления Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 枚举函数表不完整。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 列舉函式表不完整。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan列挙関数テーブルが不完全です。";
        return "Incomplete Vulkan enumeration function table.";
    }
    if (strcmp(msg, "Incomplete Vulkan function table.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Incomplete Vulkan function table.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tabla de funciones Vulkan incompleta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Table de fonctions Vulkan incomplète.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unvollständige Vulkan-Funktionstabelle.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tabella delle funzioni Vulkan incompleta.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ελλιπής πίνακας συναρτήσεων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неполная таблица функций Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 函数表不完整。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 函式表不完整。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan関数テーブルが不完全です。";
        return "Incomplete Vulkan function table.";
    }
    if (strcmp(msg, "Inconsistent optimization levels for one HIR module.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Inconsistent optimization levels for one HIR module.";
        if (strncmp(lang_code, "es", 2) == 0) return "Niveles de optimización incoherentes para un mismo módulo HIR.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Niveaux d’optimisation incohérents pour un même module HIR.";
        if (strncmp(lang_code, "de", 2) == 0) return "Inkonsistente Optimierungsstufen für ein HIR-Modul.";
        if (strncmp(lang_code, "it", 2) == 0) return "Livelli di ottimizzazione incoerenti per uno stesso modulo HIR.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ασυνεπή επίπεδα βελτιστοποίησης για μία μονάδα HIR.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несогласованные уровни оптимизации для одного модуля HIR.";
        if (strncmp(lang_code, "zh", 2) == 0) return "同一 HIR 模块的优化级别不一致。";
        if (strncmp(lang_code, "tw", 2) == 0) return "同一 HIR 模組的最佳化層級不一致。";
        if (strncmp(lang_code, "ja", 2) == 0) return "1つのHIRモジュールに対する最適化レベルが一貫していません。";
        return "Inconsistent optimization levels for one HIR module.";
    }
    if (strcmp(msg, "Index out of bounds.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Index out of bounds.";
        if (strncmp(lang_code, "es", 2) == 0) return "Índice fuera de los límites.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Index hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Index außerhalb der Grenzen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Indice fuori dai limiti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δείκτης εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс вне допустимых границ.";
        if (strncmp(lang_code, "zh", 2) == 0) return "索引越界。";
        if (strncmp(lang_code, "tw", 2) == 0) return "索引越界。";
        if (strncmp(lang_code, "ja", 2) == 0) return "インデックスが境界外です。";
        return "Index out of bounds.";
    }
    if (strcmp(msg, "Input line is too long.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Input line is too long.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La línea de entrada es demasiado larga.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "La ligne d’entrée est trop longue.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Eingabezeile ist zu lang.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "La riga di input è troppo lunga.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η γραμμή εισόδου είναι πολύ μεγάλη.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Строка ввода слишком длинная.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "输入行过长。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "輸入行過長。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "入力行が長すぎます。\n";
        return "Input line is too long.\n";
    }
    if (strcmp(msg, "Inputs %s and %s map to the same output %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Inputs %s and %s map to the same output %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Las entradas %s y %s se asignan a la misma salida %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les entrées %s et %s correspondent à la même sortie %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Eingaben %s und %s werden derselben Ausgabe %s zugeordnet.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Gli input %s e %s sono associati allo stesso output %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι είσοδοι %s και %s αντιστοιχίζονται στην ίδια έξοδο %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вводы %s и %s отображаются в один и тот же вывод %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "输入 %s 和 %s 映射到同一输出 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "輸入 %s 和 %s 對應至同一輸出 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "入力%sと%sが同じ出力%sにマッピングされています。\n";
        return "Inputs %s and %s map to the same output %s.\n";
    }
    if (strcmp(msg, "Invalid --path option.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid --path option.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Opción --path no válida.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Option --path non valide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Option --path.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Opzione --path non valida.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη επιλογή --path.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый параметр --path.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "--path 选项无效。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "--path 選項無效。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "--pathオプションが無効です。\n";
        return "Invalid --path option.\n";
    }
    if (strcmp(msg, "Invalid Direct3D 12 program preparation request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Direct3D 12 program preparation request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de preparación de programa Direct3D 12 no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de préparation de programme Direct3D 12 non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Anfrage zur Vorbereitung eines Direct3D 12-Programms.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta di preparazione del programma Direct3D 12 non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα προετοιμασίας προγράμματος Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос на подготовку программы Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 程序准备请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 程式準備要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12プログラム準備要求が無効です。";
        return "Invalid Direct3D 12 program preparation request.";
    }
    if (strcmp(msg, "Invalid GPU device name.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid GPU device name.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Nombre de dispositivo GPU no válido.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nom de périphérique GPU non valide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger GPU-Gerätename.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Nome del dispositivo GPU non valido.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο όνομα συσκευής GPU.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое имя устройства GPU.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "GPU 设备名称无效。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "GPU 裝置名稱無效。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "GPUデバイス名が無効です。\n";
        return "Invalid GPU device name.\n";
    }
    if (strcmp(msg, "Invalid OpenGL ES device list.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid OpenGL ES device list.";
        if (strncmp(lang_code, "es", 2) == 0) return "Lista de dispositivos OpenGL ES no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Liste de périphériques OpenGL ES non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige OpenGL ES-Geräteliste.";
        if (strncmp(lang_code, "it", 2) == 0) return "Elenco dei dispositivi OpenGL ES non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη λίστα συσκευών OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый список устройств OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 设备列表无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 裝置清單無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESデバイスリストが無効です。";
        return "Invalid OpenGL ES device list.";
    }
    if (strcmp(msg, "Invalid OpenGL ES dispatch limit request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid OpenGL ES dispatch limit request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de límite de despacho OpenGL ES no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de limite de lancement OpenGL ES non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Anfrage für die OpenGL ES-Dispatch-Grenze.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta del limite di dispatch OpenGL ES non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα ορίου αποστολής OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос предела диспетчеризации OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 调度限制请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 調度限制要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESディスパッチ上限要求が無効です。";
        return "Invalid OpenGL ES dispatch limit request.";
    }
    if (strcmp(msg, "Invalid OpenGL ES execution request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid OpenGL ES execution request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de ejecución OpenGL ES no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande d’exécution OpenGL ES non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige OpenGL ES-Ausführungsanfrage.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta di esecuzione OpenGL ES non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα εκτέλεσης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос выполнения OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 执行请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 執行要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES実行要求が無効です。";
        return "Invalid OpenGL ES execution request.";
    }
    if (strcmp(msg, "Invalid OpenGL ES execution.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid OpenGL ES execution.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ejecución OpenGL ES no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Exécution OpenGL ES non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige OpenGL ES-Ausführung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Esecuzione OpenGL ES non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη εκτέλεση OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое выполнение OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 执行无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 執行無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES実行が無効です。";
        return "Invalid OpenGL ES execution.";
    }
    if (strcmp(msg, "Invalid OpenGL ES finish request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid OpenGL ES finish request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de finalización OpenGL ES no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de finalisation OpenGL ES non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige OpenGL ES-Abschlussanfrage.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta di completamento OpenGL ES non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα ολοκλήρωσης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос завершения OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 完成请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 完成要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES完了要求が無効です。";
        return "Invalid OpenGL ES finish request.";
    }
    if (strcmp(msg, "Invalid OpenGL ES kernel index.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid OpenGL ES kernel index.";
        if (strncmp(lang_code, "es", 2) == 0) return "Índice de kernel OpenGL ES no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Index de kernel OpenGL ES non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger OpenGL ES-Kernelindex.";
        if (strncmp(lang_code, "it", 2) == 0) return "Indice del kernel OpenGL ES non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος δείκτης πυρήνα OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый индекс ядра OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 内核索引无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 核心索引無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESカーネルインデックスが無効です。";
        return "Invalid OpenGL ES kernel index.";
    }
    if (strcmp(msg, "Invalid OpenGL ES program preparation request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid OpenGL ES program preparation request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de preparación de programa OpenGL ES no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de préparation de programme OpenGL ES non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Anfrage zur Vorbereitung eines OpenGL ES-Programms.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta di preparazione del programma OpenGL ES non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα προετοιμασίας προγράμματος OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос на подготовку программы OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 程序准备请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 程式準備要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESプログラム準備要求が無効です。";
        return "Invalid OpenGL ES program preparation request.";
    }
    if (strcmp(msg, "Invalid REPL state.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid REPL state.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Estado de REPL no válido.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "État de REPL non valide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger REPL-Zustand.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Stato REPL non valido.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη κατάσταση REPL.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое состояние REPL.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "REPL 状态无效。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "REPL 狀態無效。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "REPLの状態が無効です。\n";
        return "Invalid REPL state.\n";
    }
    if (strcmp(msg, "Invalid Vulkan device list.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan device list.";
        if (strncmp(lang_code, "es", 2) == 0) return "Lista de dispositivos Vulkan no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Liste de périphériques Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Vulkan-Geräteliste.";
        if (strncmp(lang_code, "it", 2) == 0) return "Elenco dei dispositivi Vulkan non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη λίστα συσκευών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый список устройств Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 设备列表无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 裝置清單無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanデバイスリストが無効です。";
        return "Invalid Vulkan device list.";
    }
    if (strcmp(msg, "Invalid Vulkan dispatch limit request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan dispatch limit request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de límite de despacho Vulkan no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de limite de lancement Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Anfrage für die Vulkan-Dispatch-Grenze.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta del limite di dispatch Vulkan non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα ορίου αποστολής Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос предела диспетчеризации Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 调度限制请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 調度限制要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスパッチ上限要求が無効です。";
        return "Invalid Vulkan dispatch limit request.";
    }
    if (strcmp(msg, "Invalid Vulkan execution request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan execution request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de ejecución Vulkan no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande d’exécution Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Vulkan-Ausführungsanfrage.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta di esecuzione Vulkan non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα εκτέλεσης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос выполнения Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 执行请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 執行要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan実行要求が無効です。";
        return "Invalid Vulkan execution request.";
    }
    if (strcmp(msg, "Invalid Vulkan execution.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan execution.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ejecución Vulkan no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Exécution Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Vulkan-Ausführung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Esecuzione Vulkan non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη εκτέλεση Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое выполнение Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 执行无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 執行無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan実行が無効です。";
        return "Invalid Vulkan execution.";
    }
    if (strcmp(msg, "Invalid Vulkan finish request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan finish request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de finalización Vulkan no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de finalisation Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Vulkan-Abschlussanfrage.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta di completamento Vulkan non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα ολοκλήρωσης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос завершения Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 完成请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 完成要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan完了要求が無効です。";
        return "Invalid Vulkan finish request.";
    }
    if (strcmp(msg, "Invalid Vulkan kernel index.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan kernel index.";
        if (strncmp(lang_code, "es", 2) == 0) return "Índice de kernel Vulkan no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Index de kernel Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Vulkan-Kernelindex.";
        if (strncmp(lang_code, "it", 2) == 0) return "Indice del kernel Vulkan non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος δείκτης πυρήνα Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый индекс ядра Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 内核索引无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 核心索引無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanカーネルインデックスが無効です。";
        return "Invalid Vulkan kernel index.";
    }
    if (strcmp(msg, "Invalid Vulkan program preparation request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan program preparation request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de preparación de programa Vulkan no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de préparation de programme Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Anfrage zur Vorbereitung eines Vulkan-Programms.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta di preparazione del programma Vulkan non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα προετοιμασίας προγράμματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос на подготовку программы Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 程序准备请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 程式準備要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanプログラム準備要求が無効です。";
        return "Invalid Vulkan program preparation request.";
    }
    if (strcmp(msg, "Invalid Vulkan shader compiler input.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid Vulkan shader compiler input.";
        if (strncmp(lang_code, "es", 2) == 0) return "Entrada del compilador de shaders Vulkan no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Entrée du compilateur de shaders Vulkan non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Eingabe für den Vulkan-Shader-Compiler.";
        if (strncmp(lang_code, "it", 2) == 0) return "Input del compilatore di shader Vulkan non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη είσοδος μεταγλωττιστή shader Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый ввод компилятора шейдеров Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 着色器编译器输入无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 著色器編譯器輸入無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanシェーダーコンパイラの入力が無効です。";
        return "Invalid Vulkan shader compiler input.";
    }
    if (strcmp(msg, "Invalid __fast function prototype.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast function prototype.";
        if (strncmp(lang_code, "es", 2) == 0) return "Prototipo de función __fast no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Prototype de fonction __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger __fast-Funktionsprototyp.";
        if (strncmp(lang_code, "it", 2) == 0) return "Prototipo di funzione __fast non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο πρωτότυπο συνάρτησης __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый прототип функции __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 函数原型无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 函式原型無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast関数プロトタイプが無効です。";
        return "Invalid __fast function prototype.";
    }
    if (strcmp(msg, "Invalid __fast function signature for '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast function signature for '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "Firma de función __fast no válida para '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Signature de fonction __fast non valide pour '%s'.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige __fast-Funktionssignatur für '%s'.";
        if (strncmp(lang_code, "it", 2) == 0) return "Firma della funzione __fast non valida per '%s'.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη υπογραφή συνάρτησης __fast για το '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая сигнатура функции __fast для '%s'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "'%s' 的 __fast 函数签名无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "'%s' 的 __fast 函式簽章無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "'%s'の__fast関数シグネチャが無効です。";
        return "Invalid __fast function signature for '%s'.";
    }
    if (strcmp(msg, "Invalid __fast function signature.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast function signature.";
        if (strncmp(lang_code, "es", 2) == 0) return "Firma de función __fast no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Signature de fonction __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige __fast-Funktionssignatur.";
        if (strncmp(lang_code, "it", 2) == 0) return "Firma della funzione __fast non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη υπογραφή συνάρτησης __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая сигнатура функции __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 函数签名无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 函式簽章無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast関数シグネチャが無効です。";
        return "Invalid __fast function signature.";
    }
    if (strcmp(msg, "Invalid __fast loop counter.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast loop counter.";
        if (strncmp(lang_code, "es", 2) == 0) return "Contador de bucle __fast no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Compteur de boucle __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger __fast-Schleifenzähler.";
        if (strncmp(lang_code, "it", 2) == 0) return "Contatore del ciclo __fast non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος μετρητής βρόχου __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый счётчик цикла __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 循环计数器无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 迴圈計數器無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastループカウンターが無効です。";
        return "Invalid __fast loop counter.";
    }
    if (strcmp(msg, "Invalid __fast parameter shape.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast parameter shape.";
        if (strncmp(lang_code, "es", 2) == 0) return "Forma de parámetro __fast no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Forme de paramètre __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Form des __fast-Parameters.";
        if (strncmp(lang_code, "it", 2) == 0) return "Forma del parametro __fast non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο σχήμα παραμέτρου __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая форма параметра __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形参形状无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 參數形狀無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastパラメータの形状が無効です。";
        return "Invalid __fast parameter shape.";
    }
    if (strcmp(msg, "Invalid __fast parameter type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast parameter type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tipo de parámetro __fast no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Type de paramètre __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Typ des __fast-Parameters.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tipo del parametro __fast non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος τύπος παραμέτρου __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый тип параметра __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形参类型无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 參數型別無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastパラメータの型が無効です。";
        return "Invalid __fast parameter type.";
    }
    if (strcmp(msg, "Invalid __fast shape extent.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast shape extent.";
        if (strncmp(lang_code, "es", 2) == 0) return "Extensión de forma __fast no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Étendue de forme __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Ausdehnung der __fast-Form.";
        if (strncmp(lang_code, "it", 2) == 0) return "Estensione della forma __fast non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη έκταση σχήματος __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый размер измерения формы __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形状维度大小无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 形狀維度大小無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast形状の次元サイズが無効です。";
        return "Invalid __fast shape extent.";
    }
    if (strcmp(msg, "Invalid __fast signature metadata.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid __fast signature metadata.";
        if (strncmp(lang_code, "es", 2) == 0) return "Metadatos de firma __fast no válidos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Métadonnées de signature __fast non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Metadaten der __fast-Signatur.";
        if (strncmp(lang_code, "it", 2) == 0) return "Metadati della firma __fast non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρα μεταδεδομένα υπογραφής __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые метаданные сигнатуры __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 签名元数据无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 簽章中繼資料無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastシグネチャのメタデータが無効です。";
        return "Invalid __fast signature metadata.";
    }
    if (strcmp(msg, "Invalid accelerator argument slots.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator argument slots.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ranuras de argumentos del acelerador no válidas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Emplacements d’arguments de l’accélérateur non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Argument-Slots des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Slot degli argomenti dell’acceleratore non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρες θέσεις ορισμάτων επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые слоты аргументов ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器实参槽位无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器引數槽位無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ引数スロットが無効です。";
        return "Invalid accelerator argument slots.";
    }
    if (strcmp(msg, "Invalid accelerator backend status.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator backend status.";
        if (strncmp(lang_code, "es", 2) == 0) return "Estado del backend del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "État du backend de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Status des Beschleuniger-Backends.";
        if (strncmp(lang_code, "it", 2) == 0) return "Stato del backend dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη κατάσταση backend επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое состояние бэкенда ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器后端状态无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器後端狀態無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータバックエンドの状態が無効です。";
        return "Invalid accelerator backend status.";
    }
    if (strcmp(msg, "Invalid accelerator budget function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator budget function.";
        if (strncmp(lang_code, "es", 2) == 0) return "Función de presupuesto del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Fonction de budget de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Budgetfunktion des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Funzione di budget dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη συνάρτηση προϋπολογισμού επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая функция бюджета ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器预算函数无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器預算函式無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ予算関数が無効です。";
        return "Invalid accelerator budget function.";
    }
    if (strcmp(msg, "Invalid accelerator buffer binding.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator buffer binding.";
        if (strncmp(lang_code, "es", 2) == 0) return "Vinculación de búfer del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Liaison de tampon de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Pufferbindung des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Associazione del buffer dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη δέσμευση ενδιάμεσης μνήμης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая привязка буфера ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区绑定无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區繫結無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータバッファのバインディングが無効です。";
        return "Invalid accelerator buffer binding.";
    }
    if (strcmp(msg, "Invalid accelerator buffer index type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator buffer index type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tipo de índice de búfer del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Type d’index de tampon de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Pufferindextyp des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tipo dell’indice del buffer dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος τύπος δείκτη ενδιάμεσης μνήμης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый тип индекса буфера ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区索引类型无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區索引型別無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータバッファのインデックス型が無効です。";
        return "Invalid accelerator buffer index type.";
    }
    if (strcmp(msg, "Invalid accelerator buffer slot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator buffer slot.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ranura de búfer del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Emplacement de tampon de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Puffer-Slot des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Slot del buffer dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη θέση ενδιάμεσης μνήμης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый слот буфера ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器缓冲区槽位无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器緩衝區槽位無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータバッファのスロットが無効です。";
        return "Invalid accelerator buffer slot.";
    }
    if (strcmp(msg, "Invalid accelerator comparison HIR.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator comparison HIR.";
        if (strncmp(lang_code, "es", 2) == 0) return "HIR de comparación del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "HIR de comparaison de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Vergleichs-HIR des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "HIR di confronto dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο HIR σύγκρισης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый HIR сравнения ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器比较 HIR 无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器比較 HIR 無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ比較HIRが無効です。";
        return "Invalid accelerator comparison HIR.";
    }
    if (strcmp(msg, "Invalid accelerator compiler result.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator compiler result.";
        if (strncmp(lang_code, "es", 2) == 0) return "Resultado del compilador del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Résultat du compilateur de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges Ergebnis des Beschleuniger-Compilers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Risultato del compilatore dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αποτέλεσμα μεταγλωττιστή επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый результат компилятора ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器编译器结果无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器編譯器結果無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータコンパイラの結果が無効です。";
        return "Invalid accelerator compiler result.";
    }
    if (strcmp(msg, "Invalid accelerator decline reason.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator decline reason.";
        if (strncmp(lang_code, "es", 2) == 0) return "Motivo de rechazo del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Motif de refus de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Ablehnungsgrund des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Motivo di rifiuto dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος λόγος απόρριψης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая причина отказа ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器拒绝原因无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器拒絕原因無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの拒否理由が無効です。";
        return "Invalid accelerator decline reason.";
    }
    if (strcmp(msg, "Invalid accelerator device-list request.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator device-list request.";
        if (strncmp(lang_code, "es", 2) == 0) return "Solicitud de lista de dispositivos del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Demande de liste de périphériques de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Gerätelistenanfrage des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Richiesta dell’elenco dei dispositivi dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αίτημα λίστας συσκευών επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый запрос списка устройств ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备列表请求无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置清單要求無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータデバイスリスト要求が無効です。";
        return "Invalid accelerator device-list request.";
    }
    if (strcmp(msg, "Invalid accelerator device-local buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator device-local buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "Búfer local del dispositivo del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Tampon local au périphérique de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger gerätelokaler Puffer des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Buffer locale del dispositivo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη τοπική ενδιάμεση μνήμη συσκευής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый локальный буфер устройства ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备本地缓冲区无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置本機緩衝區無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスローカルバッファが無効です。";
        return "Invalid accelerator device-local buffer.";
    }
    if (strcmp(msg, "Invalid accelerator device-local declaration.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator device-local declaration.";
        if (strncmp(lang_code, "es", 2) == 0) return "Declaración local del dispositivo del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Déclaration locale au périphérique de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige gerätelokale Deklaration des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dichiarazione locale del dispositivo dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη τοπική δήλωση συσκευής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое локальное объявление устройства ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备本地声明无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置本機宣告無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスローカル宣言が無効です。";
        return "Invalid accelerator device-local declaration.";
    }
    if (strcmp(msg, "Invalid accelerator device-local transfer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator device-local transfer.";
        if (strncmp(lang_code, "es", 2) == 0) return "Transferencia local del dispositivo del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Transfert local au périphérique de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige gerätelokale Übertragung des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Trasferimento locale del dispositivo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη τοπική μεταφορά συσκευής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая локальная передача устройства ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器设备本地传输无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器裝置本機傳輸無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのデバイスローカル転送が無効です。";
        return "Invalid accelerator device-local transfer.";
    }
    if (strcmp(msg, "Invalid accelerator function HIR.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator function HIR.";
        if (strncmp(lang_code, "es", 2) == 0) return "HIR de función del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "HIR de fonction de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Funktions-HIR des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "HIR della funzione dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο HIR συνάρτησης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый HIR функции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器函数 HIR 无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器函式 HIR 無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ関数HIRが無効です。";
        return "Invalid accelerator function HIR.";
    }
    if (strcmp(msg, "Invalid accelerator function metadata.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator function metadata.";
        if (strncmp(lang_code, "es", 2) == 0) return "Metadatos de función del acelerador no válidos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Métadonnées de fonction de l’accélérateur non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Funktionsmetadaten des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Metadati della funzione dell’acceleratore non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρα μεταδεδομένα συνάρτησης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые метаданные функции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器函数元数据无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器函式中繼資料無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ関数のメタデータが無効です。";
        return "Invalid accelerator function metadata.";
    }
    if (strcmp(msg, "Invalid accelerator index arithmetic.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator index arithmetic.";
        if (strncmp(lang_code, "es", 2) == 0) return "Aritmética de índices del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Arithmétique d’index de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Indexarithmetik des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Aritmetica degli indici dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη αριθμητική δείκτη επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая арифметика индексов ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器索引运算无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器索引運算無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのインデックス演算が無効です。";
        return "Invalid accelerator index arithmetic.";
    }
    if (strcmp(msg, "Invalid accelerator kernel buffer type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator kernel buffer type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tipo de búfer de kernel del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Type de tampon de kernel de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Kernelpuffertyp des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tipo di buffer del kernel dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος τύπος ενδιάμεσης μνήμης πυρήνα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый тип буфера ядра ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器内核缓冲区类型无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器核心緩衝區型別無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータカーネルのバッファ型が無効です。";
        return "Invalid accelerator kernel buffer type.";
    }
    if (strcmp(msg, "Invalid accelerator kernel index.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator kernel index.";
        if (strncmp(lang_code, "es", 2) == 0) return "Índice de kernel del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Index de kernel de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Kernelindex des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Indice del kernel dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος δείκτης πυρήνα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый индекс ядра ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器内核索引无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器核心索引無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータカーネルのインデックスが無効です。";
        return "Invalid accelerator kernel index.";
    }
    if (strcmp(msg, "Invalid accelerator local count.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator local count.";
        if (strncmp(lang_code, "es", 2) == 0) return "Cantidad de variables locales del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nombre de variables locales de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Anzahl lokaler Variablen des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Numero di variabili locali dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο πλήθος τοπικών μεταβλητών επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое количество локальных переменных ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器局部变量数量无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域變數數量無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのローカル変数数が無効です。";
        return "Invalid accelerator local count.";
    }
    if (strcmp(msg, "Invalid accelerator local list.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator local list.";
        if (strncmp(lang_code, "es", 2) == 0) return "Lista de variables locales del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Liste de variables locales de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Liste lokaler Variablen des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Elenco delle variabili locali dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη λίστα τοπικών μεταβλητών επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый список локальных переменных ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器局部变量列表无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域變數清單無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのローカル変数リストが無効です。";
        return "Invalid accelerator local list.";
    }
    if (strcmp(msg, "Invalid accelerator parameter count.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator parameter count.";
        if (strncmp(lang_code, "es", 2) == 0) return "Cantidad de parámetros del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nombre de paramètres de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Parameteranzahl des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Numero di parametri dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο πλήθος παραμέτρων επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое количество параметров ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器形参数量无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器參數數量無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのパラメータ数が無効です。";
        return "Invalid accelerator parameter count.";
    }
    if (strcmp(msg, "Invalid accelerator parameter metadata.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator parameter metadata.";
        if (strncmp(lang_code, "es", 2) == 0) return "Metadatos de parámetros del acelerador no válidos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Métadonnées de paramètres de l’accélérateur non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Parametermetadaten des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Metadati dei parametri dell’acceleratore non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρα μεταδεδομένα παραμέτρων επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые метаданные параметров ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器形参元数据无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器參數中繼資料無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータパラメータのメタデータが無効です。";
        return "Invalid accelerator parameter metadata.";
    }
    if (strcmp(msg, "Invalid accelerator program ID.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator program ID.";
        if (strncmp(lang_code, "es", 2) == 0) return "ID de programa del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "ID de programme de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Programm-ID des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "ID del programma dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο ID προγράμματος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый ID программы ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器程序 ID 无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器程式 ID 無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータプログラムIDが無効です。";
        return "Invalid accelerator program ID.";
    }
    if (strcmp(msg, "Invalid accelerator program identifier.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator program identifier.";
        if (strncmp(lang_code, "es", 2) == 0) return "Identificador de programa del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Identifiant de programme de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Programmbezeichner des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Identificatore del programma dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο αναγνωριστικό προγράμματος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый идентификатор программы ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器程序标识符无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器程式識別碼無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータプログラム識別子が無効です。";
        return "Invalid accelerator program identifier.";
    }
    if (strcmp(msg, "Invalid accelerator program reached the Direct3D 12 backend.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator program reached the Direct3D 12 backend.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un programa del acelerador no válido llegó al backend Direct3D 12.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un programme d’accélérateur non valide a atteint le backend Direct3D 12.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein ungültiges Beschleunigerprogramm hat das Direct3D 12-Backend erreicht.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un programma dell’acceleratore non valido ha raggiunto il backend Direct3D 12.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένα μη έγκυρο πρόγραμμα επιταχυντή έφτασε στο backend Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая программа ускорителя достигла бэкенда Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的加速器程序到达了 Direct3D 12 后端。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的加速器程式到達了 Direct3D 12 後端。";
        if (strncmp(lang_code, "ja", 2) == 0) return "無効なアクセラレータプログラムがDirect3D 12バックエンドに到達しました。";
        return "Invalid accelerator program reached the Direct3D 12 backend.";
    }
    if (strcmp(msg, "Invalid accelerator program reached the OpenGL ES backend.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator program reached the OpenGL ES backend.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un programa del acelerador no válido llegó al backend OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un programme d’accélérateur non valide a atteint le backend OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein ungültiges Beschleunigerprogramm hat das OpenGL ES-Backend erreicht.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un programma dell’acceleratore non valido ha raggiunto il backend OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένα μη έγκυρο πρόγραμμα επιταχυντή έφτασε στο backend OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая программа ускорителя достигла бэкенда OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的加速器程序到达了 OpenGL ES 后端。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的加速器程式到達了 OpenGL ES 後端。";
        if (strncmp(lang_code, "ja", 2) == 0) return "無効なアクセラレータプログラムがOpenGL ESバックエンドに到達しました。";
        return "Invalid accelerator program reached the OpenGL ES backend.";
    }
    if (strcmp(msg, "Invalid accelerator program reached the Vulkan backend.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator program reached the Vulkan backend.";
        if (strncmp(lang_code, "es", 2) == 0) return "Un programa del acelerador no válido llegó al backend Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un programme d’accélérateur non valide a atteint le backend Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ein ungültiges Beschleunigerprogramm hat das Vulkan-Backend erreicht.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un programma dell’acceleratore non valido ha raggiunto il backend Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ένα μη έγκυρο πρόγραμμα επιταχυντή έφτασε στο backend Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая программа ускорителя достигла бэкенда Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的加速器程序到达了 Vulkan 后端。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的加速器程式到達了 Vulkan 後端。";
        if (strncmp(lang_code, "ja", 2) == 0) return "無効なアクセラレータプログラムがVulkanバックエンドに到達しました。";
        return "Invalid accelerator program reached the Vulkan backend.";
    }
    if (strcmp(msg, "Invalid accelerator reduction metadata.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator reduction metadata.";
        if (strncmp(lang_code, "es", 2) == 0) return "Metadatos de reducción del acelerador no válidos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Métadonnées de réduction de l’accélérateur non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Reduktionsmetadaten des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Metadati della riduzione dell’acceleratore non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρα μεταδεδομένα αναγωγής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые метаданные редукции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器归约元数据无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器歸約中繼資料無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリダクションのメタデータが無効です。";
        return "Invalid accelerator reduction metadata.";
    }
    if (strcmp(msg, "Invalid accelerator reduction result entry.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator reduction result entry.";
        if (strncmp(lang_code, "es", 2) == 0) return "Entrada de resultado de reducción del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Entrée de résultat de réduction de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Reduktionsergebniseintrag des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Voce del risultato della riduzione dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη καταχώριση αποτελέσματος αναγωγής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая запись результата редукции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器归约结果条目无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器歸約結果項目無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリダクションの結果エントリが無効です。";
        return "Invalid accelerator reduction result entry.";
    }
    if (strcmp(msg, "Invalid accelerator reduction value type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator reduction value type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tipo de valor de reducción del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Type de valeur de réduction de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Reduktionswerttyp des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tipo di valore della riduzione dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος τύπος τιμής αναγωγής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый тип значения редукции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器归约值类型无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器歸約值型別無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリダクションの値型が無効です。";
        return "Invalid accelerator reduction value type.";
    }
    if (strcmp(msg, "Invalid accelerator region plan.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator region plan.";
        if (strncmp(lang_code, "es", 2) == 0) return "Plan de región del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Plan de région de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Regionsplan des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Piano della regione dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο σχέδιο περιοχής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый план области ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器区域计划无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域計畫無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリージョンプランが無効です。";
        return "Invalid accelerator region plan.";
    }
    if (strcmp(msg, "Invalid accelerator rewrite function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator rewrite function.";
        if (strncmp(lang_code, "es", 2) == 0) return "Función de reescritura del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Fonction de réécriture de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Umschreibungsfunktion des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Funzione di riscrittura dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη συνάρτηση επανεγγραφής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая функция перезаписи ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器重写函数无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器重寫函式無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ書き換え関数が無効です。";
        return "Invalid accelerator rewrite function.";
    }
    if (strcmp(msg, "Invalid accelerator scalar binding.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator scalar binding.";
        if (strncmp(lang_code, "es", 2) == 0) return "Vinculación escalar del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Liaison scalaire de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Skalarbindung des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Associazione scalare dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη βαθμωτή δέσμευση επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая скалярная привязка ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量绑定无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量繫結無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスカラーのバインディングが無効です。";
        return "Invalid accelerator scalar binding.";
    }
    if (strcmp(msg, "Invalid accelerator scalar result binding.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator scalar result binding.";
        if (strncmp(lang_code, "es", 2) == 0) return "Vinculación del resultado escalar del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Liaison du résultat scalaire de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Bindung des skalaren Ergebnisses des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Associazione del risultato scalare dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη δέσμευση βαθμωτού αποτελέσματος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая привязка скалярного результата ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果绑定无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果繫結無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスカラー結果のバインディングが無効です。";
        return "Invalid accelerator scalar result binding.";
    }
    if (strcmp(msg, "Invalid accelerator scalar result metadata.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator scalar result metadata.";
        if (strncmp(lang_code, "es", 2) == 0) return "Metadatos del resultado escalar del acelerador no válidos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Métadonnées du résultat scalaire de l’accélérateur non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Metadaten des skalaren Ergebnisses des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Metadati del risultato scalare dell’acceleratore non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρα μεταδεδομένα βαθμωτού αποτελέσματος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые метаданные скалярного результата ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果元数据无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果中繼資料無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスカラー結果のメタデータが無効です。";
        return "Invalid accelerator scalar result metadata.";
    }
    if (strcmp(msg, "Invalid accelerator scalar result.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator scalar result.";
        if (strncmp(lang_code, "es", 2) == 0) return "Resultado escalar del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Résultat scalaire de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges skalares Ergebnis des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Risultato scalare dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο βαθμωτό αποτέλεσμα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый скалярный результат ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスカラー結果が無効です。";
        return "Invalid accelerator scalar result.";
    }
    if (strcmp(msg, "Invalid accelerator scalar type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator scalar type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tipo escalar del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Type scalaire de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Skalartyp des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tipo scalare dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος βαθμωτός τύπος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый скалярный тип ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量类型无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量型別無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスカラー型が無効です。";
        return "Invalid accelerator scalar type.";
    }
    if (strcmp(msg, "Invalid accelerator scalar-result slot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator scalar-result slot.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ranura de resultado escalar del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Emplacement de résultat scalaire de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Slot für skalare Ergebnisse des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Slot del risultato scalare dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη θέση βαθμωτού αποτελέσματος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый слот скалярного результата ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果槽位无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果槽位無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスカラー結果スロットが無効です。";
        return "Invalid accelerator scalar-result slot.";
    }
    if (strcmp(msg, "Invalid accelerator scratch budget.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator scratch budget.";
        if (strncmp(lang_code, "es", 2) == 0) return "Presupuesto temporal del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Budget de stockage temporaire de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges Scratch-Budget des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Budget temporaneo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος προϋπολογισμός προσωρινού χώρου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый бюджет временной памяти ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器暂存预算无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器暫存預算無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスクラッチ予算が無効です。";
        return "Invalid accelerator scratch budget.";
    }
    if (strcmp(msg, "Invalid accelerator session.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid accelerator session.";
        if (strncmp(lang_code, "es", 2) == 0) return "Sesión del acelerador no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Session de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Beschleunigersitzung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Sessione dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη συνεδρία επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый сеанс ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器会话无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器工作階段無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータセッションが無効です。";
        return "Invalid accelerator session.";
    }
    if (strcmp(msg, "Invalid application dependency graph.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid application dependency graph.";
        if (strncmp(lang_code, "es", 2) == 0) return "Grafo de dependencias de la aplicación no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Graphe de dépendances de l’application non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Abhängigkeitsgraph der Anwendung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Grafo delle dipendenze dell’applicazione non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο γράφημα εξαρτήσεων εφαρμογής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый граф зависимостей приложения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "应用程序依赖关系图无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "應用程式相依性圖形無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーション依存関係グラフが無効です。";
        return "Invalid application dependency graph.";
    }
    if (strcmp(msg, "Invalid bytecode function descriptor.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid bytecode function descriptor.";
        if (strncmp(lang_code, "es", 2) == 0) return "Descriptor de función de bytecode no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Descripteur de fonction de bytecode non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Bytecode-Funktionsdeskriptor.";
        if (strncmp(lang_code, "it", 2) == 0) return "Descrittore della funzione bytecode non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος περιγραφέας συνάρτησης bytecode.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый дескриптор функции байткода.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码函数描述符无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼函式描述元無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "バイトコード関数ディスクリプタが無効です。";
        return "Invalid bytecode function descriptor.";
    }
    if (strcmp(msg, "Invalid character range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid character range.";
        if (strncmp(lang_code, "es", 2) == 0) return "Rango de caracteres no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Plage de caractères non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Zeichenbereich.";
        if (strncmp(lang_code, "it", 2) == 0) return "Intervallo di caratteri non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο εύρος χαρακτήρων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый диапазон символов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字符范围无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "字元範圍無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "文字範囲が無効です。";
        return "Invalid character range.";
    }
    if (strcmp(msg, "Invalid codepoint.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid codepoint.";
        if (strncmp(lang_code, "es", 2) == 0) return "Punto de código no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Point de code non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Codepunkt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Punto di codice non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο σημείο κώδικα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая кодовая точка.";
        if (strncmp(lang_code, "zh", 2) == 0) return "码点无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "碼位無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "コードポイントが無効です。";
        return "Invalid codepoint.";
    }
    if (strcmp(msg, "Invalid constant __fast shape extent.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid constant __fast shape extent.";
        if (strncmp(lang_code, "es", 2) == 0) return "Extensión constante de forma __fast no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Étendue constante de forme __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige konstante Ausdehnung der __fast-Form.";
        if (strncmp(lang_code, "it", 2) == 0) return "Estensione costante della forma __fast non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη σταθερή έκταση σχήματος __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый постоянный размер измерения формы __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形状的常量维度大小无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 形狀的常數維度大小無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast形状の定数次元サイズが無効です。";
        return "Invalid constant __fast shape extent.";
    }
    if (strcmp(msg, "Invalid dynamic __fast shape extent.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid dynamic __fast shape extent.";
        if (strncmp(lang_code, "es", 2) == 0) return "Extensión dinámica de forma __fast no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Étendue dynamique de forme __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige dynamische Ausdehnung der __fast-Form.";
        if (strncmp(lang_code, "it", 2) == 0) return "Estensione dinamica della forma __fast non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη δυναμική έκταση σχήματος __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый динамический размер измерения формы __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形状的动态维度大小无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 形狀的動態維度大小無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast形状の動的次元サイズが無効です。";
        return "Invalid dynamic __fast shape extent.";
    }
    if (strcmp(msg, "Invalid empty __fast shape extent.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid empty __fast shape extent.";
        if (strncmp(lang_code, "es", 2) == 0) return "Extensión vacía de forma __fast no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Étendue vide de forme __fast non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige leere Ausdehnung der __fast-Form.";
        if (strncmp(lang_code, "it", 2) == 0) return "Estensione vuota della forma __fast non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη κενή έκταση σχήματος __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый пустой размер измерения формы __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形状的空维度大小无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 形狀的空維度大小無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast形状の空の次元サイズが無効です。";
        return "Invalid empty __fast shape extent.";
    }
    if (strcmp(msg, "Invalid event flags.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid event flags.";
        if (strncmp(lang_code, "es", 2) == 0) return "Indicadores de evento no válidos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Indicateurs d’événement non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Ereignisflags.";
        if (strncmp(lang_code, "it", 2) == 0) return "Flag dell’evento non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρες σημαίες συμβάντος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые флаги события.";
        if (strncmp(lang_code, "zh", 2) == 0) return "事件标志无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "事件旗標無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "イベントフラグが無効です。";
        return "Invalid event flags.";
    }
    if (strcmp(msg, "Invalid function parameter metadata.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid function parameter metadata.";
        if (strncmp(lang_code, "es", 2) == 0) return "Metadatos de parámetros de función no válidos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Métadonnées de paramètres de fonction non valides.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Funktionsparametermetadaten.";
        if (strncmp(lang_code, "it", 2) == 0) return "Metadati dei parametri della funzione non validi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρα μεταδεδομένα παραμέτρων συνάρτησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимые метаданные параметров функции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "函数形参元数据无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "函式參數中繼資料無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数パラメータのメタデータが無効です。";
        return "Invalid function parameter metadata.";
    }
    if (strcmp(msg, "Invalid function prototype.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid function prototype.";
        if (strncmp(lang_code, "es", 2) == 0) return "Prototipo de función no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Prototype de fonction non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Funktionsprototyp.";
        if (strncmp(lang_code, "it", 2) == 0) return "Prototipo di funzione non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο πρωτότυπο συνάρτησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый прототип функции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "函数原型无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "函式原型無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数プロトタイプが無効です。";
        return "Invalid function prototype.";
    }
    if (strcmp(msg, "Invalid handle.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid handle.";
        if (strncmp(lang_code, "es", 2) == 0) return "Identificador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Handle non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges Handle.";
        if (strncmp(lang_code, "it", 2) == 0) return "Handle non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρος χειριστής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый дескриптор.";
        if (strncmp(lang_code, "zh", 2) == 0) return "句柄无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "控制代碼無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ハンドルが無効です。";
        return "Invalid handle.";
    }
    if (strcmp(msg, "Invalid input module path.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid input module path.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ruta del módulo de entrada no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Chemin du module d’entrée non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Pfad des Eingabemoduls.";
        if (strncmp(lang_code, "it", 2) == 0) return "Percorso del modulo di input non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη διαδρομή μονάδας εισόδου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый путь входного модуля.";
        if (strncmp(lang_code, "zh", 2) == 0) return "输入模块路径无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "輸入模組路徑無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "入力モジュールのパスが無効です。";
        return "Invalid input module path.";
    }
    if (strcmp(msg, "Invalid input module.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid input module.";
        if (strncmp(lang_code, "es", 2) == 0) return "Módulo de entrada no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Module d’entrée non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges Eingabemodul.";
        if (strncmp(lang_code, "it", 2) == 0) return "Modulo di input non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη μονάδα εισόδου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый входной модуль.";
        if (strncmp(lang_code, "zh", 2) == 0) return "输入模块无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "輸入模組無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "入力モジュールが無効です。";
        return "Invalid input module.";
    }
    if (strcmp(msg, "Invalid module path option %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid module path option %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Opción de ruta de módulo %s no válida.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Option de chemin de module %s non valide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Modulpfadoption %s.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Opzione del percorso del modulo %s non valida.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη επιλογή διαδρομής μονάδας %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый параметр пути модуля %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "模块路径选项 %s 无效。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "模組路徑選項 %s 無效。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュールパスオプション%sが無効です。\n";
        return "Invalid module path option %s.\n";
    }
    if (strcmp(msg, "Invalid native function parameter name.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid native function parameter name.";
        if (strncmp(lang_code, "es", 2) == 0) return "Nombre de parámetro de función nativa no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nom de paramètre de fonction native non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger Parametername einer nativen Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Nome del parametro della funzione nativa non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο όνομα παραμέτρου εγγενούς συνάρτησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое имя параметра нативной функции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "原生函数形参名称无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "原生函式參數名稱無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ネイティブ関数のパラメータ名が無効です。";
        return "Invalid native function parameter name.";
    }
    if (strcmp(msg, "Invalid native function registration.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid native function registration.";
        if (strncmp(lang_code, "es", 2) == 0) return "Registro de función nativa no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Enregistrement de fonction native non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Registrierung einer nativen Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Registrazione della funzione nativa non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη καταχώριση εγγενούς συνάρτησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая регистрация нативной функции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "原生函数注册无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "原生函式註冊無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ネイティブ関数の登録が無効です。";
        return "Invalid native function registration.";
    }
    if (strcmp(msg, "Invalid optimize-level option %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid optimize-level option %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Opción de nivel de optimización %s no válida.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Option de niveau d’optimisation %s non valide.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Optimierungsstufenoption %s.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Opzione del livello di ottimizzazione %s non valida.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη επιλογή επιπέδου βελτιστοποίησης %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый параметр уровня оптимизации %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "优化级别选项 %s 无效。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "最佳化層級選項 %s 無效。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "最適化レベルオプション%sが無効です。\n";
        return "Invalid optimize-level option %s.\n";
    }
    if (strcmp(msg, "Invalid or incomplete %s accelerator finish.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid or incomplete %s accelerator finish.";
        if (strncmp(lang_code, "es", 2) == 0) return "Finalización del acelerador %s no válida o incompleta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Finalisation de l’accélérateur %s non valide ou incomplète.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger oder unvollständiger Abschluss des %s-Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Completamento dell’acceleratore %s non valido o incompleto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη ή ελλιπής ολοκλήρωση επιταχυντή %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое или неполное завершение ускорителя %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器完成操作无效或不完整。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器完成作業無效或不完整。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sアクセラレータの完了処理が無効または不完全です。";
        return "Invalid or incomplete %s accelerator finish.";
    }
    if (strcmp(msg, "Invalid or out-of-order %s accelerator dispatch.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid or out-of-order %s accelerator dispatch.";
        if (strncmp(lang_code, "es", 2) == 0) return "Despacho del acelerador %s no válido o fuera de orden.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Lancement de l’accélérateur %s non valide ou dans un ordre incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiger oder falsch angeordneter Dispatch des %s-Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dispatch dell’acceleratore %s non valido o fuori sequenza.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη ή εκτός σειράς αποστολή επιταχυντή %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая или выполненная не по порядку диспетчеризация ускорителя %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "%s 加速器调度无效或顺序错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "%s 加速器調度無效或順序錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sアクセラレータのディスパッチが無効または順序外です。";
        return "Invalid or out-of-order %s accelerator dispatch.";
    }
    if (strcmp(msg, "Invalid private accelerator package.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid private accelerator package.";
        if (strncmp(lang_code, "es", 2) == 0) return "Paquete privado del acelerador no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Package privé de l’accélérateur non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges privates Beschleunigerpaket.";
        if (strncmp(lang_code, "it", 2) == 0) return "Pacchetto privato dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρο ιδιωτικό πακέτο επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимый закрытый пакет ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器私有包无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器私有套件無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのプライベートパッケージが無効です。";
        return "Invalid private accelerator package.";
    }
    if (strcmp(msg, "Invalid selected Direct3D 12 device.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid selected Direct3D 12 device.";
        if (strncmp(lang_code, "es", 2) == 0) return "Dispositivo Direct3D 12 seleccionado no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Périphérique Direct3D 12 sélectionné non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges ausgewähltes Direct3D 12-Gerät.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dispositivo Direct3D 12 selezionato non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη επιλεγμένη συσκευή Direct3D 12.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выбрано недопустимое устройство Direct3D 12.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所选的 Direct3D 12 设备无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所選的 Direct3D 12 裝置無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したDirect3D 12デバイスが無効です。";
        return "Invalid selected Direct3D 12 device.";
    }
    if (strcmp(msg, "Invalid selected OpenGL ES device.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid selected OpenGL ES device.";
        if (strncmp(lang_code, "es", 2) == 0) return "Dispositivo OpenGL ES seleccionado no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Périphérique OpenGL ES sélectionné non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges ausgewähltes OpenGL ES-Gerät.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dispositivo OpenGL ES selezionato non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη επιλεγμένη συσκευή OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выбрано недопустимое устройство OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所选的 OpenGL ES 设备无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所選的 OpenGL ES 裝置無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したOpenGL ESデバイスが無効です。";
        return "Invalid selected OpenGL ES device.";
    }
    if (strcmp(msg, "Invalid selected Vulkan device.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid selected Vulkan device.";
        if (strncmp(lang_code, "es", 2) == 0) return "Dispositivo Vulkan seleccionado no válido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Périphérique Vulkan sélectionné non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültiges ausgewähltes Vulkan-Gerät.";
        if (strncmp(lang_code, "it", 2) == 0) return "Dispositivo Vulkan selezionato non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη επιλεγμένη συσκευή Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выбрано недопустимое устройство Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所选的 Vulkan 设备无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所選的 Vulkan 裝置無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したVulkanデバイスが無効です。";
        return "Invalid selected Vulkan device.";
    }
    if (strcmp(msg, "Invalid shaped type annotation.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid shaped type annotation.";
        if (strncmp(lang_code, "es", 2) == 0) return "Anotación de tipo con forma no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Annotation de type avec forme non valide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Typanmerkung mit Form.";
        if (strncmp(lang_code, "it", 2) == 0) return "Annotazione di tipo con forma non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη επισημείωση τύπου με σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимая аннотация типа с формой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "带形状的类型注解无效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "帶形狀的型別註釋無效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "形状付き型注釈が無効です。";
        return "Invalid shaped type annotation.";
    }
    if (strcmp(msg, "Invalid shift amount.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Invalid shift amount.";
        if (strncmp(lang_code, "es", 2) == 0) return "Cantidad de desplazamiento no válida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Valeur de décalage invalide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ungültige Verschiebungsmenge.";
        if (strncmp(lang_code, "it", 2) == 0) return "Quantità di shift non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη έγκυρη ποσότητα ολίσθησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недопустимое количество сдвигов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的移位量。";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的移位量。";
        if (strncmp(lang_code, "ja", 2) == 0) return "シフト量が無効です。";
        return "Invalid shift amount.";
    }
    if (strcmp(msg, "JIT bytecode analysis table is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "JIT bytecode analysis table is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de análisis de bytecode JIT es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table d’analyse du bytecode JIT est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die JIT-Bytecode-Analysetabelle ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella di analisi del bytecode JIT è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας ανάλυσης bytecode JIT είναι πολύ μεγάλος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица анализа JIT-байткода слишком велика.";
        if (strncmp(lang_code, "zh", 2) == 0) return "JIT 字节码分析表过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "JIT 位元組碼分析表過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "JITバイトコード解析テーブルが大きすぎます。";
        return "JIT bytecode analysis table is too large.";
    }
    if (strcmp(msg, "JIT bytecode is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "JIT bytecode is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bytecode JIT es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bytecode JIT est trop grand.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der JIT-Bytecode ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il bytecode JIT è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το bytecode JIT είναι πολύ μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "JIT-байткод слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "JIT 字节码过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "JIT 位元組碼過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "JITバイトコードが大きすぎます。";
        return "JIT bytecode is too large.";
    }
    if (strcmp(msg, "JIT compilation is enabled. Starting the fast VM...\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "JIT compilation is enabled. Starting the fast VM...\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La compilación JIT está activada. Iniciando la máquina virtual rápida...\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "La compilation JIT est activée. Démarrage de la machine virtuelle rapide...\n";
        if (strncmp(lang_code, "de", 2) == 0) return "JIT-Kompilierung ist aktiviert. Starte die schnelle VM...\n";
        if (strncmp(lang_code, "it", 2) == 0) return "La compilazione JIT è attivata. Avvio della VM veloce...\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μεταγλώττιση JIT είναι ενεργοποιημένη. Εκκίνηση της γρήγορης εικονικής μηχανής...\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "JIT-компиляция включена. Запуск быстрой виртуальной машины...\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "触发编译已启用。正在启动快速虚拟机...\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "觸發編譯已啟用。正在啟動快速虛擬機器...\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "JIT コンパイルが有効です。高速仮想マシンを開始します...\n";
        return "JIT compilation is enabled. Starting the fast VM...\n";
    }
    if (strcmp(msg, "JIT memory protection failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "JIT memory protection failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error en la protección de memoria JIT.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la protection de la mémoire JIT.";
        if (strncmp(lang_code, "de", 2) == 0) return "JIT-Speicherschutz fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Protezione della memoria JIT non riuscita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η προστασία μνήμης JIT απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось установить защиту памяти JIT.";
        if (strncmp(lang_code, "zh", 2) == 0) return "JIT 内存保护失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "JIT 記憶體保護失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "JITメモリ保護に失敗しました。";
        return "JIT memory protection failed.";
    }
    if (strcmp(msg, "JIT temporary-variable table is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "JIT temporary-variable table is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de variables temporales JIT es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table de variables temporaires JIT est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die JIT-Tabelle für temporäre Variablen ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella delle variabili temporanee JIT è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας προσωρινών μεταβλητών JIT είναι πολύ μεγάλος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица временных переменных JIT слишком велика.";
        if (strncmp(lang_code, "zh", 2) == 0) return "JIT 临时变量表过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "JIT 暫存變數表過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "JIT一時変数テーブルが大きすぎます。";
        return "JIT temporary-variable table is too large.";
    }
    if (strcmp(msg, "LHS is not a symbol or an array element.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "LHS is not a symbol or an array element.";
        if (strncmp(lang_code, "es", 2) == 0) return "LHS no es un símbolo ni un elemento de un array.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La partie gauche n'est ni un symbole ni un élément de tableau.";
        if (strncmp(lang_code, "de", 2) == 0) return "LHS ist weder ein Symbol noch ein Array-Element.";
        if (strncmp(lang_code, "it", 2) == 0) return "LHS non è un simbolo né un elemento di un array.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αριστερή πλευρά δεν είναι ούτε σύμβολο ούτε στοιχείο πίνακα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Левая часть не является ни символом, ни элементом массива.";
        if (strncmp(lang_code, "zh", 2) == 0) return "左侧不是符号或数组元素。";
        if (strncmp(lang_code, "tw", 2) == 0) return "左側不是符號或陣列元素。";
        if (strncmp(lang_code, "ja", 2) == 0) return "左辺がシンボルか配列要素ではありません。";
        return "LHS is not a symbol or an array element.";
    }
    if (strcmp(msg, "LHS is not a term or an array element.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "LHS is not a term or an array element.";
        if (strncmp(lang_code, "es", 2) == 0) return "LHS no es un término ni un elemento de un array.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La partie gauche n'est ni un terme ni un élément de tableau.";
        if (strncmp(lang_code, "de", 2) == 0) return "LHS ist weder ein Term noch ein Array-Element.";
        if (strncmp(lang_code, "it", 2) == 0) return "LHS non è un termine né un elemento di un array.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αριστερή πλευρά δεν είναι ούτε όρος ούτε στοιχείο πίνακα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Левая часть не является ни выражением, ни элементом массива.";
        if (strncmp(lang_code, "zh", 2) == 0) return "左侧不是项或数组元素。";
        if (strncmp(lang_code, "tw", 2) == 0) return "左側不是項或陣列元素。";
        if (strncmp(lang_code, "ja", 2) == 0) return "左辺が項か配列要素ではありません。";
        return "LHS is not a term or an array element.";
    }
    if (strcmp(msg, "LIR: Out of memory error.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "LIR: Out of memory error.";
        if (strncmp(lang_code, "es", 2) == 0) return "LIR: No hay suficiente memoria.";
        if (strncmp(lang_code, "fr", 2) == 0) return "LIR: Memoria insuficiente.";
        if (strncmp(lang_code, "de", 2) == 0) return "LIR: Nicht genügend Speicher.";
        if (strncmp(lang_code, "it", 2) == 0) return "LIR: Non c'è abbastanza memoria.";
        if (strncmp(lang_code, "el", 2) == 0) return "LIR: Δεν υπάρχει αρκετή μνήμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "LIR: Недостаточно памяти.";
        if (strncmp(lang_code, "zh", 2) == 0) return "LIR: 内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "LIR: 記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "LIR: メモリが足りません。";
        return "LIR: Out of memory error.";
    }
    if (strcmp(msg, "Legacy 1.0 bytecode cannot be embedded in an application: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Legacy 1.0 bytecode cannot be embedded in an application: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "El bytecode 1.0 heredado no se puede incrustar en una aplicación: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bytecode 1.0 hérité ne peut pas être incorporé dans une application : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Veralteter 1.0-Bytecode kann nicht in eine Anwendung eingebettet werden: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Il bytecode 1.0 legacy non può essere incorporato in un’applicazione: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Το παλαιού τύπου bytecode 1.0 δεν μπορεί να ενσωματωθεί σε εφαρμογή: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Устаревший байткод версии 1.0 нельзя встроить в приложение: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法将旧版 1.0 字节码嵌入应用程序：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法將舊版 1.0 位元組碼嵌入應用程式：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "レガシー1.0バイトコードをアプリケーションに埋め込むことはできません: %s";
        return "Legacy 1.0 bytecode cannot be embedded in an application: %s";
    }
    if (strcmp(msg, "Local initializer or assignment does not match its declared type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Local initializer or assignment does not match its declared type.";
        if (strncmp(lang_code, "es", 2) == 0) return "El inicializador o la asignación de la variable local no coincide con su tipo declarado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’initialiseur ou l’affectation de la variable locale ne correspond pas à son type déclaré.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Initialisierer oder die Zuweisung der lokalen Variable stimmt nicht mit ihrem deklarierten Typ überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’inizializzatore o l’assegnazione della variabile locale non corrisponde al tipo dichiarato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αρχικοποίηση ή η ανάθεση της τοπικής μεταβλητής δεν αντιστοιχεί στον δηλωμένο τύπο της.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Инициализатор или присваивание локальной переменной не соответствует её объявленному типу.";
        if (strncmp(lang_code, "zh", 2) == 0) return "局部变量的初始化表达式或赋值与其声明的类型不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "區域變數的初始化運算式或指派與其宣告的型別不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ローカル変数の初期化子または代入が宣言された型と一致しません。";
        return "Local initializer or assignment does not match its declared type.";
    }
    if (strcmp(msg, "Malformed accelerator HIR block.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator HIR block.";
        if (strncmp(lang_code, "es", 2) == 0) return "Bloque HIR del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Bloc HIR de l’accélérateur mal formé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebauter HIR-Block des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Blocco HIR dell’acceleratore non correttamente formato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένο μπλοκ HIR επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированный блок HIR ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器 HIR 块格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器 HIR 區塊格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータHIRブロックの形式が不正です。";
        return "Malformed accelerator HIR block.";
    }
    if (strcmp(msg, "Malformed accelerator HIR expression.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator HIR expression.";
        if (strncmp(lang_code, "es", 2) == 0) return "Expresión HIR del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Expression HIR de l’accélérateur mal formée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebauter HIR-Ausdruck des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Espressione HIR dell’acceleratore non correttamente formata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένη έκφραση HIR επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированное выражение HIR ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器 HIR 表达式格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器 HIR 運算式格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータHIR式の形式が不正です。";
        return "Malformed accelerator HIR expression.";
    }
    if (strcmp(msg, "Malformed accelerator HIR statement.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator HIR statement.";
        if (strncmp(lang_code, "es", 2) == 0) return "Sentencia HIR del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Instruction HIR de l’accélérateur mal formée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebaute HIR-Anweisung des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Istruzione HIR dell’acceleratore non correttamente formata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένη εντολή HIR επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированный оператор HIR ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器 HIR 语句格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器 HIR 陳述式格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータHIR文の形式が不正です。";
        return "Malformed accelerator HIR statement.";
    }
    if (strcmp(msg, "Malformed accelerator HIR term.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator HIR term.";
        if (strncmp(lang_code, "es", 2) == 0) return "Término HIR del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Terme HIR de l’accélérateur mal formé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebauter HIR-Term des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Termine HIR dell’acceleratore non correttamente formato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένος όρος HIR επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированный терм HIR ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器 HIR 项格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器 HIR 項格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータHIR項の形式が不正です。";
        return "Malformed accelerator HIR term.";
    }
    if (strcmp(msg, "Malformed accelerator array expression.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator array expression.";
        if (strncmp(lang_code, "es", 2) == 0) return "Expresión de array del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Expression de tableau de l’accélérateur mal formée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebauter Array-Ausdruck des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Espressione array dell’acceleratore non correttamente formata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένη έκφραση πίνακα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированное выражение массива ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器数组表达式格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器陣列運算式格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ配列式の形式が不正です。";
        return "Malformed accelerator array expression.";
    }
    if (strcmp(msg, "Malformed accelerator assignment target.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator assignment target.";
        if (strncmp(lang_code, "es", 2) == 0) return "Destino de asignación del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Cible d’affectation de l’accélérateur mal formée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebautes Zuweisungsziel des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Destinazione dell’assegnazione dell’acceleratore non correttamente formata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένος στόχος ανάθεσης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированная цель присваивания ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器赋值目标格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器指派目標格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ代入ターゲットの形式が不正です。";
        return "Malformed accelerator assignment target.";
    }
    if (strcmp(msg, "Malformed accelerator call expression.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator call expression.";
        if (strncmp(lang_code, "es", 2) == 0) return "Expresión de llamada del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Expression d’appel de l’accélérateur mal formée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebauter Aufrufausdruck des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Espressione di chiamata dell’acceleratore non correttamente formata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένη έκφραση κλήσης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированное выражение вызова ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器调用表达式格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器呼叫運算式格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ呼び出し式の形式が不正です。";
        return "Malformed accelerator call expression.";
    }
    if (strcmp(msg, "Malformed accelerator collection loop.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator collection loop.";
        if (strncmp(lang_code, "es", 2) == 0) return "Bucle de colección del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Boucle de collection de l’accélérateur mal formée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebaute Sammlungsschleife des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ciclo di raccolta dell’acceleratore non correttamente formato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένος βρόχος συλλογής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированный цикл коллекции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器集合循环格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器集合迴圈格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータコレクションループの形式が不正です。";
        return "Malformed accelerator collection loop.";
    }
    if (strcmp(msg, "Malformed accelerator dictionary expression.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator dictionary expression.";
        if (strncmp(lang_code, "es", 2) == 0) return "Expresión de diccionario del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Expression de dictionnaire de l’accélérateur mal formée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhaft aufgebauter Dictionary-Ausdruck des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Espressione dizionario dell’acceleratore non correttamente formata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κακοσχηματισμένη έκφραση λεξικού επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректно сформированное выражение словаря ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器字典表达式格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器字典運算式格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ辞書式の形式が不正です。";
        return "Malformed accelerator dictionary expression.";
    }
    if (strcmp(msg, "Malformed accelerator expression HIR.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator expression HIR.";
        if (strncmp(lang_code, "es", 2) == 0) return "HIR de expresión de acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "HIR d’expression d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter HIR eines Beschleunigerausdrucks.";
        if (strncmp(lang_code, "it", 2) == 0) return "HIR dell’espressione dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένο HIR έκφρασης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный HIR выражения ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器表达式 HIR 格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器運算式 HIR 格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ式のHIRが不正です。";
        return "Malformed accelerator expression HIR.";
    }
    if (strcmp(msg, "Malformed accelerator function control flow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator function control flow.";
        if (strncmp(lang_code, "es", 2) == 0) return "Flujo de control de función del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Flux de contrôle de fonction d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Kontrollfluss einer Beschleunigerfunktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Flusso di controllo della funzione dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένη ροή ελέγχου συνάρτησης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный поток управления функции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器函数控制流格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器函式控制流程格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ関数の制御フローが不正です。";
        return "Malformed accelerator function control flow.";
    }
    if (strcmp(msg, "Malformed accelerator loop body.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator loop body.";
        if (strncmp(lang_code, "es", 2) == 0) return "Cuerpo de bucle del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Corps de boucle d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Schleifenkörper des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Corpo del ciclo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένο σώμα βρόχου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректное тело цикла ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器循环体格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器迴圈主體格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのループ本体が不正です。";
        return "Malformed accelerator loop body.";
    }
    if (strcmp(msg, "Malformed accelerator member call.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator member call.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada a miembro del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel de membre d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Memberaufruf des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata a membro dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένη κλήση μέλους επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный вызов члена ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器成员调用格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器成員呼叫格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのメンバー呼び出しが不正です。";
        return "Malformed accelerator member call.";
    }
    if (strcmp(msg, "Malformed accelerator new expression.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator new expression.";
        if (strncmp(lang_code, "es", 2) == 0) return "Expresión new del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Expression new d’accélérateur incorrecte.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter new-Ausdruck des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Espressione new dell’acceleratore non valida.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένη έκφραση new επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректное выражение new ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器 new 表达式格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器 new 運算式格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのnew式が不正です。";
        return "Malformed accelerator new expression.";
    }
    if (strcmp(msg, "Malformed accelerator ranged loop.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator ranged loop.";
        if (strncmp(lang_code, "es", 2) == 0) return "Bucle por rango del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Boucle sur plage d’accélérateur incorrecte.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafte Bereichsschleife des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ciclo su intervallo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένος βρόχος εύρους επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный цикл по диапазону ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器范围循环格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器範圍迴圈格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの範囲ループが不正です。";
        return "Malformed accelerator ranged loop.";
    }
    if (strcmp(msg, "Malformed accelerator region control flow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator region control flow.";
        if (strncmp(lang_code, "es", 2) == 0) return "Flujo de control de región del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Flux de contrôle de région d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Kontrollfluss einer Beschleunigerregion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Flusso di controllo della regione dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένη ροή ελέγχου περιοχής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный поток управления области ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器区域控制流格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域控制流程格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリージョンの制御フローが不正です。";
        return "Malformed accelerator region control flow.";
    }
    if (strcmp(msg, "Malformed accelerator string term.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator string term.";
        if (strncmp(lang_code, "es", 2) == 0) return "Término de cadena del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Terme de chaîne de caractères d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Zeichenkettenterm des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Termine stringa dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένος όρος συμβολοσειράς επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный строковый терм ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器字符串项格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器字串項格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの文字列項が不正です。";
        return "Malformed accelerator string term.";
    }
    if (strcmp(msg, "Malformed accelerator symbol HIR.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator symbol HIR.";
        if (strncmp(lang_code, "es", 2) == 0) return "HIR de símbolo del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "HIR de symbole d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter HIR eines Beschleunigersymbols.";
        if (strncmp(lang_code, "it", 2) == 0) return "HIR del simbolo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένο HIR συμβόλου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный HIR символа ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器符号 HIR 格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器符號 HIR 格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータシンボルのHIRが不正です。";
        return "Malformed accelerator symbol HIR.";
    }
    if (strcmp(msg, "Malformed accelerator symbol term.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator symbol term.";
        if (strncmp(lang_code, "es", 2) == 0) return "Término de símbolo del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Terme de symbole d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Symbolterm des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Termine simbolo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένος όρος συμβόλου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный символьный терм ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器符号项格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器符號項格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのシンボル項が不正です。";
        return "Malformed accelerator symbol term.";
    }
    if (strcmp(msg, "Malformed accelerator term HIR.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator term HIR.";
        if (strncmp(lang_code, "es", 2) == 0) return "HIR de término del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "HIR de terme d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter HIR eines Beschleunigerterms.";
        if (strncmp(lang_code, "it", 2) == 0) return "HIR del termine dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένο HIR όρου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный HIR терма ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器项 HIR 格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器項 HIR 格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ項のHIRが不正です。";
        return "Malformed accelerator term HIR.";
    }
    if (strcmp(msg, "Malformed accelerator type test.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator type test.";
        if (strncmp(lang_code, "es", 2) == 0) return "Prueba de tipo del acelerador mal formada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Test de type d’accélérateur incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Typtest des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Test di tipo dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένος έλεγχος τύπου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректная проверка типа ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器类型测试格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器型別測試格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータの型テストが不正です。";
        return "Malformed accelerator type test.";
    }
    if (strcmp(msg, "Malformed accelerator while loop.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed accelerator while loop.";
        if (strncmp(lang_code, "es", 2) == 0) return "Bucle while del acelerador mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Boucle while d’accélérateur incorrecte.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafte while-Schleife des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ciclo while dell’acceleratore non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένος βρόχος while επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный цикл while ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器 while 循环格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器 while 迴圈格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのwhileループが不正です。";
        return "Malformed accelerator while loop.";
    }
    if (strcmp(msg, "Malformed application container.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Malformed application container.";
        if (strncmp(lang_code, "es", 2) == 0) return "Contenedor de aplicación mal formado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Conteneur d’application incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehlerhafter Anwendungscontainer.";
        if (strncmp(lang_code, "it", 2) == 0) return "Contenitore dell’applicazione non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ακατάλληλα διαμορφωμένο κοντέινερ εφαρμογής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Некорректный контейнер приложения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "应用程序容器格式错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "應用程式容器格式錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーションコンテナが不正です。";
        return "Malformed application container.";
    }
    if (strcmp(msg, "Memory mapping failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Memory mapping failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El mapeo de la memoria ha fallado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l'association mémoire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Speicherabbildung fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Mappatura della memoria fallita.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αποτυχία αντιστοίχισης μνήμης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка отображения памяти.";
        if (strncmp(lang_code, "zh", 2) == 0) return "内存映射失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記憶體映射失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "メモリマップに失敗しました。";
        return "Memory mapping failed.";
    }
    if (strcmp(msg, "Missing OpenGL ES buffer descriptors.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES buffer descriptors.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan los descriptores de búfer de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Descripteurs de tampon OpenGL ES manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Pufferdeskriptoren fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Descrittori dei buffer OpenGL ES mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι περιγραφείς ενδιάμεσης μνήμης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют дескрипторы буферов OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 缓冲区描述符。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 緩衝區描述元。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファのディスクリプタがありません。";
        return "Missing OpenGL ES buffer descriptors.";
    }
    if (strcmp(msg, "Missing OpenGL ES download snapshot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES download snapshot.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la instantánea de descarga de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Instantané de téléchargement OpenGL ES manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Download-Momentaufnahme fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Istantanea di download OpenGL ES mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το στιγμιότυπο λήψης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует снимок выгрузки OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 下载快照。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 下載快照。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESダウンロードのスナップショットがありません。";
        return "Missing OpenGL ES download snapshot.";
    }
    if (strcmp(msg, "Missing OpenGL ES execution output.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES execution output.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la salida de ejecución de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Sortie d’exécution OpenGL ES manquante.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Ausführungsausgabe fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Output dell’esecuzione OpenGL ES mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει η έξοδος εκτέλεσης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует вывод выполнения OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 执行输出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 執行輸出。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES実行の出力がありません。";
        return "Missing OpenGL ES execution output.";
    }
    if (strcmp(msg, "Missing OpenGL ES finish buffers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES finish buffers.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan los búferes de finalización de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Tampons de fin OpenGL ES manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Abschlusspuffer fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Buffer di completamento OpenGL ES mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι ενδιάμεσες μνήμες ολοκλήρωσης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют буферы завершения OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 完成缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 完成緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES完了バッファがありません。";
        return "Missing OpenGL ES finish buffers.";
    }
    if (strcmp(msg, "Missing OpenGL ES finish result words.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES finish result words.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan las palabras de resultado de finalización de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mots de résultat de fin OpenGL ES manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Abschlussergebniswörter fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Parole del risultato di completamento OpenGL ES mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι λέξεις αποτελέσματος ολοκλήρωσης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют слова результата завершения OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 完成结果字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 完成結果字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES完了結果ワードがありません。";
        return "Missing OpenGL ES finish result words.";
    }
    if (strcmp(msg, "Missing OpenGL ES output snapshot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES output snapshot.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la instantánea de salida de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Instantané de sortie OpenGL ES manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Ausgabemomentaufnahme fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Istantanea dell’output OpenGL ES mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το στιγμιότυπο εξόδου OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует снимок вывода OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 输出快照。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 輸出快照。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力のスナップショットがありません。";
        return "Missing OpenGL ES output snapshot.";
    }
    if (strcmp(msg, "Missing OpenGL ES prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el programa preparado de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Programme OpenGL ES préparé manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vorbereitetes OpenGL ES-Programm fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Programma OpenGL ES preparato mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το προετοιμασμένο πρόγραμμα OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует подготовленная программа OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少已准备的 OpenGL ES 程序。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少已準備的 OpenGL ES 程式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "準備済みのOpenGL ESプログラムがありません。";
        return "Missing OpenGL ES prepared program.";
    }
    if (strcmp(msg, "Missing OpenGL ES result buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES result buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el búfer de resultado de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Tampon de résultat OpenGL ES manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Ergebnispuffer fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Buffer del risultato OpenGL ES mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει η ενδιάμεση μνήμη αποτελέσματος OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует буфер результата OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 结果缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 結果緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果バッファがありません。";
        return "Missing OpenGL ES result buffer.";
    }
    if (strcmp(msg, "Missing OpenGL ES result words.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES result words.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan las palabras de resultado de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mots de résultat OpenGL ES manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Ergebniswörter fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Parole del risultato OpenGL ES mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι λέξεις αποτελέσματος OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют слова результата OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 结果字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 結果字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果ワードがありません。";
        return "Missing OpenGL ES result words.";
    }
    if (strcmp(msg, "Missing OpenGL ES scalar words.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES scalar words.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan las palabras escalares de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mots scalaires OpenGL ES manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Skalarwörter fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Parole scalari OpenGL ES mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι βαθμωτές λέξεις OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют скалярные слова OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 标量字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 純量字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESスカラーワードがありません。";
        return "Missing OpenGL ES scalar words.";
    }
    if (strcmp(msg, "Missing OpenGL ES upload snapshot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing OpenGL ES upload snapshot.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la instantánea de carga de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Instantané de téléversement OpenGL ES manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES-Upload-Momentaufnahme fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Istantanea di upload OpenGL ES mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το στιγμιότυπο αποστολής OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует снимок загрузки OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 OpenGL ES 上传快照。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 OpenGL ES 上傳快照。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESアップロードのスナップショットがありません。";
        return "Missing OpenGL ES upload snapshot.";
    }
    if (strcmp(msg, "Missing Vulkan buffer descriptors.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan buffer descriptors.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan los descriptores de búfer de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Descripteurs de tampon Vulkan manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Pufferdeskriptoren fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Descrittori dei buffer Vulkan mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι περιγραφείς ενδιάμεσης μνήμης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют дескрипторы буферов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 缓冲区描述符。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 緩衝區描述元。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanバッファのディスクリプタがありません。";
        return "Missing Vulkan buffer descriptors.";
    }
    if (strcmp(msg, "Missing Vulkan buffer snapshot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan buffer snapshot.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la instantánea de búfer de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Instantané de tampon Vulkan manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Puffermomentaufnahme fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Istantanea del buffer Vulkan mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το στιγμιότυπο ενδιάμεσης μνήμης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует снимок буфера Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 缓冲区快照。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 緩衝區快照。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanバッファのスナップショットがありません。";
        return "Missing Vulkan buffer snapshot.";
    }
    if (strcmp(msg, "Missing Vulkan execution output.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan execution output.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la salida de ejecución de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Sortie d’exécution Vulkan manquante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Ausführungsausgabe fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Output dell’esecuzione Vulkan mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει η έξοδος εκτέλεσης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует вывод выполнения Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 执行输出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 執行輸出。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan実行の出力がありません。";
        return "Missing Vulkan execution output.";
    }
    if (strcmp(msg, "Missing Vulkan finish buffers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan finish buffers.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan los búferes de finalización de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Tampons de fin Vulkan manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Abschlusspuffer fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Buffer di completamento Vulkan mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι ενδιάμεσες μνήμες ολοκλήρωσης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют буферы завершения Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 完成缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 完成緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan完了バッファがありません。";
        return "Missing Vulkan finish buffers.";
    }
    if (strcmp(msg, "Missing Vulkan output snapshot.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan output snapshot.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la instantánea de salida de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Instantané de sortie Vulkan manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Ausgabemomentaufnahme fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Istantanea dell’output Vulkan mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το στιγμιότυπο εξόδου Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует снимок вывода Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 输出快照。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 輸出快照。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan出力のスナップショットがありません。";
        return "Missing Vulkan output snapshot.";
    }
    if (strcmp(msg, "Missing Vulkan prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el programa preparado de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Programme Vulkan préparé manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vorbereitetes Vulkan-Programm fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Programma Vulkan preparato mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το προετοιμασμένο πρόγραμμα Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует подготовленная программа Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少已准备的 Vulkan 程序。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少已準備的 Vulkan 程式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "準備済みのVulkanプログラムがありません。";
        return "Missing Vulkan prepared program.";
    }
    if (strcmp(msg, "Missing Vulkan scalar words.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan scalar words.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan las palabras escalares de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mots scalaires Vulkan manquants.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Skalarwörter fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Parole scalari Vulkan mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι βαθμωτές λέξεις Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют скалярные слова Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 标量字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 純量字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラーワードがありません。";
        return "Missing Vulkan scalar words.";
    }
    if (strcmp(msg, "Missing Vulkan scalar-result destination.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan scalar-result destination.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el destino del resultado escalar de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Destination du résultat scalaire Vulkan manquante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Ziel des skalaren Vulkan-Ergebnisses fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Destinazione del risultato scalare Vulkan mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει ο προορισμός βαθμωτού αποτελέσματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует назначение скалярного результата Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 标量结果目标。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 純量結果目的地。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラー結果の格納先がありません。";
        return "Missing Vulkan scalar-result destination.";
    }
    if (strcmp(msg, "Missing Vulkan scalar-result identities.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing Vulkan scalar-result identities.";
        if (strncmp(lang_code, "es", 2) == 0) return "Faltan las identidades del resultado escalar de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Identités du résultat scalaire Vulkan manquantes.";
        if (strncmp(lang_code, "de", 2) == 0) return "Identitäten des skalaren Vulkan-Ergebnisses fehlen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Identità del risultato scalare Vulkan mancanti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπουν οι ταυτότητες βαθμωτού αποτελέσματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствуют идентификаторы скалярного результата Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Vulkan 标量结果标识。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Vulkan 純量結果識別資料。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラー結果のIDがありません。";
        return "Missing Vulkan scalar-result identities.";
    }
    if (strcmp(msg, "Missing accelerator context.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing accelerator context.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el contexto del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Contexte d’accélérateur manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Beschleunigerkontext fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Contesto dell’acceleratore mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το περιβάλλον επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует контекст ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器上下文。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器內容。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのコンテキストがありません。";
        return "Missing accelerator context.";
    }
    if (strcmp(msg, "Missing accelerator function plan.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing accelerator function plan.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el plan de función del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Plan de fonction d’accélérateur manquant.";
        if (strncmp(lang_code, "de", 2) == 0) return "Funktionsplan des Beschleunigers fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Piano della funzione dell’acceleratore mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει το πλάνο συνάρτησης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует план функции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器函数计划。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器函式計畫。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ関数のプランがありません。";
        return "Missing accelerator function plan.";
    }
    if (strcmp(msg, "Missing accelerator registry reservation.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Missing accelerator registry reservation.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta la reserva del registro del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Réservation du registre d’accélérateur manquante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Reservierung der Beschleunigerregistrierung fehlt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Prenotazione del registro dell’acceleratore mancante.";
        if (strncmp(lang_code, "el", 2) == 0) return "Λείπει η κράτηση μητρώου επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отсутствует резервирование реестра ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器注册表预留项。";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器登錄表保留項。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータレジストリの予約がありません。";
        return "Missing accelerator registry reservation.";
    }
    if (strcmp(msg, "Multi-dimensional subscripts are valid only inside __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Multi-dimensional subscripts are valid only inside __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los subíndices multidimensionales solo son válidos dentro de __fast func.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les indices multidimensionnels ne sont valides que dans __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Mehrdimensionale Indexausdrücke sind nur innerhalb von __fast func gültig.";
        if (strncmp(lang_code, "it", 2) == 0) return "I pedici multidimensionali sono validi solo all’interno di __fast func.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι πολυδιάστατοι δείκτες είναι έγκυροι μόνο μέσα σε __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Многомерные индексы допустимы только внутри __fast func.";
        if (strncmp(lang_code, "zh", 2) == 0) return "多维下标仅在 __fast func 内有效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "多維下標僅在 __fast func 內有效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "多次元の添字は__fast func内でのみ有効です。";
        return "Multi-dimensional subscripts are valid only inside __fast func.";
    }
    if (strcmp(msg, "Multiple initializers in module: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Multiple initializers in module: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Varios inicializadores en el módulo: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Plusieurs initialiseurs dans le module : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Mehrere Initialisierer im Modul: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Più inizializzatori nel modulo: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Πολλαπλοί αρχικοποιητές στη μονάδα: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Несколько инициализаторов в модуле: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "模块中有多个初始化器：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "模組中有多個初始化器：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュールに複数の初期化子があります: %s";
        return "Multiple initializers in module: %s";
    }
    if (strcmp(msg, "NUL byte in source program.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "NUL byte in source program.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Byte NUL en el programa fuente.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Octet NUL dans le programme source.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "NUL-Byte im Quellprogramm.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Byte NUL nel programma sorgente.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Byte NUL στο πηγαίο πρόγραμμα.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Байт NUL в исходной программе.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "源程序中存在 NUL 字节。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "原始程式中存在 NUL 位元組。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソースプログラムにNULバイトがあります。\n";
        return "NUL byte in source program.\n";
    }
    if (strcmp(msg, "NUL in source module: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "NUL in source module: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "NUL en el módulo fuente: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "NUL dans le module source : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "NUL im Quellmodul: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "NUL nel modulo sorgente: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "NUL στη μονάδα πηγαίου κώδικα: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "NUL в исходном модуле: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "源模块中存在 NUL：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "原始碼模組中存在 NUL：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソースモジュールにNULがあります: %s";
        return "NUL in source module: %s";
    }
    if (strcmp(msg, "No Vulkan physical device is available.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No Vulkan physical device is available.";
        if (strncmp(lang_code, "es", 2) == 0) return "No hay ningún dispositivo físico Vulkan disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun périphérique physique Vulkan n’est disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein physisches Vulkan-Gerät ist verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è disponibile alcun dispositivo fisico Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει διαθέσιμη φυσική συσκευή Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нет доступных физических устройств Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "没有可用的 Vulkan 物理设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "沒有可用的 Vulkan 實體裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "利用可能なVulkan物理デバイスがありません。";
        return "No Vulkan physical device is available.";
    }
    if (strcmp(msg, "No accelerator backend could enumerate devices.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No accelerator backend could enumerate devices.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ningún backend de acelerador pudo enumerar dispositivos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun backend d’accélérateur n’a pu énumérer les périphériques.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein Beschleuniger-Backend konnte Geräte aufzählen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Nessun backend dell’acceleratore ha potuto enumerare i dispositivi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κανένα backend επιταχυντή δεν μπόρεσε να απαριθμήσει συσκευές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ни одному бэкенду ускорителя не удалось перечислить устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "没有加速器后端能够枚举设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "沒有加速器後端能夠列舉裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスを列挙できるアクセラレータバックエンドがありません。";
        return "No accelerator backend could enumerate devices.";
    }
    if (strcmp(msg, "No input module was specified.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No input module was specified.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se especificó ningún módulo de entrada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun module d’entrée n’a été spécifié.";
        if (strncmp(lang_code, "de", 2) == 0) return "Es wurde kein Eingabemodul angegeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è stato specificato alcun modulo di input.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν καθορίστηκε μονάδα εισόδου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Модуль ввода не указан.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未指定输入模块。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未指定輸入模組。";
        if (strncmp(lang_code, "ja", 2) == 0) return "入力モジュールが指定されていません。";
        return "No input module was specified.";
    }
    if (strcmp(msg, "No resolver is available for required module '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No resolver is available for required module '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "No hay ningún solucionador disponible para el módulo requerido '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun résolveur n’est disponible pour le module requis '%s'.";
        if (strncmp(lang_code, "de", 2) == 0) return "Für das erforderliche Modul '%s' ist kein Resolver verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è disponibile alcun risolutore per il modulo richiesto '%s'.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει διαθέσιμος επιλυτής για την απαιτούμενη μονάδα '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Для требуемого модуля '%s' нет доступного средства разрешения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所需模块 '%s' 没有可用的解析器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所需模組 '%s' 沒有可用的解析器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "必要なモジュール'%s'に使用できるリゾルバがありません。";
        return "No resolver is available for required module '%s'.";
    }
    if (strcmp(msg, "No suitable GPU device is available.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No suitable GPU device is available.";
        if (strncmp(lang_code, "es", 2) == 0) return "No hay ningún dispositivo GPU adecuado disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun périphérique GPU approprié n’est disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein geeignetes GPU-Gerät ist verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è disponibile alcun dispositivo GPU adatto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει διαθέσιμη κατάλληλη συσκευή GPU.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нет доступного подходящего устройства GPU.";
        if (strncmp(lang_code, "zh", 2) == 0) return "没有可用的合适 GPU 设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "沒有可用的合適 GPU 裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "利用可能な適切なGPUデバイスがありません。";
        return "No suitable GPU device is available.";
    }
    if (strcmp(msg, "No suitable GPU device is available.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No suitable GPU device is available.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "No hay ningún dispositivo GPU adecuado disponible.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun périphérique GPU approprié n’est disponible.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein geeignetes GPU-Gerät ist verfügbar.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è disponibile alcun dispositivo GPU adatto.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει διαθέσιμη κατάλληλη συσκευή GPU.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нет доступного подходящего устройства GPU.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "没有可用的合适 GPU 设备。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "沒有可用的合適 GPU 裝置。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "利用可能な適切なGPUデバイスがありません。\n";
        return "No suitable GPU device is available.\n";
    }
    if (strcmp(msg, "No suitable OpenGL ES 3.1 compute device is available.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No suitable OpenGL ES 3.1 compute device is available.";
        if (strncmp(lang_code, "es", 2) == 0) return "No hay ningún dispositivo de cómputo OpenGL ES 3.1 adecuado disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun périphérique de calcul OpenGL ES 3.1 approprié n’est disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein geeignetes OpenGL ES 3.1-Compute-Gerät ist verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è disponibile alcun dispositivo di calcolo OpenGL ES 3.1 adatto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει διαθέσιμη κατάλληλη συσκευή υπολογισμού OpenGL ES 3.1.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нет доступного подходящего вычислительного устройства OpenGL ES 3.1.";
        if (strncmp(lang_code, "zh", 2) == 0) return "没有可用的合适 OpenGL ES 3.1 计算设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "沒有可用的合適 OpenGL ES 3.1 計算裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "利用可能な適切なOpenGL ES 3.1コンピュートデバイスがありません。";
        return "No suitable OpenGL ES 3.1 compute device is available.";
    }
    if (strcmp(msg, "No suitable Vulkan 1.2 compute device is available.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "No suitable Vulkan 1.2 compute device is available.";
        if (strncmp(lang_code, "es", 2) == 0) return "No hay ningún dispositivo de cómputo Vulkan 1.2 adecuado disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Aucun périphérique de calcul Vulkan 1.2 approprié n’est disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein geeignetes Vulkan 1.2-Compute-Gerät ist verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è disponibile alcun dispositivo di calcolo Vulkan 1.2 adatto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει διαθέσιμη κατάλληλη συσκευή υπολογισμού Vulkan 1.2.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Нет доступного подходящего вычислительного устройства Vulkan 1.2.";
        if (strncmp(lang_code, "zh", 2) == 0) return "没有可用的合适 Vulkan 1.2 计算设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "沒有可用的合適 Vulkan 1.2 計算裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "利用可能な適切なVulkan 1.2コンピュートデバイスがありません。";
        return "No suitable Vulkan 1.2 compute device is available.";
    }
    if (strcmp(msg, "Noct Programming Language\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Noct Programming Language\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Lenguaje de programación Noct\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Langage de programmation Noct\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Programmiersprache Noct\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Linguaggio di programmazione Noct\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Γλώσσα προγραμματισμού Noct\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Язык программирования Noct\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "Noct 编程语言\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "Noct 程式語言\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "Noct プログラミング言語\n";
        return "Noct Programming Language\n";
    }
    if (strcmp(msg, "Nondeterministic accelerator kernel index.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Nondeterministic accelerator kernel index.";
        if (strncmp(lang_code, "es", 2) == 0) return "Índice de kernel del acelerador no determinista.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Index de kernel d’accélérateur non déterministe.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht deterministischer Index des Beschleuniger-Kernels.";
        if (strncmp(lang_code, "it", 2) == 0) return "Indice del kernel dell’acceleratore non deterministico.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη ντετερμινιστικός δείκτης πυρήνα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недетерминированный индекс ядра ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器内核索引不确定。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器核心索引不具確定性。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータカーネルのインデックスが非決定的です。";
        return "Nondeterministic accelerator kernel index.";
    }
    if (strcmp(msg, "Nondeterministic accelerator scalar binding.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Nondeterministic accelerator scalar binding.";
        if (strncmp(lang_code, "es", 2) == 0) return "Vinculación escalar del acelerador no determinista.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Liaison scalaire d’accélérateur non déterministe.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht deterministische Skalarbindung des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Associazione scalare dell’acceleratore non deterministica.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη ντετερμινιστική βαθμωτή δέσμευση επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недетерминированная скалярная привязка ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量绑定不确定。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量繫結不具確定性。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラーバインディングが非決定的です。";
        return "Nondeterministic accelerator scalar binding.";
    }
    if (strcmp(msg, "Nondeterministic accelerator scalar result.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Nondeterministic accelerator scalar result.";
        if (strncmp(lang_code, "es", 2) == 0) return "Resultado escalar del acelerador no determinista.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Résultat scalaire d’accélérateur non déterministe.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht deterministisches skalares Ergebnis des Beschleunigers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Risultato scalare dell’acceleratore non deterministico.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη ντετερμινιστικό βαθμωτό αποτέλεσμα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недетерминированный скалярный результат ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器标量结果不确定。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器純量結果不具確定性。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果が非決定的です。";
        return "Nondeterministic accelerator scalar result.";
    }
    if (strcmp(msg, "Not a dictionary.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Not a dictionary.";
        if (strncmp(lang_code, "es", 2) == 0) return "No es un diccionario.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Ce n'est pas un dictionnaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein Dictionary.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è un dizionario.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι λεξικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Это не словарь.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不是字典。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不是字典。";
        if (strncmp(lang_code, "ja", 2) == 0) return "辞書ではありません。";
        return "Not a dictionary.";
    }
    if (strcmp(msg, "Not a function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Not a function.";
        if (strncmp(lang_code, "es", 2) == 0) return "No es una función.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Ce n'est pas une fonction.";
        if (strncmp(lang_code, "de", 2) == 0) return "Keine Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è una funzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι συνάρτηση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Это не функция.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不是函数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不是函式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数ではありません。";
        return "Not a function.";
    }
    if (strcmp(msg, "Not a packed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Not a packed.";
        if (strncmp(lang_code, "es", 2) == 0) return "No es un packed.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Ce n'est pas un packed.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein Packed.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è un packed.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι packed.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Это не packed.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不是 packed。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不是 packed。";
        if (strncmp(lang_code, "ja", 2) == 0) return "packed ではありません。";
        return "Not a packed.";
    }
    if (strcmp(msg, "Not an array or a dictionary.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Not an array or a dictionary.";
        if (strncmp(lang_code, "es", 2) == 0) return "No es un array ni un diccionario.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Ce n'est ni un tableau ni un dictionnaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Weder ein Array noch ein Dictionary.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è né un array né un dizionario.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι ούτε πίνακας ούτε λεξικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Это не массив и не словарь.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不是数组或字典。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不是陣列或字典。";
        if (strncmp(lang_code, "ja", 2) == 0) return "配列でも辞書でもありません。";
        return "Not an array or a dictionary.";
    }
    if (strcmp(msg, "Not an array.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Not an array.";
        if (strncmp(lang_code, "es", 2) == 0) return "No es un array.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Ce n'est pas un tableau.";
        if (strncmp(lang_code, "de", 2) == 0) return "Kein Array.";
        if (strncmp(lang_code, "it", 2) == 0) return "Non è un array.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν είναι πίνακας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Это не массив.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不是数组。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不是陣列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "配列ではありません。";
        return "Not an array.";
    }
    if (strcmp(msg, "Offset is out-of-range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Offset is out-of-range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El desplazamiento está fuera de rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le décalage est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Versatz liegt außerhalb des Bereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’offset è fuori intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μετατόπιση είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Смещение вне диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "偏移量超出范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "位移量超出範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "オフセットが範囲外です。";
        return "Offset is out-of-range.";
    }
    if (strcmp(msg, "Only numeric literals are available inside __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Only numeric literals are available inside __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "Dentro de __fast func solo están disponibles los literales numéricos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Seuls les littéraux numériques sont disponibles dans __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Innerhalb von __fast func sind nur numerische Literale verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "All’interno di __fast func sono disponibili solo i letterali numerici.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μόνο αριθμητικά λεκτικά είναι διαθέσιμα μέσα σε __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Внутри __fast func доступны только числовые литералы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 内仅可使用数值字面量。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 內僅可使用數值常值。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func内では数値リテラルのみ使用できます。";
        return "Only numeric literals are available inside __fast func.";
    }
    if (strcmp(msg, "OpenGL ES active dispatch sequence is incomplete.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES active dispatch sequence is incomplete.";
        if (strncmp(lang_code, "es", 2) == 0) return "La secuencia de despachos activos de OpenGL ES está incompleta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La séquence de lancements actifs OpenGL ES est incomplète.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die aktive OpenGL ES-Dispatchsequenz ist unvollständig.";
        if (strncmp(lang_code, "it", 2) == 0) return "La sequenza di dispatch attivi OpenGL ES è incompleta.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ακολουθία ενεργών αποστολών OpenGL ES είναι ελλιπής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Последовательность активных диспетчеризаций OpenGL ES неполна.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 活动调度序列不完整。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 使用中調度序列不完整。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESのアクティブなディスパッチシーケンスが不完全です。";
        return "OpenGL ES active dispatch sequence is incomplete.";
    }
    if (strcmp(msg, "OpenGL ES buffer exceeds the device storage-block limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES buffer exceeds the device storage-block limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer de OpenGL ES supera el límite de bloques de almacenamiento del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon OpenGL ES dépasse la limite de blocs de stockage du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Puffer überschreitet die Speicherblockgrenze des Geräts.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer OpenGL ES supera il limite dei blocchi di archiviazione del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη OpenGL ES υπερβαίνει το όριο μπλοκ αποθήκευσης της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер OpenGL ES превышает предел блока хранилища устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 缓冲区超过设备存储块限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 緩衝區超過裝置儲存區塊限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファがデバイスのストレージブロック上限を超えています。";
        return "OpenGL ES buffer exceeds the device storage-block limit.";
    }
    if (strcmp(msg, "OpenGL ES buffer extent metadata is inconsistent.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES buffer extent metadata is inconsistent.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los metadatos de extensión del búfer de OpenGL ES son incoherentes.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées d’étendue du tampon OpenGL ES sont incohérentes.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausdehnungsmetadaten des OpenGL ES-Puffers sind inkonsistent.";
        if (strncmp(lang_code, "it", 2) == 0) return "I metadati dell’estensione del buffer OpenGL ES non sono coerenti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα μεταδεδομένα έκτασης της ενδιάμεσης μνήμης OpenGL ES δεν είναι συνεπή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Метаданные размера измерения буфера OpenGL ES несогласованы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 缓冲区维度大小元数据不一致。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 緩衝區維度大小中繼資料不一致。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファの次元サイズメタデータが一貫していません。";
        return "OpenGL ES buffer extent metadata is inconsistent.";
    }
    if (strcmp(msg, "OpenGL ES buffer metadata does not match the prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES buffer metadata does not match the prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los metadatos del búfer de OpenGL ES no coinciden con el programa preparado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées du tampon OpenGL ES ne correspondent pas au programme préparé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Metadaten des OpenGL ES-Puffers stimmen nicht mit dem vorbereiteten Programm überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "I metadati del buffer OpenGL ES non corrispondono al programma preparato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα μεταδεδομένα της ενδιάμεσης μνήμης OpenGL ES δεν αντιστοιχούν στο προετοιμασμένο πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Метаданные буфера OpenGL ES не соответствуют подготовленной программе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 缓冲区元数据与已准备的程序不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 緩衝區中繼資料與已準備的程式不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファのメタデータが準備済みのプログラムと一致しません。";
        return "OpenGL ES buffer metadata does not match the prepared program.";
    }
    if (strcmp(msg, "OpenGL ES buffer size is not representable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES buffer size is not representable.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del búfer de OpenGL ES no se puede representar.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du tampon OpenGL ES n’est pas représentable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe des OpenGL ES-Puffers ist nicht darstellbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del buffer OpenGL ES non è rappresentabile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος της ενδιάμεσης μνήμης OpenGL ES δεν μπορεί να αναπαρασταθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер буфера OpenGL ES невозможно представить.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 缓冲区大小无法表示。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 緩衝區大小無法表示。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファのサイズを表現できません。";
        return "OpenGL ES buffer size is not representable.";
    }
    if (strcmp(msg, "OpenGL ES buffer table does not match the prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES buffer table does not match the prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de búferes de OpenGL ES no coincide con el programa preparado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des tampons OpenGL ES ne correspond pas au programme préparé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Puffertabelle stimmt nicht mit dem vorbereiteten Programm überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei buffer OpenGL ES non corrisponde al programma preparato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας ενδιάμεσων μνημών OpenGL ES δεν αντιστοιχεί στο προετοιμασμένο πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица буферов OpenGL ES не соответствует подготовленной программе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 缓冲区表与已准备的程序不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 緩衝區表與已準備的程式不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファテーブルが準備済みのプログラムと一致しません。";
        return "OpenGL ES buffer table does not match the prepared program.";
    }
    if (strcmp(msg, "OpenGL ES compute shader source is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES compute shader source is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El código fuente del shader de cómputo OpenGL ES es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le code source du shader de calcul OpenGL ES est trop volumineux.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Quellcode des OpenGL ES-Compute-Shaders ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il codice sorgente del compute shader OpenGL ES è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πηγαίος κώδικας του compute shader OpenGL ES είναι πολύ μεγάλος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Исходный код вычислительного шейдера OpenGL ES слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 计算着色器源代码过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 計算著色器原始碼過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESコンピュートシェーダーのソースコードが大きすぎます。";
        return "OpenGL ES compute shader source is too large.";
    }
    if (strcmp(msg, "OpenGL ES dispatch exceeds the device workgroup-count limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES dispatch exceeds the device workgroup-count limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El despacho de OpenGL ES supera el límite de cantidad de grupos de trabajo del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le lancement OpenGL ES dépasse la limite du nombre de groupes de travail du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Dispatch überschreitet die Arbeitsgruppenanzahlgrenze des Geräts.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispatch OpenGL ES supera il limite del numero di gruppi di lavoro del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αποστολή OpenGL ES υπερβαίνει το όριο πλήθους ομάδων εργασίας της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диспетчеризация OpenGL ES превышает предел количества рабочих групп устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 调度超过设备工作组数量限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 調度超過裝置工作群組數量限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESディスパッチがデバイスのワークグループ数上限を超えています。";
        return "OpenGL ES dispatch exceeds the device workgroup-count limit.";
    }
    if (strcmp(msg, "OpenGL ES dispatch range changed after execution creation.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES dispatch range changed after execution creation.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango de despacho de OpenGL ES cambió tras crear la ejecución.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage de lancement OpenGL ES a changé après la création de l’exécution.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Dispatchbereich wurde nach Erstellung der Ausführung geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo di dispatch OpenGL ES è cambiato dopo la creazione dell’esecuzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αποστολής OpenGL ES άλλαξε μετά τη δημιουργία της εκτέλεσης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон диспетчеризации OpenGL ES изменился после создания выполнения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 调度范围在创建执行后发生了变化。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 調度範圍在建立執行後發生變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESディスパッチ範囲が実行の作成後に変更されました。";
        return "OpenGL ES dispatch range changed after execution creation.";
    }
    if (strcmp(msg, "OpenGL ES dispatch range is missing from the scalar block.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES dispatch range is missing from the scalar block.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el rango de despacho de OpenGL ES en el bloque escalar.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage de lancement OpenGL ES est absente du bloc scalaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Dispatchbereich fehlt im Skalarblock.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo di dispatch OpenGL ES manca nel blocco scalare.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αποστολής OpenGL ES λείπει από το βαθμωτό μπλοκ.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон диспетчеризации OpenGL ES отсутствует в скалярном блоке.";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量块中缺少 OpenGL ES 调度范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量區塊中缺少 OpenGL ES 調度範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラーブロックにOpenGL ESディスパッチ範囲がありません。";
        return "OpenGL ES dispatch range is missing from the scalar block.";
    }
    if (strcmp(msg, "OpenGL ES execution has no active dispatch.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES execution has no active dispatch.";
        if (strncmp(lang_code, "es", 2) == 0) return "La ejecución de OpenGL ES no tiene ningún despacho activo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’exécution OpenGL ES n’a aucun lancement actif.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Ausführung hat keinen aktiven Dispatch.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’esecuzione OpenGL ES non ha alcun dispatch attivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η εκτέλεση OpenGL ES δεν έχει ενεργή αποστολή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выполнение OpenGL ES не имеет активной диспетчеризации.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 执行没有活动调度。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 執行沒有使用中調度。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES実行にアクティブなディスパッチがありません。";
        return "OpenGL ES execution has no active dispatch.";
    }
    if (strcmp(msg, "OpenGL ES execution was already finished.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES execution was already finished.";
        if (strncmp(lang_code, "es", 2) == 0) return "La ejecución de OpenGL ES ya finalizó.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’exécution OpenGL ES est déjà terminée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Ausführung wurde bereits abgeschlossen.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’esecuzione OpenGL ES è già terminata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η εκτέλεση OpenGL ES έχει ήδη ολοκληρωθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выполнение OpenGL ES уже завершено.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 执行已完成。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 執行已完成。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES実行はすでに完了しています。";
        return "OpenGL ES execution was already finished.";
    }
    if (strcmp(msg, "OpenGL ES finish buffer table changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES finish buffer table changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de búferes de finalización de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des tampons de fin OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Abschlusspuffertabelle wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei buffer di completamento OpenGL ES è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας ενδιάμεσων μνημών ολοκλήρωσης OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица буферов завершения OpenGL ES изменилась.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 完成缓冲区表已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 完成緩衝區表已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES完了バッファテーブルが変更されました。";
        return "OpenGL ES finish buffer table changed.";
    }
    if (strcmp(msg, "OpenGL ES finish result table changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES finish result table changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de resultados de finalización de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des résultats de fin OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Abschlussergebnistabelle wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei risultati di completamento OpenGL ES è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας αποτελεσμάτων ολοκλήρωσης OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица результатов завершения OpenGL ES изменилась.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 完成结果表已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 完成結果表已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES完了結果テーブルが変更されました。";
        return "OpenGL ES finish result table changed.";
    }
    if (strcmp(msg, "OpenGL ES kernels were dispatched out of order.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES kernels were dispatched out of order.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los kernels de OpenGL ES se despacharon fuera de orden.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les kernels OpenGL ES ont été lancés dans le désordre.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Kernel wurden in falscher Reihenfolge dispatched.";
        if (strncmp(lang_code, "it", 2) == 0) return "I kernel OpenGL ES sono stati sottoposti a dispatch fuori ordine.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι πυρήνες OpenGL ES απεστάλησαν εκτός σειράς.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ядра OpenGL ES были диспетчеризованы не по порядку.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 内核未按顺序调度。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 核心未依序調度。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESカーネルが順序どおりにディスパッチされませんでした。";
        return "OpenGL ES kernels were dispatched out of order.";
    }
    if (strcmp(msg, "OpenGL ES output buffer activity changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer activity changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La actividad del búfer de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’activité du tampon de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Aktivität des OpenGL ES-Ausgabepuffers wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’attività del buffer di output OpenGL ES è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η δραστηριότητα της ενδιάμεσης μνήμης εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Активность буфера вывода OpenGL ES изменилась.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区活动状态已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區活動狀態已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファのアクティビティが変更されました。";
        return "OpenGL ES output buffer activity changed.";
    }
    if (strcmp(msg, "OpenGL ES output buffer became invalid while unmapping.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer became invalid while unmapping.";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer de salida de OpenGL ES dejó de ser válido durante el desmapeo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon de sortie OpenGL ES est devenu non valide pendant la suppression du mappage.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Ausgabepuffer wurde beim Aufheben der Zuordnung ungültig.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer di output OpenGL ES è diventato non valido durante l’annullamento della mappatura.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη εξόδου OpenGL ES έγινε μη έγκυρη κατά την κατάργηση αντιστοίχισης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер вывода OpenGL ES стал недействительным при отмене отображения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区在取消映射时失效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區在取消對應時失效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファがマッピング解除中に無効になりました。";
        return "OpenGL ES output buffer became invalid while unmapping.";
    }
    if (strcmp(msg, "OpenGL ES output buffer element type changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer element type changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tipo de elemento del búfer de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le type d’élément du tampon de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Elementtyp des OpenGL ES-Ausgabepuffers wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il tipo di elemento del buffer di output OpenGL ES è cambiato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τύπος στοιχείου της ενδιάμεσης μνήμης εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Тип элемента буфера вывода OpenGL ES изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区元素类型已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區元素型別已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファの要素型が変更されました。";
        return "OpenGL ES output buffer element type changed.";
    }
    if (strcmp(msg, "OpenGL ES output buffer element width changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer element width changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El ancho de elemento del búfer de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La largeur d’élément du tampon de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Elementbreite des OpenGL ES-Ausgabepuffers wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La larghezza degli elementi del buffer di output OpenGL ES è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πλάτος στοιχείου της ενδιάμεσης μνήμης εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ширина элемента буфера вывода OpenGL ES изменилась.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区元素宽度已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區元素寬度已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファの要素幅が変更されました。";
        return "OpenGL ES output buffer element width changed.";
    }
    if (strcmp(msg, "OpenGL ES output buffer extent changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer extent changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La extensión del búfer de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’étendue du tampon de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausdehnung des OpenGL ES-Ausgabepuffers wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’estensione del buffer di output OpenGL ES è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έκταση της ενδιάμεσης μνήμης εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер измерения буфера вывода OpenGL ES изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区维度大小已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區維度大小已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファの次元サイズが変更されました。";
        return "OpenGL ES output buffer extent changed.";
    }
    if (strcmp(msg, "OpenGL ES output buffer origin changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer origin changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El origen del búfer de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’origine du tampon de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Ursprung des OpenGL ES-Ausgabepuffers wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’origine del buffer di output OpenGL ES è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αρχή της ενδιάμεσης μνήμης εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Начало буфера вывода OpenGL ES изменилось.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区起点已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區起點已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファの原点が変更されました。";
        return "OpenGL ES output buffer origin changed.";
    }
    if (strcmp(msg, "OpenGL ES output buffer size changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer size changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del búfer de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du tampon de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Größe des OpenGL ES-Ausgabepuffers wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del buffer di output OpenGL ES è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος της ενδιάμεσης μνήμης εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер буфера вывода OpenGL ES изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区大小已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區大小已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファのサイズが変更されました。";
        return "OpenGL ES output buffer size changed.";
    }
    if (strcmp(msg, "OpenGL ES output buffer slot changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output buffer slot changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La ranura del búfer de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’emplacement du tampon de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Slot des OpenGL ES-Ausgabepuffers wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Lo slot del buffer di output OpenGL ES è cambiato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η θέση της ενδιάμεσης μνήμης εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слот буфера вывода OpenGL ES изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出缓冲区槽位已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出緩衝區槽位已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力バッファのスロットが変更されました。";
        return "OpenGL ES output buffer slot changed.";
    }
    if (strcmp(msg, "OpenGL ES output size is not representable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output size is not representable.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño de salida de OpenGL ES no se puede representar.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille de sortie OpenGL ES n’est pas représentable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Ausgabegröße ist nicht darstellbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione dell’output OpenGL ES non è rappresentabile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος εξόδου OpenGL ES δεν μπορεί να αναπαρασταθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер вывода OpenGL ES невозможно представить.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出大小无法表示。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出大小無法表示。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力のサイズを表現できません。";
        return "OpenGL ES output size is not representable.";
    }
    if (strcmp(msg, "OpenGL ES output transfer plan changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output transfer plan changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El plan de transferencia de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le plan de transfert de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Ausgabeübertragungsplan wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il piano di trasferimento dell’output OpenGL ES è cambiato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πλάνο μεταφοράς εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "План передачи вывода OpenGL ES изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出传输计划已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出傳輸計畫已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力の転送プランが変更されました。";
        return "OpenGL ES output transfer plan changed.";
    }
    if (strcmp(msg, "OpenGL ES output upload plan changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES output upload plan changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El plan de carga de salida de OpenGL ES cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le plan de téléversement de sortie OpenGL ES a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Ausgabeuploadplan wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il piano di upload dell’output OpenGL ES è cambiato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πλάνο αποστολής εξόδου OpenGL ES άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "План загрузки вывода OpenGL ES изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 输出上传计划已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 輸出上傳計畫已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES出力のアップロードプランが変更されました。";
        return "OpenGL ES output upload plan changed.";
    }
    if (strcmp(msg, "OpenGL ES received an empty active dispatch.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES received an empty active dispatch.";
        if (strncmp(lang_code, "es", 2) == 0) return "OpenGL ES recibió un despacho activo vacío.";
        if (strncmp(lang_code, "fr", 2) == 0) return "OpenGL ES a reçu un lancement actif vide.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES hat einen leeren aktiven Dispatch empfangen.";
        if (strncmp(lang_code, "it", 2) == 0) return "OpenGL ES ha ricevuto un dispatch attivo vuoto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το OpenGL ES έλαβε κενή ενεργή αποστολή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "OpenGL ES получил пустую активную диспетчеризацию.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 收到空的活动调度。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 收到空的使用中調度。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESが空のアクティブなディスパッチを受信しました。";
        return "OpenGL ES received an empty active dispatch.";
    }
    if (strcmp(msg, "OpenGL ES received too many active dispatches.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES received too many active dispatches.";
        if (strncmp(lang_code, "es", 2) == 0) return "OpenGL ES recibió demasiados despachos activos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "OpenGL ES a reçu trop de lancements actifs.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES hat zu viele aktive Dispatches empfangen.";
        if (strncmp(lang_code, "it", 2) == 0) return "OpenGL ES ha ricevuto troppi dispatch attivi.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το OpenGL ES έλαβε υπερβολικά πολλές ενεργές αποστολές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "OpenGL ES получил слишком много активных диспетчеризаций.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 收到过多活动调度。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 收到過多使用中調度。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESが受信したアクティブなディスパッチが多すぎます。";
        return "OpenGL ES received too many active dispatches.";
    }
    if (strcmp(msg, "OpenGL ES reported an error while preparing a compute program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES reported an error while preparing a compute program.";
        if (strncmp(lang_code, "es", 2) == 0) return "OpenGL ES notificó un error al preparar un programa de cómputo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "OpenGL ES a signalé une erreur pendant la préparation d’un programme de calcul.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES hat beim Vorbereiten eines Compute-Programms einen Fehler gemeldet.";
        if (strncmp(lang_code, "it", 2) == 0) return "OpenGL ES ha segnalato un errore durante la preparazione di un programma di calcolo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το OpenGL ES ανέφερε σφάλμα κατά την προετοιμασία προγράμματος υπολογισμού.";
        if (strncmp(lang_code, "ru", 2) == 0) return "OpenGL ES сообщил об ошибке при подготовке вычислительной программы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 在准备计算程序时报告错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 在準備計算程式時回報錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESがコンピュートプログラムの準備中にエラーを報告しました。";
        return "OpenGL ES reported an error while preparing a compute program.";
    }
    if (strcmp(msg, "OpenGL ES result block does not match the prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES result block does not match the prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque de resultados de OpenGL ES no coincide con el programa preparado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc de résultats OpenGL ES ne correspond pas au programme préparé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Ergebnisblock stimmt nicht mit dem vorbereiteten Programm überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco dei risultati OpenGL ES non corrisponde al programma preparato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μπλοκ αποτελεσμάτων OpenGL ES δεν αντιστοιχεί στο προετοιμασμένο πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Блок результатов OpenGL ES не соответствует подготовленной программе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 结果块与已准备的程序不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 結果區塊與已準備的程式不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果ブロックが準備済みのプログラムと一致しません。";
        return "OpenGL ES result block does not match the prepared program.";
    }
    if (strcmp(msg, "OpenGL ES result block exceeds the device storage-block limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES result block exceeds the device storage-block limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque de resultados de OpenGL ES supera el límite de bloques de almacenamiento del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc de résultats OpenGL ES dépasse la limite de blocs de stockage du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Ergebnisblock überschreitet die Speicherblockgrenze des Geräts.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco dei risultati OpenGL ES supera il limite dei blocchi di archiviazione del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μπλοκ αποτελεσμάτων OpenGL ES υπερβαίνει το όριο μπλοκ αποθήκευσης της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Блок результатов OpenGL ES превышает предел блока хранилища устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 结果块超过设备存储块限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 結果區塊超過裝置儲存區塊限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果ブロックがデバイスのストレージブロック上限を超えています。";
        return "OpenGL ES result block exceeds the device storage-block limit.";
    }
    if (strcmp(msg, "OpenGL ES result block size overflowed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES result block size overflowed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del bloque de resultados de OpenGL ES se desbordó.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du bloc de résultats OpenGL ES a dépassé la capacité.";
        if (strncmp(lang_code, "de", 2) == 0) return "Bei der Größe des OpenGL ES-Ergebnisblocks ist ein Überlauf aufgetreten.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del blocco dei risultati OpenGL ES ha causato un overflow.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος του μπλοκ αποτελεσμάτων OpenGL ES υπερχείλισε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение размера блока результатов OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 结果块大小溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 結果區塊大小溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果ブロックのサイズがオーバーフローしました。";
        return "OpenGL ES result block size overflowed.";
    }
    if (strcmp(msg, "OpenGL ES result buffer became invalid while unmapping.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES result buffer became invalid while unmapping.";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer de resultados de OpenGL ES dejó de ser válido durante el desmapeo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon de résultats OpenGL ES est devenu non valide pendant la suppression du mappage.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Ergebnispuffer wurde beim Aufheben der Zuordnung ungültig.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer dei risultati OpenGL ES è diventato non valido durante l’annullamento della mappatura.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη αποτελεσμάτων OpenGL ES έγινε μη έγκυρη κατά την κατάργηση αντιστοίχισης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер результатов OpenGL ES стал недействительным при отмене отображения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 结果缓冲区在取消映射时失效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 結果緩衝區在取消對應時失效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果バッファがマッピング解除中に無効になりました。";
        return "OpenGL ES result buffer became invalid while unmapping.";
    }
    if (strcmp(msg, "OpenGL ES result size is not representable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES result size is not representable.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del resultado de OpenGL ES no se puede representar.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du résultat OpenGL ES n’est pas représentable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die OpenGL ES-Ergebnisgröße ist nicht darstellbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del risultato OpenGL ES non è rappresentabile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος αποτελέσματος OpenGL ES δεν μπορεί να αναπαρασταθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер результата OpenGL ES невозможно представить.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 结果大小无法表示。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 結果大小無法表示。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES結果のサイズを表現できません。";
        return "OpenGL ES result size is not representable.";
    }
    if (strcmp(msg, "OpenGL ES scalar block does not match the prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES scalar block does not match the prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque escalar de OpenGL ES no coincide con el programa preparado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc scalaire OpenGL ES ne correspond pas au programme préparé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Skalarblock stimmt nicht mit dem vorbereiteten Programm überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco scalare OpenGL ES non corrisponde al programma preparato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το βαθμωτό μπλοκ OpenGL ES δεν αντιστοιχεί στο προετοιμασμένο πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Скалярный блок OpenGL ES не соответствует подготовленной программе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 标量块与已准备的程序不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 純量區塊與已準備的程式不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESスカラーブロックが準備済みのプログラムと一致しません。";
        return "OpenGL ES scalar block does not match the prepared program.";
    }
    if (strcmp(msg, "OpenGL ES scalar block exceeds the device storage-block limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES scalar block exceeds the device storage-block limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque escalar de OpenGL ES supera el límite de bloques de almacenamiento del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc scalaire OpenGL ES dépasse la limite de blocs de stockage du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der OpenGL ES-Skalarblock überschreitet die Speicherblockgrenze des Geräts.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco scalare OpenGL ES supera il limite dei blocchi di archiviazione del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το βαθμωτό μπλοκ OpenGL ES υπερβαίνει το όριο μπλοκ αποθήκευσης της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Скалярный блок OpenGL ES превышает предел блока хранилища устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 标量块超过设备存储块限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 純量區塊超過裝置儲存區塊限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESスカラーブロックがデバイスのストレージブロック上限を超えています。";
        return "OpenGL ES scalar block exceeds the device storage-block limit.";
    }
    if (strcmp(msg, "OpenGL ES scalar block size overflowed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES scalar block size overflowed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del bloque escalar de OpenGL ES se desbordó.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du bloc scalaire OpenGL ES a dépassé la capacité.";
        if (strncmp(lang_code, "de", 2) == 0) return "Bei der Größe des OpenGL ES-Skalarblocks ist ein Überlauf aufgetreten.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del blocco scalare OpenGL ES ha causato un overflow.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος του βαθμωτού μπλοκ OpenGL ES υπερχείλισε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Произошло переполнение размера скалярного блока OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 标量块大小溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 純量區塊大小溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESスカラーブロックのサイズがオーバーフローしました。";
        return "OpenGL ES scalar block size overflowed.";
    }
    if (strcmp(msg, "OpenGL ES supports only 32-bit accelerator buffers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "OpenGL ES supports only 32-bit accelerator buffers.";
        if (strncmp(lang_code, "es", 2) == 0) return "OpenGL ES solo admite búferes de acelerador de 32 bits.";
        if (strncmp(lang_code, "fr", 2) == 0) return "OpenGL ES ne prend en charge que les tampons d’accélérateur 32 bits.";
        if (strncmp(lang_code, "de", 2) == 0) return "OpenGL ES unterstützt nur 32-Bit-Beschleunigerpuffer.";
        if (strncmp(lang_code, "it", 2) == 0) return "OpenGL ES supporta solo buffer dell’acceleratore a 32 bit.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το OpenGL ES υποστηρίζει μόνο ενδιάμεσες μνήμες επιταχυντή 32 bit.";
        if (strncmp(lang_code, "ru", 2) == 0) return "OpenGL ES поддерживает только 32-битные буферы ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "OpenGL ES 仅支持 32 位加速器缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "OpenGL ES 僅支援 32 位元加速器緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESは32ビットのアクセラレータバッファのみをサポートします。";
        return "OpenGL ES supports only 32-bit accelerator buffers.";
    }
    if (strcmp(msg, "Out of memory building module graph.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory building module graph.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear el grafo de módulos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la construction du graphe des modules.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen des Modulgraphen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione del grafo dei moduli.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία του γράφου μονάδων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при построении графа модулей.";
        if (strncmp(lang_code, "zh", 2) == 0) return "构建模块图时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立模組圖時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュールグラフの構築中にメモリ不足になりました。";
        return "Out of memory building module graph.";
    }
    if (strcmp(msg, "Out of memory inspecting application.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory inspecting application.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al inspeccionar la aplicación.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’inspection de l’application.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Untersuchen der Anwendung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante l’ispezione dell’applicazione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την επιθεώρηση της εφαρμογής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при проверке приложения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "检查应用程序时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檢查應用程式時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アプリケーションの検査中にメモリ不足になりました。";
        return "Out of memory inspecting application.";
    }
    if (strcmp(msg, "Out of memory reading module %s.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory reading module %s.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al leer el módulo %s.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la lecture du module %s.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Lesen des Moduls %s.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la lettura del modulo %s.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την ανάγνωση της μονάδας %s.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при чтении модуля %s.";
        if (strncmp(lang_code, "zh", 2) == 0) return "读取模块 %s 时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "讀取模組 %s 時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュール%sの読み取り中にメモリ不足になりました。";
        return "Out of memory reading module %s.";
    }
    if (strcmp(msg, "Out of memory validating module graph.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory validating module graph.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al validar el grafo de módulos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la validation du graphe des modules.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Validieren des Modulgraphen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la convalida del grafo dei moduli.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την επικύρωση του γράφου μονάδων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при проверке графа модулей.";
        if (strncmp(lang_code, "zh", 2) == 0) return "验证模块图时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "驗證模組圖時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュールグラフの検証中にメモリ不足になりました。";
        return "Out of memory validating module graph.";
    }
    if (strcmp(msg, "Out of memory while allocating accelerator kernel.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while allocating accelerator kernel.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al asignar el kernel del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’allocation du kernel d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Zuweisen des Beschleuniger-Kernels.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante l’allocazione del kernel dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την εκχώρηση του πυρήνα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при выделении ядра ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "分配加速器内核时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "配置加速器核心時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータカーネルの割り当て中にメモリ不足になりました。";
        return "Out of memory while allocating accelerator kernel.";
    }
    if (strcmp(msg, "Out of memory while building __fast signature.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while building __fast signature.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear la firma de __fast.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la construction de la signature __fast.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen der __fast-Signatur.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione della firma __fast.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία της υπογραφής __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при построении сигнатуры __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "构建 __fast 签名时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 __fast 簽章時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastシグネチャの構築中にメモリ不足になりました。";
        return "Out of memory while building __fast signature.";
    }
    if (strcmp(msg, "Out of memory while building accelerator IR.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while building accelerator IR.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear el IR del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la construction de l’IR d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen der Beschleuniger-IR.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione dell’IR dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία του IR επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при построении IR ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "构建加速器 IR 时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立加速器 IR 時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータIRの構築中にメモリ不足になりました。";
        return "Out of memory while building accelerator IR.";
    }
    if (strcmp(msg, "Out of memory while copying OpenGL ES scalar metadata.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while copying OpenGL ES scalar metadata.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al copiar los metadatos escalares de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la copie des métadonnées scalaires OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Kopieren der OpenGL ES-Skalarmetadaten.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la copia dei metadati scalari OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την αντιγραφή των βαθμωτών μεταδεδομένων OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при копировании скалярных метаданных OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "复制 OpenGL ES 标量元数据时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "複製 OpenGL ES 純量中繼資料時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESスカラーメタデータのコピー中にメモリ不足になりました。";
        return "Out of memory while copying OpenGL ES scalar metadata.";
    }
    if (strcmp(msg, "Out of memory while creating OpenGL ES buffers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while creating OpenGL ES buffers.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear los búferes de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la création des tampons OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen der OpenGL ES-Puffer.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione dei buffer OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία των ενδιάμεσων μνημών OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при создании буферов OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "创建 OpenGL ES 缓冲区时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 OpenGL ES 緩衝區時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESバッファの作成中にメモリ不足になりました。";
        return "Out of memory while creating OpenGL ES buffers.";
    }
    if (strcmp(msg, "Out of memory while creating Vulkan buffers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while creating Vulkan buffers.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear los búferes de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la création des tampons Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen der Vulkan-Puffer.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione dei buffer Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία των ενδιάμεσων μνημών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при создании буферов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "创建 Vulkan 缓冲区时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 Vulkan 緩衝區時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanバッファの作成中にメモリ不足になりました。";
        return "Out of memory while creating Vulkan buffers.";
    }
    if (strcmp(msg, "Out of memory while creating Vulkan descriptor sets.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while creating Vulkan descriptor sets.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear los conjuntos de descriptores de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la création des ensembles de descripteurs Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen der Vulkan-Deskriptorsätze.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione dei set di descrittori Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία των συνόλων περιγραφέων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при создании наборов дескрипторов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "创建 Vulkan 描述符集时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 Vulkan 描述元集時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスクリプタセットの作成中にメモリ不足になりました。";
        return "Out of memory while creating Vulkan descriptor sets.";
    }
    if (strcmp(msg, "Out of memory while creating a Vulkan execution.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while creating a Vulkan execution.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear una ejecución de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la création d’une exécution Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen einer Vulkan-Ausführung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione di un’esecuzione Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία εκτέλεσης Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при создании выполнения Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "创建 Vulkan 执行时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 Vulkan 執行時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan実行の作成中にメモリ不足になりました。";
        return "Out of memory while creating a Vulkan execution.";
    }
    if (strcmp(msg, "Out of memory while creating an OpenGL ES execution.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while creating an OpenGL ES execution.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al crear una ejecución de OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la création d’une exécution OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Erstellen einer OpenGL ES-Ausführung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la creazione di un’esecuzione OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τη δημιουργία εκτέλεσης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при создании выполнения OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "创建 OpenGL ES 执行时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 OpenGL ES 執行時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ES実行の作成中にメモリ不足になりました。";
        return "Out of memory while creating an OpenGL ES execution.";
    }
    if (strcmp(msg, "Out of memory while enumerating OpenGL ES devices.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while enumerating OpenGL ES devices.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al enumerar los dispositivos OpenGL ES.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’énumération des périphériques OpenGL ES.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzählen der OpenGL ES-Geräte.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante l’enumerazione dei dispositivi OpenGL ES.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την απαρίθμηση συσκευών OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при перечислении устройств OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "枚举 OpenGL ES 设备时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "列舉 OpenGL ES 裝置時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "OpenGL ESデバイスの列挙中にメモリ不足になりました。";
        return "Out of memory while enumerating OpenGL ES devices.";
    }
    if (strcmp(msg, "Out of memory while enumerating Vulkan devices.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while enumerating Vulkan devices.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al enumerar los dispositivos Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’énumération des périphériques Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzählen der Vulkan-Geräte.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante l’enumerazione dei dispositivi Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την απαρίθμηση συσκευών Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при перечислении устройств Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "枚举 Vulkan 设备时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "列舉 Vulkan 裝置時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanデバイスの列挙中にメモリ不足になりました。";
        return "Out of memory while enumerating Vulkan devices.";
    }
    if (strcmp(msg, "Out of memory while planning accelerator function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while planning accelerator function.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al planificar la función del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la planification de la fonction d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Planen der Beschleunigerfunktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la pianificazione della funzione dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τον σχεδιασμό της συνάρτησης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при планировании функции ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "规划加速器函数时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "規劃加速器函式時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ関数の計画中にメモリ不足になりました。";
        return "Out of memory while planning accelerator function.";
    }
    if (strcmp(msg, "Out of memory while planning accelerator region.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while planning accelerator region.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al planificar la región del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de la planification de la région d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Planen der Beschleunigerregion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la pianificazione della regione dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά τον σχεδιασμό της περιοχής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при планировании области ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "规划加速器区域时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "規劃加速器區域時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリージョンの計画中にメモリ不足になりました。";
        return "Out of memory while planning accelerator region.";
    }
    if (strcmp(msg, "Out of memory while recording accelerator buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while recording accelerator buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al registrar el búfer del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’enregistrement du tampon d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzeichnen des Beschleunigerpuffers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la registrazione del buffer dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την καταγραφή της ενδιάμεσης μνήμης επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при записи буфера ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "记录加速器缓冲区时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記錄加速器緩衝區時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータバッファの記録中にメモリ不足になりました。";
        return "Out of memory while recording accelerator buffer.";
    }
    if (strcmp(msg, "Out of memory while recording accelerator kernel.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while recording accelerator kernel.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al registrar el kernel del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’enregistrement du kernel d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzeichnen des Beschleuniger-Kernels.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la registrazione del kernel dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την καταγραφή του πυρήνα επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при записи ядра ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "记录加速器内核时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記錄加速器核心時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータカーネルの記録中にメモリ不足になりました。";
        return "Out of memory while recording accelerator kernel.";
    }
    if (strcmp(msg, "Out of memory while recording accelerator range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while recording accelerator range.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al registrar el rango del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’enregistrement de la plage d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzeichnen des Beschleunigerbereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la registrazione dell’intervallo dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την καταγραφή του εύρους επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при записи диапазона ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "记录加速器范围时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記錄加速器範圍時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータ範囲の記録中にメモリ不足になりました。";
        return "Out of memory while recording accelerator range.";
    }
    if (strcmp(msg, "Out of memory while recording accelerator region.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while recording accelerator region.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al registrar la región del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’enregistrement de la région d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzeichnen der Beschleunigerregion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la registrazione della regione dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την καταγραφή της περιοχής επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при записи области ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "记录加速器区域时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記錄加速器區域時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータリージョンの記録中にメモリ不足になりました。";
        return "Out of memory while recording accelerator region.";
    }
    if (strcmp(msg, "Out of memory while recording accelerator scalar result.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while recording accelerator scalar result.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al registrar el resultado escalar del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’enregistrement du résultat scalaire d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzeichnen des skalaren Beschleunigerergebnisses.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la registrazione del risultato scalare dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την καταγραφή του βαθμωτού αποτελέσματος επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при записи скалярного результата ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "记录加速器标量结果时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記錄加速器純量結果時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのスカラー結果の記録中にメモリ不足になりました。";
        return "Out of memory while recording accelerator scalar result.";
    }
    if (strcmp(msg, "Out of memory while recording accelerator scalar.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while recording accelerator scalar.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al registrar el escalar del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’enregistrement du scalaire d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Aufzeichnen des Beschleunigerskalars.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la registrazione dello scalare dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την καταγραφή του βαθμωτού επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при записи скаляра ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "记录加速器标量时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記錄加速器純量時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータスカラーの記録中にメモリ不足になりました。";
        return "Out of memory while recording accelerator scalar.";
    }
    if (strcmp(msg, "Out of memory while storing Vulkan descriptor sets.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while storing Vulkan descriptor sets.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al almacenar los conjuntos de descriptores de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors du stockage des ensembles de descripteurs Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Speichern der Vulkan-Deskriptorsätze.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante l’archiviazione dei set di descrittori Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την αποθήκευση των συνόλων περιγραφέων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при сохранении наборов дескрипторов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "存储 Vulkan 描述符集时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "儲存 Vulkan 描述元集時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスクリプタセットの保存中にメモリ不足になりました。";
        return "Out of memory while storing Vulkan descriptor sets.";
    }
    if (strcmp(msg, "Out of memory while writing Vulkan descriptors.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory while writing Vulkan descriptors.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente al escribir los descriptores de Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante lors de l’écriture des descripteurs Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher beim Schreiben der Vulkan-Deskriptoren.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente durante la scrittura dei descrittori Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη κατά την εγγραφή των περιγραφέων Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти при записи дескрипторов Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "写入 Vulkan 描述符时内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "寫入 Vulkan 描述元時記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスクリプタの書き込み中にメモリ不足になりました。";
        return "Out of memory while writing Vulkan descriptors.";
    }
    if (strcmp(msg, "Out of memory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει αρκετή μνήμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти.";
        if (strncmp(lang_code, "zh", 2) == 0) return "内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "メモリが足りません。";
        return "Out of memory.";
    }
    if (strcmp(msg, "Out of memory.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out of memory.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Δεν υπάρχει αρκετή μνήμη.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "内存不足。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "記憶體不足。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "メモリが足りません。\n";
        return "Out of memory.\n";
    }
    if (strcmp(msg, "Out-of-memory.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Out-of-memory.";
        if (strncmp(lang_code, "es", 2) == 0) return "Memoria insuficiente.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mémoire insuffisante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht genügend Speicher.";
        if (strncmp(lang_code, "it", 2) == 0) return "Memoria insufficiente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ανεπαρκής μνήμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Недостаточно памяти.";
        if (strncmp(lang_code, "zh", 2) == 0) return "内存不足。";
        if (strncmp(lang_code, "tw", 2) == 0) return "記憶體不足。";
        if (strncmp(lang_code, "ja", 2) == 0) return "メモリ不足です。";
        return "Out-of-memory.";
    }
    if (strcmp(msg, "Output %s for input %s collides with input %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Output %s for input %s collides with input %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La salida %s para la entrada %s entra en conflicto con la entrada %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "La sortie %s pour l’entrée %s entre en conflit avec l’entrée %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausgabe %s für die Eingabe %s kollidiert mit der Eingabe %s.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "L’output %s per l’input %s è in conflitto con l’input %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έξοδος %s για την είσοδο %s συγκρούεται με την είσοδο %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вывод %s для ввода %s конфликтует с вводом %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "输入 %s 的输出 %s 与输入 %s 冲突。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "輸入 %s 的輸出 %s 與輸入 %s 衝突。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "入力%sの出力%sが入力%sと競合します。\n";
        return "Output %s for input %s collides with input %s.\n";
    }
    if (strcmp(msg, "Output directory already exists.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Output directory already exists.";
        if (strncmp(lang_code, "es", 2) == 0) return "El directorio de salida ya existe.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le répertoire de sortie existe déjà.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Ausgabeverzeichnis ist bereits vorhanden.";
        if (strncmp(lang_code, "it", 2) == 0) return "La directory di output esiste già.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο κατάλογος εξόδου υπάρχει ήδη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Каталог вывода уже существует.";
        if (strncmp(lang_code, "zh", 2) == 0) return "输出目录已存在。";
        if (strncmp(lang_code, "tw", 2) == 0) return "輸出目錄已存在。";
        if (strncmp(lang_code, "ja", 2) == 0) return "出力ディレクトリはすでに存在します。";
        return "Output directory already exists.";
    }
    if (strcmp(msg, "Packed destination range is out-of-bounds.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed destination range is out-of-bounds.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango de destino de Packed está fuera de los límites.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage de destination de Packed est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Zielbereich von Packed liegt außerhalb der Grenzen.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo di destinazione di Packed è fuori dai limiti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος προορισμού του Packed είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон назначения Packed выходит за границы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Packed 目标范围越界。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Packed 目的地範圍越界。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Packedの格納先範囲が境界外です。";
        return "Packed destination range is out-of-bounds.";
    }
    if (strcmp(msg, "Packed element type must be a byte.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed element type must be a byte.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tipo de elemento de Packed debe ser un byte.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le type d’élément de Packed doit être un octet.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Elementtyp von Packed muss ein Byte sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il tipo di elemento di Packed deve essere un byte.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τύπος στοιχείου του Packed πρέπει να είναι byte.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Тип элемента Packed должен быть байтом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Packed 元素类型必须为字节。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Packed 元素型別必須為位元組。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Packedの要素型はバイトである必要があります。";
        return "Packed element type must be a byte.";
    }
    if (strcmp(msg, "Packed element types do not match.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed element types do not match.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los tipos de elemento de Packed no coinciden.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les types d’élément de Packed ne correspondent pas.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Elementtypen von Packed stimmen nicht überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "I tipi di elemento di Packed non corrispondono.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι τύποι στοιχείων του Packed δεν αντιστοιχούν.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Типы элементов Packed не совпадают.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Packed 元素类型不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Packed 元素型別不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Packedの要素型が一致しません。";
        return "Packed element types do not match.";
    }
    if (strcmp(msg, "Packed index %ld is out-of-range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed index %ld is out-of-range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El índice %ld de Packed está fuera de rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’index %ld de Packed est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Index %ld von Packed liegt außerhalb des Bereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’indice %ld di Packed è fuori intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο δείκτης %ld του Packed είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс %ld Packed вне диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Packed 索引 %ld 超出范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Packed 索引 %ld 超出範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Packedのインデックス%ldが範囲外です。";
        return "Packed index %ld is out-of-range.";
    }
    if (strcmp(msg, "Packed is not a file mapping.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed is not a file mapping.";
        if (strncmp(lang_code, "es", 2) == 0) return "Packed no es un mapeo de archivos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Packed n’est pas un mappage de fichier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Packed ist keine Dateizuordnung.";
        if (strncmp(lang_code, "it", 2) == 0) return "Packed non è una mappatura di file.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το Packed δεν είναι αντιστοίχιση αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Packed не является отображением файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Packed 不是文件映射。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Packed 不是檔案對應。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Packedはファイルマッピングではありません。";
        return "Packed is not a file mapping.";
    }
    if (strcmp(msg, "Packed is unmapped.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed is unmapped.";
        if (strncmp(lang_code, "es", 2) == 0) return "Packed no está mapeado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Packed n’est pas mappé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Packed ist nicht zugeordnet.";
        if (strncmp(lang_code, "it", 2) == 0) return "Packed non è mappato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το Packed δεν είναι αντιστοιχισμένο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Packed не отображён.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Packed 未映射。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Packed 未對應。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Packedはマッピングされていません。";
        return "Packed is unmapped.";
    }
    if (strcmp(msg, "Packed size is 0.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed size is 0.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del packed es 0.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du packed est 0.";
        if (strncmp(lang_code, "de", 2) == 0) return "Packed-Größe ist 0.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del packed è 0.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος του packed είναι 0.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер packed равен 0.";
        if (strncmp(lang_code, "zh", 2) == 0) return "packed 的大小为 0。";
        if (strncmp(lang_code, "tw", 2) == 0) return "packed 的大小為 0。";
        if (strncmp(lang_code, "ja", 2) == 0) return "packed のサイズが 0 です。";
        return "Packed size is 0.";
    }
    if (strcmp(msg, "Packed size is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed size is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño del packed es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille du packed est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Packed-Größe ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione del packed è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος του packed είναι πολύ μεγάλο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер packed слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "packed 的大小过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "packed 的大小過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "packed のサイズが大きすぎます。";
        return "Packed size is too large.";
    }
    if (strcmp(msg, "Packed source range is out-of-bounds.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Packed source range is out-of-bounds.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango de origen de Packed está fuera de los límites.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage source de Packed est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Quellbereich von Packed liegt außerhalb der Grenzen.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo sorgente di Packed è fuori dai limiti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος προέλευσης του Packed είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Исходный диапазон Packed выходит за границы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Packed 源范围越界。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Packed 來源範圍越界。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Packedのソース範囲が境界外です。";
        return "Packed source range is out-of-bounds.";
    }
    if (strcmp(msg, "Port number is out-of-range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Port number is out-of-range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El número de puerto está fuera de rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le numéro de port est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Portnummer liegt außerhalb des Bereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il numero di porta è fuori intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο αριθμός θύρας είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Номер порта вне диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "端口号超出范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "連接埠號碼超出範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ポート番号が範囲外です。";
        return "Port number is out-of-range.";
    }
    if (strcmp(msg, "Private accelerator package is detached.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Private accelerator package is detached.";
        if (strncmp(lang_code, "es", 2) == 0) return "El paquete privado del acelerador está desconectado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le paquet privé de l’accélérateur est détaché.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das private Beschleunigerpaket ist getrennt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il pacchetto privato dell’acceleratore è scollegato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το ιδιωτικό πακέτο επιταχυντή έχει αποσυνδεθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Закрытый пакет ускорителя отсоединён.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器私有包已分离。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器私有套件已分離。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのプライベートパッケージが切り離されています。";
        return "Private accelerator package is detached.";
    }
    if (strcmp(msg, "REPL input has unmatched delimiters.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "REPL input has unmatched delimiters.";
        if (strncmp(lang_code, "es", 2) == 0) return "La entrada del REPL contiene delimitadores sin pareja.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’entrée du REPL contient des délimiteurs non appariés.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die REPL-Eingabe enthält nicht übereinstimmende Begrenzungszeichen.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’input REPL contiene delimitatori non corrispondenti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η είσοδος REPL περιέχει μη αντιστοιχισμένους οριοθέτες.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ввод REPL содержит непарные разделители.";
        if (strncmp(lang_code, "zh", 2) == 0) return "REPL 输入包含不匹配的分隔符。";
        if (strncmp(lang_code, "tw", 2) == 0) return "REPL 輸入包含不相符的分隔符號。";
        if (strncmp(lang_code, "ja", 2) == 0) return "REPL入力に対応しない区切り記号があります。";
        return "REPL input has unmatched delimiters.";
    }
    if (strcmp(msg, "REPL input is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "REPL input is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "La entrada del REPL es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’entrée du REPL est trop volumineuse.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die REPL-Eingabe ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’input REPL è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η είσοδος REPL είναι πολύ μεγάλη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ввод REPL слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "REPL 输入过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "REPL 輸入過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "REPL入力が大きすぎます。";
        return "REPL input is too large.";
    }
    if (strcmp(msg, "Read length must be greater than zero.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Read length must be greater than zero.";
        if (strncmp(lang_code, "es", 2) == 0) return "La longitud de lectura debe ser mayor que cero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La longueur de lecture doit être supérieure à zéro.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Leselänge muss größer als null sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "La lunghezza di lettura deve essere maggiore di zero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μήκος ανάγνωσης πρέπει να είναι μεγαλύτερο από μηδέν.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Длина чтения должна быть больше нуля.";
        if (strncmp(lang_code, "zh", 2) == 0) return "读取长度必须大于零。";
        if (strncmp(lang_code, "tw", 2) == 0) return "讀取長度必須大於零。";
        if (strncmp(lang_code, "ja", 2) == 0) return "読み取り長は0より大きい必要があります。";
        return "Read length must be greater than zero.";
    }
    if (strcmp(msg, "Receive size is out-of-range.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Receive size is out-of-range.";
        if (strncmp(lang_code, "es", 2) == 0) return "El tamaño de recepción está fuera de rango.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La taille de réception est hors limites.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Empfangsgröße liegt außerhalb des Bereichs.";
        if (strncmp(lang_code, "it", 2) == 0) return "La dimensione di ricezione è fuori intervallo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μέγεθος λήψης είναι εκτός ορίων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер приёма вне диапазона.";
        if (strncmp(lang_code, "zh", 2) == 0) return "接收大小超出范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "接收大小超出範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "受信サイズが範囲外です。";
        return "Receive size is out-of-range.";
    }
    if (strcmp(msg, "Regex error: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Regex error: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de expresión regular: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur d’expression régulière : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Fehler im regulären Ausdruck: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore dell’espressione regolare: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα κανονικής έκφρασης: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка регулярного выражения: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "正则表达式错误：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "規則運算式錯誤：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "正規表現エラー: %s";
        return "Regex error: %s";
    }
    if (strcmp(msg, "Regex too complex.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Regex too complex.";
        if (strncmp(lang_code, "es", 2) == 0) return "La expresión regular es demasiado compleja.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’expression régulière est trop complexe.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der reguläre Ausdruck ist zu komplex.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’espressione regolare è troppo complessa.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η κανονική έκφραση είναι υπερβολικά σύνθετη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Регулярное выражение слишком сложное.";
        if (strncmp(lang_code, "zh", 2) == 0) return "正则表达式过于复杂。";
        if (strncmp(lang_code, "tw", 2) == 0) return "規則運算式過於複雜。";
        if (strncmp(lang_code, "ja", 2) == 0) return "正規表現が複雑すぎます。";
        return "Regex too complex.";
    }
    if (strcmp(msg, "Regex too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Regex too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "La expresión regular es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’expression régulière est trop volumineuse.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der reguläre Ausdruck ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’espressione regolare è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η κανονική έκφραση είναι πολύ μεγάλη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Регулярное выражение слишком велико.";
        if (strncmp(lang_code, "zh", 2) == 0) return "正则表达式过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "規則運算式過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "正規表現が大きすぎます。";
        return "Regex too large.";
    }
    if (strcmp(msg, "RelaxedPrecision present") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "RelaxedPrecision present";
        if (strncmp(lang_code, "es", 2) == 0) return "RelaxedPrecision presente";
        if (strncmp(lang_code, "fr", 2) == 0) return "RelaxedPrecision présent";
        if (strncmp(lang_code, "de", 2) == 0) return "RelaxedPrecision vorhanden";
        if (strncmp(lang_code, "it", 2) == 0) return "RelaxedPrecision presente";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπάρχει RelaxedPrecision";
        if (strncmp(lang_code, "ru", 2) == 0) return "Присутствует RelaxedPrecision";
        if (strncmp(lang_code, "zh", 2) == 0) return "存在 RelaxedPrecision";
        if (strncmp(lang_code, "tw", 2) == 0) return "存在 RelaxedPrecision";
        if (strncmp(lang_code, "ja", 2) == 0) return "RelaxedPrecisionあり";
        return "RelaxedPrecision present";
    }
    if (strcmp(msg, "Return operand does not match the declared type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Return operand does not match the declared type.";
        if (strncmp(lang_code, "es", 2) == 0) return "El operando de retorno no coincide con el tipo declarado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’opérande de retour ne correspond pas au type déclaré.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Rückgabeoperand stimmt nicht mit dem deklarierten Typ überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’operando di restituzione non corrisponde al tipo dichiarato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τελεστέος επιστροφής δεν αντιστοιχεί στον δηλωμένο τύπο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Операнд возврата не соответствует объявленному типу.";
        if (strncmp(lang_code, "zh", 2) == 0) return "返回操作数与声明的类型不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "傳回運算元與宣告的型別不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "戻りオペランドが宣言された型と一致しません。";
        return "Return operand does not match the declared type.";
    }
    if (strcmp(msg, "SIMD FMA: vreg stack overflow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SIMD FMA: vreg stack overflow.";
        if (strncmp(lang_code, "es", 2) == 0) return "SIMD FMA: desbordamiento de la pila de vreg.";
        if (strncmp(lang_code, "fr", 2) == 0) return "SIMD FMA : dépassement de capacité de la pile vreg.";
        if (strncmp(lang_code, "de", 2) == 0) return "SIMD FMA: Überlauf des vreg-Stacks.";
        if (strncmp(lang_code, "it", 2) == 0) return "SIMD FMA: overflow dello stack vreg.";
        if (strncmp(lang_code, "el", 2) == 0) return "SIMD FMA: υπερχείλιση στοίβας vreg.";
        if (strncmp(lang_code, "ru", 2) == 0) return "SIMD FMA: переполнение стека vreg.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SIMD FMA：vreg 栈溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SIMD FMA：vreg 堆疊溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SIMD FMA: vregスタックがオーバーフローしました。";
        return "SIMD FMA: vreg stack overflow.";
    }
    if (strcmp(msg, "SIMD: ranked cache dependency plan exceeded vreg budget.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SIMD: ranked cache dependency plan exceeded vreg budget.";
        if (strncmp(lang_code, "es", 2) == 0) return "SIMD: el plan de dependencias de caché clasificado superó el presupuesto de vreg.";
        if (strncmp(lang_code, "fr", 2) == 0) return "SIMD : le plan classé des dépendances de cache a dépassé le budget vreg.";
        if (strncmp(lang_code, "de", 2) == 0) return "SIMD: Der geordnete Cacheabhängigkeitsplan hat das vreg-Budget überschritten.";
        if (strncmp(lang_code, "it", 2) == 0) return "SIMD: il piano classificato delle dipendenze della cache ha superato il budget vreg.";
        if (strncmp(lang_code, "el", 2) == 0) return "SIMD: το ταξινομημένο πλάνο εξαρτήσεων cache υπερέβη τον προϋπολογισμό vreg.";
        if (strncmp(lang_code, "ru", 2) == 0) return "SIMD: ранжированный план зависимостей кеша превысил бюджет vreg.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SIMD：分级缓存依赖计划超出 vreg 预算。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SIMD：分級快取相依性計畫超出 vreg 預算。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SIMD: ランク付けされたキャッシュ依存関係プランがvreg予算を超えました。";
        return "SIMD: ranked cache dependency plan exceeded vreg budget.";
    }
    if (strcmp(msg, "SIMD: too many temps.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SIMD: too many temps.";
        if (strncmp(lang_code, "es", 2) == 0) return "SIMD: demasiados temporales.";
        if (strncmp(lang_code, "fr", 2) == 0) return "SIMD : trop de valeurs temporaires.";
        if (strncmp(lang_code, "de", 2) == 0) return "SIMD: Zu viele temporäre Werte.";
        if (strncmp(lang_code, "it", 2) == 0) return "SIMD: troppi valori temporanei.";
        if (strncmp(lang_code, "el", 2) == 0) return "SIMD: υπερβολικά πολλές προσωρινές τιμές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "SIMD: слишком много временных значений.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SIMD：临时值过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SIMD：暫存值過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SIMD: 一時値が多すぎます。";
        return "SIMD: too many temps.";
    }
    if (strcmp(msg, "SIMD: unexpected vector expression.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SIMD: unexpected vector expression.";
        if (strncmp(lang_code, "es", 2) == 0) return "SIMD: expresión vectorial inesperada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "SIMD : expression vectorielle inattendue.";
        if (strncmp(lang_code, "de", 2) == 0) return "SIMD: Unerwarteter Vektorausdruck.";
        if (strncmp(lang_code, "it", 2) == 0) return "SIMD: espressione vettoriale imprevista.";
        if (strncmp(lang_code, "el", 2) == 0) return "SIMD: μη αναμενόμενη διανυσματική έκφραση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "SIMD: неожиданное векторное выражение.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SIMD：意外的向量表达式。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SIMD：非預期的向量運算式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SIMD: 予期しないベクトル式です。";
        return "SIMD: unexpected vector expression.";
    }
    if (strcmp(msg, "SIMD: unplanned term.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SIMD: unplanned term.";
        if (strncmp(lang_code, "es", 2) == 0) return "SIMD: término no planificado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "SIMD : terme non planifié.";
        if (strncmp(lang_code, "de", 2) == 0) return "SIMD: Nicht geplanter Term.";
        if (strncmp(lang_code, "it", 2) == 0) return "SIMD: termine non pianificato.";
        if (strncmp(lang_code, "el", 2) == 0) return "SIMD: μη σχεδιασμένος όρος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "SIMD: незапланированный терм.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SIMD：未规划的项。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SIMD：未規劃的項。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SIMD: 計画されていない項です。";
        return "SIMD: unplanned term.";
    }
    if (strcmp(msg, "SIMD: vreg budget exceeded.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SIMD: vreg budget exceeded.";
        if (strncmp(lang_code, "es", 2) == 0) return "SIMD: se superó el presupuesto de vreg.";
        if (strncmp(lang_code, "fr", 2) == 0) return "SIMD : budget vreg dépassé.";
        if (strncmp(lang_code, "de", 2) == 0) return "SIMD: vreg-Budget überschritten.";
        if (strncmp(lang_code, "it", 2) == 0) return "SIMD: budget vreg superato.";
        if (strncmp(lang_code, "el", 2) == 0) return "SIMD: υπέρβαση προϋπολογισμού vreg.";
        if (strncmp(lang_code, "ru", 2) == 0) return "SIMD: превышен бюджет vreg.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SIMD：超出 vreg 预算。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SIMD：超出 vreg 預算。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SIMD: vreg予算を超えました。";
        return "SIMD: vreg budget exceeded.";
    }
    if (strcmp(msg, "SIMD: vreg stack overflow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SIMD: vreg stack overflow.";
        if (strncmp(lang_code, "es", 2) == 0) return "SIMD: desbordamiento de la pila de vreg.";
        if (strncmp(lang_code, "fr", 2) == 0) return "SIMD : dépassement de capacité de la pile vreg.";
        if (strncmp(lang_code, "de", 2) == 0) return "SIMD: Überlauf des vreg-Stacks.";
        if (strncmp(lang_code, "it", 2) == 0) return "SIMD: overflow dello stack vreg.";
        if (strncmp(lang_code, "el", 2) == 0) return "SIMD: υπερχείλιση στοίβας vreg.";
        if (strncmp(lang_code, "ru", 2) == 0) return "SIMD: переполнение стека vreg.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SIMD：vreg 栈溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SIMD：vreg 堆疊溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SIMD: vregスタックがオーバーフローしました。";
        return "SIMD: vreg stack overflow.";
    }
    if (strcmp(msg, "SUBJNZ operand is not an integer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "SUBJNZ operand is not an integer.";
        if (strncmp(lang_code, "es", 2) == 0) return "El operando de SUBJNZ no es un entero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’opérande de SUBJNZ n’est pas un entier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der SUBJNZ-Operand ist keine ganze Zahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’operando di SUBJNZ non è un intero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο τελεστέος SUBJNZ δεν είναι ακέραιος αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Операнд SUBJNZ не является целым числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "SUBJNZ 操作数不是整数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "SUBJNZ 運算元不是整數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "SUBJNZのオペランドが整数ではありません。";
        return "SUBJNZ operand is not an integer.";
    }
    if (strcmp(msg, "Searching directory %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Searching directory %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Buscando en el directorio %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Recherche dans le répertoire %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Durchsuche Verzeichnis %s.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Ricerca nella directory %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Αναζήτηση στον κατάλογο %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Поиск в каталоге %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "正在搜索目录 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "正在搜尋目錄 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "ディレクトリ%sを検索中です。\n";
        return "Searching directory %s.\n";
    }
    if (strcmp(msg, "Shaped parameter types are valid only on __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Shaped parameter types are valid only on __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los tipos de parámetro con forma solo son válidos en __fast func.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les types de paramètres dotés d’une forme ne sont valides que dans __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Parametertypen mit Form sind nur bei __fast func gültig.";
        if (strncmp(lang_code, "it", 2) == 0) return "I tipi di parametro con forma sono validi solo in __fast func.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι τύποι παραμέτρων με σχήμα είναι έγκυροι μόνο σε __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Типы параметров с формой допустимы только в __fast func.";
        if (strncmp(lang_code, "zh", 2) == 0) return "带形状的形参类型仅对 __fast func 有效。";
        if (strncmp(lang_code, "tw", 2) == 0) return "具有形狀的參數型別僅對 __fast func 有效。";
        if (strncmp(lang_code, "ja", 2) == 0) return "形状付きのパラメータ型は__fast funcでのみ有効です。";
        return "Shaped parameter types are valid only on __fast func.";
    }
    if (strcmp(msg, "Shared value is nested too deeply.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Shared value is nested too deeply.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor compartido está anidado a demasiada profundidad.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur partagée est imbriquée trop profondément.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der gemeinsame Wert ist zu tief verschachtelt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore condiviso è annidato troppo in profondità.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η κοινόχρηστη τιμή είναι πολύ βαθιά εμφωλευμένη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Общее значение вложено слишком глубоко.";
        if (strncmp(lang_code, "zh", 2) == 0) return "共享值嵌套过深。";
        if (strncmp(lang_code, "tw", 2) == 0) return "共用值巢狀層級過深。";
        if (strncmp(lang_code, "ja", 2) == 0) return "共有値のネストが深すぎます。";
        return "Shared value is nested too deeply.";
    }
    if (strcmp(msg, "Skipping empty directory %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Skipping empty directory %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Omitiendo el directorio vacío %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Répertoire vide ignoré : %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Leeres Verzeichnis %s wird übersprungen.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Directory vuota %s ignorata.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Παράλειψη του κενoύ καταλόγου %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Пропуск пустого каталога %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "跳过空目录 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "跳過空目錄 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "空のディレクトリ%sをスキップしています。\n";
        return "Skipping empty directory %s.\n";
    }
    if (strcmp(msg, "Socket is already closed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Socket is already closed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El socket ya está cerrado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le socket est déjà fermé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Socket ist bereits geschlossen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il socket è già chiuso.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το socket είναι ήδη κλειστό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сокет уже закрыт.";
        if (strncmp(lang_code, "zh", 2) == 0) return "套接字已关闭。";
        if (strncmp(lang_code, "tw", 2) == 0) return "通訊端已關閉。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソケットはすでに閉じています。";
        return "Socket is already closed.";
    }
    if (strcmp(msg, "Socket is already registered to a poller.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Socket is already registered to a poller.";
        if (strncmp(lang_code, "es", 2) == 0) return "El socket ya está registrado en un sondeador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le socket est déjà enregistré auprès d’un scrutateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Socket ist bereits bei einem Poller registriert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il socket è già registrato presso un poller.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το socket είναι ήδη καταχωρισμένο σε poller.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сокет уже зарегистрирован в средстве опроса.";
        if (strncmp(lang_code, "zh", 2) == 0) return "套接字已注册到轮询器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "通訊端已登錄至輪詢器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソケットはすでにポーラーに登録されています。";
        return "Socket is already registered to a poller.";
    }
    if (strcmp(msg, "Socket is not registered to this poller.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Socket is not registered to this poller.";
        if (strncmp(lang_code, "es", 2) == 0) return "El socket no está registrado en este sondeador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le socket n’est pas enregistré auprès de ce scrutateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Socket ist nicht bei diesem Poller registriert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il socket non è registrato presso questo poller.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το socket δεν είναι καταχωρισμένο σε αυτό το poller.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сокет не зарегистрирован в этом средстве опроса.";
        if (strncmp(lang_code, "zh", 2) == 0) return "套接字未注册到此轮询器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "通訊端未登錄至此輪詢器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソケットはこのポーラーに登録されていません。";
        return "Socket is not registered to this poller.";
    }
    if (strcmp(msg, "Socket is registered to a poller. Remove it first.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Socket is registered to a poller. Remove it first.";
        if (strncmp(lang_code, "es", 2) == 0) return "El socket está registrado en un sondeador. Elimínelo primero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le socket est enregistré auprès d’un scrutateur. Supprimez-le d’abord.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Socket ist bei einem Poller registriert. Entfernen Sie ihn zuerst.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il socket è registrato presso un poller. Rimuoverlo prima.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το socket είναι καταχωρισμένο σε poller. Αφαιρέστε το πρώτα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сокет зарегистрирован в средстве опроса. Сначала удалите его.";
        if (strncmp(lang_code, "zh", 2) == 0) return "套接字已注册到轮询器。请先将其移除。";
        if (strncmp(lang_code, "tw", 2) == 0) return "通訊端已登錄至輪詢器。請先將其移除。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソケットはポーラーに登録されています。先に削除してください。";
        return "Socket is registered to a poller. Remove it first.";
    }
    if (strcmp(msg, "Specify a command.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Specify a command.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Especifique un comando.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Veuillez spécifier une commande.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Bitte einen Befehl angeben.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Specificare un comando.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Καθορίστε μια εντολή.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Укажите команду.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "请指定一个命令。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "請指定一個命令。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "コマンドを指定してください。\n";
        return "Specify a command.\n";
    }
    if (strcmp(msg, "Stack overflow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Stack overflow.";
        if (strncmp(lang_code, "es", 2) == 0) return "Desbordamiento de pila.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Débordement de pile.";
        if (strncmp(lang_code, "de", 2) == 0) return "Stapelüberlauf.";
        if (strncmp(lang_code, "it", 2) == 0) return "Stack overflow.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερχείλιση στοίβας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переполнение стека.";
        if (strncmp(lang_code, "zh", 2) == 0) return "栈溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "堆疊溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "スタックオーバーフローです。";
        return "Stack overflow.";
    }
    if (strcmp(msg, "Stack underflow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Stack underflow.";
        if (strncmp(lang_code, "es", 2) == 0) return "Desbordamiento inferior de pila.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Sous-débordement de pile.";
        if (strncmp(lang_code, "de", 2) == 0) return "Stapelunterlauf.";
        if (strncmp(lang_code, "it", 2) == 0) return "Stack underflow.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υποχείλιση στοίβας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Опустошение стека.";
        if (strncmp(lang_code, "zh", 2) == 0) return "栈下溢。";
        if (strncmp(lang_code, "tw", 2) == 0) return "堆疊下溢。";
        if (strncmp(lang_code, "ja", 2) == 0) return "スタックアンダーフローです。";
        return "Stack underflow.";
    }
    if (strcmp(msg, "String is too long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "String is too long.";
        if (strncmp(lang_code, "es", 2) == 0) return "La cadena es demasiado larga.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La chaîne de caractères est trop longue.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Zeichenkette ist zu lang.";
        if (strncmp(lang_code, "it", 2) == 0) return "La stringa è troppo lunga.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συμβολοσειρά είναι πολύ μεγάλη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Строка слишком длинная.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字符串过长。";
        if (strncmp(lang_code, "tw", 2) == 0) return "字串過長。";
        if (strncmp(lang_code, "ja", 2) == 0) return "文字列が長すぎます。";
        return "String is too long.";
    }
    if (strcmp(msg, "Subscript is negative.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Subscript is negative.";
        if (strncmp(lang_code, "es", 2) == 0) return "El subíndice es negativo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'indice est négatif.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Index ist negativ.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'indice è negativo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο δείκτης είναι αρνητικός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс отрицательный.";
        if (strncmp(lang_code, "zh", 2) == 0) return "下标为负数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "下標為負數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "添字が負の値です。";
        return "Subscript is negative.";
    }
    if (strcmp(msg, "Subscript not a string.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Subscript not a string.";
        if (strncmp(lang_code, "es", 2) == 0) return "El subíndice no es un string.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'indice n'est pas une chaîne.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Index ist kein String.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'indice non è un string.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο δείκτης δεν είναι συμβολοσειρά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс не является строкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "下标不是字符串。";
        if (strncmp(lang_code, "tw", 2) == 0) return "下標不是字串。";
        if (strncmp(lang_code, "ja", 2) == 0) return "添字が文字列ではありません。";
        return "Subscript not a string.";
    }
    if (strcmp(msg, "Subscript not an integer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Subscript not an integer.";
        if (strncmp(lang_code, "es", 2) == 0) return "El subíndice no es un entero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'indice n'est pas un entier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Index ist keine ganze Zahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'indice non è un intero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο δείκτης δεν είναι ακέραιος αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Индекс не является целым числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "下标不是整数";
        if (strncmp(lang_code, "tw", 2) == 0) return "下標不是整數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "添字が整数ではありません。";
        return "Subscript not an integer.";
    }
    if (strcmp(msg, "Symbol \"%s\" not found.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Symbol \"%s\" not found.";
        if (strncmp(lang_code, "es", 2) == 0) return "El símbolo \"%s\" no se ha encontrado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le symbole \"%s\" est introuvable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Symbol \"%s\" nicht gefunden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Simbolo \"%s\" non trovato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το σύμβολο \"%s\" δεν βρέθηκε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Символ \"%s\" не найден.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未找到符号 \"%s\"。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未找到符號 \"%s\"。";
        if (strncmp(lang_code, "ja", 2) == 0) return "シンボル \"%s\" がみつかりません。";
        return "Symbol \"%s\" not found.";
    }
    if (strcmp(msg, "System.import() accepts source files only.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "System.import() accepts source files only.";
        if (strncmp(lang_code, "es", 2) == 0) return "System.import() solo acepta archivos fuente.";
        if (strncmp(lang_code, "fr", 2) == 0) return "System.import() n’accepte que les fichiers sources.";
        if (strncmp(lang_code, "de", 2) == 0) return "System.import() akzeptiert nur Quelldateien.";
        if (strncmp(lang_code, "it", 2) == 0) return "System.import() accetta solo file sorgente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το System.import() δέχεται μόνο αρχεία πηγαίου κώδικα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "System.import() принимает только исходные файлы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "System.import() 仅接受源文件。";
        if (strncmp(lang_code, "tw", 2) == 0) return "System.import() 僅接受原始碼檔案。";
        if (strncmp(lang_code, "ja", 2) == 0) return "System.import()はソースファイルのみ受け付けます。";
        return "System.import() accepts source files only.";
    }
    if (strcmp(msg, "The '__noct_nap_' symbol prefix is reserved.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The '__noct_nap_' symbol prefix is reserved.";
        if (strncmp(lang_code, "es", 2) == 0) return "El prefijo de símbolo '__noct_nap_' está reservado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le préfixe de symbole '__noct_nap_' est réservé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Symbolpräfix '__noct_nap_' ist reserviert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il prefisso del simbolo '__noct_nap_' è riservato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πρόθεμα συμβόλου '__noct_nap_' είναι δεσμευμένο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Префикс символа '__noct_nap_' зарезервирован.";
        if (strncmp(lang_code, "zh", 2) == 0) return "符号前缀 '__noct_nap_' 已保留。";
        if (strncmp(lang_code, "tw", 2) == 0) return "符號前置詞 '__noct_nap_' 已保留。";
        if (strncmp(lang_code, "ja", 2) == 0) return "シンボル接頭辞'__noct_nap_'は予約されています。";
        return "The '__noct_nap_' symbol prefix is reserved.";
    }
    if (strcmp(msg, "The --app option may be specified only once.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The --app option may be specified only once.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "La opción --app solo puede especificarse una vez.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’option --app ne peut être spécifiée qu’une seule fois.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Option --app darf nur einmal angegeben werden.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "L’opzione --app può essere specificata una sola volta.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επιλογή --app μπορεί να καθοριστεί μόνο μία φορά.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Параметр --app можно указать только один раз.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "选项 --app 只能指定一次。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "選項 --app 只能指定一次。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "オプション--appは一度だけ指定できます。\n";
        return "The --app option may be specified only once.\n";
    }
    if (strcmp(msg, "The Vulkan accelerator device is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The Vulkan accelerator device is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "El dispositivo acelerador Vulkan no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le périphérique d’accélérateur Vulkan n’est pas disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Vulkan-Beschleunigergerät ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispositivo dell’acceleratore Vulkan non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συσκευή επιταχυντή Vulkan δεν είναι διαθέσιμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Устройство ускорителя Vulkan недоступно.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 加速器设备不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 加速器裝置無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanアクセラレータデバイスは利用できません。";
        return "The Vulkan accelerator device is unavailable.";
    }
    if (strcmp(msg, "The Vulkan loader did not expose Vulkan 1.2 physical-device properties.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The Vulkan loader did not expose Vulkan 1.2 physical-device properties.";
        if (strncmp(lang_code, "es", 2) == 0) return "El cargador de Vulkan no expuso las propiedades del dispositivo físico de Vulkan 1.2.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le chargeur Vulkan n’a pas exposé les propriétés de périphérique physique Vulkan 1.2.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Loader hat die Eigenschaften physischer Vulkan 1.2-Geräte nicht bereitgestellt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il loader Vulkan non ha esposto le proprietà del dispositivo fisico Vulkan 1.2.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο φορτωτής Vulkan δεν εξέθεσε τις ιδιότητες φυσικής συσκευής Vulkan 1.2.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Загрузчик Vulkan не предоставил свойства физического устройства Vulkan 1.2.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 加载器未公开 Vulkan 1.2 物理设备属性。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 載入器未公開 Vulkan 1.2 實體裝置屬性。";
        if (strncmp(lang_code, "ja", 2) == 0) return "VulkanローダーがVulkan 1.2の物理デバイスプロパティを公開しませんでした。";
        return "The Vulkan loader did not expose Vulkan 1.2 physical-device properties.";
    }
    if (strcmp(msg, "The number of indices does not match the __fast parameter rank.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The number of indices does not match the __fast parameter rank.";
        if (strncmp(lang_code, "es", 2) == 0) return "La cantidad de índices no coincide con el número de dimensiones del parámetro __fast.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nombre d’index ne correspond pas au nombre de dimensions du paramètre __fast.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Anzahl der Indizes stimmt nicht mit der Dimensionsanzahl des __fast-Parameters überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il numero di indici non corrisponde al numero di dimensioni del parametro __fast.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το πλήθος δεικτών δεν αντιστοιχεί στον αριθμό διαστάσεων της παραμέτρου __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Количество индексов не соответствует числу измерений параметра __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "索引数量与 __fast 形参的维数不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "索引數量與 __fast 參數的維度數不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "インデックス数が__fastパラメータの次元数と一致しません。";
        return "The number of indices does not match the __fast parameter rank.";
    }
    if (strcmp(msg, "The requested GPU device was not found.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The requested GPU device was not found.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se encontró el dispositivo GPU solicitado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le périphérique GPU demandé est introuvable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das angeforderte GPU-Gerät wurde nicht gefunden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispositivo GPU richiesto non è stato trovato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ζητούμενη συσκευή GPU δεν βρέθηκε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Запрошенное устройство GPU не найдено.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未找到请求的 GPU 设备。";
        if (strncmp(lang_code, "tw", 2) == 0) return "找不到要求的 GPU 裝置。";
        if (strncmp(lang_code, "ja", 2) == 0) return "要求されたGPUデバイスが見つかりません。";
        return "The requested GPU device was not found.";
    }
    if (strcmp(msg, "The requested GPU name is ambiguous; use a backend-qualified selector.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The requested GPU name is ambiguous; use a backend-qualified selector.";
        if (strncmp(lang_code, "es", 2) == 0) return "El nombre de GPU solicitado es ambiguo; use un selector cualificado por backend.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nom de GPU demandé est ambigu ; utilisez un sélecteur qualifié par le backend.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der angeforderte GPU-Name ist mehrdeutig; verwenden Sie einen Backend-qualifizierten Selektor.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il nome GPU richiesto è ambiguo; usare un selettore qualificato dal backend.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το ζητούμενο όνομα GPU είναι διφορούμενο· χρησιμοποιήστε επιλογέα προσδιορισμένο με backend.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Запрошенное имя GPU неоднозначно; используйте селектор с указанием бэкенда.";
        if (strncmp(lang_code, "zh", 2) == 0) return "请求的 GPU 名称有歧义；请使用带后端限定的选择器。";
        if (strncmp(lang_code, "tw", 2) == 0) return "要求的 GPU 名稱有歧義；請使用帶後端限定的選取器。";
        if (strncmp(lang_code, "ja", 2) == 0) return "要求されたGPU名が曖昧です。バックエンドで修飾されたセレクタを使用してください。";
        return "The requested GPU name is ambiguous; use a backend-qualified selector.";
    }
    if (strcmp(msg, "The selected Direct3D 12 device is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The selected Direct3D 12 device is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "El dispositivo Direct3D 12 seleccionado no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le périphérique Direct3D 12 sélectionné n’est pas disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das ausgewählte Direct3D 12-Gerät ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispositivo Direct3D 12 selezionato non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επιλεγμένη συσκευή Direct3D 12 δεν είναι διαθέσιμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выбранное устройство Direct3D 12 недоступно.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所选 Direct3D 12 设备不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所選 Direct3D 12 裝置無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したDirect3D 12デバイスは利用できません。";
        return "The selected Direct3D 12 device is unavailable.";
    }
    if (strcmp(msg, "The selected GPU backend is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The selected GPU backend is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "El backend de GPU seleccionado no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le backend GPU sélectionné n’est pas disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das ausgewählte GPU-Backend ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il backend GPU selezionato non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το επιλεγμένο backend GPU δεν είναι διαθέσιμο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выбранный бэкенд GPU недоступен.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所选 GPU 后端不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所選 GPU 後端無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したGPUバックエンドは利用できません。";
        return "The selected GPU backend is unavailable.";
    }
    if (strcmp(msg, "The selected OpenGL ES device is no longer available.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The selected OpenGL ES device is no longer available.";
        if (strncmp(lang_code, "es", 2) == 0) return "El dispositivo OpenGL ES seleccionado ya no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le périphérique OpenGL ES sélectionné n’est plus disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das ausgewählte OpenGL ES-Gerät ist nicht mehr verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispositivo OpenGL ES selezionato non è più disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επιλεγμένη συσκευή OpenGL ES δεν είναι πλέον διαθέσιμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выбранное устройство OpenGL ES больше недоступно.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所选 OpenGL ES 设备不再可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所選 OpenGL ES 裝置已無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したOpenGL ESデバイスは利用できなくなりました。";
        return "The selected OpenGL ES device is no longer available.";
    }
    if (strcmp(msg, "The selected Vulkan compute queue is unavailable.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "The selected Vulkan compute queue is unavailable.";
        if (strncmp(lang_code, "es", 2) == 0) return "La cola de cómputo Vulkan seleccionada no está disponible.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La file d’attente de calcul Vulkan sélectionnée n’est pas disponible.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die ausgewählte Vulkan-Compute-Warteschlange ist nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "La coda di calcolo Vulkan selezionata non è disponibile.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η επιλεγμένη ουρά υπολογισμού Vulkan δεν είναι διαθέσιμη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выбранная вычислительная очередь Vulkan недоступна.";
        if (strncmp(lang_code, "zh", 2) == 0) return "所选 Vulkan 计算队列不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "所選 Vulkan 計算佇列無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "選択したVulkanコンピュートキューは利用できません。";
        return "The selected Vulkan compute queue is unavailable.";
    }
    if (strcmp(msg, "This expression is not available inside __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "This expression is not available inside __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "Esta expresión no está disponible dentro de __fast func.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Cette expression n’est pas disponible dans __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Dieser Ausdruck ist innerhalb von __fast func nicht verfügbar.";
        if (strncmp(lang_code, "it", 2) == 0) return "Questa espressione non è disponibile all’interno di __fast func.";
        if (strncmp(lang_code, "el", 2) == 0) return "Αυτή η έκφραση δεν είναι διαθέσιμη μέσα σε __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Это выражение недоступно внутри __fast func.";
        if (strncmp(lang_code, "zh", 2) == 0) return "此表达式在 __fast func 内不可用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "此運算式在 __fast func 內無法使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "この式は__fast func内では使用できません。";
        return "This expression is not available inside __fast func.";
    }
    if (strcmp(msg, "Thread error: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Thread error: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de hilo: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur de thread : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Threadfehler: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore del thread: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Σφάλμα νήματος: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ошибка потока: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "线程错误：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "執行緒錯誤：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "スレッドエラー: %s";
        return "Thread error: %s";
    }
    if (strcmp(msg, "Thread is already joined.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Thread is already joined.";
        if (strncmp(lang_code, "es", 2) == 0) return "Ya se realizó el join del hilo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le thread a déjà été joint.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Thread wurde bereits zusammengeführt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il thread è già stato sottoposto a join.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το νήμα έχει ήδη ενωθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Поток уже присоединён.";
        if (strncmp(lang_code, "zh", 2) == 0) return "线程已联结。";
        if (strncmp(lang_code, "tw", 2) == 0) return "執行緒已聯結。";
        if (strncmp(lang_code, "ja", 2) == 0) return "スレッドはすでにjoinされています。";
        return "Thread is already joined.";
    }
    if (strcmp(msg, "Too big code.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too big code.";
        if (strncmp(lang_code, "es", 2) == 0) return "El código es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le code est trop volumineux.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Code ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il codice è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο κώδικας είναι πολύ μεγάλος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Код слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "代码过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "程式碼過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "コードが大きすぎます。";
        return "Too big code.";
    }
    if (strcmp(msg, "Too many __fast parameters.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many __fast parameters.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiados parámetros __fast.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de paramètres __fast.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele __fast-Parameter.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppi parametri __fast.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλές παράμετροι __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много параметров __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形参过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 參數過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastパラメータが多すぎます。";
        return "Too many __fast parameters.";
    }
    if (strcmp(msg, "Too many accelerator locals.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many accelerator locals.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiadas variables locales del acelerador.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de variables locales d’accélérateur.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele lokale Beschleunigervariablen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppe variabili locali dell’acceleratore.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλές τοπικές μεταβλητές επιταχυντή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много локальных переменных ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "加速器局部变量过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "加速器區域變數過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータのローカル変数が多すぎます。";
        return "Too many accelerator locals.";
    }
    if (strcmp(msg, "Too many anonymous functions.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many anonymous functions.";
        if (strncmp(lang_code, "es", 2) == 0) return "Hay demasiadas funciones anónimas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de fonctions anonymes.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele anonyme Funktionen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ci sono troppe funzioni anonime.";
        if (strncmp(lang_code, "el", 2) == 0) return "Πάρα πολλές ανώνυμες συναρτήσεις.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много анонимных функций.";
        if (strncmp(lang_code, "zh", 2) == 0) return "匿名函数过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "匿名函式過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "無名関数が多すぎます。";
        return "Too many anonymous functions.";
    }
    if (strcmp(msg, "Too many capture groups.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many capture groups.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiados grupos de captura.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de groupes de capture.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Erfassungsgruppen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppi gruppi di cattura.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλές ομάδες καταγραφής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много групп захвата.";
        if (strncmp(lang_code, "zh", 2) == 0) return "捕获组过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "擷取群組過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キャプチャグループが多すぎます。";
        return "Too many capture groups.";
    }
    if (strcmp(msg, "Too many character classes.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many character classes.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiadas clases de caracteres.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de classes de caractères.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Zeichenklassen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppe classi di caratteri.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλές κλάσεις χαρακτήρων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много классов символов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字符类过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "字元類別過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "文字クラスが多すぎます。";
        return "Too many character classes.";
    }
    if (strcmp(msg, "Too many direct __fast call edges.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many direct __fast call edges.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiadas aristas de llamada directa a __fast.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop d’arêtes d’appel direct vers __fast.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele direkte __fast-Aufrufkanten.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppi archi di chiamata diretta a __fast.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλές ακμές άμεσης κλήσης __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много рёбер прямых вызовов __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "直接 __fast 调用边过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "直接 __fast 呼叫邊過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "直接__fast呼び出しのエッジが多すぎます。";
        return "Too many direct __fast call edges.";
    }
    if (strcmp(msg, "Too many function parameters.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many function parameters.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiados parámetros de función.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de paramètres de fonction.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Funktionsparameter.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppi parametri di funzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλές παράμετροι συνάρτησης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много параметров функции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "函数形参过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "函式參數過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数パラメータが多すぎます。";
        return "Too many function parameters.";
    }
    if (strcmp(msg, "Too many function prototypes in the require graph.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many function prototypes in the require graph.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiados prototipos de función en el grafo de require.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de prototypes de fonction dans le graphe require.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Funktionsprototypen im require-Graphen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppi prototipi di funzione nel grafo require.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλά πρωτότυπα συναρτήσεων στο γράφημα require.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много прототипов функций в графе require.";
        if (strncmp(lang_code, "zh", 2) == 0) return "require 图中的函数原型过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "require 圖中的函式原型過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "requireグラフ内の関数プロトタイプが多すぎます。";
        return "Too many function prototypes in the require graph.";
    }
    if (strcmp(msg, "Too many functions or function parameters.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many functions or function parameters.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiadas funciones o parámetros de función.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de fonctions ou de paramètres de fonction.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Funktionen oder Funktionsparameter.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppe funzioni o troppi parametri di funzione.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερβολικά πολλές συναρτήσεις ή παράμετροι συναρτήσεων.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много функций или параметров функций.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "函数或函数形参过多。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "函式或函式參數過多。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数または関数パラメータが多すぎます。\n";
        return "Too many functions or function parameters.\n";
    }
    if (strcmp(msg, "Too many functions.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many functions.";
        if (strncmp(lang_code, "es", 2) == 0) return "Hay demasiadas funciones.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de fonctions.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Funktionen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ci sono troppe funzioni.";
        if (strncmp(lang_code, "el", 2) == 0) return "Πάρα πολλές συναρτήσεις.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много функций.";
        if (strncmp(lang_code, "zh", 2) == 0) return "函数过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "函式過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数が多すぎます。";
        return "Too many functions.";
    }
    if (strcmp(msg, "Too many jumps.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many jumps.";
        if (strncmp(lang_code, "es", 2) == 0) return "Hay demasiados saltos.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de sauts.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Sprünge.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ci sono troppi salti.";
        if (strncmp(lang_code, "el", 2) == 0) return "Πάρα πολλά άλματα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много переходов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "跳转过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "跳轉過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ジャンプが多すぎます。";
        return "Too many jumps.";
    }
    if (strcmp(msg, "Too many local variables.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many local variables.";
        if (strncmp(lang_code, "es", 2) == 0) return "Hay demasiadas variables locales.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de variables locales.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele lokale Variablen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ci sono troppe variabili locali.";
        if (strncmp(lang_code, "el", 2) == 0) return "Πάρα πολλές τοπικές μεταβλητές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много локальных переменных.";
        if (strncmp(lang_code, "zh", 2) == 0) return "局部变量过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "區域變數過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "ローカル変数が多すぎます。";
        return "Too many local variables.";
    }
    if (strcmp(msg, "Too many parameters.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many parameters.";
        if (strncmp(lang_code, "es", 2) == 0) return "Hay demasiados parámetros.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de paramètres.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele Parameter.";
        if (strncmp(lang_code, "it", 2) == 0) return "Ci sono troppi parametri.";
        if (strncmp(lang_code, "el", 2) == 0) return "Πάρα πολλές παράμετροι.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много параметров.";
        if (strncmp(lang_code, "zh", 2) == 0) return "参数过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "參數過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "パラメータが多すぎます。";
        return "Too many parameters.";
    }
    if (strcmp(msg, "Too many pinned global variables.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many pinned global variables.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiadas variables globales fijadas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de variables globales epinglees.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele angepinnte globale Variablen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppe variabili globali bloccate.";
        if (strncmp(lang_code, "el", 2) == 0) return "Πάρα πολλές καρφιτσωμένες καθολικές μεταβλητές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много закрепленных глобальных переменных.";
        if (strncmp(lang_code, "zh", 2) == 0) return "固定全局变量过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "固定全域變數過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "固定されたグローバル変数が多すぎます。";
        return "Too many pinned global variables.";
    }
    if (strcmp(msg, "Too many pinned local variables.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Too many pinned local variables.";
        if (strncmp(lang_code, "es", 2) == 0) return "Demasiadas variables locales fijadas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Trop de variables locales epinglees.";
        if (strncmp(lang_code, "de", 2) == 0) return "Zu viele angepinnte lokale Variablen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Troppe variabili locali bloccate.";
        if (strncmp(lang_code, "el", 2) == 0) return "Πάρα πολλές καρφιτσωμένες τοπικές μεταβλητές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Слишком много закрепленных локальных переменных.";
        if (strncmp(lang_code, "zh", 2) == 0) return "固定局部变量过多。";
        if (strncmp(lang_code, "tw", 2) == 0) return "固定區域變數過多。";
        if (strncmp(lang_code, "ja", 2) == 0) return "固定されたローカル変数が多すぎます。";
        return "Too many pinned local variables.";
    }
    if (strcmp(msg, "Unexpected OpenGL ES finish result words.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unexpected OpenGL ES finish result words.";
        if (strncmp(lang_code, "es", 2) == 0) return "Palabras de resultado de finalización de OpenGL ES inesperadas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mots de résultat de fin OpenGL ES inattendus.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unerwartete OpenGL ES-Abschlussergebniswörter.";
        if (strncmp(lang_code, "it", 2) == 0) return "Parole del risultato di completamento OpenGL ES impreviste.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη αναμενόμενες λέξεις αποτελέσματος ολοκλήρωσης OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неожиданные слова результата завершения OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "意外的 OpenGL ES 完成结果字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非預期的 OpenGL ES 完成結果字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "予期しないOpenGL ES完了結果ワードです。";
        return "Unexpected OpenGL ES finish result words.";
    }
    if (strcmp(msg, "Unexpected OpenGL ES result words.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unexpected OpenGL ES result words.";
        if (strncmp(lang_code, "es", 2) == 0) return "Palabras de resultado de OpenGL ES inesperadas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mots de résultat OpenGL ES inattendus.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unerwartete OpenGL ES-Ergebniswörter.";
        if (strncmp(lang_code, "it", 2) == 0) return "Parole del risultato OpenGL ES impreviste.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη αναμενόμενες λέξεις αποτελέσματος OpenGL ES.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неожиданные слова результата OpenGL ES.";
        if (strncmp(lang_code, "zh", 2) == 0) return "意外的 OpenGL ES 结果字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非預期的 OpenGL ES 結果字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "予期しないOpenGL ES結果ワードです。";
        return "Unexpected OpenGL ES result words.";
    }
    if (strcmp(msg, "Unexpected Vulkan scalar-result destination.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unexpected Vulkan scalar-result destination.";
        if (strncmp(lang_code, "es", 2) == 0) return "Destino del resultado escalar de Vulkan inesperado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Destination du résultat scalaire Vulkan inattendue.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unerwartetes Ziel des skalaren Vulkan-Ergebnisses.";
        if (strncmp(lang_code, "it", 2) == 0) return "Destinazione del risultato scalare Vulkan imprevista.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη αναμενόμενος προορισμός βαθμωτού αποτελέσματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неожиданное назначение скалярного результата Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "意外的 Vulkan 标量结果目标。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非預期的 Vulkan 純量結果目的地。";
        if (strncmp(lang_code, "ja", 2) == 0) return "予期しないVulkanスカラー結果の格納先です。";
        return "Unexpected Vulkan scalar-result destination.";
    }
    if (strcmp(msg, "Unexpected Vulkan scalar-result identities.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unexpected Vulkan scalar-result identities.";
        if (strncmp(lang_code, "es", 2) == 0) return "Identidades del resultado escalar de Vulkan inesperadas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Identités du résultat scalaire Vulkan inattendues.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unerwartete Identitäten des skalaren Vulkan-Ergebnisses.";
        if (strncmp(lang_code, "it", 2) == 0) return "Identità del risultato scalare Vulkan impreviste.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη αναμενόμενες ταυτότητες βαθμωτού αποτελέσματος Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неожиданные идентификаторы скалярного результата Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "意外的 Vulkan 标量结果标识。";
        if (strncmp(lang_code, "tw", 2) == 0) return "非預期的 Vulkan 純量結果識別資料。";
        if (strncmp(lang_code, "ja", 2) == 0) return "予期しないVulkanスカラー結果のIDです。";
        return "Unexpected Vulkan scalar-result identities.";
    }
    if (strcmp(msg, "Unexpected end of file.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unexpected end of file.";
        if (strncmp(lang_code, "es", 2) == 0) return "Fin de archivo inesperado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Fin de fichier inattendue.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unerwartetes Dateiende.";
        if (strncmp(lang_code, "it", 2) == 0) return "Fine del file imprevista.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη αναμενόμενο τέλος αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неожиданный конец файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "文件意外结束。";
        if (strncmp(lang_code, "tw", 2) == 0) return "檔案意外結束。";
        if (strncmp(lang_code, "ja", 2) == 0) return "予期しないファイル終端です。";
        return "Unexpected end of file.";
    }
    if (strcmp(msg, "Unknown __fast numeric intrinsic.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unknown __fast numeric intrinsic.";
        if (strncmp(lang_code, "es", 2) == 0) return "Función intrínseca numérica __fast desconocida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Fonction intrinsèque numérique __fast inconnue.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unbekannte numerische intrinsische __fast-Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Funzione intrinseca numerica __fast sconosciuta.";
        if (strncmp(lang_code, "el", 2) == 0) return "Άγνωστη αριθμητική ενσωματωμένη συνάρτηση __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неизвестная числовая встроенная функция __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未知的 __fast 数值内置函数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未知的 __fast 數值內建函式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "不明な__fast数値組み込み関数です。";
        return "Unknown __fast numeric intrinsic.";
    }
    if (strcmp(msg, "Unknown error.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unknown error.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error desconocido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Erreur inconnue.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unbekannter Fehler.";
        if (strncmp(lang_code, "it", 2) == 0) return "Errore sconosciuto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Άγνωστο σφάλμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неизвестная ошибка.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未知错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未知錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "不明なエラーです。";
        return "Unknown error.";
    }
    if (strcmp(msg, "Unknown opcode %d at pc=%d.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unknown opcode %d at pc=%d.";
        if (strncmp(lang_code, "es", 2) == 0) return "Opcode desconocido %d en pc=%d.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Opcode inconnu %d à pc=%d.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unbekannter Opcode %d bei pc=%d.";
        if (strncmp(lang_code, "it", 2) == 0) return "Opcode sconosciuto %d in pc=%d.";
        if (strncmp(lang_code, "el", 2) == 0) return "Άγνωστο opcode %d στο pc=%d.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неизвестный код операции %d по адресу pc=%d.";
        if (strncmp(lang_code, "zh", 2) == 0) return "pc=%d 处存在未知操作码 %d。";
        if (strncmp(lang_code, "tw", 2) == 0) return "pc=%d 處存在未知操作碼 %d。";
        if (strncmp(lang_code, "ja", 2) == 0) return "pc=%dに不明なオペコード%dがあります。";
        return "Unknown opcode %d at pc=%d.";
    }
    if (strcmp(msg, "Unknown opcode.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unknown opcode.";
        if (strncmp(lang_code, "es", 2) == 0) return "Opcode desconocido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Opcode inconnu.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unbekannter Opcode.";
        if (strncmp(lang_code, "it", 2) == 0) return "Opcode sconosciuto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Άγνωστο opcode.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неизвестный код операции.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未知操作码。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未知操作碼。";
        if (strncmp(lang_code, "ja", 2) == 0) return "不明なオペコードです。";
        return "Unknown opcode.";
    }
    if (strcmp(msg, "Unknown option %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unknown option %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Opción desconocida %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Option inconnue : %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Unbekannte Option %s.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Opzione sconosciuta: %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Άγνωστη επιλογή %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неизвестная опция %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "未知选项 %s。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "未知選項 %s。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "不明なオプション%s\n";
        return "Unknown option %s.\n";
    }
    if (strcmp(msg, "Unknown type name '%s'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unknown type name '%s'.";
        if (strncmp(lang_code, "es", 2) == 0) return "Nombre de tipo desconocido '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nom de type inconnu '%s'.";
        if (strncmp(lang_code, "de", 2) == 0) return "Unbekannter Typname '%s'.";
        if (strncmp(lang_code, "it", 2) == 0) return "Nome di tipo sconosciuto '%s'.";
        if (strncmp(lang_code, "el", 2) == 0) return "Άγνωστο όνομα τύπου '%s'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неизвестное имя типа '%s'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未知类型名称 '%s'。";
        if (strncmp(lang_code, "tw", 2) == 0) return "未知型別名稱 '%s'。";
        if (strncmp(lang_code, "ja", 2) == 0) return "不明な型名'%s'です。";
        return "Unknown type name '%s'.";
    }
    if (strcmp(msg, "Unmatched parenthesis.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unmatched parenthesis.";
        if (strncmp(lang_code, "es", 2) == 0) return "Paréntesis sin pareja.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Parenthèse non appariée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht übereinstimmende Klammer.";
        if (strncmp(lang_code, "it", 2) == 0) return "Parentesi non corrispondente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη αντιστοιχισμένη παρένθεση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Непарная скобка.";
        if (strncmp(lang_code, "zh", 2) == 0) return "括号不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "括號不相符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "対応しない括弧があります。";
        return "Unmatched parenthesis.";
    }
    if (strcmp(msg, "Unsupported (?...) construct.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported (?...) construct.";
        if (strncmp(lang_code, "es", 2) == 0) return "Construcción (?...) no admitida.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Construction (?...) non prise en charge.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstütztes (?...)-Konstrukt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Costrutto (?...) non supportato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενη δομή (?...).";
        if (strncmp(lang_code, "ru", 2) == 0) return "Конструкция (?...) не поддерживается.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持 (?...) 构造。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援 (?...) 建構。";
        if (strncmp(lang_code, "ja", 2) == 0) return "(?...)構文はサポートされていません。";
        return "Unsupported (?...) construct.";
    }
    if (strcmp(msg, "Unsupported assignment target inside __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported assignment target inside __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "Destino de asignación no admitido dentro de __fast func.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Cible d’affectation non prise en charge dans __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstütztes Zuweisungsziel innerhalb von __fast func.";
        if (strncmp(lang_code, "it", 2) == 0) return "Destinazione dell’assegnazione non supportata all’interno di __fast func.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενος στόχος ανάθεσης μέσα σε __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемая цель присваивания внутри __fast func.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 内存在不支持的赋值目标。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 內存在不支援的指派目標。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func内の代入先はサポートされていません。";
        return "Unsupported assignment target inside __fast func.";
    }
    if (strcmp(msg, "Unsupported block in __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported block in __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "Bloque no admitido en __fast func.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Bloc non pris en charge dans __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstützter Block in __fast func.";
        if (strncmp(lang_code, "it", 2) == 0) return "Blocco non supportato in __fast func.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενο μπλοκ σε __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемый блок в __fast func.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 中存在不支持的块。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 中存在不支援的區塊。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func内のブロックはサポートされていません。";
        return "Unsupported block in __fast func.";
    }
    if (strcmp(msg, "Unsupported file mode.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported file mode.";
        if (strncmp(lang_code, "es", 2) == 0) return "Modo de archivo no admitido.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Mode de fichier non pris en charge.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstützter Dateimodus.";
        if (strncmp(lang_code, "it", 2) == 0) return "Modalità file non supportata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενη λειτουργία αρχείου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемый режим файла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持的文件模式。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援的檔案模式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないファイルモードです。";
        return "Unsupported file mode.";
    }
    if (strcmp(msg, "Unsupported instruction reached the Vulkan shader generator.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported instruction reached the Vulkan shader generator.";
        if (strncmp(lang_code, "es", 2) == 0) return "Una instrucción no admitida llegó al generador de shaders Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Une instruction non prise en charge a atteint le générateur de shaders Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Eine nicht unterstützte Anweisung hat den Vulkan-Shadergenerator erreicht.";
        if (strncmp(lang_code, "it", 2) == 0) return "Un’istruzione non supportata ha raggiunto il generatore di shader Vulkan.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενη εντολή έφτασε στη γεννήτρια shader Vulkan.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемая инструкция достигла генератора шейдеров Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 着色器生成器收到不支持的指令。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 著色器產生器收到不支援的指令。";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていない命令がVulkanシェーダージェネレータに到達しました。";
        return "Unsupported instruction reached the Vulkan shader generator.";
    }
    if (strcmp(msg, "Unsupported or malformed application version.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported or malformed application version.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Versión de aplicación no admitida o mal formada.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Version d’application non prise en charge ou incorrecte.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstützte oder fehlerhafte Anwendungsversion.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Versione dell’applicazione non supportata o non valida.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενη ή ακατάλληλα διαμορφωμένη έκδοση εφαρμογής.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемая или некорректная версия приложения.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "应用程序版本不受支持或格式错误。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "應用程式版本不受支援或格式錯誤。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないか不正なアプリケーションバージョンです。\n";
        return "Unsupported or malformed application version.\n";
    }
    if (strcmp(msg, "Unsupported or malformed application version: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported or malformed application version: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Versión de aplicación no admitida o mal formada: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Version d’application non prise en charge ou incorrecte : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstützte oder fehlerhafte Anwendungsversion: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Versione dell’applicazione non supportata o non valida: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενη ή ακατάλληλα διαμορφωμένη έκδοση εφαρμογής: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемая или некорректная версия приложения: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "应用程序版本不受支持或格式错误：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "應用程式版本不受支援或格式錯誤：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないか不正なアプリケーションバージョンです: %s";
        return "Unsupported or malformed application version: %s";
    }
    if (strcmp(msg, "Unsupported or malformed bytecode version.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported or malformed bytecode version.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Versión de bytecode no admitida o mal formada.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Version de bytecode non prise en charge ou incorrecte.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstützte oder fehlerhafte Bytecodeversion.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Versione del bytecode non supportata o non valida.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενη ή ακατάλληλα διαμορφωμένη έκδοση bytecode.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемая или некорректная версия байткода.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码版本不受支持或格式错误。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼版本不受支援或格式錯誤。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないか不正なバイトコードバージョンです。\n";
        return "Unsupported or malformed bytecode version.\n";
    }
    if (strcmp(msg, "Unsupported or malformed bytecode version: %s") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported or malformed bytecode version: %s";
        if (strncmp(lang_code, "es", 2) == 0) return "Versión de bytecode no admitida o mal formada: %s";
        if (strncmp(lang_code, "fr", 2) == 0) return "Version de bytecode non prise en charge ou incorrecte : %s";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstützte oder fehlerhafte Bytecodeversion: %s";
        if (strncmp(lang_code, "it", 2) == 0) return "Versione del bytecode non supportata o non valida: %s";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενη ή ακατάλληλα διαμορφωμένη έκδοση bytecode: %s";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемая или некорректная версия байткода: %s";
        if (strncmp(lang_code, "zh", 2) == 0) return "字节码版本不受支持或格式错误：%s";
        if (strncmp(lang_code, "tw", 2) == 0) return "位元組碼版本不受支援或格式錯誤：%s";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないか不正なバイトコードバージョンです: %s";
        return "Unsupported or malformed bytecode version: %s";
    }
    if (strcmp(msg, "Unsupported packed element type in __fast func.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unsupported packed element type in __fast func.";
        if (strncmp(lang_code, "es", 2) == 0) return "Tipo de elemento packed no admitido en __fast func.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Type d’élément packed non pris en charge dans __fast func.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht unterstützter packed-Elementtyp in __fast func.";
        if (strncmp(lang_code, "it", 2) == 0) return "Tipo di elemento packed non supportato in __fast func.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη υποστηριζόμενος τύπος στοιχείου packed σε __fast func.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неподдерживаемый тип элемента packed в __fast func.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast func 中存在不支持的 packed 元素类型。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast func 中存在不支援的 packed 元素型別。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast func内のpacked要素型はサポートされていません。";
        return "Unsupported packed element type in __fast func.";
    }
    if (strcmp(msg, "Unterminated character class.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Unterminated character class.";
        if (strncmp(lang_code, "es", 2) == 0) return "Clase de caracteres sin terminar.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Classe de caractères non terminée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Nicht abgeschlossene Zeichenklasse.";
        if (strncmp(lang_code, "it", 2) == 0) return "Classe di caratteri non terminata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Μη τερματισμένη κλάση χαρακτήρων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Незавершённый класс символов.";
        if (strncmp(lang_code, "zh", 2) == 0) return "字符类未终止。";
        if (strncmp(lang_code, "tw", 2) == 0) return "字元類別未終止。";
        if (strncmp(lang_code, "ja", 2) == 0) return "文字クラスが終了していません。";
        return "Unterminated character class.";
    }
    if (strcmp(msg, "Usage\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Usage\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Uso\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Utilisation\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Verwendung\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Utilizzo\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Χρήση\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Использование\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "用法\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "用法\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "使い方\n";
        return "Usage\n";
    }
    if (strcmp(msg, "Value for key %s is not a dictionary.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not a dictionary.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es un diccionario.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas un dictionnaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist kein Dictionary.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è un dizionario.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι λεξικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является словарем.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是字典。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是字典。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が辞書ではありません。";
        return "Value for key %s is not a dictionary.";
    }
    if (strcmp(msg, "Value for key %s is not a double.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not a double.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es un double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas un double.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist kein Double.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è un double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是 double。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是 double。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が double ではありません。";
        return "Value for key %s is not a double.";
    }
    if (strcmp(msg, "Value for key %s is not a float.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not a float.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es un número decimal.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas un nombre à virgule flottante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist keine Gleitkommazahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è un numero decimale.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι δεκαδικός αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является числом с плавающей точкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是浮点数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是浮點數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が浮動小数点数ではありません。";
        return "Value for key %s is not a float.";
    }
    if (strcmp(msg, "Value for key %s is not a function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not a function.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es una función.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas une fonction.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist keine Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è una funzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι συνάρτηση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является функцией.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是函数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是函式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が関数ではありません。";
        return "Value for key %s is not a function.";
    }
    if (strcmp(msg, "Value for key %s is not a long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not a long.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es un long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas un long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist kein Long.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è un long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是 long。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是 long。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が long ではありません。";
        return "Value for key %s is not a long.";
    }
    if (strcmp(msg, "Value for key %s is not a string.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not a string.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es una cadena.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas une chaîne de caractères.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist keine Zeichenkette.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è una stringa.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι συμβολοσειρά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является строкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是字符串。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是字串。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が文字列ではありません。";
        return "Value for key %s is not a string.";
    }
    if (strcmp(msg, "Value for key %s is not an array.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not an array.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es una matriz.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas un tableau.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist kein Array.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è un array.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι πίνακας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является массивом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是数组。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是陣列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が配列ではありません。";
        return "Value for key %s is not an array.";
    }
    if (strcmp(msg, "Value for key %s is not an int.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value for key %s is not an int.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor de la clave %s no es un int.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur associée à la clé %s n'est pas un int.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert für den Schlüssel %s ist kein Int.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore per la chiave %s non è un int.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή για το κλειδί %s δεν είναι int.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение для ключа %s не является int.";
        if (strncmp(lang_code, "zh", 2) == 0) return "键 %s 对应的值不是 int。";
        if (strncmp(lang_code, "tw", 2) == 0) return "鍵 %s 對應的值不是 int。";
        if (strncmp(lang_code, "ja", 2) == 0) return "キー %s に対応する値が int ではありません。";
        return "Value for key %s is not an int.";
    }
    if (strcmp(msg, "Value is not a float.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a float.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un número decimal.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas un nombre à virgule flottante.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist keine Gleitkommazahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un numero decimale.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι δεκαδικός αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является числом с плавающей точкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是浮点数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是浮點數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が浮動小数点数ではありません。";
        return "Value is not a float.";
    }
    if (strcmp(msg, "Value is not a function.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a function.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es una función.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas une fonction.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist keine Funktion.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è una funzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι συνάρτηση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является функцией.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是函数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是函式。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が関数ではありません。";
        return "Value is not a function.";
    }
    if (strcmp(msg, "Value is not a long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a long.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas un long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist kein Long.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是 long。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是 long。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が long ではありません。";
        return "Value is not a long.";
    }
    if (strcmp(msg, "Value is not a number or a string.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a number or a string.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un número ni un string.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est ni un nombre ni une chaîne.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist weder eine Zahl noch ein String.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è né un numero né un string.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι ούτε αριθμός ούτε συμβολοσειρά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является ни числом, ни строкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是数字或字符串。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是數字或字串。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が数か文字列ではありません。";
        return "Value is not a number or a string.";
    }
    if (strcmp(msg, "Value is not a number.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a number.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un número.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas un nombre.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist keine Zahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un numero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是数字。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是數字。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が数ではありません。";
        return "Value is not a number.";
    }
    if (strcmp(msg, "Value is not a packed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a packed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un packed.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n’est pas un packed.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert ist kein packed.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un packed.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι packed.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является packed.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是 packed。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是 packed。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値はpackedではありません。";
        return "Value is not a packed.";
    }
    if (strcmp(msg, "Value is not a string, an array, or a dictionary.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a string, an array, or a dictionary.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un string, un array ni un diccionario.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est ni une chaîne, ni un tableau, ni un dictionnaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist weder ein String, noch ein Array noch ein Dictionary.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è né un string, né un array né un dizionario.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι ούτε συμβολοσειρά, ούτε πίνακας, ούτε λεξικό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является ни строкой, ни массивом, ни словарем.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是字符串、数组或字典。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是字串、陣列或字典。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が文字列、配列、辞書ではありません。";
        return "Value is not a string, an array, or a dictionary.";
    }
    if (strcmp(msg, "Value is not a string.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not a string.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un string.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas une chaîne de caractères.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist kein String.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un string.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι συμβολοσειρά.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является строкой.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是字符串。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是字串。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が文字列ではありません。";
        return "Value is not a string.";
    }
    if (strcmp(msg, "Value is not an array.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not an array.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un array.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas un tableau.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist kein Array.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un array.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι πίνακας.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является массивом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是数组。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是陣列。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が配列ではありません。";
        return "Value is not an array.";
    }
    if (strcmp(msg, "Value is not an int.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not an int.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un int.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas un int.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist kein Int.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un int.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι int.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является int.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是 int。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是 int。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が int ではありません。";
        return "Value is not an int.";
    }
    if (strcmp(msg, "Value is not an integer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is not an integer.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor no es un entero.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur n'est pas un entier.";
        if (strncmp(lang_code, "de", 2) == 0) return "Wert ist keine ganze Zahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore non è un intero.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή δεν είναι ακέραιος αριθμός.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение не является целым числом.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值不是整数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值不是整數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値が整数ではありません。";
        return "Value is not an integer.";
    }
    if (strcmp(msg, "Value is out of range for int.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is out of range for int.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor está fuera de rango para int.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur est hors limites pour int.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert liegt außerhalb des Bereichs für int.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore è fuori intervallo per int.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή είναι εκτός ορίων για int.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение вне диапазона int.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值超出 int 的范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值超出 int 的範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値がintの範囲外です。";
        return "Value is out of range for int.";
    }
    if (strcmp(msg, "Value is out of range for long.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Value is out of range for long.";
        if (strncmp(lang_code, "es", 2) == 0) return "El valor está fuera de rango para long.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La valeur est hors limites pour long.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Wert liegt außerhalb des Bereichs für long.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il valore è fuori intervallo per long.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η τιμή είναι εκτός ορίων για long.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Значение вне диапазона long.";
        if (strncmp(lang_code, "zh", 2) == 0) return "值超出 long 的范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "值超出 long 的範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "値がlongの範囲外です。";
        return "Value is out of range for long.";
    }
    if (strcmp(msg, "Variable '%s' is already declared in this scope.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Variable '%s' is already declared in this scope.";
        if (strncmp(lang_code, "es", 2) == 0) return "La variable '%s' ya está declarada en este ámbito.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La variable '%s' est déjà déclarée dans cette portée.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Variable '%s' ist in diesem Gültigkeitsbereich bereits deklariert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La variabile '%s' è già dichiarata in questo ambito.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μεταβλητή '%s' έχει ήδη δηλωθεί σε αυτή την εμβέλεια.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переменная '%s' уже объявлена в этой области видимости.";
        if (strncmp(lang_code, "zh", 2) == 0) return "变量 '%s' 已在此作用域中声明。";
        if (strncmp(lang_code, "tw", 2) == 0) return "變數 '%s' 已在此範圍中宣告。";
        if (strncmp(lang_code, "ja", 2) == 0) return "変数'%s'はこのスコープですでに宣言されています。";
        return "Variable '%s' is already declared in this scope.";
    }
    if (strcmp(msg, "Variable '%s' is used before its declaration.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Variable '%s' is used before its declaration.";
        if (strncmp(lang_code, "es", 2) == 0) return "La variable '%s' se usa antes de su declaración.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La variable '%s' est utilisée avant sa déclaration.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Variable '%s' wird vor ihrer Deklaration verwendet.";
        if (strncmp(lang_code, "it", 2) == 0) return "La variabile '%s' viene usata prima della sua dichiarazione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η μεταβλητή '%s' χρησιμοποιείται πριν από τη δήλωσή της.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переменная '%s' используется до её объявления.";
        if (strncmp(lang_code, "zh", 2) == 0) return "变量 '%s' 在声明前被使用。";
        if (strncmp(lang_code, "tw", 2) == 0) return "變數 '%s' 在宣告前被使用。";
        if (strncmp(lang_code, "ja", 2) == 0) return "変数'%s'が宣言前に使用されています。";
        return "Variable '%s' is used before its declaration.";
    }
    if (strcmp(msg, "Version %s\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Version %s\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Versión %s\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Version %s\n";
        if (strncmp(lang_code, "de", 2) == 0) return "Version %s\n";
        if (strncmp(lang_code, "it", 2) == 0) return "Versione %s\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Έκδοση %s\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Версия %s\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "版本 %s\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "版本 %s\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "バージョン %s\n";
        return "Version %s\n";
    }
    if (strcmp(msg, "Vulkan 1.2 is required, but the loader only exposes Vulkan 1.0.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan 1.2 is required, but the loader only exposes Vulkan 1.0.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se requiere Vulkan 1.2, pero el cargador solo expone Vulkan 1.0.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Vulkan 1.2 est requis, mais le chargeur n’expose que Vulkan 1.0.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan 1.2 ist erforderlich, aber der Loader stellt nur Vulkan 1.0 bereit.";
        if (strncmp(lang_code, "it", 2) == 0) return "È richiesto Vulkan 1.2, ma il loader espone solo Vulkan 1.0.";
        if (strncmp(lang_code, "el", 2) == 0) return "Απαιτείται Vulkan 1.2, αλλά ο φορτωτής εκθέτει μόνο Vulkan 1.0.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Требуется Vulkan 1.2, но загрузчик предоставляет только Vulkan 1.0.";
        if (strncmp(lang_code, "zh", 2) == 0) return "需要 Vulkan 1.2，但加载器仅公开 Vulkan 1.0。";
        if (strncmp(lang_code, "tw", 2) == 0) return "需要 Vulkan 1.2，但載入器僅公開 Vulkan 1.0。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan 1.2が必要ですが、ローダーはVulkan 1.0のみを公開しています。";
        return "Vulkan 1.2 is required, but the loader only exposes Vulkan 1.0.";
    }
    if (strcmp(msg, "Vulkan 1.2 or newer is required.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan 1.2 or newer is required.";
        if (strncmp(lang_code, "es", 2) == 0) return "Se requiere Vulkan 1.2 o posterior.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Vulkan 1.2 ou une version ultérieure est requis.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan 1.2 oder neuer ist erforderlich.";
        if (strncmp(lang_code, "it", 2) == 0) return "È richiesto Vulkan 1.2 o versione successiva.";
        if (strncmp(lang_code, "el", 2) == 0) return "Απαιτείται Vulkan 1.2 ή νεότερο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Требуется Vulkan 1.2 или новее.";
        if (strncmp(lang_code, "zh", 2) == 0) return "需要 Vulkan 1.2 或更高版本。";
        if (strncmp(lang_code, "tw", 2) == 0) return "需要 Vulkan 1.2 或更新版本。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan 1.2以降が必要です。";
        return "Vulkan 1.2 or newer is required.";
    }
    if (strcmp(msg, "Vulkan accelerator submission failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan accelerator submission failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falló el envío del acelerador Vulkan.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de la soumission de l’accélérateur Vulkan.";
        if (strncmp(lang_code, "de", 2) == 0) return "Übermittlung an den Vulkan-Beschleuniger fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "Invio dell’acceleratore Vulkan non riuscito.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η υποβολή επιταχυντή Vulkan απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Не удалось отправить задание ускорителю Vulkan.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 加速器提交失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 加速器提交失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanアクセラレータのサブミットに失敗しました。";
        return "Vulkan accelerator submission failed.";
    }
    if (strcmp(msg, "Vulkan active kernel has an inactive data buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan active kernel has an inactive data buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "El kernel activo de Vulkan tiene un búfer de datos inactivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le kernel Vulkan actif possède un tampon de données inactif.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der aktive Vulkan-Kernel hat einen inaktiven Datenpuffer.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il kernel Vulkan attivo ha un buffer dati inattivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο ενεργός πυρήνας Vulkan έχει ανενεργή ενδιάμεση μνήμη δεδομένων.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Активное ядро Vulkan имеет неактивный буфер данных.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 活动内核包含非活动数据缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 使用中核心包含非使用中資料緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクティブなVulkanカーネルに非アクティブなデータバッファがあります。";
        return "Vulkan active kernel has an inactive data buffer.";
    }
    if (strcmp(msg, "Vulkan buffer exceeds the device storage-buffer limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan buffer exceeds the device storage-buffer limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El búfer de Vulkan supera el límite de búferes de almacenamiento del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le tampon Vulkan dépasse la limite de tampons de stockage du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Puffer überschreitet die Speicherpuffergrenze des Geräts.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il buffer Vulkan supera il limite dei buffer di archiviazione del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ενδιάμεση μνήμη Vulkan υπερβαίνει το όριο ενδιάμεσων μνημών αποθήκευσης της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Буфер Vulkan превышает предел буфера хранилища устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 缓冲区超过设备存储缓冲区限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 緩衝區超過裝置儲存緩衝區限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanバッファがデバイスのストレージバッファ上限を超えています。";
        return "Vulkan buffer exceeds the device storage-buffer limit.";
    }
    if (strcmp(msg, "Vulkan buffer extent is inconsistent.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan buffer extent is inconsistent.";
        if (strncmp(lang_code, "es", 2) == 0) return "La extensión del búfer de Vulkan es incoherente.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’étendue du tampon Vulkan est incohérente.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Ausdehnung des Vulkan-Puffers ist inkonsistent.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’estensione del buffer Vulkan non è coerente.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έκταση της ενδιάμεσης μνήμης Vulkan δεν είναι συνεπής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер измерения буфера Vulkan несогласован.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 缓冲区维度大小不一致。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 緩衝區維度大小不一致。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanバッファの次元サイズが一貫していません。";
        return "Vulkan buffer extent is inconsistent.";
    }
    if (strcmp(msg, "Vulkan buffer metadata changed after planning.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan buffer metadata changed after planning.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los metadatos del búfer de Vulkan cambiaron tras la planificación.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les métadonnées du tampon Vulkan ont changé après la planification.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Metadaten des Vulkan-Puffers wurden nach der Planung geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "I metadati del buffer Vulkan sono cambiati dopo la pianificazione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Τα μεταδεδομένα της ενδιάμεσης μνήμης Vulkan άλλαξαν μετά τον σχεδιασμό.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Метаданные буфера Vulkan изменились после планирования.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 缓冲区元数据在规划后发生了变化。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 緩衝區中繼資料在規劃後發生變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanバッファのメタデータが計画後に変更されました。";
        return "Vulkan buffer metadata changed after planning.";
    }
    if (strcmp(msg, "Vulkan buffer table does not match the prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan buffer table does not match the prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de búferes de Vulkan no coincide con el programa preparado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des tampons Vulkan ne correspond pas au programme préparé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Vulkan-Puffertabelle stimmt nicht mit dem vorbereiteten Programm überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei buffer Vulkan non corrisponde al programma preparato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας ενδιάμεσων μνημών Vulkan δεν αντιστοιχεί στο προετοιμασμένο πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица буферов Vulkan не соответствует подготовленной программе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 缓冲区表与已准备的程序不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 緩衝區表與已準備的程式不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanバッファテーブルが準備済みのプログラムと一致しません。";
        return "Vulkan buffer table does not match the prepared program.";
    }
    if (strcmp(msg, "Vulkan device '%s' was not found.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan device '%s' was not found.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se encontró el dispositivo Vulkan '%s'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le périphérique Vulkan '%s' est introuvable.";
        if (strncmp(lang_code, "de", 2) == 0) return "Das Vulkan-Gerät '%s' wurde nicht gefunden.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispositivo Vulkan '%s' non è stato trovato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συσκευή Vulkan '%s' δεν βρέθηκε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Устройство Vulkan '%s' не найдено.";
        if (strncmp(lang_code, "zh", 2) == 0) return "未找到 Vulkan 设备 '%s'。";
        if (strncmp(lang_code, "tw", 2) == 0) return "找不到 Vulkan 裝置 '%s'。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanデバイス'%s'が見つかりません。";
        return "Vulkan device '%s' was not found.";
    }
    if (strcmp(msg, "Vulkan device name '%s' is ambiguous.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan device name '%s' is ambiguous.";
        if (strncmp(lang_code, "es", 2) == 0) return "El nombre de dispositivo Vulkan '%s' es ambiguo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le nom de périphérique Vulkan '%s' est ambigu.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Name des Vulkan-Geräts '%s' ist mehrdeutig.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il nome del dispositivo Vulkan '%s' è ambiguo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το όνομα συσκευής Vulkan '%s' είναι διφορούμενο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Имя устройства Vulkan '%s' неоднозначно.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 设备名称 '%s' 有歧义。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 裝置名稱 '%s' 有歧義。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanデバイス名'%s'が曖昧です。";
        return "Vulkan device name '%s' is ambiguous.";
    }
    if (strcmp(msg, "Vulkan dispatch exceeds the device workgroup-count limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan dispatch exceeds the device workgroup-count limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El despacho de Vulkan supera el límite de cantidad de grupos de trabajo del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le lancement Vulkan dépasse la limite du nombre de groupes de travail du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Dispatch überschreitet die Arbeitsgruppenanzahlgrenze des Geräts.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il dispatch Vulkan supera il limite del numero di gruppi di lavoro del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η αποστολή Vulkan υπερβαίνει το όριο πλήθους ομάδων εργασίας της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диспетчеризация Vulkan превышает предел количества рабочих групп устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 调度超过设备工作组数量限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 調度超過裝置工作群組數量限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスパッチがデバイスのワークグループ数上限を超えています。";
        return "Vulkan dispatch exceeds the device workgroup-count limit.";
    }
    if (strcmp(msg, "Vulkan dispatch range changed after execution creation.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan dispatch range changed after execution creation.";
        if (strncmp(lang_code, "es", 2) == 0) return "El rango de despacho de Vulkan cambió tras crear la ejecución.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage de lancement Vulkan a changé après la création de l’exécution.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Dispatchbereich wurde nach Erstellung der Ausführung geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo di dispatch Vulkan è cambiato dopo la creazione dell’esecuzione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αποστολής Vulkan άλλαξε μετά τη δημιουργία της εκτέλεσης.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон диспетчеризации Vulkan изменился после создания выполнения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 调度范围在创建执行后发生了变化。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 調度範圍在建立執行後發生變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanディスパッチ範囲が実行の作成後に変更されました。";
        return "Vulkan dispatch range changed after execution creation.";
    }
    if (strcmp(msg, "Vulkan dispatch range is missing from the scalar block.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan dispatch range is missing from the scalar block.";
        if (strncmp(lang_code, "es", 2) == 0) return "Falta el rango de despacho de Vulkan en el bloque escalar.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La plage de lancement Vulkan est absente du bloc scalaire.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Dispatchbereich fehlt im Skalarblock.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’intervallo di dispatch Vulkan manca nel blocco scalare.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το εύρος αποστολής Vulkan λείπει από το βαθμωτό μπλοκ.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Диапазон диспетчеризации Vulkan отсутствует в скалярном блоке.";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量块中缺少 Vulkan 调度范围。";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量區塊中缺少 Vulkan 調度範圍。";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラーブロックにVulkanディスパッチ範囲がありません。";
        return "Vulkan dispatch range is missing from the scalar block.";
    }
    if (strcmp(msg, "Vulkan execution has no recording command buffer.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan execution has no recording command buffer.";
        if (strncmp(lang_code, "es", 2) == 0) return "La ejecución de Vulkan no tiene ningún búfer de comandos en grabación.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’exécution Vulkan n’a aucun tampon de commandes en cours d’enregistrement.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Vulkan-Ausführung hat keinen Befehlspuffer im Aufzeichnungszustand.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’esecuzione Vulkan non ha alcun buffer dei comandi in registrazione.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η εκτέλεση Vulkan δεν έχει ενδιάμεση μνήμη εντολών σε καταγραφή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выполнение Vulkan не имеет записываемого буфера команд.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 执行没有正在记录的命令缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 執行沒有正在記錄的命令緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan実行に記録中のコマンドバッファがありません。";
        return "Vulkan execution has no recording command buffer.";
    }
    if (strcmp(msg, "Vulkan execution was already submitted.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan execution was already submitted.";
        if (strncmp(lang_code, "es", 2) == 0) return "La ejecución de Vulkan ya fue enviada.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’exécution Vulkan a déjà été soumise.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Vulkan-Ausführung wurde bereits übermittelt.";
        if (strncmp(lang_code, "it", 2) == 0) return "L’esecuzione Vulkan è già stata inviata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η εκτέλεση Vulkan έχει ήδη υποβληθεί.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Выполнение Vulkan уже отправлено.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 执行已提交。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 執行已提交。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan実行はすでにサブミットされています。";
        return "Vulkan execution was already submitted.";
    }
    if (strcmp(msg, "Vulkan finish buffer table changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan finish buffer table changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de búferes de finalización de Vulkan cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des tampons de fin Vulkan a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Vulkan-Abschlusspuffertabelle wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei buffer di completamento Vulkan è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας ενδιάμεσων μνημών ολοκλήρωσης Vulkan άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица буферов завершения Vulkan изменилась.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 完成缓冲区表已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 完成緩衝區表已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan完了バッファテーブルが変更されました。";
        return "Vulkan finish buffer table changed.";
    }
    if (strcmp(msg, "Vulkan kernels were dispatched out of order.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan kernels were dispatched out of order.";
        if (strncmp(lang_code, "es", 2) == 0) return "Los kernels de Vulkan se despacharon fuera de orden.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les kernels Vulkan ont été lancés dans le désordre.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Kernel wurden in falscher Reihenfolge ausgeführt.";
        if (strncmp(lang_code, "it", 2) == 0) return "I kernel Vulkan sono stati sottoposti a dispatch fuori ordine.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι πυρήνες Vulkan απεστάλησαν εκτός σειράς.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ядра Vulkan были диспетчеризованы в неверном порядке.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 内核的调度顺序错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 核心的調度順序錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanカーネルが順序どおりにディスパッチされませんでした。";
        return "Vulkan kernels were dispatched out of order.";
    }
    if (strcmp(msg, "Vulkan output transfer plan changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan output transfer plan changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "El plan de transferencia de salida de Vulkan cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le plan de transfert de sortie Vulkan a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Ausgabeübertragungsplan wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il piano di trasferimento dell'output Vulkan è cambiato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το σχέδιο μεταφοράς εξόδου Vulkan άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "План передачи вывода Vulkan изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 输出传输计划已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 輸出傳輸計畫已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan出力転送プランが変更されました。";
        return "Vulkan output transfer plan changed.";
    }
    if (strcmp(msg, "Vulkan output was requested before completion.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan output was requested before completion.";
        if (strncmp(lang_code, "es", 2) == 0) return "La salida de Vulkan se solicitó antes de finalizar.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La sortie Vulkan a été demandée avant la fin.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Vulkan-Ausgabe wurde vor Abschluss angefordert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'output Vulkan è stato richiesto prima del completamento.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έξοδος Vulkan ζητήθηκε πριν από την ολοκλήρωση.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вывод Vulkan был запрошен до завершения.";
        if (strncmp(lang_code, "zh", 2) == 0) return "在完成之前请求了 Vulkan 输出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "在完成之前要求了 Vulkan 輸出。";
        if (strncmp(lang_code, "ja", 2) == 0) return "完了前にVulkan出力が要求されました。";
        return "Vulkan output was requested before completion.";
    }
    if (strcmp(msg, "Vulkan received an empty active dispatch.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan received an empty active dispatch.";
        if (strncmp(lang_code, "es", 2) == 0) return "Vulkan recibió un despacho activo vacío.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Vulkan a reçu un lancement actif vide.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan hat einen leeren aktiven Dispatch erhalten.";
        if (strncmp(lang_code, "it", 2) == 0) return "Vulkan ha ricevuto un dispatch attivo vuoto.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το Vulkan έλαβε μια κενή ενεργή αποστολή.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Vulkan получил пустую активную диспетчеризацию.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 收到空的活动调度。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 收到空的作用中調度。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanが空のアクティブなディスパッチを受け取りました。";
        return "Vulkan received an empty active dispatch.";
    }
    if (strcmp(msg, "Vulkan result block does not match the prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan result block does not match the prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque de resultados de Vulkan no coincide con el programa preparado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc de résultats Vulkan ne correspond pas au programme préparé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Ergebnisblock stimmt nicht mit dem vorbereiteten Programm überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco dei risultati Vulkan non corrisponde al programma preparato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μπλοκ αποτελεσμάτων Vulkan δεν αντιστοιχεί στο προετοιμασμένο πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Блок результатов Vulkan не соответствует подготовленной программе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 结果块与准备的程序不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 結果區塊與準備的程式不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan結果ブロックが準備済みプログラムと一致しません。";
        return "Vulkan result block does not match the prepared program.";
    }
    if (strcmp(msg, "Vulkan result block exceeds the device storage-buffer limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan result block exceeds the device storage-buffer limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque de resultados de Vulkan supera el límite del búfer de almacenamiento del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc de résultats Vulkan dépasse la limite du tampon de stockage du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Ergebnisblock überschreitet die Grenze des Gerätespeicherpuffers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco dei risultati Vulkan supera il limite del buffer di archiviazione del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μπλοκ αποτελεσμάτων Vulkan υπερβαίνει το όριο της ενδιάμεσης μνήμης αποθήκευσης της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Блок результатов Vulkan превышает предел буфера хранилища устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 结果块超出设备存储缓冲区限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 結果區塊超出裝置儲存緩衝區限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkan結果ブロックがデバイスストレージバッファの上限を超えています。";
        return "Vulkan result block exceeds the device storage-buffer limit.";
    }
    if (strcmp(msg, "Vulkan scalar block does not match the prepared program.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan scalar block does not match the prepared program.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque escalar de Vulkan no coincide con el programa preparado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc scalaire Vulkan ne correspond pas au programme préparé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Skalarblock stimmt nicht mit dem vorbereiteten Programm überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco scalare Vulkan non corrisponde al programma preparato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το βαθμωτό μπλοκ Vulkan δεν αντιστοιχεί στο προετοιμασμένο πρόγραμμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Скалярный блок Vulkan не соответствует подготовленной программе.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 标量块与准备的程序不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 純量區塊與準備的程式不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラーブロックが準備済みプログラムと一致しません。";
        return "Vulkan scalar block does not match the prepared program.";
    }
    if (strcmp(msg, "Vulkan scalar block exceeds the device storage-buffer limit.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan scalar block exceeds the device storage-buffer limit.";
        if (strncmp(lang_code, "es", 2) == 0) return "El bloque escalar de Vulkan supera el límite del búfer de almacenamiento del dispositivo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Le bloc scalaire Vulkan dépasse la limite du tampon de stockage du périphérique.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Skalarblock überschreitet die Grenze des Gerätespeicherpuffers.";
        if (strncmp(lang_code, "it", 2) == 0) return "Il blocco scalare Vulkan supera il limite del buffer di archiviazione del dispositivo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το βαθμωτό μπλοκ Vulkan υπερβαίνει το όριο της ενδιάμεσης μνήμης αποθήκευσης της συσκευής.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Скалярный блок Vulkan превышает предел буфера хранилища устройства.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 标量块超出设备存储缓冲区限制。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 純量區塊超出裝置儲存緩衝區限制。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラーブロックがデバイスストレージバッファの上限を超えています。";
        return "Vulkan scalar block exceeds the device storage-buffer limit.";
    }
    if (strcmp(msg, "Vulkan scalar-result identity changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan scalar-result identity changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La identidad del resultado escalar de Vulkan cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’identité du résultat scalaire Vulkan a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Identität des skalaren Vulkan-Ergebnisses wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'identità del risultato scalare Vulkan è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η ταυτότητα του βαθμωτού αποτελέσματος Vulkan άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Идентификатор скалярного результата Vulkan изменился.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 标量结果标识已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 純量結果識別已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラー結果の識別情報が変更されました。";
        return "Vulkan scalar-result identity changed.";
    }
    if (strcmp(msg, "Vulkan scalar-result table changed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan scalar-result table changed.";
        if (strncmp(lang_code, "es", 2) == 0) return "La tabla de resultados escalares de Vulkan cambió.";
        if (strncmp(lang_code, "fr", 2) == 0) return "La table des résultats scalaires Vulkan a changé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Tabelle der skalaren Vulkan-Ergebnisse wurde geändert.";
        if (strncmp(lang_code, "it", 2) == 0) return "La tabella dei risultati scalari Vulkan è cambiata.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο πίνακας βαθμωτών αποτελεσμάτων Vulkan άλλαξε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Таблица скалярных результатов Vulkan изменилась.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 标量结果表已更改。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 純量結果表已變更。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanスカラー結果テーブルが変更されました。";
        return "Vulkan scalar-result table changed.";
    }
    if (strcmp(msg, "Vulkan shader assembler returned malformed SPIR-V.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan shader assembler returned malformed SPIR-V.";
        if (strncmp(lang_code, "es", 2) == 0) return "El ensamblador de shaders Vulkan devolvió SPIR-V con formato incorrecto.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’assembleur de shaders Vulkan a renvoyé du SPIR-V mal formé.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Shader-Assembler hat fehlerhaftes SPIR-V zurückgegeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'assembler degli shader Vulkan ha restituito SPIR-V non valido.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο assembler shader Vulkan επέστρεψε κακοσχηματισμένο SPIR-V.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ассемблер шейдеров Vulkan вернул некорректный SPIR-V.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 着色器汇编器返回了格式错误的 SPIR-V。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 著色器組譯器傳回了格式錯誤的 SPIR-V。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanシェーダーアセンブラが不正なSPIR-Vを返しました。";
        return "Vulkan shader assembler returned malformed SPIR-V.";
    }
    if (strcmp(msg, "Vulkan shader assembler returned no result.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan shader assembler returned no result.";
        if (strncmp(lang_code, "es", 2) == 0) return "El ensamblador de shaders Vulkan no devolvió ningún resultado.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’assembleur de shaders Vulkan n’a renvoyé aucun résultat.";
        if (strncmp(lang_code, "de", 2) == 0) return "Der Vulkan-Shader-Assembler hat kein Ergebnis zurückgegeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'assembler degli shader Vulkan non ha restituito alcun risultato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Ο assembler shader Vulkan δεν επέστρεψε αποτέλεσμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Ассемблер шейдеров Vulkan не вернул результат.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 着色器汇编器未返回结果。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 著色器組譯器未傳回結果。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanシェーダーアセンブラから結果が返されませんでした。";
        return "Vulkan shader assembler returned no result.";
    }
    if (strcmp(msg, "Vulkan shader assembly failed:\x20") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan shader assembly failed:\x20";
        if (strncmp(lang_code, "es", 2) == 0) return "Error al ensamblar el shader Vulkan:\x20";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de l’assemblage du shader Vulkan :\x20";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan-Shader-Assemblierung fehlgeschlagen:\x20";
        if (strncmp(lang_code, "it", 2) == 0) return "Assemblaggio dello shader Vulkan non riuscito:\x20";
        if (strncmp(lang_code, "el", 2) == 0) return "Η συναρμολόγηση του shader Vulkan απέτυχε:\x20";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой сборки шейдера Vulkan:\x20";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 着色器汇编失败：\x20";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 著色器組譯失敗：\x20";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanシェーダーのアセンブルに失敗しました:\x20";
        return "Vulkan shader assembly failed:\x20";
    }
    if (strcmp(msg, "Vulkan supports only 32-bit accelerator buffers.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Vulkan supports only 32-bit accelerator buffers.";
        if (strncmp(lang_code, "es", 2) == 0) return "Vulkan solo admite búferes de acelerador de 32 bits.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Vulkan ne prend en charge que les tampons d’accélérateur 32 bits.";
        if (strncmp(lang_code, "de", 2) == 0) return "Vulkan unterstützt nur 32-Bit-Beschleunigerpuffer.";
        if (strncmp(lang_code, "it", 2) == 0) return "Vulkan supporta solo buffer dell'acceleratore a 32 bit.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το Vulkan υποστηρίζει μόνο ενδιάμεσες μνήμες επιταχυντή 32 bit.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Vulkan поддерживает только 32-битные буферы ускорителя.";
        if (strncmp(lang_code, "zh", 2) == 0) return "Vulkan 仅支持 32 位加速器缓冲区。";
        if (strncmp(lang_code, "tw", 2) == 0) return "Vulkan 僅支援 32 位元加速器緩衝區。";
        if (strncmp(lang_code, "ja", 2) == 0) return "Vulkanは32ビットのアクセラレータバッファのみをサポートします。";
        return "Vulkan supports only 32-bit accelerator buffers.";
    }
    if (strcmp(msg, "Write-only file mappings are not supported.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Write-only file mappings are not supported.";
        if (strncmp(lang_code, "es", 2) == 0) return "No se admiten los mapeos de archivos de solo escritura.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les mappages de fichier en écriture seule ne sont pas pris en charge.";
        if (strncmp(lang_code, "de", 2) == 0) return "Dateizuordnungen mit ausschließlichem Schreibzugriff werden nicht unterstützt.";
        if (strncmp(lang_code, "it", 2) == 0) return "Le mappature di file di sola scrittura non sono supportate.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι αντιστοιχίσεις αρχείου μόνο για εγγραφή δεν υποστηρίζονται.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Отображения файлов только для записи не поддерживаются.";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持只写文件映射。";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援唯寫檔案對應。";
        if (strncmp(lang_code, "ja", 2) == 0) return "書き込み専用のファイルマッピングはサポートされていません。";
        return "Write-only file mappings are not supported.";
    }
    if (strcmp(msg, "Wrong number of arguments for __fast intrinsic.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "Wrong number of arguments for __fast intrinsic.";
        if (strncmp(lang_code, "es", 2) == 0) return "Número incorrecto de argumentos para la función intrínseca __fast.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Nombre incorrect d’arguments pour la fonction intrinsèque __fast.";
        if (strncmp(lang_code, "de", 2) == 0) return "Falsche Anzahl von Argumenten für die intrinsische Funktion __fast.";
        if (strncmp(lang_code, "it", 2) == 0) return "Numero errato di argomenti per la funzione intrinseca __fast.";
        if (strncmp(lang_code, "el", 2) == 0) return "Εσφαλμένος αριθμός ορισμάτων για την ενσωματωμένη συνάρτηση __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Неверное количество аргументов для встроенной функции __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "内置函数 __fast 的实参数量错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "內建函式 __fast 的引數數量錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "組み込み関数__fastの引数の数が正しくありません。";
        return "Wrong number of arguments for __fast intrinsic.";
    }
    if (strcmp(msg, "__fast call '%s': argument %u does not match the exact shape.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call '%s': argument %u does not match the exact shape.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast '%s': el argumento %u no coincide con la forma exacta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast « %s » : l’argument %u ne correspond pas exactement à la forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf „%s“: Argument %u stimmt nicht exakt mit der Form überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast '%s': l'argomento %u non corrisponde esattamente alla forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast '%s': το όρισμα %u δεν αντιστοιχεί ακριβώς στο σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast «%s»: аргумент %u не соответствует точной форме.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用“%s”：实参 %u 与确切形状不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫「%s」：引數 %u 與確切形狀不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し「%s」: 引数%uが正確な形状と一致しません。";
        return "__fast call '%s': argument %u does not match the exact shape.";
    }
    if (strcmp(msg, "__fast call '%s': argument %u has the wrong packed element type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call '%s': argument %u has the wrong packed element type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast '%s': el argumento %u tiene un tipo de elemento packed incorrecto.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast « %s » : l’argument %u a un type d’élément packed incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf „%s“: Argument %u hat den falschen packed-Elementtyp.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast '%s': l'argomento %u ha un tipo di elemento packed errato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast '%s': το όρισμα %u έχει εσφαλμένο τύπο στοιχείου packed.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast «%s»: аргумент %u имеет неверный тип элемента packed.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用“%s”：实参 %u 的 packed 元素类型错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫「%s」：引數 %u 的 packed 元素型別錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し「%s」: 引数%uのpacked要素型が正しくありません。";
        return "__fast call '%s': argument %u has the wrong packed element type.";
    }
    if (strcmp(msg, "__fast call '%s': argument %u has the wrong primitive type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call '%s': argument %u has the wrong primitive type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast '%s': el argumento %u tiene un tipo primitivo incorrecto.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast « %s » : l’argument %u a un type primitif incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf „%s“: Argument %u hat den falschen primitiven Typ.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast '%s': l'argomento %u ha un tipo primitivo errato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast '%s': το όρισμα %u έχει εσφαλμένο πρωτογενή τύπο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast «%s»: аргумент %u имеет неверный примитивный тип.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用“%s”：实参 %u 的基本类型错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫「%s」：引數 %u 的基本型別錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し「%s」: 引数%uのプリミティブ型が正しくありません。";
        return "__fast call '%s': argument %u has the wrong primitive type.";
    }
    if (strcmp(msg, "__fast call '%s': shape element count overflow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call '%s': shape element count overflow.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast '%s': desbordamiento de la cantidad de elementos de la forma.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast « %s » : dépassement de capacité du nombre d’éléments de la forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf „%s“: Überlauf der Formelementanzahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast '%s': overflow del numero di elementi della forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast '%s': υπερχείλιση πλήθους στοιχείων σχήματος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast «%s»: переполнение количества элементов формы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用“%s”：形状元素数量溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫「%s」：形狀元素數量溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し「%s」: 形状の要素数がオーバーフローしました。";
        return "__fast call '%s': shape element count overflow.";
    }
    if (strcmp(msg, "__fast call '%s': shape extents must be positive.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call '%s': shape extents must be positive.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast '%s': las extensiones de la forma deben ser positivas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast « %s » : les étendues de la forme doivent être positives.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf „%s“: Formausdehnungen müssen positiv sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast '%s': le estensioni della forma devono essere positive.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast '%s': οι εκτάσεις σχήματος πρέπει να είναι θετικές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast «%s»: размеры формы должны быть положительными.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用“%s”：形状的维度大小必须为正数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫「%s」：形狀的維度大小必須為正數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し「%s」: 形状の次元サイズは正である必要があります。";
        return "__fast call '%s': shape extents must be positive.";
    }
    if (strcmp(msg, "__fast call: argument does not match the exact shape.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call: argument does not match the exact shape.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast: el argumento no coincide con la forma exacta.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast : l’argument ne correspond pas exactement à la forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf: Das Argument stimmt nicht exakt mit der Form überein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast: l'argomento non corrisponde esattamente alla forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast: το όρισμα δεν αντιστοιχεί ακριβώς στο σχήμα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast: аргумент не соответствует точной форме.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用：实参与确切形状不匹配。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫：引數與確切形狀不符。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し: 引数が正確な形状と一致しません。";
        return "__fast call: argument does not match the exact shape.";
    }
    if (strcmp(msg, "__fast call: argument has the wrong packed element type.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call: argument has the wrong packed element type.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast: el argumento tiene un tipo de elemento packed incorrecto.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast : l’argument a un type d’élément packed incorrect.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf: Das Argument hat den falschen packed-Elementtyp.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast: l'argomento ha un tipo di elemento packed errato.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast: το όρισμα έχει εσφαλμένο τύπο στοιχείου packed.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast: аргумент имеет неверный тип элемента packed.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用：实参的 packed 元素类型错误。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫：引數的 packed 元素型別錯誤。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し: 引数のpacked要素型が正しくありません。";
        return "__fast call: argument has the wrong packed element type.";
    }
    if (strcmp(msg, "__fast call: shape element count overflow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call: shape element count overflow.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast: desbordamiento de la cantidad de elementos de la forma.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast : dépassement de capacité du nombre d’éléments de la forme.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf: Überlauf der Formelementanzahl.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast: overflow del numero di elementi della forma.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast: υπερχείλιση πλήθους στοιχείων σχήματος.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast: переполнение количества элементов формы.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用：形状元素数量溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫：形狀元素數量溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し: 形状の要素数がオーバーフローしました。";
        return "__fast call: shape element count overflow.";
    }
    if (strcmp(msg, "__fast call: shape extents must be positive.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast call: shape extents must be positive.";
        if (strncmp(lang_code, "es", 2) == 0) return "Llamada __fast: las extensiones de la forma deben ser positivas.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Appel __fast : les étendues de la forme doivent être positives.";
        if (strncmp(lang_code, "de", 2) == 0) return "__fast-Aufruf: Formausdehnungen müssen positiv sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Chiamata __fast: le estensioni della forma devono essere positive.";
        if (strncmp(lang_code, "el", 2) == 0) return "Κλήση __fast: οι εκτάσεις σχήματος πρέπει να είναι θετικές.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Вызов __fast: размеры формы должны быть положительными.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 调用：形状的维度大小必须为正数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 呼叫：形狀的維度大小必須為正數。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fast呼び出し: 形状の次元サイズは正である必要があります。";
        return "__fast call: shape extents must be positive.";
    }
    if (strcmp(msg, "__fast row-major index overflow.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast row-major index overflow.";
        if (strncmp(lang_code, "es", 2) == 0) return "Desbordamiento del índice por filas de __fast.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Dépassement de capacité de l’index par lignes de __fast.";
        if (strncmp(lang_code, "de", 2) == 0) return "Überlauf des zeilenorientierten __fast-Index.";
        if (strncmp(lang_code, "it", 2) == 0) return "Overflow dell'indice per righe di __fast.";
        if (strncmp(lang_code, "el", 2) == 0) return "Υπερχείλιση δείκτη διάταξης κατά γραμμές του __fast.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Переполнение построчного индекса __fast.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 行优先索引溢出。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 列優先索引溢位。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastの行優先インデックスがオーバーフローしました。";
        return "__fast row-major index overflow.";
    }
    if (strcmp(msg, "__fast shape extent is too large.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__fast shape extent is too large.";
        if (strncmp(lang_code, "es", 2) == 0) return "La extensión de la forma de __fast es demasiado grande.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L’étendue de la forme de __fast est trop grande.";
        if (strncmp(lang_code, "de", 2) == 0) return "Die Formausdehnung von __fast ist zu groß.";
        if (strncmp(lang_code, "it", 2) == 0) return "L'estensione della forma di __fast è troppo grande.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η έκταση σχήματος του __fast είναι υπερβολικά μεγάλη.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Размер формы __fast слишком велик.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__fast 形状的维度大小过大。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__fast 形狀的維度大小過大。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__fastの形状の次元サイズが大きすぎます。";
        return "__fast shape extent is too large.";
    }
    if (strcmp(msg, "__inline functions must also be static.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "__inline functions must also be static.";
        if (strncmp(lang_code, "es", 2) == 0) return "Las funciones __inline también deben ser static.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les fonctions __inline doivent également être static.";
        if (strncmp(lang_code, "de", 2) == 0) return "__inline-Funktionen müssen auch static sein.";
        if (strncmp(lang_code, "it", 2) == 0) return "Le funzioni __inline devono essere anche static.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι συναρτήσεις __inline πρέπει επίσης να είναι static.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Функции __inline также должны быть static.";
        if (strncmp(lang_code, "zh", 2) == 0) return "__inline 函数还必须为 static。";
        if (strncmp(lang_code, "tw", 2) == 0) return "__inline 函式也必須是 static。";
        if (strncmp(lang_code, "ja", 2) == 0) return "__inline関数はstaticでもある必要があります。";
        return "__inline functions must also be static.";
    }
    if (strcmp(msg, "argument has two binding types") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "argument has two binding types";
        if (strncmp(lang_code, "es", 2) == 0) return "el argumento tiene dos tipos de vinculación";
        if (strncmp(lang_code, "fr", 2) == 0) return "l’argument a deux types de liaison";
        if (strncmp(lang_code, "de", 2) == 0) return "Argument hat zwei Bindungstypen";
        if (strncmp(lang_code, "it", 2) == 0) return "l'argomento ha due tipi di associazione";
        if (strncmp(lang_code, "el", 2) == 0) return "το όρισμα έχει δύο τύπους δέσμευσης";
        if (strncmp(lang_code, "ru", 2) == 0) return "аргумент имеет два типа привязки";
        if (strncmp(lang_code, "zh", 2) == 0) return "实参有两种绑定类型";
        if (strncmp(lang_code, "tw", 2) == 0) return "引數有兩種繫結型別";
        if (strncmp(lang_code, "ja", 2) == 0) return "引数に2つのバインディング型があります";
        return "argument has two binding types";
    }
    if (strcmp(msg, "atan2() requires float or double operands.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "atan2() requires float or double operands.";
        if (strncmp(lang_code, "es", 2) == 0) return "atan2() requiere operandos float o double.";
        if (strncmp(lang_code, "fr", 2) == 0) return "atan2() exige des opérandes float ou double.";
        if (strncmp(lang_code, "de", 2) == 0) return "atan2() erfordert Operanden vom Typ float oder double.";
        if (strncmp(lang_code, "it", 2) == 0) return "atan2() richiede operandi float o double.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η atan2() απαιτεί τελεστέους float ή double.";
        if (strncmp(lang_code, "ru", 2) == 0) return "atan2() требует операнды типа float или double.";
        if (strncmp(lang_code, "zh", 2) == 0) return "atan2() 需要 float 或 double 操作数。";
        if (strncmp(lang_code, "tw", 2) == 0) return "atan2() 需要 float 或 double 運算元。";
        if (strncmp(lang_code, "ja", 2) == 0) return "atan2()にはfloatまたはdoubleのオペランドが必要です。";
        return "atan2() requires float or double operands.";
    }
    if (strcmp(msg, "buffer binding limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "buffer binding limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de vinculaciones de búferes";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite de liaisons de tampons dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Grenze für Pufferbindungen überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite delle associazioni dei buffer superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου δεσμεύσεων ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел привязок буферов";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出缓冲区绑定限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出緩衝區繫結限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファのバインディング上限を超えました";
        return "buffer binding limit exceeded";
    }
    if (strcmp(msg, "chdir() failed.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "chdir() failed.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de chdir().\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de chdir().\n";
        if (strncmp(lang_code, "de", 2) == 0) return "chdir() fehlgeschlagen.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "chdir() non riuscito.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η chdir() απέτυχε.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой chdir().\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "chdir() 失败。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "chdir() 失敗。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "chdir()に失敗しました。\n";
        return "chdir() failed.\n";
    }
    if (strcmp(msg, "continue appeared outside loop.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "continue appeared outside loop.";
        if (strncmp(lang_code, "es", 2) == 0) return "continue es utilizado fuera de un bucle.";
        if (strncmp(lang_code, "fr", 2) == 0) return "L'instruction continue est apparue en dehors d'une boucle.";
        if (strncmp(lang_code, "de", 2) == 0) return "continue wurde außerhalb einer Schleife verwendet.";
        if (strncmp(lang_code, "it", 2) == 0) return "continue è stato utilizzato al di fuori di un ciclo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η εντολή continue εμφανίστηκε εκτός βρόχου.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Оператор continue использован вне цикла.";
        if (strncmp(lang_code, "zh", 2) == 0) return "continue 语句出现在循环外部。";
        if (strncmp(lang_code, "tw", 2) == 0) return "continue 敘述出現在迴圈外部。";
        if (strncmp(lang_code, "ja", 2) == 0) return "continue文がループの外で使用されました。";
        return "continue appeared outside loop.";
    }
    if (strcmp(msg, "device buffer escapes its GPU session") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device buffer escapes its GPU session";
        if (strncmp(lang_code, "es", 2) == 0) return "el búfer del dispositivo escapa de su sesión de GPU";
        if (strncmp(lang_code, "fr", 2) == 0) return "le tampon du périphérique sort de sa session GPU";
        if (strncmp(lang_code, "de", 2) == 0) return "Gerätepuffer entkommt seiner GPU-Sitzung";
        if (strncmp(lang_code, "it", 2) == 0) return "il buffer del dispositivo sfugge alla propria sessione GPU";
        if (strncmp(lang_code, "el", 2) == 0) return "η ενδιάμεση μνήμη συσκευής διαφεύγει από τη συνεδρία GPU της";
        if (strncmp(lang_code, "ru", 2) == 0) return "буфер устройства выходит за пределы своего сеанса GPU";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备缓冲区逸出其 GPU 会话";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置緩衝區逸出其 GPU 工作階段";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスバッファがそのGPUセッション外にエスケープしています";
        return "device buffer escapes its GPU session";
    }
    if (strcmp(msg, "device buffer has a host argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device buffer has a host argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "el búfer del dispositivo tiene una ranura de argumento del host";
        if (strncmp(lang_code, "fr", 2) == 0) return "le tampon du périphérique a un emplacement d’argument de l’hôte";
        if (strncmp(lang_code, "de", 2) == 0) return "Gerätepuffer hat einen Host-Argumentslot";
        if (strncmp(lang_code, "it", 2) == 0) return "il buffer del dispositivo ha uno slot di argomento dell'host";
        if (strncmp(lang_code, "el", 2) == 0) return "η ενδιάμεση μνήμη συσκευής έχει θέση ορίσματος κεντρικού συστήματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "буфер устройства имеет слот аргумента хоста";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备缓冲区具有主机实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置緩衝區具有主機引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスバッファにホスト引数スロットがあります";
        return "device buffer has a host argument slot";
    }
    if (strcmp(msg, "device buffer has no later consumer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device buffer has no later consumer";
        if (strncmp(lang_code, "es", 2) == 0) return "el búfer del dispositivo no tiene ningún consumidor posterior";
        if (strncmp(lang_code, "fr", 2) == 0) return "le tampon du périphérique n’a aucun consommateur ultérieur";
        if (strncmp(lang_code, "de", 2) == 0) return "Gerätepuffer hat keinen späteren Verbraucher";
        if (strncmp(lang_code, "it", 2) == 0) return "il buffer del dispositivo non ha un consumatore successivo";
        if (strncmp(lang_code, "el", 2) == 0) return "η ενδιάμεση μνήμη συσκευής δεν έχει μεταγενέστερο καταναλωτή";
        if (strncmp(lang_code, "ru", 2) == 0) return "буфер устройства не имеет последующего потребителя";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备缓冲区没有后续消费者";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置緩衝區沒有後續使用者";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスバッファに後続のコンシューマーがありません";
        return "device buffer has no later consumer";
    }
    if (strcmp(msg, "device buffer has no producer kernel") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device buffer has no producer kernel";
        if (strncmp(lang_code, "es", 2) == 0) return "el búfer del dispositivo no tiene ningún kernel productor";
        if (strncmp(lang_code, "fr", 2) == 0) return "le tampon du périphérique n’a aucun kernel producteur";
        if (strncmp(lang_code, "de", 2) == 0) return "Gerätepuffer hat keinen Erzeuger-Kernel";
        if (strncmp(lang_code, "it", 2) == 0) return "il buffer del dispositivo non ha un kernel produttore";
        if (strncmp(lang_code, "el", 2) == 0) return "η ενδιάμεση μνήμη συσκευής δεν έχει πυρήνα παραγωγό";
        if (strncmp(lang_code, "ru", 2) == 0) return "буфер устройства не имеет ядра-производителя";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备缓冲区没有生产者内核";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置緩衝區沒有產生者核心";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスバッファにプロデューサーカーネルがありません";
        return "device buffer has no producer kernel";
    }
    if (strcmp(msg, "device buffer requires host materialization") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device buffer requires host materialization";
        if (strncmp(lang_code, "es", 2) == 0) return "el búfer del dispositivo requiere materialización en el host";
        if (strncmp(lang_code, "fr", 2) == 0) return "le tampon du périphérique exige une matérialisation sur l’hôte";
        if (strncmp(lang_code, "de", 2) == 0) return "Gerätepuffer erfordert Host-Materialisierung";
        if (strncmp(lang_code, "it", 2) == 0) return "il buffer del dispositivo richiede la materializzazione sull'host";
        if (strncmp(lang_code, "el", 2) == 0) return "η ενδιάμεση μνήμη συσκευής απαιτεί υλοποίηση στο κεντρικό σύστημα";
        if (strncmp(lang_code, "ru", 2) == 0) return "буфер устройства требует материализации на хосте";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备缓冲区需要在主机端实体化";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置緩衝區需要在主機端具現化";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスバッファをホスト側で実体化する必要があります";
        return "device buffer requires host materialization";
    }
    if (strcmp(msg, "device producer does not cover its extent") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer does not cover its extent";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del dispositivo no cubre su extensión";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du périphérique ne couvre pas son étendue";
        if (strncmp(lang_code, "de", 2) == 0) return "Geräteerzeuger deckt seine Ausdehnung nicht ab";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del dispositivo non copre la propria estensione";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός συσκευής δεν καλύπτει την έκτασή του";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель устройства не покрывает свой размер";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者未覆盖其维度大小";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者未涵蓋其維度大小";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーがその次元サイズを網羅していません";
        return "device producer does not cover its extent";
    }
    if (strcmp(msg, "device producer does not start at zero") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer does not start at zero";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del dispositivo no empieza en cero";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du périphérique ne commence pas à zéro";
        if (strncmp(lang_code, "de", 2) == 0) return "Geräteerzeuger beginnt nicht bei null";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del dispositivo non inizia da zero";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός συσκευής δεν ξεκινά από το μηδέν";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель устройства начинается не с нуля";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者未从零开始";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者未從零開始";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーがゼロから開始していません";
        return "device producer does not start at zero";
    }
    if (strcmp(msg, "device producer has an invalid trip difference") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer has an invalid trip difference";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del dispositivo tiene una diferencia de iteración no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du périphérique a une différence d’itération non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "Geräteerzeuger hat eine ungültige Iterationsdifferenz";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del dispositivo ha una differenza di iterazione non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός συσκευής έχει μη έγκυρη διαφορά επανάληψης";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель устройства имеет недопустимую разность итераций";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者具有无效的迭代差值";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者具有無效的迭代差值";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーの反復差分が無効です";
        return "device producer has an invalid trip difference";
    }
    if (strcmp(msg, "device producer has an invalid trip expression") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer has an invalid trip expression";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del dispositivo tiene una expresión de iteración no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du périphérique a une expression d’itération non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "Geräteerzeuger hat einen ungültigen Iterationsausdruck";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del dispositivo ha un'espressione di iterazione non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός συσκευής έχει μη έγκυρη έκφραση επανάληψης";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель устройства имеет недопустимое выражение итерации";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者具有无效的迭代表达式";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者具有無效的迭代表達式";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーの反復式が無効です";
        return "device producer has an invalid trip expression";
    }
    if (strcmp(msg, "device producer has multiple invocation indices") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer has multiple invocation indices";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del dispositivo tiene varios índices de invocación";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du périphérique a plusieurs index d’invocation";
        if (strncmp(lang_code, "de", 2) == 0) return "Geräteerzeuger hat mehrere Aufrufindizes";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del dispositivo ha più indici di invocazione";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός συσκευής έχει πολλούς δείκτες επίκλησης";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель устройства имеет несколько индексов вызова";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者具有多个调用索引";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者具有多個呼叫索引";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーに複数の呼び出しインデックスがあります";
        return "device producer has multiple invocation indices";
    }
    if (strcmp(msg, "device producer index is not exact") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer index is not exact";
        if (strncmp(lang_code, "es", 2) == 0) return "el índice del productor del dispositivo no es exacto";
        if (strncmp(lang_code, "fr", 2) == 0) return "l’index du producteur du périphérique n’est pas exact";
        if (strncmp(lang_code, "de", 2) == 0) return "Index des Geräteerzeugers ist nicht exakt";
        if (strncmp(lang_code, "it", 2) == 0) return "l'indice del produttore del dispositivo non è esatto";
        if (strncmp(lang_code, "el", 2) == 0) return "ο δείκτης παραγωγού συσκευής δεν είναι ακριβής";
        if (strncmp(lang_code, "ru", 2) == 0) return "индекс производителя устройства не является точным";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者索引不精确";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者索引不精確";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーのインデックスが正確ではありません";
        return "device producer index is not exact";
    }
    if (strcmp(msg, "device producer is not a full definition") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer is not a full definition";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del dispositivo no es una definición completa";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du périphérique n’est pas une définition complète";
        if (strncmp(lang_code, "de", 2) == 0) return "Geräteerzeuger ist keine vollständige Definition";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del dispositivo non è una definizione completa";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός συσκευής δεν είναι πλήρης ορισμός";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель устройства не является полным определением";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者不是完整定义";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者不是完整定義";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーが完全な定義ではありません";
        return "device producer is not a full definition";
    }
    if (strcmp(msg, "device producer range is not exact") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer range is not exact";
        if (strncmp(lang_code, "es", 2) == 0) return "el rango del productor del dispositivo no es exacto";
        if (strncmp(lang_code, "fr", 2) == 0) return "la plage du producteur du périphérique n’est pas exacte";
        if (strncmp(lang_code, "de", 2) == 0) return "Bereich des Geräteerzeugers ist nicht exakt";
        if (strncmp(lang_code, "it", 2) == 0) return "l'intervallo del produttore del dispositivo non è esatto";
        if (strncmp(lang_code, "el", 2) == 0) return "το εύρος παραγωγού συσκευής δεν είναι ακριβές";
        if (strncmp(lang_code, "ru", 2) == 0) return "диапазон производителя устройства не является точным";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者范围不精确";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者範圍不精確";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーの範囲が正確ではありません";
        return "device producer range is not exact";
    }
    if (strcmp(msg, "device producer reads before definition") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer reads before definition";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del dispositivo lee antes de la definición";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du périphérique lit avant la définition";
        if (strncmp(lang_code, "de", 2) == 0) return "Geräteerzeuger liest vor der Definition";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del dispositivo legge prima della definizione";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός συσκευής διαβάζει πριν από τον ορισμό";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель устройства выполняет чтение до определения";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者在定义前读取";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者在定義前讀取";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーが定義前に読み取っています";
        return "device producer reads before definition";
    }
    if (strcmp(msg, "device producer store is not unique") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device producer store is not unique";
        if (strncmp(lang_code, "es", 2) == 0) return "el almacenamiento del productor del dispositivo no es único";
        if (strncmp(lang_code, "fr", 2) == 0) return "le stockage du producteur du périphérique n’est pas unique";
        if (strncmp(lang_code, "de", 2) == 0) return "Speichervorgang des Geräteerzeugers ist nicht eindeutig";
        if (strncmp(lang_code, "it", 2) == 0) return "l'archiviazione del produttore del dispositivo non è univoca";
        if (strncmp(lang_code, "el", 2) == 0) return "η αποθήκευση του παραγωγού συσκευής δεν είναι μοναδική";
        if (strncmp(lang_code, "ru", 2) == 0) return "операция сохранения производителя устройства не уникальна";
        if (strncmp(lang_code, "zh", 2) == 0) return "设备生产者存储操作不唯一";
        if (strncmp(lang_code, "tw", 2) == 0) return "裝置產生者儲存操作不唯一";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスプロデューサーのストアが一意ではありません";
        return "device producer store is not unique";
    }
    if (strcmp(msg, "device-only Direct3D 12 buffer has a host argument") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "device-only Direct3D 12 buffer has a host argument";
        if (strncmp(lang_code, "es", 2) == 0) return "el búfer de Direct3D 12 exclusivo del dispositivo tiene un argumento del host";
        if (strncmp(lang_code, "fr", 2) == 0) return "le tampon Direct3D 12 réservé au périphérique a un argument de l’hôte";
        if (strncmp(lang_code, "de", 2) == 0) return "Reiner Direct3D 12-Gerätepuffer hat ein Host-Argument";
        if (strncmp(lang_code, "it", 2) == 0) return "il buffer Direct3D 12 riservato al dispositivo ha un argomento dell'host";
        if (strncmp(lang_code, "el", 2) == 0) return "η ενδιάμεση μνήμη Direct3D 12 μόνο για συσκευή έχει όρισμα κεντρικού συστήματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "буфер Direct3D 12 только для устройства имеет аргумент хоста";
        if (strncmp(lang_code, "zh", 2) == 0) return "仅限设备的 Direct3D 12 缓冲区具有主机实参";
        if (strncmp(lang_code, "tw", 2) == 0) return "僅限裝置的 Direct3D 12 緩衝區具有主機引數";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイス専用Direct3D 12バッファにホスト引数があります";
        return "device-only Direct3D 12 buffer has a host argument";
    }
    if (strcmp(msg, "duplicate buffer argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "duplicate buffer argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento de búfer duplicada";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument de tampon en double";
        if (strncmp(lang_code, "de", 2) == 0) return "doppelter Puffer-Argumentslot";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento del buffer duplicato";
        if (strncmp(lang_code, "el", 2) == 0) return "διπλότυπη θέση ορίσματος ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "дублирующийся слот аргумента буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "重复的缓冲区实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "重複的緩衝區引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ引数スロットが重複しています";
        return "duplicate buffer argument slot";
    }
    if (strcmp(msg, "duplicate device binding") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "duplicate device binding";
        if (strncmp(lang_code, "es", 2) == 0) return "vinculación de dispositivo duplicada";
        if (strncmp(lang_code, "fr", 2) == 0) return "liaison de périphérique en double";
        if (strncmp(lang_code, "de", 2) == 0) return "doppelte Gerätebindung";
        if (strncmp(lang_code, "it", 2) == 0) return "associazione del dispositivo duplicata";
        if (strncmp(lang_code, "el", 2) == 0) return "διπλότυπη δέσμευση συσκευής";
        if (strncmp(lang_code, "ru", 2) == 0) return "дублирующаяся привязка устройства";
        if (strncmp(lang_code, "zh", 2) == 0) return "重复的设备绑定";
        if (strncmp(lang_code, "tw", 2) == 0) return "重複的裝置繫結";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスのバインディングが重複しています";
        return "duplicate device binding";
    }
    if (strcmp(msg, "duplicate runtime argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "duplicate runtime argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento del entorno de ejecución duplicada";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument de l’environnement d’exécution en double";
        if (strncmp(lang_code, "de", 2) == 0) return "doppelter Laufzeitargument-Slot";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento dell'ambiente di esecuzione duplicato";
        if (strncmp(lang_code, "el", 2) == 0) return "διπλότυπη θέση ορίσματος περιβάλλοντος εκτέλεσης";
        if (strncmp(lang_code, "ru", 2) == 0) return "дублирующийся слот аргумента среды выполнения";
        if (strncmp(lang_code, "zh", 2) == 0) return "重复的运行时环境实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "重複的執行階段環境引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "実行環境引数スロットが重複しています";
        return "duplicate runtime argument slot";
    }
    if (strcmp(msg, "duplicate scalar argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "duplicate scalar argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento escalar duplicada";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument scalaire en double";
        if (strncmp(lang_code, "de", 2) == 0) return "doppelter Skalar-Argumentslot";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento scalare duplicato";
        if (strncmp(lang_code, "el", 2) == 0) return "διπλότυπη θέση βαθμωτού ορίσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "дублирующийся слот скалярного аргумента";
        if (strncmp(lang_code, "zh", 2) == 0) return "重复的标量实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "重複的純量引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー引数スロットが重複しています";
        return "duplicate scalar argument slot";
    }
    if (strcmp(msg, "duplicate scalar result argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "duplicate scalar result argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento de resultado escalar duplicada";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument de résultat scalaire en double";
        if (strncmp(lang_code, "de", 2) == 0) return "doppelter Argumentslot für skalares Ergebnis";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento del risultato scalare duplicato";
        if (strncmp(lang_code, "el", 2) == 0) return "διπλότυπη θέση ορίσματος βαθμωτού αποτελέσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "дублирующийся слот аргумента скалярного результата";
        if (strncmp(lang_code, "zh", 2) == 0) return "重复的标量结果实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "重複的純量結果引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果引数スロットが重複しています";
        return "duplicate scalar result argument slot";
    }
    if (strcmp(msg, "else appeared after else.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "else appeared after else.";
        if (strncmp(lang_code, "es", 2) == 0) return "else utilizado después de else.";
        if (strncmp(lang_code, "fr", 2) == 0) return "else apparaît après else.";
        if (strncmp(lang_code, "de", 2) == 0) return "else wurde nach else verwendet.";
        if (strncmp(lang_code, "it", 2) == 0) return "else è stato utilizzato dopo else.";
        if (strncmp(lang_code, "el", 2) == 0) return "το else χρησιμοποιήθηκε μετά από το else.";
        if (strncmp(lang_code, "ru", 2) == 0) return "else использовано после else.";
        if (strncmp(lang_code, "zh", 2) == 0) return "else 出现在 else 之后。";
        if (strncmp(lang_code, "tw", 2) == 0) return "else 出現在 else 之後。";
        if (strncmp(lang_code, "ja", 2) == 0) return "else文がelse文の後ろで使用されました。";
        return "else appeared after else.";
    }
    if (strcmp(msg, "else-if appeared after else.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "else-if appeared after else.";
        if (strncmp(lang_code, "es", 2) == 0) return "else-if utilizado después de else.";
        if (strncmp(lang_code, "fr", 2) == 0) return "else-if est apparu après else.";
        if (strncmp(lang_code, "de", 2) == 0) return "else-if wurde nach else verwendet.";
        if (strncmp(lang_code, "it", 2) == 0) return "else-if è stato utilizzato dopo else.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το else-if εμφανίστηκε μετά το else.";
        if (strncmp(lang_code, "ru", 2) == 0) return "else-if появился после else.";
        if (strncmp(lang_code, "zh", 2) == 0) return "else 之后出现了 else-if。";
        if (strncmp(lang_code, "tw", 2) == 0) return "else 之後出現了 else-if。";
        if (strncmp(lang_code, "ja", 2) == 0) return "else if文がelse文の後ろで使用されました。";
        return "else-if appeared after else.";
    }
    if (strcmp(msg, "else-if block appeared without if block.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "else-if block appeared without if block.";
        if (strncmp(lang_code, "es", 2) == 0) return "bloque else-if utilizado sin un bloque if.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Un bloc else-if est apparu sans bloc if.";
        if (strncmp(lang_code, "de", 2) == 0) return "else-if-Block wurde ohne zugehörigen if-Block verwendet.";
        if (strncmp(lang_code, "it", 2) == 0) return "blocco else-if è stato utilizzato senza un blocco if.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το μπλοκ else-if εμφανίστηκε χωρίς μπλοκ if.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Блок else-if появился без блока if.";
        if (strncmp(lang_code, "zh", 2) == 0) return "出现了没有 if 块的 else-if 块。";
        if (strncmp(lang_code, "tw", 2) == 0) return "出現了沒有 if 區塊的 else-if 區塊。";
        if (strncmp(lang_code, "ja", 2) == 0) return "else if文がifブロックの後ろ以外で使用されました。";
        return "else-if block appeared without if block.";
    }
    if (strcmp(msg, "empty Direct3D 12 dispatch reached the backend") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "empty Direct3D 12 dispatch reached the backend";
        if (strncmp(lang_code, "es", 2) == 0) return "un despacho vacío de Direct3D 12 llegó al backend";
        if (strncmp(lang_code, "fr", 2) == 0) return "un lancement Direct3D 12 vide a atteint le backend";
        if (strncmp(lang_code, "de", 2) == 0) return "leerer Direct3D 12-Dispatch hat das Backend erreicht";
        if (strncmp(lang_code, "it", 2) == 0) return "un dispatch Direct3D 12 vuoto ha raggiunto il backend";
        if (strncmp(lang_code, "el", 2) == 0) return "μια κενή αποστολή Direct3D 12 έφτασε στο backend";
        if (strncmp(lang_code, "ru", 2) == 0) return "пустая диспетчеризация Direct3D 12 достигла бэкенда";
        if (strncmp(lang_code, "zh", 2) == 0) return "空的 Direct3D 12 调度到达后端";
        if (strncmp(lang_code, "tw", 2) == 0) return "空的 Direct3D 12 調度到達後端";
        if (strncmp(lang_code, "ja", 2) == 0) return "空のDirect3D 12ディスパッチがバックエンドに到達しました";
        return "empty Direct3D 12 dispatch reached the backend";
    }
    if (strcmp(msg, "empty Direct3D 12 execution has an active buffer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "empty Direct3D 12 execution has an active buffer";
        if (strncmp(lang_code, "es", 2) == 0) return "una ejecución vacía de Direct3D 12 tiene un búfer activo";
        if (strncmp(lang_code, "fr", 2) == 0) return "une exécution Direct3D 12 vide a un tampon actif";
        if (strncmp(lang_code, "de", 2) == 0) return "leere Direct3D 12-Ausführung hat einen aktiven Puffer";
        if (strncmp(lang_code, "it", 2) == 0) return "un'esecuzione Direct3D 12 vuota ha un buffer attivo";
        if (strncmp(lang_code, "el", 2) == 0) return "μια κενή εκτέλεση Direct3D 12 έχει ενεργή ενδιάμεση μνήμη";
        if (strncmp(lang_code, "ru", 2) == 0) return "пустое выполнение Direct3D 12 имеет активный буфер";
        if (strncmp(lang_code, "zh", 2) == 0) return "空的 Direct3D 12 执行具有活动缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "空的 Direct3D 12 執行具有作用中緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "空のDirect3D 12実行にアクティブなバッファがあります";
        return "empty Direct3D 12 execution has an active buffer";
    }
    if (strcmp(msg, "empty accelerator region") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "empty accelerator region";
        if (strncmp(lang_code, "es", 2) == 0) return "región de acelerador vacía";
        if (strncmp(lang_code, "fr", 2) == 0) return "région d’accélérateur vide";
        if (strncmp(lang_code, "de", 2) == 0) return "leere Beschleunigerregion";
        if (strncmp(lang_code, "it", 2) == 0) return "regione dell'acceleratore vuota";
        if (strncmp(lang_code, "el", 2) == 0) return "κενή περιοχή επιταχυντή";
        if (strncmp(lang_code, "ru", 2) == 0) return "пустая область ускорителя";
        if (strncmp(lang_code, "zh", 2) == 0) return "空的加速器区域";
        if (strncmp(lang_code, "tw", 2) == 0) return "空的加速器區域";
        if (strncmp(lang_code, "ja", 2) == 0) return "空のアクセラレータリージョン";
        return "empty accelerator region";
    }
    if (strcmp(msg, "execvp() failed for %s.\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "execvp() failed for %s.\n";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de execvp() para %s.\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de execvp() pour %s.\n";
        if (strncmp(lang_code, "de", 2) == 0) return "execvp() für %s fehlgeschlagen.\n";
        if (strncmp(lang_code, "it", 2) == 0) return "execvp() non riuscito per %s.\n";
        if (strncmp(lang_code, "el", 2) == 0) return "Η execvp() απέτυχε για %s.\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой execvp() для %s.\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "对 %s 执行 execvp() 失败。\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "對 %s 執行 execvp() 失敗。\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "%sのexecvp()に失敗しました。\n";
        return "execvp() failed for %s.\n";
    }
    if (strcmp(msg, "failed to close a Direct3D 12 command list") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to close a Direct3D 12 command list";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo cerrar una lista de comandos de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la fermeture d’une liste de commandes Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Befehlsliste konnte nicht geschlossen werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile chiudere un elenco di comandi Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία κλεισίματος λίστας εντολών Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось закрыть список команд Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法关闭 Direct3D 12 命令列表";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法關閉 Direct3D 12 命令清單";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12コマンドリストを閉じられませんでした";
        return "failed to close a Direct3D 12 command list";
    }
    if (strcmp(msg, "failed to convert a DXGI adapter name") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to convert a DXGI adapter name";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo convertir un nombre de adaptador DXGI";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la conversion d’un nom d’adaptateur DXGI";
        if (strncmp(lang_code, "de", 2) == 0) return "DXGI-Adaptername konnte nicht konvertiert werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile convertire un nome di adattatore DXGI";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία μετατροπής ονόματος προσαρμογέα DXGI";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось преобразовать имя адаптера DXGI";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法转换 DXGI 适配器名称";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法轉換 DXGI 介面卡名稱";
        if (strncmp(lang_code, "ja", 2) == 0) return "DXGIアダプター名を変換できませんでした";
        return "failed to convert a DXGI adapter name";
    }
    if (strcmp(msg, "failed to create a Direct3D 12 buffer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to create a Direct3D 12 buffer";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo crear un búfer de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la création d’un tampon Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Puffer konnte nicht erstellt werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile creare un buffer Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία δημιουργίας ενδιάμεσης μνήμης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось создать буфер Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12バッファを作成できませんでした";
        return "failed to create a Direct3D 12 buffer";
    }
    if (strcmp(msg, "failed to create a Direct3D 12 command allocator") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to create a Direct3D 12 command allocator";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo crear un asignador de comandos de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la création d’un allocateur de commandes Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Befehlszuordner konnte nicht erstellt werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile creare un allocatore di comandi Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία δημιουργίας εκχωρητή εντολών Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось создать распределитель команд Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 命令分配器";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 命令配置器";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12コマンドアロケータを作成できませんでした";
        return "failed to create a Direct3D 12 command allocator";
    }
    if (strcmp(msg, "failed to create a Direct3D 12 command list") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to create a Direct3D 12 command list";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo crear una lista de comandos de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la création d’une liste de commandes Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Befehlsliste konnte nicht erstellt werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile creare un elenco di comandi Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία δημιουργίας λίστας εντολών Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось создать список команд Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 命令列表";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 命令清單";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12コマンドリストを作成できませんでした";
        return "failed to create a Direct3D 12 command list";
    }
    if (strcmp(msg, "failed to create a Direct3D 12 descriptor heap") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to create a Direct3D 12 descriptor heap";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo crear un montón de descriptores de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la création d’un tas de descripteurs Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Deskriptorheap konnte nicht erstellt werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile creare un heap di descrittori Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία δημιουργίας σωρού περιγραφέων Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось создать кучу дескрипторов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 Direct3D 12 描述符堆";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 Direct3D 12 描述元堆積";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12ディスクリプタヒープを作成できませんでした";
        return "failed to create a Direct3D 12 descriptor heap";
    }
    if (strcmp(msg, "failed to create an execution") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to create an execution";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo crear una ejecución";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la création d’une exécution";
        if (strncmp(lang_code, "de", 2) == 0) return "Ausführung konnte nicht erstellt werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile creare un'esecuzione";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία δημιουργίας εκτέλεσης";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось создать выполнение";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建执行";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立執行";
        if (strncmp(lang_code, "ja", 2) == 0) return "実行を作成できませんでした";
        return "failed to create an execution";
    }
    if (strcmp(msg, "failed to create the DXGI factory") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to create the DXGI factory";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo crear la fábrica DXGI";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la création de la fabrique DXGI";
        if (strncmp(lang_code, "de", 2) == 0) return "DXGI-Factory konnte nicht erstellt werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile creare la factory DXGI";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία δημιουργίας του εργοστασίου DXGI";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось создать фабрику DXGI";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法创建 DXGI 工厂";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法建立 DXGI Factory";
        if (strncmp(lang_code, "ja", 2) == 0) return "DXGIファクトリを作成できませんでした";
        return "failed to create the DXGI factory";
    }
    if (strcmp(msg, "failed to dispatch a kernel") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to dispatch a kernel";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo despachar un kernel";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec du lancement d’un kernel";
        if (strncmp(lang_code, "de", 2) == 0) return "Kernel konnte nicht ausgeführt werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile eseguire il dispatch di un kernel";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία αποστολής πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось диспетчеризовать ядро";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法调度内核";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法調度核心";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルをディスパッチできませんでした";
        return "failed to dispatch a kernel";
    }
    if (strcmp(msg, "failed to drain the Direct3D 12 queue submission") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to drain the Direct3D 12 queue submission";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo vaciar el envío a la cola de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la purge de la soumission à la file d’attente Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Warteschlangenübermittlung konnte nicht geleert werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile svuotare l'invio alla coda Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία εκκένωσης της υποβολής ουράς Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось опустошить отправку в очередь Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法排空 Direct3D 12 队列提交";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法清空 Direct3D 12 佇列提交";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12キューへの送信をドレインできませんでした";
        return "failed to drain the Direct3D 12 queue submission";
    }
    if (strcmp(msg, "failed to enumerate DXGI adapters") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to enumerate DXGI adapters";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudieron enumerar los adaptadores DXGI";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de l’énumération des adaptateurs DXGI";
        if (strncmp(lang_code, "de", 2) == 0) return "DXGI-Adapter konnten nicht aufgelistet werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile enumerare gli adattatori DXGI";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία απαρίθμησης προσαρμογέων DXGI";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось перечислить адаптеры DXGI";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法枚举 DXGI 适配器";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法列舉 DXGI 介面卡";
        if (strncmp(lang_code, "ja", 2) == 0) return "DXGIアダプターを列挙できませんでした";
        return "failed to enumerate DXGI adapters";
    }
    if (strcmp(msg, "failed to finish an execution") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to finish an execution";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo finalizar una ejecución";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la finalisation d’une exécution";
        if (strncmp(lang_code, "de", 2) == 0) return "Ausführung konnte nicht abgeschlossen werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile completare un'esecuzione";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία ολοκλήρωσης εκτέλεσης";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось завершить выполнение";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法完成执行";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法完成執行";
        if (strncmp(lang_code, "ja", 2) == 0) return "実行を完了できませんでした";
        return "failed to finish an execution";
    }
    if (strcmp(msg, "failed to map a Direct3D 12 readback buffer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to map a Direct3D 12 readback buffer";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo mapear un búfer de lectura de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec du mappage d’un tampon de relecture Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Rücklesepuffer konnte nicht zugeordnet werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile mappare un buffer di rilettura Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία αντιστοίχισης ενδιάμεσης μνήμης ανάγνωσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось отобразить буфер обратного чтения Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法映射 Direct3D 12 回读缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對應 Direct3D 12 回讀緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12リードバックバッファをマッピングできませんでした";
        return "failed to map a Direct3D 12 readback buffer";
    }
    if (strcmp(msg, "failed to map a Direct3D 12 result buffer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to map a Direct3D 12 result buffer";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo mapear un búfer de resultados de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec du mappage d’un tampon de résultats Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Ergebnispuffer konnte nicht zugeordnet werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile mappare un buffer dei risultati Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία αντιστοίχισης ενδιάμεσης μνήμης αποτελεσμάτων Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось отобразить буфер результатов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法映射 Direct3D 12 结果缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對應 Direct3D 12 結果緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12結果バッファをマッピングできませんでした";
        return "failed to map a Direct3D 12 result buffer";
    }
    if (strcmp(msg, "failed to map a Direct3D 12 upload buffer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to map a Direct3D 12 upload buffer";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo mapear un búfer de carga de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec du mappage d’un tampon de téléversement Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Upload-Puffer konnte nicht zugeordnet werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile mappare un buffer di caricamento Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία αντιστοίχισης ενδιάμεσης μνήμης μεταφόρτωσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось отобразить буфер загрузки Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法映射 Direct3D 12 上传缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法對應 Direct3D 12 上傳緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12アップロードバッファをマッピングできませんでした";
        return "failed to map a Direct3D 12 upload buffer";
    }
    if (strcmp(msg, "failed to signal the Direct3D 12 queue fence") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "failed to signal the Direct3D 12 queue fence";
        if (strncmp(lang_code, "es", 2) == 0) return "no se pudo señalizar la valla de la cola de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de la signalisation de la barrière de la file d’attente Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "Direct3D 12-Warteschlangen-Fence konnte nicht signalisiert werden";
        if (strncmp(lang_code, "it", 2) == 0) return "impossibile segnalare il fence della coda Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "αποτυχία σηματοδότησης του φράκτη ουράς Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "не удалось подать сигнал fence очереди Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无法向 Direct3D 12 队列栅栏发出信号";
        if (strncmp(lang_code, "tw", 2) == 0) return "無法向 Direct3D 12 佇列柵欄發出訊號";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12キューフェンスをシグナルできませんでした";
        return "failed to signal the Direct3D 12 queue fence";
    }
    if (strcmp(msg, "float contraction is not prohibited") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "float contraction is not prohibited";
        if (strncmp(lang_code, "es", 2) == 0) return "la contracción de float no está prohibida";
        if (strncmp(lang_code, "fr", 2) == 0) return "la contraction de float n’est pas interdite";
        if (strncmp(lang_code, "de", 2) == 0) return "float-Kontraktion ist nicht verboten";
        if (strncmp(lang_code, "it", 2) == 0) return "la contrazione float non è vietata";
        if (strncmp(lang_code, "el", 2) == 0) return "η συστολή float δεν απαγορεύεται";
        if (strncmp(lang_code, "ru", 2) == 0) return "сокращение float не запрещено";
        if (strncmp(lang_code, "zh", 2) == 0) return "未禁止 float 收缩";
        if (strncmp(lang_code, "tw", 2) == 0) return "未禁止 float 收縮";
        if (strncmp(lang_code, "ja", 2) == 0) return "floatの縮約が禁止されていません";
        return "float contraction is not prohibited";
    }
    if (strcmp(msg, "float result exceeds bound") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "float result exceeds bound";
        if (strncmp(lang_code, "es", 2) == 0) return "el resultado float supera el límite";
        if (strncmp(lang_code, "fr", 2) == 0) return "le résultat float dépasse la limite";
        if (strncmp(lang_code, "de", 2) == 0) return "float-Ergebnis überschreitet die Grenze";
        if (strncmp(lang_code, "it", 2) == 0) return "il risultato float supera il limite";
        if (strncmp(lang_code, "el", 2) == 0) return "το αποτέλεσμα float υπερβαίνει το όριο";
        if (strncmp(lang_code, "ru", 2) == 0) return "результат float превышает границу";
        if (strncmp(lang_code, "zh", 2) == 0) return "float 结果超出边界";
        if (strncmp(lang_code, "tw", 2) == 0) return "float 結果超出界限";
        if (strncmp(lang_code, "ja", 2) == 0) return "float結果が上限を超えています";
        return "float result exceeds bound";
    }
    if (strcmp(msg, "fork() failed.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "fork() failed.";
        if (strncmp(lang_code, "es", 2) == 0) return "Error de fork().";
        if (strncmp(lang_code, "fr", 2) == 0) return "Échec de fork().";
        if (strncmp(lang_code, "de", 2) == 0) return "fork() fehlgeschlagen.";
        if (strncmp(lang_code, "it", 2) == 0) return "fork() non riuscito.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η fork() απέτυχε.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Сбой fork().";
        if (strncmp(lang_code, "zh", 2) == 0) return "fork() 失败。";
        if (strncmp(lang_code, "tw", 2) == 0) return "fork() 失敗。";
        if (strncmp(lang_code, "ja", 2) == 0) return "fork()に失敗しました。";
        return "fork() failed.";
    }
    if (strcmp(msg, "host Direct3D 12 buffer is missing its argument") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "host Direct3D 12 buffer is missing its argument";
        if (strncmp(lang_code, "es", 2) == 0) return "al búfer del host de Direct3D 12 le falta su argumento";
        if (strncmp(lang_code, "fr", 2) == 0) return "l’argument du tampon de l’hôte Direct3D 12 est manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "Host-Puffer von Direct3D 12 fehlt sein Argument";
        if (strncmp(lang_code, "it", 2) == 0) return "al buffer dell'host Direct3D 12 manca il proprio argomento";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει το όρισμα της ενδιάμεσης μνήμης κεντρικού συστήματος Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "у буфера хоста Direct3D 12 отсутствует аргумент";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 主机缓冲区缺少其实参";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 主機緩衝區缺少其引數";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12ホストバッファに引数がありません";
        return "host Direct3D 12 buffer is missing its argument";
    }
    if (strcmp(msg, "host buffer is not host visible") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "host buffer is not host visible";
        if (strncmp(lang_code, "es", 2) == 0) return "el búfer del host no es visible para el host";
        if (strncmp(lang_code, "fr", 2) == 0) return "le tampon de l’hôte n’est pas visible par l’hôte";
        if (strncmp(lang_code, "de", 2) == 0) return "Host-Puffer ist für den Host nicht sichtbar";
        if (strncmp(lang_code, "it", 2) == 0) return "il buffer dell'host non è visibile all'host";
        if (strncmp(lang_code, "el", 2) == 0) return "η ενδιάμεση μνήμη κεντρικού συστήματος δεν είναι ορατή στο κεντρικό σύστημα";
        if (strncmp(lang_code, "ru", 2) == 0) return "буфер хоста не виден хосту";
        if (strncmp(lang_code, "zh", 2) == 0) return "主机缓冲区对主机不可见";
        if (strncmp(lang_code, "tw", 2) == 0) return "主機緩衝區對主機不可見";
        if (strncmp(lang_code, "ja", 2) == 0) return "ホストバッファがホストから参照できません";
        return "host buffer is not host visible";
    }
    if (strcmp(msg, "inactive Direct3D 12 buffer requests a transfer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "inactive Direct3D 12 buffer requests a transfer";
        if (strncmp(lang_code, "es", 2) == 0) return "un búfer inactivo de Direct3D 12 solicita una transferencia";
        if (strncmp(lang_code, "fr", 2) == 0) return "un tampon Direct3D 12 inactif demande un transfert";
        if (strncmp(lang_code, "de", 2) == 0) return "inaktiver Direct3D 12-Puffer fordert eine Übertragung an";
        if (strncmp(lang_code, "it", 2) == 0) return "un buffer Direct3D 12 inattivo richiede un trasferimento";
        if (strncmp(lang_code, "el", 2) == 0) return "μια ανενεργή ενδιάμεση μνήμη Direct3D 12 ζητά μεταφορά";
        if (strncmp(lang_code, "ru", 2) == 0) return "неактивный буфер Direct3D 12 запрашивает передачу";
        if (strncmp(lang_code, "zh", 2) == 0) return "非活动 Direct3D 12 缓冲区请求传输";
        if (strncmp(lang_code, "tw", 2) == 0) return "非作用中 Direct3D 12 緩衝區要求傳輸";
        if (strncmp(lang_code, "ja", 2) == 0) return "非アクティブなDirect3D 12バッファが転送を要求しています";
        return "inactive Direct3D 12 buffer requests a transfer";
    }
    if (strcmp(msg, "incomplete Direct3D 12 execution objects") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "incomplete Direct3D 12 execution objects";
        if (strncmp(lang_code, "es", 2) == 0) return "objetos de ejecución de Direct3D 12 incompletos";
        if (strncmp(lang_code, "fr", 2) == 0) return "objets d’exécution Direct3D 12 incomplets";
        if (strncmp(lang_code, "de", 2) == 0) return "unvollständige Direct3D 12-Ausführungsobjekte";
        if (strncmp(lang_code, "it", 2) == 0) return "oggetti di esecuzione Direct3D 12 incompleti";
        if (strncmp(lang_code, "el", 2) == 0) return "ελλιπή αντικείμενα εκτέλεσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "неполные объекты выполнения Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 执行对象不完整";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 執行物件不完整";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12実行オブジェクトが不完全です";
        return "incomplete Direct3D 12 execution objects";
    }
    if (strcmp(msg, "incomplete Direct3D 12 kernel objects") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "incomplete Direct3D 12 kernel objects";
        if (strncmp(lang_code, "es", 2) == 0) return "objetos de kernel de Direct3D 12 incompletos";
        if (strncmp(lang_code, "fr", 2) == 0) return "objets de kernel Direct3D 12 incomplets";
        if (strncmp(lang_code, "de", 2) == 0) return "unvollständige Direct3D 12-Kernelobjekte";
        if (strncmp(lang_code, "it", 2) == 0) return "oggetti kernel Direct3D 12 incompleti";
        if (strncmp(lang_code, "el", 2) == 0) return "ελλιπή αντικείμενα πυρήνα Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "неполные объекты ядра Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 内核对象不完整";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 核心物件不完整";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12カーネルオブジェクトが不完全です";
        return "incomplete Direct3D 12 kernel objects";
    }
    if (strcmp(msg, "incomplete Direct3D 12 prepared program") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "incomplete Direct3D 12 prepared program";
        if (strncmp(lang_code, "es", 2) == 0) return "programa preparado de Direct3D 12 incompleto";
        if (strncmp(lang_code, "fr", 2) == 0) return "programme Direct3D 12 préparé incomplet";
        if (strncmp(lang_code, "de", 2) == 0) return "unvollständiges vorbereitetes Direct3D 12-Programm";
        if (strncmp(lang_code, "it", 2) == 0) return "programma Direct3D 12 preparato incompleto";
        if (strncmp(lang_code, "el", 2) == 0) return "ελλιπές προετοιμασμένο πρόγραμμα Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "неполная подготовленная программа Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "准备的 Direct3D 12 程序不完整";
        if (strncmp(lang_code, "tw", 2) == 0) return "準備的 Direct3D 12 程式不完整";
        if (strncmp(lang_code, "ja", 2) == 0) return "準備済みDirect3D 12プログラムが不完全です";
        return "incomplete Direct3D 12 prepared program";
    }
    if (strcmp(msg, "incorrect value count") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "incorrect value count";
        if (strncmp(lang_code, "es", 2) == 0) return "cantidad de valores incorrecta";
        if (strncmp(lang_code, "fr", 2) == 0) return "nombre de valeurs incorrect";
        if (strncmp(lang_code, "de", 2) == 0) return "falsche Wertanzahl";
        if (strncmp(lang_code, "it", 2) == 0) return "numero di valori errato";
        if (strncmp(lang_code, "el", 2) == 0) return "εσφαλμένο πλήθος τιμών";
        if (strncmp(lang_code, "ru", 2) == 0) return "неверное количество значений";
        if (strncmp(lang_code, "zh", 2) == 0) return "值数量错误";
        if (strncmp(lang_code, "tw", 2) == 0) return "值數量錯誤";
        if (strncmp(lang_code, "ja", 2) == 0) return "値の数が正しくありません";
        return "incorrect value count";
    }
    if (strcmp(msg, "instruction limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "instruction limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de instrucciones";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite d’instructions dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Anweisungsgrenze überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite delle istruzioni superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου εντολών";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел инструкций";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出指令限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出指令限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "命令の上限を超えました";
        return "instruction limit exceeded";
    }
    if (strcmp(msg, "invalid Direct3D 12 buffer origin") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid Direct3D 12 buffer origin";
        if (strncmp(lang_code, "es", 2) == 0) return "origen de búfer de Direct3D 12 no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "origine de tampon Direct3D 12 non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Ursprung des Direct3D 12-Puffers";
        if (strncmp(lang_code, "it", 2) == 0) return "origine del buffer Direct3D 12 non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη προέλευση ενδιάμεσης μνήμης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое происхождение буфера Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的 Direct3D 12 缓冲区来源";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的 Direct3D 12 緩衝區來源";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12バッファの生成元が無効です";
        return "invalid Direct3D 12 buffer origin";
    }
    if (strcmp(msg, "invalid Direct3D 12 device list") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid Direct3D 12 device list";
        if (strncmp(lang_code, "es", 2) == 0) return "lista de dispositivos Direct3D 12 no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "liste de périphériques Direct3D 12 non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Direct3D 12-Geräteliste";
        if (strncmp(lang_code, "it", 2) == 0) return "elenco di dispositivi Direct3D 12 non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη λίστα συσκευών Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый список устройств Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的 Direct3D 12 设备列表";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的 Direct3D 12 裝置清單";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12デバイスリストが無効です";
        return "invalid Direct3D 12 device list";
    }
    if (strcmp(msg, "invalid Direct3D 12 device-only buffer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid Direct3D 12 device-only buffer";
        if (strncmp(lang_code, "es", 2) == 0) return "búfer de Direct3D 12 exclusivo del dispositivo no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "tampon Direct3D 12 réservé au périphérique non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger reiner Direct3D 12-Gerätepuffer";
        if (strncmp(lang_code, "it", 2) == 0) return "buffer Direct3D 12 riservato al dispositivo non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη ενδιάμεση μνήμη Direct3D 12 μόνο για συσκευή";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый буфер Direct3D 12 только для устройства";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的仅限设备 Direct3D 12 缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的僅限裝置 Direct3D 12 緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイス専用Direct3D 12バッファが無効です";
        return "invalid Direct3D 12 device-only buffer";
    }
    if (strcmp(msg, "invalid Direct3D 12 execution request") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid Direct3D 12 execution request";
        if (strncmp(lang_code, "es", 2) == 0) return "solicitud de ejecución de Direct3D 12 no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "demande d’exécution Direct3D 12 non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Direct3D 12-Ausführungsanforderung";
        if (strncmp(lang_code, "it", 2) == 0) return "richiesta di esecuzione Direct3D 12 non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο αίτημα εκτέλεσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый запрос выполнения Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的 Direct3D 12 执行请求";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的 Direct3D 12 執行要求";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12実行要求が無効です";
        return "invalid Direct3D 12 execution request";
    }
    if (strcmp(msg, "invalid Direct3D 12 kernel index") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid Direct3D 12 kernel index";
        if (strncmp(lang_code, "es", 2) == 0) return "índice de kernel de Direct3D 12 no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "index de kernel Direct3D 12 non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Direct3D 12-Kernelindex";
        if (strncmp(lang_code, "it", 2) == 0) return "indice del kernel Direct3D 12 non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος δείκτης πυρήνα Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый индекс ядра Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的 Direct3D 12 内核索引";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的 Direct3D 12 核心索引";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12カーネルインデックスが無効です";
        return "invalid Direct3D 12 kernel index";
    }
    if (strcmp(msg, "invalid ID bound") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid ID bound";
        if (strncmp(lang_code, "es", 2) == 0) return "límite de ID no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite d’ID non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige ID-Grenze";
        if (strncmp(lang_code, "it", 2) == 0) return "limite ID non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο όριο ID";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимая граница ID";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的 ID 边界";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的 ID 界限";
        if (strncmp(lang_code, "ja", 2) == 0) return "IDの上限が無効です";
        return "invalid ID bound";
    }
    if (strcmp(msg, "invalid active Direct3D 12 execution") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid active Direct3D 12 execution";
        if (strncmp(lang_code, "es", 2) == 0) return "ejecución activa de Direct3D 12 no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "exécution Direct3D 12 active non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige aktive Direct3D 12-Ausführung";
        if (strncmp(lang_code, "it", 2) == 0) return "esecuzione Direct3D 12 attiva non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη ενεργή εκτέλεση Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое активное выполнение Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的活动 Direct3D 12 执行";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的作用中 Direct3D 12 執行";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクティブなDirect3D 12実行が無効です";
        return "invalid active Direct3D 12 execution";
    }
    if (strcmp(msg, "invalid backend payload owner") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid backend payload owner";
        if (strncmp(lang_code, "es", 2) == 0) return "propietario de carga útil del backend no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "propriétaire de charge utile du backend non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Eigentümer der Backend-Nutzlast";
        if (strncmp(lang_code, "it", 2) == 0) return "proprietario del carico utile del backend non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος κάτοχος ωφέλιμου φορτίου backend";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый владелец полезной нагрузки бэкенда";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的后端有效载荷所有者";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的後端有效負載擁有者";
        if (strncmp(lang_code, "ja", 2) == 0) return "バックエンドペイロードの所有者が無効です";
        return "invalid backend payload owner";
    }
    if (strcmp(msg, "invalid binary size expression") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid binary size expression";
        if (strncmp(lang_code, "es", 2) == 0) return "expresión de tamaño binario no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "expression de taille binaire non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger binärer Größenausdruck";
        if (strncmp(lang_code, "it", 2) == 0) return "espressione di dimensione binaria non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη δυαδική έκφραση μεγέθους";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое двоичное выражение размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的二元大小表达式";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的二元大小運算式";
        if (strncmp(lang_code, "ja", 2) == 0) return "二項サイズ式が無効です";
        return "invalid binary size expression";
    }
    if (strcmp(msg, "invalid buffer argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid buffer argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento de búfer no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument de tampon non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Puffer-Argumentslot";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento del buffer non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη θέση ορίσματος ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый слот аргумента буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的缓冲区实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的緩衝區引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ引数スロットが無効です";
        return "invalid buffer argument slot";
    }
    if (strcmp(msg, "invalid buffer byte range") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid buffer byte range";
        if (strncmp(lang_code, "es", 2) == 0) return "rango de bytes del búfer no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "plage d’octets du tampon non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Bytebereich des Puffers";
        if (strncmp(lang_code, "it", 2) == 0) return "intervallo di byte del buffer non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο εύρος byte ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый диапазон байтов буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的缓冲区字节范围";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的緩衝區位元組範圍";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファのバイト範囲が無効です";
        return "invalid buffer byte range";
    }
    if (strcmp(msg, "invalid buffer element kind") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid buffer element kind";
        if (strncmp(lang_code, "es", 2) == 0) return "clase de elemento de búfer no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "catégorie d’élément de tampon non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Art des Pufferelements";
        if (strncmp(lang_code, "it", 2) == 0) return "tipo di elemento del buffer non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο είδος στοιχείου ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый вид элемента буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的缓冲区元素种类";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的緩衝區元素種類";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ要素の種類が無効です";
        return "invalid buffer element kind";
    }
    if (strcmp(msg, "invalid buffer element width") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid buffer element width";
        if (strncmp(lang_code, "es", 2) == 0) return "ancho de elemento de búfer no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "largeur d’élément de tampon non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Breite des Pufferelements";
        if (strncmp(lang_code, "it", 2) == 0) return "larghezza dell'elemento del buffer non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο πλάτος στοιχείου ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимая ширина элемента буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的缓冲区元素宽度";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的緩衝區元素寬度";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ要素の幅が無効です";
        return "invalid buffer element width";
    }
    if (strcmp(msg, "invalid buffer range end") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid buffer range end";
        if (strncmp(lang_code, "es", 2) == 0) return "fin de rango de búfer no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "fin de plage de tampon non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiges Ende des Pufferbereichs";
        if (strncmp(lang_code, "it", 2) == 0) return "fine dell'intervallo del buffer non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο τέλος εύρους ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый конец диапазона буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的缓冲区范围结束位置";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的緩衝區範圍結束位置";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ範囲の終端が無効です";
        return "invalid buffer range end";
    }
    if (strcmp(msg, "invalid buffer range start") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid buffer range start";
        if (strncmp(lang_code, "es", 2) == 0) return "inicio de rango de búfer no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "début de plage de tampon non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Anfang des Pufferbereichs";
        if (strncmp(lang_code, "it", 2) == 0) return "inizio dell'intervallo del buffer non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη αρχή εύρους ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое начало диапазона буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的缓冲区范围起始位置";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的緩衝區範圍起始位置";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ範囲の始端が無効です";
        return "invalid buffer range start";
    }
    if (strcmp(msg, "invalid buffer value type") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid buffer value type";
        if (strncmp(lang_code, "es", 2) == 0) return "tipo de valor de búfer no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "type de valeur de tampon non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Pufferwerttyp";
        if (strncmp(lang_code, "it", 2) == 0) return "tipo di valore del buffer non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος τύπος τιμής ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый тип значения буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的缓冲区值类型";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的緩衝區值型別";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ値の型が無効です";
        return "invalid buffer value type";
    }
    if (strcmp(msg, "invalid device buffer byte extent") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid device buffer byte extent";
        if (strncmp(lang_code, "es", 2) == 0) return "extensión en bytes del búfer del dispositivo no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "étendue en octets du tampon du périphérique non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Byteausdehnung des Gerätepuffers";
        if (strncmp(lang_code, "it", 2) == 0) return "estensione in byte del buffer del dispositivo non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη έκταση byte ενδιάμεσης μνήμης συσκευής";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый размер буфера устройства в байтах";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的设备缓冲区字节大小";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的裝置緩衝區位元組大小";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスバッファのバイト次元サイズが無効です";
        return "invalid device buffer byte extent";
    }
    if (strcmp(msg, "invalid device buffer extent") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid device buffer extent";
        if (strncmp(lang_code, "es", 2) == 0) return "extensión del búfer del dispositivo no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "étendue du tampon du périphérique non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Ausdehnung des Gerätepuffers";
        if (strncmp(lang_code, "it", 2) == 0) return "estensione del buffer del dispositivo non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη έκταση ενδιάμεσης μνήμης συσκευής";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый размер буфера устройства";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的设备缓冲区维度大小";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的裝置緩衝區維度大小";
        if (strncmp(lang_code, "ja", 2) == 0) return "デバイスバッファの次元サイズが無効です";
        return "invalid device buffer extent";
    }
    if (strcmp(msg, "invalid empty Direct3D 12 execution state") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid empty Direct3D 12 execution state";
        if (strncmp(lang_code, "es", 2) == 0) return "estado de ejecución vacía de Direct3D 12 no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "état d’exécution Direct3D 12 vide non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger leerer Direct3D 12-Ausführungszustand";
        if (strncmp(lang_code, "it", 2) == 0) return "stato di esecuzione Direct3D 12 vuota non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη κατάσταση κενής εκτέλεσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое состояние пустого выполнения Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的空 Direct3D 12 执行状态";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的空 Direct3D 12 執行狀態";
        if (strncmp(lang_code, "ja", 2) == 0) return "空のDirect3D 12実行状態が無効です";
        return "invalid empty Direct3D 12 execution state";
    }
    if (strcmp(msg, "invalid instruction") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid instruction";
        if (strncmp(lang_code, "es", 2) == 0) return "instrucción no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "instruction non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Anweisung";
        if (strncmp(lang_code, "it", 2) == 0) return "istruzione non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη εντολή";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимая инструкция";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效指令";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效指令";
        if (strncmp(lang_code, "ja", 2) == 0) return "命令が無効です";
        return "invalid instruction";
    }
    if (strcmp(msg, "invalid instruction length") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid instruction length";
        if (strncmp(lang_code, "es", 2) == 0) return "longitud de instrucción no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "longueur d’instruction non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Anweisungslänge";
        if (strncmp(lang_code, "it", 2) == 0) return "lunghezza dell'istruzione non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο μήκος εντολής";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимая длина инструкции";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的指令长度";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的指令長度";
        if (strncmp(lang_code, "ja", 2) == 0) return "命令長が無効です";
        return "invalid instruction length";
    }
    if (strcmp(msg, "invalid instruction result type") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid instruction result type";
        if (strncmp(lang_code, "es", 2) == 0) return "tipo de resultado de instrucción no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "type de résultat d’instruction non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Ergebnistyp der Anweisung";
        if (strncmp(lang_code, "it", 2) == 0) return "tipo di risultato dell'istruzione non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος τύπος αποτελέσματος εντολής";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый тип результата инструкции";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的指令结果类型";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的指令結果型別";
        if (strncmp(lang_code, "ja", 2) == 0) return "命令の結果型が無効です";
        return "invalid instruction result type";
    }
    if (strcmp(msg, "invalid kernel buffer range end") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid kernel buffer range end";
        if (strncmp(lang_code, "es", 2) == 0) return "fin de rango de búfer del kernel no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "fin de plage de tampon du kernel non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiges Ende des Kernelpufferbereichs";
        if (strncmp(lang_code, "it", 2) == 0) return "fine dell'intervallo del buffer del kernel non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο τέλος εύρους ενδιάμεσης μνήμης πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый конец диапазона буфера ядра";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的内核缓冲区范围结束位置";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的核心緩衝區範圍結束位置";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルバッファ範囲の終端が無効です";
        return "invalid kernel buffer range end";
    }
    if (strcmp(msg, "invalid kernel buffer range start") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid kernel buffer range start";
        if (strncmp(lang_code, "es", 2) == 0) return "inicio de rango de búfer del kernel no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "début de plage de tampon du kernel non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Anfang des Kernelpufferbereichs";
        if (strncmp(lang_code, "it", 2) == 0) return "inizio dell'intervallo del buffer del kernel non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη αρχή εύρους ενδιάμεσης μνήμης πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое начало диапазона буфера ядра";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的内核缓冲区范围起始位置";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的核心緩衝區範圍起始位置";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルバッファ範囲の始端が無効です";
        return "invalid kernel buffer range start";
    }
    if (strcmp(msg, "invalid kernel start") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid kernel start";
        if (strncmp(lang_code, "es", 2) == 0) return "inicio de kernel no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "début de kernel non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Kernelanfang";
        if (strncmp(lang_code, "it", 2) == 0) return "inizio del kernel non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη αρχή πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое начало ядра";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的内核起始位置";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的核心起始位置";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルの開始位置が無効です";
        return "invalid kernel start";
    }
    if (strcmp(msg, "invalid kernel stop") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid kernel stop";
        if (strncmp(lang_code, "es", 2) == 0) return "fin de kernel no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "fin de kernel non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiges Kernelende";
        if (strncmp(lang_code, "it", 2) == 0) return "fine del kernel non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο τέλος πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый конец ядра";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的内核停止位置";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的核心停止位置";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルの停止位置が無効です";
        return "invalid kernel stop";
    }
    if (strcmp(msg, "invalid kernel trip") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid kernel trip";
        if (strncmp(lang_code, "es", 2) == 0) return "iteración de kernel no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "itération de kernel non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Kerneliteration";
        if (strncmp(lang_code, "it", 2) == 0) return "iterazione del kernel non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη επανάληψη πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимая итерация ядра";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的内核迭代";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的核心迭代";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルの反復が無効です";
        return "invalid kernel trip";
    }
    if (strcmp(msg, "invalid local buffer argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid local buffer argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento de búfer local no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument de tampon local non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger lokaler Puffer-Argumentslot";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento del buffer locale non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη θέση ορίσματος τοπικής ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый слот аргумента локального буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的局部缓冲区实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的區域緩衝區引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "ローカルバッファ引数スロットが無効です";
        return "invalid local buffer argument slot";
    }
    if (strcmp(msg, "invalid loop block id") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid loop block id";
        if (strncmp(lang_code, "es", 2) == 0) return "id de bloque de bucle no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "id de bloc de boucle non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Schleifenblock-ID";
        if (strncmp(lang_code, "it", 2) == 0) return "id del blocco del ciclo non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο id μπλοκ βρόχου";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый id блока цикла";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的循环块 id";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的迴圈區塊 id";
        if (strncmp(lang_code, "ja", 2) == 0) return "ループブロックのidが無効です";
        return "invalid loop block id";
    }
    if (strcmp(msg, "invalid region block id") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid region block id";
        if (strncmp(lang_code, "es", 2) == 0) return "id de bloque de región no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "id de bloc de région non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Regionsblock-ID";
        if (strncmp(lang_code, "it", 2) == 0) return "id del blocco della regione non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο id μπλοκ περιοχής";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый id блока области";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的区域块 id";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的區域區塊 id";
        if (strncmp(lang_code, "ja", 2) == 0) return "リージョンブロックのidが無効です";
        return "invalid region block id";
    }
    if (strcmp(msg, "invalid scalar argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid scalar argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento escalar no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument scalaire non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Skalar-Argumentslot";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento scalare non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη θέση βαθμωτού ορίσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый слот скалярного аргумента";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的标量实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的純量引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー引数スロットが無効です";
        return "invalid scalar argument slot";
    }
    if (strcmp(msg, "invalid scalar result argument slot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid scalar result argument slot";
        if (strncmp(lang_code, "es", 2) == 0) return "ranura de argumento de resultado escalar no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "emplacement d’argument de résultat scalaire non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Argumentslot für skalares Ergebnis";
        if (strncmp(lang_code, "it", 2) == 0) return "slot di argomento del risultato scalare non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη θέση ορίσματος βαθμωτού αποτελέσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый слот аргумента скалярного результата";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的标量结果实参槽位";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的純量結果引數槽位";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果引数スロットが無効です";
        return "invalid scalar result argument slot";
    }
    if (strcmp(msg, "invalid scalar result identity") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid scalar result identity";
        if (strncmp(lang_code, "es", 2) == 0) return "identidad de resultado escalar no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "identité de résultat scalaire non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Identität des skalaren Ergebnisses";
        if (strncmp(lang_code, "it", 2) == 0) return "identità del risultato scalare non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη ταυτότητα βαθμωτού αποτελέσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый идентификатор скалярного результата";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的标量结果标识";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的純量結果識別";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果の識別情報が無効です";
        return "invalid scalar result identity";
    }
    if (strcmp(msg, "invalid scalar result producer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid scalar result producer";
        if (strncmp(lang_code, "es", 2) == 0) return "productor de resultado escalar no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "producteur de résultat scalaire non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Erzeuger des skalaren Ergebnisses";
        if (strncmp(lang_code, "it", 2) == 0) return "produttore del risultato scalare non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος παραγωγός βαθμωτού αποτελέσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый производитель скалярного результата";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的标量结果生产者";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的純量結果產生者";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果のプロデューサーが無効です";
        return "invalid scalar result producer";
    }
    if (strcmp(msg, "invalid scalar result type") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid scalar result type";
        if (strncmp(lang_code, "es", 2) == 0) return "tipo de resultado escalar no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "type de résultat scalaire non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Typ des skalaren Ergebnisses";
        if (strncmp(lang_code, "it", 2) == 0) return "tipo di risultato scalare non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος τύπος βαθμωτού αποτελέσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый тип скалярного результата";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的标量结果类型";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的純量結果型別";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果の型が無効です";
        return "invalid scalar result type";
    }
    if (strcmp(msg, "invalid scalar type") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid scalar type";
        if (strncmp(lang_code, "es", 2) == 0) return "tipo escalar no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "type scalaire non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Skalartyp";
        if (strncmp(lang_code, "it", 2) == 0) return "tipo scalare non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος βαθμωτός τύπος";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый скалярный тип";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的标量类型";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的純量型別";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー型が無効です";
        return "invalid scalar type";
    }
    if (strcmp(msg, "invalid shader kernel index") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid shader kernel index";
        if (strncmp(lang_code, "es", 2) == 0) return "índice de kernel de shader no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "index de kernel de shader non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Shader-Kernelindex";
        if (strncmp(lang_code, "it", 2) == 0) return "indice del kernel shader non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος δείκτης πυρήνα shader";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый индекс ядра шейдера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的着色器内核索引";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的著色器核心索引";
        if (strncmp(lang_code, "ja", 2) == 0) return "シェーダーカーネルインデックスが無効です";
        return "invalid shader kernel index";
    }
    if (strcmp(msg, "invalid size multiplication") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid size multiplication";
        if (strncmp(lang_code, "es", 2) == 0) return "multiplicación de tamaño no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "multiplication de taille non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültige Größenmultiplikation";
        if (strncmp(lang_code, "it", 2) == 0) return "moltiplicazione della dimensione non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρος πολλαπλασιασμός μεγέθους";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое умножение размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的大小乘法";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的大小乘法";
        if (strncmp(lang_code, "ja", 2) == 0) return "サイズの乗算が無効です";
        return "invalid size multiplication";
    }
    if (strcmp(msg, "invalid size opcode") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid size opcode";
        if (strncmp(lang_code, "es", 2) == 0) return "código de operación de tamaño no válido";
        if (strncmp(lang_code, "fr", 2) == 0) return "code d’opération de taille non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger Größen-Opcode";
        if (strncmp(lang_code, "it", 2) == 0) return "opcode della dimensione non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρο opcode μεγέθους";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимый код операции размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的大小操作码";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的大小操作碼";
        if (strncmp(lang_code, "ja", 2) == 0) return "サイズオペコードが無効です";
        return "invalid size opcode";
    }
    if (strcmp(msg, "invalid size scalar reference") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid size scalar reference";
        if (strncmp(lang_code, "es", 2) == 0) return "referencia escalar de tamaño no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "référence scalaire de taille non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger skalarer Größenverweis";
        if (strncmp(lang_code, "it", 2) == 0) return "riferimento scalare della dimensione non valido";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη βαθμωτή αναφορά μεγέθους";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимая скалярная ссылка размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的大小标量引用";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的大小純量參照";
        if (strncmp(lang_code, "ja", 2) == 0) return "サイズスカラー参照が無効です";
        return "invalid size scalar reference";
    }
    if (strcmp(msg, "invalid zero-clamped size expression") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "invalid zero-clamped size expression";
        if (strncmp(lang_code, "es", 2) == 0) return "expresión de tamaño limitada a cero no válida";
        if (strncmp(lang_code, "fr", 2) == 0) return "expression de taille bornée à zéro non valide";
        if (strncmp(lang_code, "de", 2) == 0) return "ungültiger auf null begrenzter Größenausdruck";
        if (strncmp(lang_code, "it", 2) == 0) return "espressione di dimensione limitata a zero non valida";
        if (strncmp(lang_code, "el", 2) == 0) return "μη έγκυρη έκφραση μεγέθους περιορισμένη στο μηδέν";
        if (strncmp(lang_code, "ru", 2) == 0) return "недопустимое выражение размера с ограничением нулём";
        if (strncmp(lang_code, "zh", 2) == 0) return "无效的零钳制大小表达式";
        if (strncmp(lang_code, "tw", 2) == 0) return "無效的零箝制大小運算式";
        if (strncmp(lang_code, "ja", 2) == 0) return "ゼロクランプされたサイズ式が無効です";
        return "invalid zero-clamped size expression";
    }
    if (strcmp(msg, "kernel buffer table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "kernel buffer table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "la tabla de búferes del kernel no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "la table des tampons du kernel ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Kernelpuffertabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "la tabella dei buffer del kernel non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "ο πίνακας ενδιάμεσων μνημών πυρήνα δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "таблица буферов ядра не соответствует";
        if (strncmp(lang_code, "zh", 2) == 0) return "内核缓冲区表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "核心緩衝區表不符";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルバッファテーブルが一致しません";
        return "kernel buffer table mismatch";
    }
    if (strcmp(msg, "kernel buffer type mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "kernel buffer type mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "el tipo de búfer del kernel no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "le type de tampon du kernel ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Kernelpuffertyp stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "il tipo di buffer del kernel non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "ο τύπος ενδιάμεσης μνήμης πυρήνα δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "тип буфера ядра не соответствует";
        if (strncmp(lang_code, "zh", 2) == 0) return "内核缓冲区类型不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "核心緩衝區型別不符";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルバッファの型が一致しません";
        return "kernel buffer type mismatch";
    }
    if (strcmp(msg, "kernel limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "kernel limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de kernels";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite de kernels dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Kernelgrenze überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite dei kernel superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου πυρήνων";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел ядер";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出内核限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出核心限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルの上限を超えました";
        return "kernel limit exceeded";
    }
    if (strcmp(msg, "kernel scalar table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "kernel scalar table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "la tabla de escalares del kernel no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "la table des scalaires du kernel ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Kernelskalartabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "la tabella degli scalari del kernel non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "ο πίνακας βαθμωτών τιμών πυρήνα δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "таблица скаляров ядра не соответствует";
        if (strncmp(lang_code, "zh", 2) == 0) return "内核标量表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "核心純量表不符";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルスカラーテーブルが一致しません";
        return "kernel scalar table mismatch";
    }
    if (strcmp(msg, "missing Direct3D 12 buffer metadata") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 buffer metadata";
        if (strncmp(lang_code, "es", 2) == 0) return "faltan metadatos del búfer de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "métadonnées du tampon Direct3D 12 manquantes";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Metadaten des Direct3D 12-Puffers";
        if (strncmp(lang_code, "it", 2) == 0) return "metadati del buffer Direct3D 12 mancanti";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπουν μεταδεδομένα ενδιάμεσης μνήμης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствуют метаданные буфера Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 缓冲区元数据";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 緩衝區中繼資料";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12バッファのメタデータがありません";
        return "missing Direct3D 12 buffer metadata";
    }
    if (strcmp(msg, "missing Direct3D 12 buffer snapshot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 buffer snapshot";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la instantánea del búfer de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "instantané du tampon Direct3D 12 manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Momentaufnahme des Direct3D 12-Puffers";
        if (strncmp(lang_code, "it", 2) == 0) return "istantanea del buffer Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει στιγμιότυπο ενδιάμεσης μνήμης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует снимок буфера Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 缓冲区快照";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 緩衝區快照";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12バッファのスナップショットがありません";
        return "missing Direct3D 12 buffer snapshot";
    }
    if (strcmp(msg, "missing Direct3D 12 buffer table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 buffer table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de búferes de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des tampons Direct3D 12 manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Puffertabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella dei buffer Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας ενδιάμεσων μνημών Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица буферов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 缓冲区表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 緩衝區表";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12バッファテーブルがありません";
        return "missing Direct3D 12 buffer table";
    }
    if (strcmp(msg, "missing Direct3D 12 download snapshot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 download snapshot";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la instantánea de descarga de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "instantané de téléchargement Direct3D 12 manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Download-Momentaufnahme";
        if (strncmp(lang_code, "it", 2) == 0) return "istantanea di download Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει στιγμιότυπο λήψης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует снимок загрузки Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 下载快照";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 下載快照";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12ダウンロードスナップショットがありません";
        return "missing Direct3D 12 download snapshot";
    }
    if (strcmp(msg, "missing Direct3D 12 execution") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 execution";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la ejecución de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "exécution Direct3D 12 manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Ausführung";
        if (strncmp(lang_code, "it", 2) == 0) return "esecuzione Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει εκτέλεση Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует выполнение Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 执行";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 執行";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12実行がありません";
        return "missing Direct3D 12 execution";
    }
    if (strcmp(msg, "missing Direct3D 12 execution output") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 execution output";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la salida de la ejecución de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "sortie d’exécution Direct3D 12 manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Ausgabe der Direct3D 12-Ausführung";
        if (strncmp(lang_code, "it", 2) == 0) return "output dell'esecuzione Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει έξοδος εκτέλεσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует вывод выполнения Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 执行输出";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 執行輸出";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12実行の出力がありません";
        return "missing Direct3D 12 execution output";
    }
    if (strcmp(msg, "missing Direct3D 12 output table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 output table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de salidas de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des sorties Direct3D 12 manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Ausgabetabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella degli output Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας εξόδου Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица вывода Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 输出表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 輸出表";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12出力テーブルがありません";
        return "missing Direct3D 12 output table";
    }
    if (strcmp(msg, "missing Direct3D 12 program") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 program";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el programa de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "programme Direct3D 12 manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlendes Direct3D 12-Programm";
        if (strncmp(lang_code, "it", 2) == 0) return "programma Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πρόγραμμα Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует программа Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 程序";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 程式";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12プログラムがありません";
        return "missing Direct3D 12 program";
    }
    if (strcmp(msg, "missing Direct3D 12 readback buffer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 readback buffer";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el búfer de lectura de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "tampon de relecture Direct3D 12 manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Direct3D 12-Rücklesepuffer";
        if (strncmp(lang_code, "it", 2) == 0) return "buffer di rilettura Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει ενδιάμεση μνήμη ανάγνωσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует буфер обратного чтения Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 回读缓冲区";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 回讀緩衝區";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12リードバックバッファがありません";
        return "missing Direct3D 12 readback buffer";
    }
    if (strcmp(msg, "missing Direct3D 12 result resources") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 result resources";
        if (strncmp(lang_code, "es", 2) == 0) return "faltan recursos de resultados de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "ressources de résultats Direct3D 12 manquantes";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Ergebnisressourcen";
        if (strncmp(lang_code, "it", 2) == 0) return "risorse dei risultati Direct3D 12 mancanti";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπουν πόροι αποτελεσμάτων Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствуют ресурсы результатов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 结果资源";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 結果資源";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12結果リソースがありません";
        return "missing Direct3D 12 result resources";
    }
    if (strcmp(msg, "missing Direct3D 12 result words") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 result words";
        if (strncmp(lang_code, "es", 2) == 0) return "faltan palabras de resultados de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "mots de résultats Direct3D 12 manquants";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Ergebniswörter";
        if (strncmp(lang_code, "it", 2) == 0) return "parole dei risultati Direct3D 12 mancanti";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπουν λέξεις αποτελεσμάτων Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствуют слова результатов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 结果字";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 結果字組";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12結果ワードがありません";
        return "missing Direct3D 12 result words";
    }
    if (strcmp(msg, "missing Direct3D 12 scalar words") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 scalar words";
        if (strncmp(lang_code, "es", 2) == 0) return "faltan palabras escalares de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "mots scalaires Direct3D 12 manquants";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Skalarwörter";
        if (strncmp(lang_code, "it", 2) == 0) return "parole scalari Direct3D 12 mancanti";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπουν βαθμωτές λέξεις Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствуют скалярные слова Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 标量字";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 純量字組";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12スカラーワードがありません";
        return "missing Direct3D 12 scalar words";
    }
    if (strcmp(msg, "missing Direct3D 12 upload snapshot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing Direct3D 12 upload snapshot";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la instantánea de carga de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "instantané de téléversement Direct3D 12 manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Direct3D 12-Upload-Momentaufnahme";
        if (strncmp(lang_code, "it", 2) == 0) return "istantanea di caricamento Direct3D 12 mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει στιγμιότυπο μεταφόρτωσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует снимок выгрузки Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少 Direct3D 12 上传快照";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少 Direct3D 12 上傳快照";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12アップロードスナップショットがありません";
        return "missing Direct3D 12 upload snapshot";
    }
    if (strcmp(msg, "missing accelerator kernel table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing accelerator kernel table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de kernels del acelerador";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des kernels de l’accélérateur manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Beschleuniger-Kerneltabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella dei kernel dell'acceleratore mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας πυρήνων επιταχυντή";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица ядер ускорителя";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器内核表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器核心表";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータカーネルテーブルがありません";
        return "missing accelerator kernel table";
    }
    if (strcmp(msg, "missing accelerator program") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing accelerator program";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el programa del acelerador";
        if (strncmp(lang_code, "fr", 2) == 0) return "programme de l’accélérateur manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlendes Beschleunigerprogramm";
        if (strncmp(lang_code, "it", 2) == 0) return "programma dell'acceleratore mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πρόγραμμα επιταχυντή";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует программа ускорителя";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少加速器程序";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少加速器程式";
        if (strncmp(lang_code, "ja", 2) == 0) return "アクセラレータプログラムがありません";
        return "missing accelerator program";
    }
    if (strcmp(msg, "missing buffer name") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing buffer name";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el nombre del búfer";
        if (strncmp(lang_code, "fr", 2) == 0) return "nom du tampon manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Puffername";
        if (strncmp(lang_code, "it", 2) == 0) return "nome del buffer mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει όνομα ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует имя буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少缓冲区名称";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少緩衝區名稱";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファ名がありません";
        return "missing buffer name";
    }
    if (strcmp(msg, "missing buffer table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing buffer table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de búferes";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des tampons manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Puffertabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella dei buffer mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας ενδιάμεσων μνημών";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица буферов";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少缓冲区表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少緩衝區表";
        if (strncmp(lang_code, "ja", 2) == 0) return "バッファテーブルがありません";
        return "missing buffer table";
    }
    if (strcmp(msg, "missing compute entry contract") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing compute entry contract";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el contrato del punto de entrada de cómputo";
        if (strncmp(lang_code, "fr", 2) == 0) return "contrat du point d’entrée de calcul manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Vertrag des Compute-Einstiegspunkts";
        if (strncmp(lang_code, "it", 2) == 0) return "contratto del punto di ingresso di calcolo mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει συμβόλαιο σημείου εισόδου υπολογισμού";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует контракт точки входа вычислений";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少计算入口协定";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少計算進入點合約";
        if (strncmp(lang_code, "ja", 2) == 0) return "コンピュートエントリのコントラクトがありません";
        return "missing compute entry contract";
    }
    if (strcmp(msg, "missing function name") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing function name";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el nombre de la función";
        if (strncmp(lang_code, "fr", 2) == 0) return "nom de fonction manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Funktionsname";
        if (strncmp(lang_code, "it", 2) == 0) return "nome della funzione mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει όνομα συνάρτησης";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует имя функции";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少函数名称";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少函式名稱";
        if (strncmp(lang_code, "ja", 2) == 0) return "関数名がありません";
        return "missing function name";
    }
    if (strcmp(msg, "missing instruction table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing instruction table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de instrucciones";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des instructions manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Anweisungstabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella delle istruzioni mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας εντολών";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица инструкций";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少指令表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少指令表";
        if (strncmp(lang_code, "ja", 2) == 0) return "命令テーブルがありません";
        return "missing instruction table";
    }
    if (strcmp(msg, "missing kernel name") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing kernel name";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el nombre del kernel";
        if (strncmp(lang_code, "fr", 2) == 0) return "nom du kernel manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Kernelname";
        if (strncmp(lang_code, "it", 2) == 0) return "nome del kernel mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει όνομα πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует имя ядра";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少内核名称";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少核心名稱";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネル名がありません";
        return "missing kernel name";
    }
    if (strcmp(msg, "missing kernel table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing kernel table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de kernels";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des kernels manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Kerneltabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella dei kernel mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας πυρήνων";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица ядер";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少内核表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少核心表";
        if (strncmp(lang_code, "ja", 2) == 0) return "カーネルテーブルがありません";
        return "missing kernel table";
    }
    if (strcmp(msg, "missing scalar name") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing scalar name";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el nombre del escalar";
        if (strncmp(lang_code, "fr", 2) == 0) return "nom du scalaire manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Skalarname";
        if (strncmp(lang_code, "it", 2) == 0) return "nome dello scalare mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει όνομα βαθμωτού";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует имя скаляра";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少标量名称";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少純量名稱";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー名がありません";
        return "missing scalar name";
    }
    if (strcmp(msg, "missing scalar result name") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing scalar result name";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el nombre del resultado escalar";
        if (strncmp(lang_code, "fr", 2) == 0) return "nom du résultat scalaire manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Name des skalaren Ergebnisses";
        if (strncmp(lang_code, "it", 2) == 0) return "nome del risultato scalare mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει όνομα βαθμωτού αποτελέσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует имя скалярного результата";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少标量结果名称";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少純量結果名稱";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果名がありません";
        return "missing scalar result name";
    }
    if (strcmp(msg, "missing scalar result table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing scalar result table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de resultados escalares";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des résultats scalaires manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Tabelle der skalaren Ergebnisse";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella dei risultati scalari mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας βαθμωτών αποτελεσμάτων";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица скалярных результатов";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少标量结果表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少純量結果表";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果テーブルがありません";
        return "missing scalar result table";
    }
    if (strcmp(msg, "missing scalar table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing scalar table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de escalares";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des scalaires manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Skalartabelle";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella degli scalari mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας βαθμωτών τιμών";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица скаляров";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少标量表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少純量表";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラーテーブルがありません";
        return "missing scalar table";
    }
    if (strcmp(msg, "missing shader output") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing shader output";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la salida del shader";
        if (strncmp(lang_code, "fr", 2) == 0) return "sortie du shader manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Shader-Ausgabe";
        if (strncmp(lang_code, "it", 2) == 0) return "output dello shader mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει έξοδος shader";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует вывод шейдера";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少着色器输出";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少著色器輸出";
        if (strncmp(lang_code, "ja", 2) == 0) return "シェーダー出力がありません";
        return "missing shader output";
    }
    if (strcmp(msg, "missing size expression table") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing size expression table";
        if (strncmp(lang_code, "es", 2) == 0) return "falta la tabla de expresiones de tamaño";
        if (strncmp(lang_code, "fr", 2) == 0) return "table des expressions de taille manquante";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlende Tabelle der Größenausdrücke";
        if (strncmp(lang_code, "it", 2) == 0) return "tabella delle espressioni di dimensione mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πίνακας εκφράσεων μεγέθους";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует таблица выражений размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少大小表达式表";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少大小運算式表";
        if (strncmp(lang_code, "ja", 2) == 0) return "サイズ式テーブルがありません";
        return "missing size expression table";
    }
    if (strcmp(msg, "missing source name") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing source name";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el nombre de la fuente";
        if (strncmp(lang_code, "fr", 2) == 0) return "nom de source manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender Quellname";
        if (strncmp(lang_code, "it", 2) == 0) return "nome della sorgente mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει όνομα πηγής";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует имя источника";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少源名称";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少來源名稱";
        if (strncmp(lang_code, "ja", 2) == 0) return "ソース名がありません";
        return "missing source name";
    }
    if (strcmp(msg, "missing strict Float32 contract") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing strict Float32 contract";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el contrato estricto Float32";
        if (strncmp(lang_code, "fr", 2) == 0) return "contrat Float32 strict manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender strenger Float32-Vertrag";
        if (strncmp(lang_code, "it", 2) == 0) return "contratto Float32 rigoroso mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει αυστηρό συμβόλαιο Float32";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует строгий контракт Float32";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少严格的 Float32 协定";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少嚴格的 Float32 合約";
        if (strncmp(lang_code, "ja", 2) == 0) return "厳密なFloat32コントラクトがありません";
        return "missing strict Float32 contract";
    }
    if (strcmp(msg, "missing typed kernel") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing typed kernel";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el kernel tipado";
        if (strncmp(lang_code, "fr", 2) == 0) return "kernel typé manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender typisierter Kernel";
        if (strncmp(lang_code, "it", 2) == 0) return "kernel tipizzato mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πυρήνας με τύπους";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует типизированное ядро";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少有类型的内核";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少具型別的核心";
        if (strncmp(lang_code, "ja", 2) == 0) return "型付きカーネルがありません";
        return "missing typed kernel";
    }
    if (strcmp(msg, "missing typed shader kernel") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "missing typed shader kernel";
        if (strncmp(lang_code, "es", 2) == 0) return "falta el kernel de shader tipado";
        if (strncmp(lang_code, "fr", 2) == 0) return "kernel de shader typé manquant";
        if (strncmp(lang_code, "de", 2) == 0) return "fehlender typisierter Shader-Kernel";
        if (strncmp(lang_code, "it", 2) == 0) return "kernel shader tipizzato mancante";
        if (strncmp(lang_code, "el", 2) == 0) return "λείπει πυρήνας shader με τύπους";
        if (strncmp(lang_code, "ru", 2) == 0) return "отсутствует типизированное ядро шейдера";
        if (strncmp(lang_code, "zh", 2) == 0) return "缺少有类型的着色器内核";
        if (strncmp(lang_code, "tw", 2) == 0) return "缺少具型別的著色器核心";
        if (strncmp(lang_code, "ja", 2) == 0) return "型付きシェーダーカーネルがありません";
        return "missing typed shader kernel";
    }
    if (strcmp(msg, "negative size constant") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "negative size constant";
        if (strncmp(lang_code, "es", 2) == 0) return "constante de tamaño negativa";
        if (strncmp(lang_code, "fr", 2) == 0) return "constante de taille négative";
        if (strncmp(lang_code, "de", 2) == 0) return "negative Größenkonstante";
        if (strncmp(lang_code, "it", 2) == 0) return "costante di dimensione negativa";
        if (strncmp(lang_code, "el", 2) == 0) return "αρνητική σταθερά μεγέθους";
        if (strncmp(lang_code, "ru", 2) == 0) return "отрицательная константа размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "负大小常量";
        if (strncmp(lang_code, "tw", 2) == 0) return "負大小常數";
        if (strncmp(lang_code, "ja", 2) == 0) return "負のサイズ定数";
        return "negative size constant";
    }
    if (strcmp(msg, "nonconstant integer divisor") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nonconstant integer divisor";
        if (strncmp(lang_code, "es", 2) == 0) return "divisor entero no constante";
        if (strncmp(lang_code, "fr", 2) == 0) return "diviseur entier non constant";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht konstanter ganzzahliger Divisor";
        if (strncmp(lang_code, "it", 2) == 0) return "divisore intero non costante";
        if (strncmp(lang_code, "el", 2) == 0) return "μη σταθερός ακέραιος διαιρέτης";
        if (strncmp(lang_code, "ru", 2) == 0) return "неконстантный целочисленный делитель";
        if (strncmp(lang_code, "zh", 2) == 0) return "非常量整数除数";
        if (strncmp(lang_code, "tw", 2) == 0) return "非常數整數除數";
        if (strncmp(lang_code, "ja", 2) == 0) return "非定数の整数除数";
        return "nonconstant integer divisor";
    }
    if (strcmp(msg, "nonconstant shift count") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nonconstant shift count";
        if (strncmp(lang_code, "es", 2) == 0) return "cantidad de desplazamiento no constante";
        if (strncmp(lang_code, "fr", 2) == 0) return "nombre de décalages non constant";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht konstante Verschiebungsanzahl";
        if (strncmp(lang_code, "it", 2) == 0) return "numero di spostamenti non costante";
        if (strncmp(lang_code, "el", 2) == 0) return "μη σταθερό πλήθος μετατόπισης";
        if (strncmp(lang_code, "ru", 2) == 0) return "неконстантное число сдвигов";
        if (strncmp(lang_code, "zh", 2) == 0) return "非常量移位计数";
        if (strncmp(lang_code, "tw", 2) == 0) return "非常數位移計數";
        if (strncmp(lang_code, "ja", 2) == 0) return "非定数のシフト数";
        return "nonconstant shift count";
    }
    if (strcmp(msg, "nondeterministic buffer binding") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nondeterministic buffer binding";
        if (strncmp(lang_code, "es", 2) == 0) return "vinculación de búfer no determinista";
        if (strncmp(lang_code, "fr", 2) == 0) return "liaison de tampon non déterministe";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht deterministische Pufferbindung";
        if (strncmp(lang_code, "it", 2) == 0) return "associazione del buffer non deterministica";
        if (strncmp(lang_code, "el", 2) == 0) return "μη ντετερμινιστική δέσμευση ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "недетерминированная привязка буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "不确定的缓冲区绑定";
        if (strncmp(lang_code, "tw", 2) == 0) return "不確定的緩衝區繫結";
        if (strncmp(lang_code, "ja", 2) == 0) return "非決定的なバッファのバインディング";
        return "nondeterministic buffer binding";
    }
    if (strcmp(msg, "nondeterministic kernel index") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nondeterministic kernel index";
        if (strncmp(lang_code, "es", 2) == 0) return "índice de kernel no determinista";
        if (strncmp(lang_code, "fr", 2) == 0) return "index de kernel non déterministe";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht deterministischer Kernelindex";
        if (strncmp(lang_code, "it", 2) == 0) return "indice del kernel non deterministico";
        if (strncmp(lang_code, "el", 2) == 0) return "μη ντετερμινιστικός δείκτης πυρήνα";
        if (strncmp(lang_code, "ru", 2) == 0) return "недетерминированный индекс ядра";
        if (strncmp(lang_code, "zh", 2) == 0) return "不确定的内核索引";
        if (strncmp(lang_code, "tw", 2) == 0) return "不確定的核心索引";
        if (strncmp(lang_code, "ja", 2) == 0) return "非決定的なカーネルインデックス";
        return "nondeterministic kernel index";
    }
    if (strcmp(msg, "nondeterministic scalar result entry") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nondeterministic scalar result entry";
        if (strncmp(lang_code, "es", 2) == 0) return "entrada de resultado escalar no determinista";
        if (strncmp(lang_code, "fr", 2) == 0) return "entrée de résultat scalaire non déterministe";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht deterministischer Eintrag des skalaren Ergebnisses";
        if (strncmp(lang_code, "it", 2) == 0) return "voce del risultato scalare non deterministica";
        if (strncmp(lang_code, "el", 2) == 0) return "μη ντετερμινιστική καταχώριση βαθμωτού αποτελέσματος";
        if (strncmp(lang_code, "ru", 2) == 0) return "недетерминированная запись скалярного результата";
        if (strncmp(lang_code, "zh", 2) == 0) return "不确定的标量结果条目";
        if (strncmp(lang_code, "tw", 2) == 0) return "不確定的純量結果項目";
        if (strncmp(lang_code, "ja", 2) == 0) return "非決定的なスカラー結果エントリ";
        return "nondeterministic scalar result entry";
    }
    if (strcmp(msg, "nondeterministic shader kernel index") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nondeterministic shader kernel index";
        if (strncmp(lang_code, "es", 2) == 0) return "índice de kernel de shader no determinista";
        if (strncmp(lang_code, "fr", 2) == 0) return "index de kernel de shader non déterministe";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht deterministischer Shader-Kernelindex";
        if (strncmp(lang_code, "it", 2) == 0) return "indice del kernel shader non deterministico";
        if (strncmp(lang_code, "el", 2) == 0) return "μη ντετερμινιστικός δείκτης πυρήνα shader";
        if (strncmp(lang_code, "ru", 2) == 0) return "недетерминированный индекс ядра шейдера";
        if (strncmp(lang_code, "zh", 2) == 0) return "不确定的着色器内核索引";
        if (strncmp(lang_code, "tw", 2) == 0) return "不確定的著色器核心索引";
        if (strncmp(lang_code, "ja", 2) == 0) return "非決定的なシェーダーカーネルインデックス";
        return "nondeterministic shader kernel index";
    }
    if (strcmp(msg, "noninteger size scalar") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "noninteger size scalar";
        if (strncmp(lang_code, "es", 2) == 0) return "escalar de tamaño no entero";
        if (strncmp(lang_code, "fr", 2) == 0) return "scalaire de taille non entier";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht ganzzahliger Größenskalar";
        if (strncmp(lang_code, "it", 2) == 0) return "scalare di dimensione non intero";
        if (strncmp(lang_code, "el", 2) == 0) return "μη ακέραιο βαθμωτό μέγεθος";
        if (strncmp(lang_code, "ru", 2) == 0) return "нецелочисленный скаляр размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "非整数大小标量";
        if (strncmp(lang_code, "tw", 2) == 0) return "非整數大小純量";
        if (strncmp(lang_code, "ja", 2) == 0) return "非整数のサイズスカラー";
        return "noninteger size scalar";
    }
    if (strcmp(msg, "nonpositive device buffer extent") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nonpositive device buffer extent";
        if (strncmp(lang_code, "es", 2) == 0) return "extensión no positiva del búfer del dispositivo";
        if (strncmp(lang_code, "fr", 2) == 0) return "étendue non positive du tampon du périphérique";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht positive Ausdehnung des Gerätepuffers";
        if (strncmp(lang_code, "it", 2) == 0) return "estensione non positiva del buffer del dispositivo";
        if (strncmp(lang_code, "el", 2) == 0) return "μη θετική έκταση ενδιάμεσης μνήμης συσκευής";
        if (strncmp(lang_code, "ru", 2) == 0) return "неположительный размер буфера устройства";
        if (strncmp(lang_code, "zh", 2) == 0) return "非正数设备缓冲区维度大小";
        if (strncmp(lang_code, "tw", 2) == 0) return "非正數裝置緩衝區維度大小";
        if (strncmp(lang_code, "ja", 2) == 0) return "正でないデバイスバッファの次元サイズ";
        return "nonpositive device buffer extent";
    }
    if (strcmp(msg, "nonpositive integer divisor") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "nonpositive integer divisor";
        if (strncmp(lang_code, "es", 2) == 0) return "divisor entero no positivo";
        if (strncmp(lang_code, "fr", 2) == 0) return "diviseur entier non positif";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht positiver ganzzahliger Divisor";
        if (strncmp(lang_code, "it", 2) == 0) return "divisore intero non positivo";
        if (strncmp(lang_code, "el", 2) == 0) return "μη θετικός ακέραιος διαιρέτης";
        if (strncmp(lang_code, "ru", 2) == 0) return "неположительный целочисленный делитель";
        if (strncmp(lang_code, "zh", 2) == 0) return "非正数整数除数";
        if (strncmp(lang_code, "tw", 2) == 0) return "非正數整數除數";
        if (strncmp(lang_code, "ja", 2) == 0) return "正でない整数除数";
        return "nonpositive integer divisor";
    }
    if (strcmp(msg, "null kernel") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "null kernel";
        if (strncmp(lang_code, "es", 2) == 0) return "kernel nulo";
        if (strncmp(lang_code, "fr", 2) == 0) return "kernel nul";
        if (strncmp(lang_code, "de", 2) == 0) return "Null-Kernel";
        if (strncmp(lang_code, "it", 2) == 0) return "kernel nullo";
        if (strncmp(lang_code, "el", 2) == 0) return "μηδενικός πυρήνας";
        if (strncmp(lang_code, "ru", 2) == 0) return "нулевое ядро";
        if (strncmp(lang_code, "zh", 2) == 0) return "空内核";
        if (strncmp(lang_code, "tw", 2) == 0) return "空核心";
        if (strncmp(lang_code, "ja", 2) == 0) return "nullカーネル";
        return "null kernel";
    }
    if (strcmp(msg, "null program") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "null program";
        if (strncmp(lang_code, "es", 2) == 0) return "programa nulo";
        if (strncmp(lang_code, "fr", 2) == 0) return "programme nul";
        if (strncmp(lang_code, "de", 2) == 0) return "Null-Programm";
        if (strncmp(lang_code, "it", 2) == 0) return "programma nullo";
        if (strncmp(lang_code, "el", 2) == 0) return "μηδενικό πρόγραμμα";
        if (strncmp(lang_code, "ru", 2) == 0) return "нулевая программа";
        if (strncmp(lang_code, "zh", 2) == 0) return "空程序";
        if (strncmp(lang_code, "tw", 2) == 0) return "空程式";
        if (strncmp(lang_code, "ja", 2) == 0) return "nullプログラム";
        return "null program";
    }
    if (strcmp(msg, "out of memory creating Direct3D 12 buffer metadata") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "out of memory creating Direct3D 12 buffer metadata";
        if (strncmp(lang_code, "es", 2) == 0) return "memoria insuficiente al crear metadatos de búfer de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "mémoire insuffisante lors de la création des métadonnées du tampon Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht genügend Speicher beim Erstellen der Metadaten des Direct3D 12-Puffers";
        if (strncmp(lang_code, "it", 2) == 0) return "memoria insufficiente durante la creazione dei metadati del buffer Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "ανεπαρκής μνήμη κατά τη δημιουργία μεταδεδομένων ενδιάμεσης μνήμης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недостаточно памяти для создания метаданных буфера Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "创建 Direct3D 12 缓冲区元数据时内存不足";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 Direct3D 12 緩衝區中繼資料時記憶體不足";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12バッファのメタデータ作成中にメモリ不足になりました";
        return "out of memory creating Direct3D 12 buffer metadata";
    }
    if (strcmp(msg, "out of memory creating a Direct3D 12 execution") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "out of memory creating a Direct3D 12 execution";
        if (strncmp(lang_code, "es", 2) == 0) return "memoria insuficiente al crear una ejecución de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "mémoire insuffisante lors de la création d’une exécution Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht genügend Speicher beim Erstellen einer Direct3D 12-Ausführung";
        if (strncmp(lang_code, "it", 2) == 0) return "memoria insufficiente durante la creazione di un'esecuzione Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "ανεπαρκής μνήμη κατά τη δημιουργία εκτέλεσης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недостаточно памяти для создания выполнения Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "创建 Direct3D 12 执行时内存不足";
        if (strncmp(lang_code, "tw", 2) == 0) return "建立 Direct3D 12 執行時記憶體不足";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12実行の作成中にメモリ不足になりました";
        return "out of memory creating a Direct3D 12 execution";
    }
    if (strcmp(msg, "out of memory retaining Direct3D 12 kernels") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "out of memory retaining Direct3D 12 kernels";
        if (strncmp(lang_code, "es", 2) == 0) return "memoria insuficiente al conservar kernels de Direct3D 12";
        if (strncmp(lang_code, "fr", 2) == 0) return "mémoire insuffisante lors de la conservation des kernels Direct3D 12";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht genügend Speicher beim Beibehalten von Direct3D 12-Kerneln";
        if (strncmp(lang_code, "it", 2) == 0) return "memoria insufficiente durante la conservazione dei kernel Direct3D 12";
        if (strncmp(lang_code, "el", 2) == 0) return "ανεπαρκής μνήμη κατά τη διατήρηση πυρήνων Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "недостаточно памяти для сохранения ядер Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "保留 Direct3D 12 内核时内存不足";
        if (strncmp(lang_code, "tw", 2) == 0) return "保留 Direct3D 12 核心時記憶體不足";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12カーネルの保持中にメモリ不足になりました";
        return "out of memory retaining Direct3D 12 kernels";
    }
    if (strcmp(msg, "out of memory while generating shader source") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "out of memory while generating shader source";
        if (strncmp(lang_code, "es", 2) == 0) return "memoria insuficiente al generar el código fuente del shader";
        if (strncmp(lang_code, "fr", 2) == 0) return "mémoire insuffisante lors de la génération du code source du shader";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht genügend Speicher beim Erzeugen des Shader-Quellcodes";
        if (strncmp(lang_code, "it", 2) == 0) return "memoria insufficiente durante la generazione del codice sorgente dello shader";
        if (strncmp(lang_code, "el", 2) == 0) return "ανεπαρκής μνήμη κατά τη δημιουργία πηγαίου κώδικα shader";
        if (strncmp(lang_code, "ru", 2) == 0) return "недостаточно памяти при создании исходного кода шейдера";
        if (strncmp(lang_code, "zh", 2) == 0) return "生成着色器源代码时内存不足";
        if (strncmp(lang_code, "tw", 2) == 0) return "產生著色器原始碼時記憶體不足";
        if (strncmp(lang_code, "ja", 2) == 0) return "シェーダーのソースコード生成中にメモリ不足になりました";
        return "out of memory while generating shader source";
    }
    if (strcmp(msg, "out of memory while recording DXGI adapters") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "out of memory while recording DXGI adapters";
        if (strncmp(lang_code, "es", 2) == 0) return "memoria insuficiente al registrar adaptadores DXGI";
        if (strncmp(lang_code, "fr", 2) == 0) return "mémoire insuffisante lors de l’enregistrement des adaptateurs DXGI";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht genügend Speicher beim Aufzeichnen der DXGI-Adapter";
        if (strncmp(lang_code, "it", 2) == 0) return "memoria insufficiente durante la registrazione degli adattatori DXGI";
        if (strncmp(lang_code, "el", 2) == 0) return "ανεπαρκής μνήμη κατά την καταγραφή προσαρμογέων DXGI";
        if (strncmp(lang_code, "ru", 2) == 0) return "недостаточно памяти при записи адаптеров DXGI";
        if (strncmp(lang_code, "zh", 2) == 0) return "记录 DXGI 适配器时内存不足";
        if (strncmp(lang_code, "tw", 2) == 0) return "記錄 DXGI 介面卡時記憶體不足";
        if (strncmp(lang_code, "ja", 2) == 0) return "DXGIアダプターの記録中にメモリ不足になりました";
        return "out of memory while recording DXGI adapters";
    }
    if (strcmp(msg, "package_init() must have no parameters and declare ': void'.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "package_init() must have no parameters and declare ': void'.";
        if (strncmp(lang_code, "es", 2) == 0) return "package_init() no debe tener parámetros y debe declarar ': void'.";
        if (strncmp(lang_code, "fr", 2) == 0) return "package_init() ne doit avoir aucun paramètre et doit déclarer ': void'.";
        if (strncmp(lang_code, "de", 2) == 0) return "package_init() darf keine Parameter haben und muss ': void' deklarieren.";
        if (strncmp(lang_code, "it", 2) == 0) return "package_init() non deve avere parametri e deve dichiarare ': void'.";
        if (strncmp(lang_code, "el", 2) == 0) return "Η package_init() δεν πρέπει να έχει παραμέτρους και πρέπει να δηλώνει ': void'.";
        if (strncmp(lang_code, "ru", 2) == 0) return "package_init() не должна иметь параметров и должна объявлять ': void'.";
        if (strncmp(lang_code, "zh", 2) == 0) return "package_init() 必须没有形参并声明 ': void'。";
        if (strncmp(lang_code, "tw", 2) == 0) return "package_init() 必須沒有參數並宣告 ': void'。";
        if (strncmp(lang_code, "ja", 2) == 0) return "package_init()にはパラメータを指定せず、': void'を宣言する必要があります。";
        return "package_init() must have no parameters and declare ': void'.";
    }
    if (strcmp(msg, "parameter count limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "parameter count limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de parámetros";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite du nombre de paramètres dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Parameteranzahlgrenze überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite del numero di parametri superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου πλήθους παραμέτρων";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел количества параметров";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出形参数量限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出參數數量限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "パラメータ数の上限を超えました";
        return "parameter count limit exceeded";
    }
    if (strcmp(msg, "rejected a dispatch range") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "rejected a dispatch range";
        if (strncmp(lang_code, "es", 2) == 0) return "se rechazó un rango de despacho";
        if (strncmp(lang_code, "fr", 2) == 0) return "une plage de lancement a été rejetée";
        if (strncmp(lang_code, "de", 2) == 0) return "ein Dispatch-Bereich wurde abgelehnt";
        if (strncmp(lang_code, "it", 2) == 0) return "un intervallo di dispatch è stato rifiutato";
        if (strncmp(lang_code, "el", 2) == 0) return "απορρίφθηκε εύρος αποστολής";
        if (strncmp(lang_code, "ru", 2) == 0) return "диапазон диспетчеризации отклонён";
        if (strncmp(lang_code, "zh", 2) == 0) return "调度范围被拒绝";
        if (strncmp(lang_code, "tw", 2) == 0) return "調度範圍遭拒絕";
        if (strncmp(lang_code, "ja", 2) == 0) return "ディスパッチ範囲が拒否されました";
        return "rejected a dispatch range";
    }
    if (strcmp(msg, "scalar binding limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar binding limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de vinculaciones escalares";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite de liaisons scalaires dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Grenze für Skalarbindungen überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite delle associazioni scalari superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου βαθμωτών δεσμεύσεων";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел скалярных привязок";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出标量绑定限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出純量繫結限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラーのバインディング上限を超えました";
        return "scalar binding limit exceeded";
    }
    if (strcmp(msg, "scalar result collides with buffer input") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result collides with buffer input";
        if (strncmp(lang_code, "es", 2) == 0) return "el resultado escalar colisiona con la entrada del búfer";
        if (strncmp(lang_code, "fr", 2) == 0) return "le résultat scalaire entre en conflit avec l’entrée du tampon";
        if (strncmp(lang_code, "de", 2) == 0) return "skalares Ergebnis kollidiert mit der Puffereingabe";
        if (strncmp(lang_code, "it", 2) == 0) return "il risultato scalare è in conflitto con l'input del buffer";
        if (strncmp(lang_code, "el", 2) == 0) return "το βαθμωτό αποτέλεσμα συγκρούεται με την είσοδο ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "скалярный результат конфликтует с вводом буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量结果与缓冲区输入冲突";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量結果與緩衝區輸入衝突";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果がバッファ入力と競合しています";
        return "scalar result collides with buffer input";
    }
    if (strcmp(msg, "scalar result collides with scalar input") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result collides with scalar input";
        if (strncmp(lang_code, "es", 2) == 0) return "el resultado escalar colisiona con la entrada escalar";
        if (strncmp(lang_code, "fr", 2) == 0) return "le résultat scalaire entre en conflit avec l’entrée scalaire";
        if (strncmp(lang_code, "de", 2) == 0) return "skalares Ergebnis kollidiert mit der Skalareingabe";
        if (strncmp(lang_code, "it", 2) == 0) return "il risultato scalare è in conflitto con l'input scalare";
        if (strncmp(lang_code, "el", 2) == 0) return "το βαθμωτό αποτέλεσμα συγκρούεται με τη βαθμωτή είσοδο";
        if (strncmp(lang_code, "ru", 2) == 0) return "скалярный результат конфликтует со скалярным вводом";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量结果与标量输入冲突";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量結果與純量輸入衝突";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果がスカラー入力と競合しています";
        return "scalar result collides with scalar input";
    }
    if (strcmp(msg, "scalar result consumer metadata mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result consumer metadata mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "los metadatos del consumidor del resultado escalar no coinciden";
        if (strncmp(lang_code, "fr", 2) == 0) return "les métadonnées du consommateur du résultat scalaire ne correspondent pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Metadaten des Verbrauchers des skalaren Ergebnisses stimmen nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "i metadati del consumatore del risultato scalare non corrispondono";
        if (strncmp(lang_code, "el", 2) == 0) return "τα μεταδεδομένα καταναλωτή βαθμωτού αποτελέσματος δεν αντιστοιχούν";
        if (strncmp(lang_code, "ru", 2) == 0) return "метаданные потребителя скалярного результата не соответствуют";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量结果消费者元数据不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量結果使用者中繼資料不符";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果のコンシューマーメタデータが一致しません";
        return "scalar result consumer metadata mismatch";
    }
    if (strcmp(msg, "scalar result has a foreign producer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result has a foreign producer";
        if (strncmp(lang_code, "es", 2) == 0) return "el resultado escalar tiene un productor ajeno";
        if (strncmp(lang_code, "fr", 2) == 0) return "le résultat scalaire a un producteur étranger";
        if (strncmp(lang_code, "de", 2) == 0) return "skalares Ergebnis hat einen fremden Erzeuger";
        if (strncmp(lang_code, "it", 2) == 0) return "il risultato scalare ha un produttore estraneo";
        if (strncmp(lang_code, "el", 2) == 0) return "το βαθμωτό αποτέλεσμα έχει ξένο παραγωγό";
        if (strncmp(lang_code, "ru", 2) == 0) return "скалярный результат имеет стороннего производителя";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量结果具有外部生产者";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量結果具有外部產生者";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果に外部のプロデューサーがあります";
        return "scalar result has a foreign producer";
    }
    if (strcmp(msg, "scalar result limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de resultados escalares";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite de résultats scalaires dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Grenze für skalare Ergebnisse überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite dei risultati scalari superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου βαθμωτών αποτελεσμάτων";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел скалярных результатов";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出标量结果限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出純量結果限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果の上限を超えました";
        return "scalar result limit exceeded";
    }
    if (strcmp(msg, "scalar result load precedes its producer") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result load precedes its producer";
        if (strncmp(lang_code, "es", 2) == 0) return "la carga del resultado escalar precede a su productor";
        if (strncmp(lang_code, "fr", 2) == 0) return "le chargement du résultat scalaire précède son producteur";
        if (strncmp(lang_code, "de", 2) == 0) return "Laden des skalaren Ergebnisses erfolgt vor seinem Erzeuger";
        if (strncmp(lang_code, "it", 2) == 0) return "il caricamento del risultato scalare precede il suo produttore";
        if (strncmp(lang_code, "el", 2) == 0) return "η φόρτωση βαθμωτού αποτελέσματος προηγείται του παραγωγού του";
        if (strncmp(lang_code, "ru", 2) == 0) return "загрузка скалярного результата предшествует его производителю";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量结果加载先于其生产者";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量結果載入早於其產生者";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果のロードがそのプロデューサーより前にあります";
        return "scalar result load precedes its producer";
    }
    if (strcmp(msg, "scalar result producer is not unique") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result producer is not unique";
        if (strncmp(lang_code, "es", 2) == 0) return "el productor del resultado escalar no es único";
        if (strncmp(lang_code, "fr", 2) == 0) return "le producteur du résultat scalaire n’est pas unique";
        if (strncmp(lang_code, "de", 2) == 0) return "Erzeuger des skalaren Ergebnisses ist nicht eindeutig";
        if (strncmp(lang_code, "it", 2) == 0) return "il produttore del risultato scalare non è univoco";
        if (strncmp(lang_code, "el", 2) == 0) return "ο παραγωγός βαθμωτού αποτελέσματος δεν είναι μοναδικός";
        if (strncmp(lang_code, "ru", 2) == 0) return "производитель скалярного результата не уникален";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量结果生产者不唯一";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量結果產生者不唯一";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果のプロデューサーが一意ではありません";
        return "scalar result producer is not unique";
    }
    if (strcmp(msg, "scalar result reference out of range") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "scalar result reference out of range";
        if (strncmp(lang_code, "es", 2) == 0) return "referencia de resultado escalar fuera de rango";
        if (strncmp(lang_code, "fr", 2) == 0) return "référence de résultat scalaire hors limites";
        if (strncmp(lang_code, "de", 2) == 0) return "Verweis auf skalares Ergebnis außerhalb des Bereichs";
        if (strncmp(lang_code, "it", 2) == 0) return "riferimento del risultato scalare fuori intervallo";
        if (strncmp(lang_code, "el", 2) == 0) return "αναφορά βαθμωτού αποτελέσματος εκτός ορίων";
        if (strncmp(lang_code, "ru", 2) == 0) return "ссылка на скалярный результат вне диапазона";
        if (strncmp(lang_code, "zh", 2) == 0) return "标量结果引用超出范围";
        if (strncmp(lang_code, "tw", 2) == 0) return "純量結果參照超出範圍";
        if (strncmp(lang_code, "ja", 2) == 0) return "スカラー結果の参照が範囲外です";
        return "scalar result reference out of range";
    }
    if (strcmp(msg, "shader buffer table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "shader buffer table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "la tabla de búferes del shader no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "la table des tampons du shader ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Shader-Puffertabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "la tabella dei buffer dello shader non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "ο πίνακας ενδιάμεσων μνημών shader δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "таблица буферов шейдера не соответствует";
        if (strncmp(lang_code, "zh", 2) == 0) return "着色器缓冲区表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "著色器緩衝區表不符";
        if (strncmp(lang_code, "ja", 2) == 0) return "シェーダーバッファテーブルが一致しません";
        return "shader buffer table mismatch";
    }
    if (strcmp(msg, "shader scalar table mismatch") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "shader scalar table mismatch";
        if (strncmp(lang_code, "es", 2) == 0) return "la tabla de escalares del shader no coincide";
        if (strncmp(lang_code, "fr", 2) == 0) return "la table des scalaires du shader ne correspond pas";
        if (strncmp(lang_code, "de", 2) == 0) return "Shader-Skalartabelle stimmt nicht überein";
        if (strncmp(lang_code, "it", 2) == 0) return "la tabella degli scalari dello shader non corrisponde";
        if (strncmp(lang_code, "el", 2) == 0) return "ο πίνακας βαθμωτών τιμών shader δεν αντιστοιχεί";
        if (strncmp(lang_code, "ru", 2) == 0) return "таблица скаляров шейдера не соответствует";
        if (strncmp(lang_code, "zh", 2) == 0) return "着色器标量表不匹配";
        if (strncmp(lang_code, "tw", 2) == 0) return "著色器純量表不符";
        if (strncmp(lang_code, "ja", 2) == 0) return "シェーダースカラーテーブルが一致しません";
        return "shader scalar table mismatch";
    }
    if (strcmp(msg, "shader source fragment exceeded its limit") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "shader source fragment exceeded its limit";
        if (strncmp(lang_code, "es", 2) == 0) return "el fragmento de código fuente del shader superó su límite";
        if (strncmp(lang_code, "fr", 2) == 0) return "le fragment de code source du shader a dépassé sa limite";
        if (strncmp(lang_code, "de", 2) == 0) return "Shader-Quellcodefragment hat seine Grenze überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "il frammento di codice sorgente dello shader ha superato il proprio limite";
        if (strncmp(lang_code, "el", 2) == 0) return "το τμήμα πηγαίου κώδικα shader υπερέβη το όριό του";
        if (strncmp(lang_code, "ru", 2) == 0) return "фрагмент исходного кода шейдера превысил свой предел";
        if (strncmp(lang_code, "zh", 2) == 0) return "着色器源代码片段超出其限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "著色器原始碼片段超出其限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "シェーダーのソースコード断片が上限を超えました";
        return "shader source fragment exceeded its limit";
    }
    if (strcmp(msg, "shift count out of range") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "shift count out of range";
        if (strncmp(lang_code, "es", 2) == 0) return "cantidad de desplazamiento fuera de rango";
        if (strncmp(lang_code, "fr", 2) == 0) return "nombre de décalages hors limites";
        if (strncmp(lang_code, "de", 2) == 0) return "Verschiebungsanzahl außerhalb des Bereichs";
        if (strncmp(lang_code, "it", 2) == 0) return "numero di spostamenti fuori intervallo";
        if (strncmp(lang_code, "el", 2) == 0) return "πλήθος μετατόπισης εκτός ορίων";
        if (strncmp(lang_code, "ru", 2) == 0) return "число сдвигов вне диапазона";
        if (strncmp(lang_code, "zh", 2) == 0) return "移位计数超出范围";
        if (strncmp(lang_code, "tw", 2) == 0) return "位移計數超出範圍";
        if (strncmp(lang_code, "ja", 2) == 0) return "シフト数が範囲外です";
        return "shift count out of range";
    }
    if (strcmp(msg, "short module") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "short module";
        if (strncmp(lang_code, "es", 2) == 0) return "módulo demasiado corto";
        if (strncmp(lang_code, "fr", 2) == 0) return "module trop court";
        if (strncmp(lang_code, "de", 2) == 0) return "zu kurzes Modul";
        if (strncmp(lang_code, "it", 2) == 0) return "modulo troppo corto";
        if (strncmp(lang_code, "el", 2) == 0) return "πολύ σύντομη μονάδα";
        if (strncmp(lang_code, "ru", 2) == 0) return "слишком короткий модуль";
        if (strncmp(lang_code, "zh", 2) == 0) return "模块过短";
        if (strncmp(lang_code, "tw", 2) == 0) return "模組過短";
        if (strncmp(lang_code, "ja", 2) == 0) return "モジュールが短すぎます";
        return "short module";
    }
    if (strcmp(msg, "size expression limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "size expression limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de expresiones de tamaño";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite d’expressions de taille dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Grenze für Größenausdrücke überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite delle espressioni di dimensione superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου εκφράσεων μεγέθους";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел выражений размера";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出大小表达式限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出大小運算式限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "サイズ式の上限を超えました";
        return "size expression limit exceeded";
    }
    if (strcmp(msg, "sparse runtime argument namespace") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "sparse runtime argument namespace";
        if (strncmp(lang_code, "es", 2) == 0) return "espacio de nombres de argumentos del entorno de ejecución disperso";
        if (strncmp(lang_code, "fr", 2) == 0) return "espace de noms d’arguments de l’environnement d’exécution clairsemé";
        if (strncmp(lang_code, "de", 2) == 0) return "lückenhafter Namensraum für Laufzeitargumente";
        if (strncmp(lang_code, "it", 2) == 0) return "spazio dei nomi degli argomenti dell'ambiente di esecuzione sparso";
        if (strncmp(lang_code, "el", 2) == 0) return "αραιός χώρος ονομάτων ορισμάτων περιβάλλοντος εκτέλεσης";
        if (strncmp(lang_code, "ru", 2) == 0) return "разреженное пространство имён аргументов среды выполнения";
        if (strncmp(lang_code, "zh", 2) == 0) return "稀疏的运行时环境实参命名空间";
        if (strncmp(lang_code, "tw", 2) == 0) return "稀疏的執行階段環境引數命名空間";
        if (strncmp(lang_code, "ja", 2) == 0) return "実行環境引数の名前空間が疎です";
        return "sparse runtime argument namespace";
    }
    if (strcmp(msg, "static declarations require a relative source path.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "static declarations require a relative source path.";
        if (strncmp(lang_code, "es", 2) == 0) return "Las declaraciones static requieren una ruta de código fuente relativa.";
        if (strncmp(lang_code, "fr", 2) == 0) return "Les déclarations static exigent un chemin de code source relatif.";
        if (strncmp(lang_code, "de", 2) == 0) return "static-Deklarationen erfordern einen relativen Quellcodepfad.";
        if (strncmp(lang_code, "it", 2) == 0) return "Le dichiarazioni static richiedono un percorso del codice sorgente relativo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Οι δηλώσεις static απαιτούν σχετική διαδρομή πηγαίου κώδικα.";
        if (strncmp(lang_code, "ru", 2) == 0) return "Объявления static требуют относительный путь к исходному коду.";
        if (strncmp(lang_code, "zh", 2) == 0) return "static 声明需要相对源代码路径。";
        if (strncmp(lang_code, "tw", 2) == 0) return "static 宣告需要相對原始碼路徑。";
        if (strncmp(lang_code, "ja", 2) == 0) return "static宣言には相対ソースコードパスが必要です。";
        return "static declarations require a relative source path.";
    }
    if (strcmp(msg, "the Direct3D 12 device was removed") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "the Direct3D 12 device was removed";
        if (strncmp(lang_code, "es", 2) == 0) return "el dispositivo Direct3D 12 se eliminó";
        if (strncmp(lang_code, "fr", 2) == 0) return "le périphérique Direct3D 12 a été supprimé";
        if (strncmp(lang_code, "de", 2) == 0) return "das Direct3D 12-Gerät wurde entfernt";
        if (strncmp(lang_code, "it", 2) == 0) return "il dispositivo Direct3D 12 è stato rimosso";
        if (strncmp(lang_code, "el", 2) == 0) return "η συσκευή Direct3D 12 αφαιρέθηκε";
        if (strncmp(lang_code, "ru", 2) == 0) return "устройство Direct3D 12 было удалено";
        if (strncmp(lang_code, "zh", 2) == 0) return "Direct3D 12 设备已移除";
        if (strncmp(lang_code, "tw", 2) == 0) return "Direct3D 12 裝置已移除";
        if (strncmp(lang_code, "ja", 2) == 0) return "Direct3D 12デバイスが削除されました";
        return "the Direct3D 12 device was removed";
    }
    if (strcmp(msg, "unexpected Direct3D 12 buffer snapshot") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unexpected Direct3D 12 buffer snapshot";
        if (strncmp(lang_code, "es", 2) == 0) return "instantánea de búfer de Direct3D 12 inesperada";
        if (strncmp(lang_code, "fr", 2) == 0) return "instantané de tampon Direct3D 12 inattendu";
        if (strncmp(lang_code, "de", 2) == 0) return "unerwartete Momentaufnahme des Direct3D 12-Puffers";
        if (strncmp(lang_code, "it", 2) == 0) return "istantanea del buffer Direct3D 12 imprevista";
        if (strncmp(lang_code, "el", 2) == 0) return "μη αναμενόμενο στιγμιότυπο ενδιάμεσης μνήμης Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "неожиданный снимок буфера Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "意外的 Direct3D 12 缓冲区快照";
        if (strncmp(lang_code, "tw", 2) == 0) return "未預期的 Direct3D 12 緩衝區快照";
        if (strncmp(lang_code, "ja", 2) == 0) return "予期しないDirect3D 12バッファスナップショットです";
        return "unexpected Direct3D 12 buffer snapshot";
    }
    if (strcmp(msg, "unexpected Direct3D 12 result words") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unexpected Direct3D 12 result words";
        if (strncmp(lang_code, "es", 2) == 0) return "palabras de resultados de Direct3D 12 inesperadas";
        if (strncmp(lang_code, "fr", 2) == 0) return "mots de résultats Direct3D 12 inattendus";
        if (strncmp(lang_code, "de", 2) == 0) return "unerwartete Direct3D 12-Ergebniswörter";
        if (strncmp(lang_code, "it", 2) == 0) return "parole dei risultati Direct3D 12 impreviste";
        if (strncmp(lang_code, "el", 2) == 0) return "μη αναμενόμενες λέξεις αποτελεσμάτων Direct3D 12";
        if (strncmp(lang_code, "ru", 2) == 0) return "неожиданные слова результатов Direct3D 12";
        if (strncmp(lang_code, "zh", 2) == 0) return "意外的 Direct3D 12 结果字";
        if (strncmp(lang_code, "tw", 2) == 0) return "未預期的 Direct3D 12 結果字組";
        if (strncmp(lang_code, "ja", 2) == 0) return "予期しないDirect3D 12結果ワードです";
        return "unexpected Direct3D 12 result words";
    }
    if (strcmp(msg, "unimplemented expr: %d\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unimplemented expr: %d\n";
        if (strncmp(lang_code, "es", 2) == 0) return "expr no implementada: %d\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "expr non implémentée : %d\n";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht implementierte expr: %d\n";
        if (strncmp(lang_code, "it", 2) == 0) return "expr non implementata: %d\n";
        if (strncmp(lang_code, "el", 2) == 0) return "μη υλοποιημένη expr: %d\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "нереализованное expr: %d\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "未实现的 expr：%d\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "未實作的 expr：%d\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "未実装のexpr: %d\n";
        return "unimplemented expr: %d\n";
    }
    if (strcmp(msg, "unknown assembler failure") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unknown assembler failure";
        if (strncmp(lang_code, "es", 2) == 0) return "fallo desconocido del ensamblador";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec inconnu de l’assembleur";
        if (strncmp(lang_code, "de", 2) == 0) return "unbekannter Assemblerfehler";
        if (strncmp(lang_code, "it", 2) == 0) return "errore sconosciuto dell'assembler";
        if (strncmp(lang_code, "el", 2) == 0) return "άγνωστη αποτυχία assembler";
        if (strncmp(lang_code, "ru", 2) == 0) return "неизвестный сбой ассемблера";
        if (strncmp(lang_code, "zh", 2) == 0) return "未知汇编器故障";
        if (strncmp(lang_code, "tw", 2) == 0) return "未知組譯器失敗";
        if (strncmp(lang_code, "ja", 2) == 0) return "不明なアセンブラエラー";
        return "unknown assembler failure";
    }
    if (strcmp(msg, "unsupported buffer origin") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unsupported buffer origin";
        if (strncmp(lang_code, "es", 2) == 0) return "origen de búfer no admitido";
        if (strncmp(lang_code, "fr", 2) == 0) return "origine de tampon non prise en charge";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht unterstützter Pufferursprung";
        if (strncmp(lang_code, "it", 2) == 0) return "origine del buffer non supportata";
        if (strncmp(lang_code, "el", 2) == 0) return "μη υποστηριζόμενη προέλευση ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "неподдерживаемое происхождение буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持的缓冲区来源";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援的緩衝區來源";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないバッファ生成元";
        return "unsupported buffer origin";
    }
    if (strcmp(msg, "unsupported device buffer extent") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unsupported device buffer extent";
        if (strncmp(lang_code, "es", 2) == 0) return "extensión de búfer del dispositivo no admitida";
        if (strncmp(lang_code, "fr", 2) == 0) return "étendue de tampon du périphérique non prise en charge";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht unterstützte Ausdehnung des Gerätepuffers";
        if (strncmp(lang_code, "it", 2) == 0) return "estensione del buffer del dispositivo non supportata";
        if (strncmp(lang_code, "el", 2) == 0) return "μη υποστηριζόμενη έκταση ενδιάμεσης μνήμης συσκευής";
        if (strncmp(lang_code, "ru", 2) == 0) return "неподдерживаемый размер буфера устройства";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持的设备缓冲区维度大小";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援的裝置緩衝區維度大小";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないデバイスバッファの次元サイズ";
        return "unsupported device buffer extent";
    }
    if (strcmp(msg, "unsupported shader source dialect") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unsupported shader source dialect";
        if (strncmp(lang_code, "es", 2) == 0) return "dialecto de código fuente de shader no admitido";
        if (strncmp(lang_code, "fr", 2) == 0) return "dialecte de code source de shader non pris en charge";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht unterstützter Shader-Quellcodedialekt";
        if (strncmp(lang_code, "it", 2) == 0) return "dialetto del codice sorgente dello shader non supportato";
        if (strncmp(lang_code, "el", 2) == 0) return "μη υποστηριζόμενη διάλεκτος πηγαίου κώδικα shader";
        if (strncmp(lang_code, "ru", 2) == 0) return "неподдерживаемый диалект исходного кода шейдера";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持的着色器源代码方言";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援的著色器原始碼方言";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていないシェーダーソースコード方言";
        return "unsupported shader source dialect";
    }
    if (strcmp(msg, "unsupported typed shader instruction") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unsupported typed shader instruction";
        if (strncmp(lang_code, "es", 2) == 0) return "instrucción de shader tipada no admitida";
        if (strncmp(lang_code, "fr", 2) == 0) return "instruction de shader typée non prise en charge";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht unterstützte typisierte Shader-Anweisung";
        if (strncmp(lang_code, "it", 2) == 0) return "istruzione shader tipizzata non supportata";
        if (strncmp(lang_code, "el", 2) == 0) return "μη υποστηριζόμενη εντολή shader με τύπους";
        if (strncmp(lang_code, "ru", 2) == 0) return "неподдерживаемая типизированная инструкция шейдера";
        if (strncmp(lang_code, "zh", 2) == 0) return "不支持的有类型着色器指令";
        if (strncmp(lang_code, "tw", 2) == 0) return "不支援的具型別著色器指令";
        if (strncmp(lang_code, "ja", 2) == 0) return "サポートされていない型付きシェーダー命令";
        return "unsupported typed shader instruction";
    }
    if (strcmp(msg, "unused kernel has a buffer range") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "unused kernel has a buffer range";
        if (strncmp(lang_code, "es", 2) == 0) return "el kernel sin usar tiene un rango de búfer";
        if (strncmp(lang_code, "fr", 2) == 0) return "le kernel inutilisé a une plage de tampon";
        if (strncmp(lang_code, "de", 2) == 0) return "nicht verwendeter Kernel hat einen Pufferbereich";
        if (strncmp(lang_code, "it", 2) == 0) return "il kernel inutilizzato ha un intervallo di buffer";
        if (strncmp(lang_code, "el", 2) == 0) return "ο αχρησιμοποίητος πυρήνας έχει εύρος ενδιάμεσης μνήμης";
        if (strncmp(lang_code, "ru", 2) == 0) return "неиспользуемое ядро имеет диапазон буфера";
        if (strncmp(lang_code, "zh", 2) == 0) return "未使用的内核具有缓冲区范围";
        if (strncmp(lang_code, "tw", 2) == 0) return "未使用的核心具有緩衝區範圍";
        if (strncmp(lang_code, "ja", 2) == 0) return "未使用のカーネルにバッファ範囲があります";
        return "unused kernel has a buffer range";
    }
    if (strcmp(msg, "validator allocation failed") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "validator allocation failed";
        if (strncmp(lang_code, "es", 2) == 0) return "falló la asignación del validador";
        if (strncmp(lang_code, "fr", 2) == 0) return "échec de l’allocation du validateur";
        if (strncmp(lang_code, "de", 2) == 0) return "Zuordnung für den Validator fehlgeschlagen";
        if (strncmp(lang_code, "it", 2) == 0) return "allocazione del validatore non riuscita";
        if (strncmp(lang_code, "el", 2) == 0) return "η εκχώρηση του επικυρωτή απέτυχε";
        if (strncmp(lang_code, "ru", 2) == 0) return "сбой выделения памяти для валидатора";
        if (strncmp(lang_code, "zh", 2) == 0) return "验证器分配失败";
        if (strncmp(lang_code, "tw", 2) == 0) return "驗證器配置失敗";
        if (strncmp(lang_code, "ja", 2) == 0) return "バリデータの割り当てに失敗しました";
        return "validator allocation failed";
    }
    if (strcmp(msg, "value limit exceeded") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "value limit exceeded";
        if (strncmp(lang_code, "es", 2) == 0) return "se superó el límite de valores";
        if (strncmp(lang_code, "fr", 2) == 0) return "limite de valeurs dépassée";
        if (strncmp(lang_code, "de", 2) == 0) return "Wertgrenze überschritten";
        if (strncmp(lang_code, "it", 2) == 0) return "limite dei valori superato";
        if (strncmp(lang_code, "el", 2) == 0) return "υπέρβαση ορίου τιμών";
        if (strncmp(lang_code, "ru", 2) == 0) return "превышен предел значений";
        if (strncmp(lang_code, "zh", 2) == 0) return "超出值限制";
        if (strncmp(lang_code, "tw", 2) == 0) return "超出值限制";
        if (strncmp(lang_code, "ja", 2) == 0) return "値の上限を超えました";
        return "value limit exceeded";
    }
    if (strcmp(msg, "var is specified without a single symbol.") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "var is specified without a single symbol.";
        if (strncmp(lang_code, "es", 2) == 0) return "var es especificado sin un solo símbolo.";
        if (strncmp(lang_code, "fr", 2) == 0) return "var est spécifié sans symbole unique.";
        if (strncmp(lang_code, "de", 2) == 0) return "var wird ohne ein einzelnes Symbol angegeben.";
        if (strncmp(lang_code, "it", 2) == 0) return "var è specificato senza un singolo simbolo.";
        if (strncmp(lang_code, "el", 2) == 0) return "Το var καθορίστηκε χωρίς ένα μοναδικό σύμβολο.";
        if (strncmp(lang_code, "ru", 2) == 0) return "var указан без единственного символа.";
        if (strncmp(lang_code, "zh", 2) == 0) return "var 未指定单个符号。";
        if (strncmp(lang_code, "tw", 2) == 0) return "var 未指定單一符號。";
        if (strncmp(lang_code, "ja", 2) == 0) return "varが変数名以外に指定されました。";
        return "var is specified without a single symbol.";
    }
    if (strcmp(msg, "vm-options:\n") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "vm-options:\n";
        if (strncmp(lang_code, "es", 2) == 0) return "opciones de la VM:\n";
        if (strncmp(lang_code, "fr", 2) == 0) return "options de la VM :\n";
        if (strncmp(lang_code, "de", 2) == 0) return "VM-Optionen:\n";
        if (strncmp(lang_code, "it", 2) == 0) return "opzioni della VM:\n";
        if (strncmp(lang_code, "el", 2) == 0) return "επιλογές VM:\n";
        if (strncmp(lang_code, "ru", 2) == 0) return "параметры VM:\n";
        if (strncmp(lang_code, "zh", 2) == 0) return "VM 选项：\n";
        if (strncmp(lang_code, "tw", 2) == 0) return "VM 選項：\n";
        if (strncmp(lang_code, "ja", 2) == 0) return "VMオプション:\n";
        return "vm-options:\n";
    }
    if (strcmp(msg, "wrong SPIR-V version") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "wrong SPIR-V version";
        if (strncmp(lang_code, "es", 2) == 0) return "versión de SPIR-V incorrecta";
        if (strncmp(lang_code, "fr", 2) == 0) return "version de SPIR-V incorrecte";
        if (strncmp(lang_code, "de", 2) == 0) return "falsche SPIR-V-Version";
        if (strncmp(lang_code, "it", 2) == 0) return "versione SPIR-V errata";
        if (strncmp(lang_code, "el", 2) == 0) return "εσφαλμένη έκδοση SPIR-V";
        if (strncmp(lang_code, "ru", 2) == 0) return "неверная версия SPIR-V";
        if (strncmp(lang_code, "zh", 2) == 0) return "SPIR-V 版本错误";
        if (strncmp(lang_code, "tw", 2) == 0) return "SPIR-V 版本錯誤";
        if (strncmp(lang_code, "ja", 2) == 0) return "SPIR-Vのバージョンが正しくありません";
        return "wrong SPIR-V version";
    }
    if (strcmp(msg, "wrong magic") == 0) {
        if (strncmp(lang_code, "en", 2) == 0) return "wrong magic";
        if (strncmp(lang_code, "es", 2) == 0) return "valor mágico incorrecto";
        if (strncmp(lang_code, "fr", 2) == 0) return "nombre magique incorrect";
        if (strncmp(lang_code, "de", 2) == 0) return "falscher Magic-Wert";
        if (strncmp(lang_code, "it", 2) == 0) return "valore magico errato";
        if (strncmp(lang_code, "el", 2) == 0) return "εσφαλμένη μαγική τιμή";
        if (strncmp(lang_code, "ru", 2) == 0) return "неверное магическое значение";
        if (strncmp(lang_code, "zh", 2) == 0) return "魔数错误";
        if (strncmp(lang_code, "tw", 2) == 0) return "魔數錯誤";
        if (strncmp(lang_code, "ja", 2) == 0) return "マジック値が正しくありません";
        return "wrong magic";
    }
    return msg;
}
