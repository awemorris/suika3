func main() {
    var p = Packed.uint8(10);
    for (i in 0..10) {
        p[i] = i;
    }
    var sum = 0;
    for (i in 0..10) {
        if (p[i] % 2 == 0) {
            continue;
        }
        sum = sum + p[i];
    }
    print(sum);
}
