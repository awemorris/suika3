func main() {
    var n = 5000;
    var p = Packed.uint8(n);
    for (i in 0..n) {
        p[i] = i % 251;
    }
    var sum = 0;
    for (i in 0..n) {
        sum = sum + p[i];
    }
    print(sum);
}
