func main() {
    var n = 10000000;
    var p = Packed.uint8(n);
    Packed.fill(p, 0, n, 3);
    var delta = 5;
    var sum = 0;
    for (i in 0..9999995) {
        sum = sum + p[i + delta] - p[i] + p[i + 1];
    }
    print(sum);
}
